interface:
  display_name: "Plush Git Closeout Queue"
  short_description: "共享 Local writer、无锁只读快照、集中锁恢复与 Git 收口"
  default_prompt: "Use $plush-git-closeout-queue 协调共享 Local writer、无锁只读快照、集中锁恢复、批次登记、显式授权的 Git 收口和队列轮换。"

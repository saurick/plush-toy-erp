interface:
  display_name: "Plush Test Governance"
  short_description: "按改动影响面选择最小充分测试并准确说明验证与证据边界"
  default_prompt: "Use $plush-test-governance 先展示执行合同并选择最小充分验证；prepare-push 默认 affected，只有高风险或发布候选经逐次确认后才使用 --full，并报告执行数、skip、未跑项与盲区。"

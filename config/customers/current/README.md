# current Customer Config

本目录是 current 客户配置包的未来落点。

当前口径：

- `current` 是第一个真实客户 / 种子客户 / 私有化客户实例。
- 本目录只作为配置草案和未来落地位置。
- 不代表 SaaS runtime tenant。
- 不新增 `tenant_id`。
- 不创建 runtime loader。
- 不改 Ent schema。

未来可放：

- 公司信息。
- logo / 主题色。
- 菜单和模块开关。
- 字段显示 / 必填。
- 编号规则。
- 打印模板选择。
- 角色模板和权限模板。
- 初始化数据。

# 部署真源约定 / Deployment Conventions

## 当前口径

- 当前唯一部署真源：`/Users/simon/projects/plush-toy-erp/server/deploy/compose/prod`
- 当前仓库只保留 `docker compose` 这一路径，未初始化 `lab-ha`、Kubernetes 和 dashboard
- 目标服务器配置较低，部署时不在服务器构建；服务端和前端镜像必须先在本地或 CI 构建完成，再上传到服务器加载运行
- 线上 migration 使用宿主机 `/usr/local/bin/atlas`，不要拉起 `arigaio/atlas:*` 临时容器，也不要把 Atlas 写入业务 Compose
- 如果后续确实需要第二套部署方式，先补文档和目录，再补脚本和发布流程

## 目录职责

### `server/deploy/compose/prod`

- 单机或单宿主机部署入口
- 保留 PostgreSQL、Jaeger、业务容器和线上迁移脚本
- 所有运行时参数优先通过 `.env` 覆盖，不要直接硬改 `compose.yml`
- 第三方基础镜像必须固定版本 tag；PostgreSQL、Jaeger 和业务镜像都不能使用 `latest`，生产 `.env` 由 `production-preflight.sh` 阻断浮动 tag、placeholder secret、debug 写入开关、PostgreSQL 非 loopback 暴露、后端 HTTP / gRPC 非 loopback 暴露和 Jaeger 非 loopback 暴露。yoyoosun 的短信能力以 `deployments/yoyoosun/env/runtime.contract.json` 为部署级真源，env、容器与 `auth.capabilities` 任一不是 `provider / enabled / not-mock` 都不得发布
- 服务器侧只允许执行 `docker load`、`docker compose up`、宿主机 Atlas migration 与 smoke；不要执行 `docker build`、`pnpm build`、`go build` 或其他重构建命令
- 服务端镜像内置 Chromium / CJK 字体用于在线 PDF，PDF 浏览器路径、并发和容器内存预算统一从 `.env` / Compose 管理

## 不可变制品与本地演练

发布候选必须从 clean current HEAD 的 committed archive 构建。`scripts/deploy/release-artifact-bundle.mjs --execute` 固定生成 `linux/amd64` Server / Web 镜像，并把 40 位 Git SHA、image content ID、loadable tar checksum、CycloneDX 依赖 SBOM、Atlas migration 序列和客户配置源指纹写入同一脱敏 manifest。manifest 中的客户配置只证明 Product Core 已提交来源，不替代目标 active revision / effective session 读回。

`scripts/deploy/local-release-rehearsal.mjs --execute --manifest <path>` 只使用唯一 `server/deploy/compose/prod/compose.yml`。每次创建 `plush_erp_release_<run-id>`，按 `status → validate / dry-run → apply → status / readback` 执行 migration，并验证 health、ready、release identity、管理员登录、本地测试配置的正式控制面协议、PDF、备份恢复和移除 bootstrap secret 后的重启恢复。无论成功失败都执行精确 Compose 清理；长期 `plush_erp`、共享库和 133 不在该演练的写入范围。

正确顺序是：最终本地门禁与 clean commit → 同 SHA 本地九岗位 / 真实浏览器生命周期验收且零残留 → 非强制 push → exact-SHA 远端 CI 绿色 → 同 SHA 制品与本地发布演练 → 明确 promotion → 目标 preflight / rollback point / backup / migration / Compose / smoke。133 只 load / pull 已构建制品，不从源码重建；任何 SHA、content ID、migration、active config 或 rollback 身份不一致都停止。

## GitHub CI/CD 与固定 133 promotion

- `.github/workflows/ci.yml` 是普通验证入口：pull request 运行 affected，main push 运行 full，旧验证可按 ref 取消；它不发布制品，也不部署 133。
- `.github/workflows/release.yml` 是独立发布入口：只接受默认分支可达的 40 位 SHA，同 fingerprint 的 exact-SHA strict 只运行或复用一次，同 SHA 的 Server/Web 镜像只构建一次并以 digest、Release manifest、SBOM 和 checksums 固化。
- promotion、smoke 和 rollback 复用已发布制品，不回到 strict 或 build。Release 已存在且身份完整时幂等复用；同版本异 SHA 或 digest 必须阻断。
- `scripts/deploy/deployment-targets.json` 首版只登记 `test-133`。目标机、用户、根目录、Compose project、env、数据库和端口都来自固定 registry，不接受浏览器或命令行临时覆盖。
- DEV-only `/__dev/version-center` 通过 loopback Bridge 调用固定 GitHub adapter 和固定目标执行器。所有写操作先形成 operation，再要求完整确认串；同一目标只能有一个执行器，终态和 `not_proven` 不自动重试。
- 133 需要 fresh 验收数据时，先把同一不可变 release promotion 到目标，再走 `database-rebuild-controller.mjs` 的只读资格计划和 `database-rebuild-executor.mjs` 的精确确认。该路径保持逻辑库名不变，fresh backup 恢复校验后保存旧物理数据目录并创建新数据代；不直接清表、不删除旧数据代、不自动 down migration。
- 普通代码回滚只允许 migration 序列与客户配置源指纹完全相同的旧 manifest；不自动 down migration，也不自动恢复数据库。schema 不兼容时改走 forward-fix 或单独评审、验证过的备份恢复。

当前远端 promotion / rollback 脚本能证明制品、备份恢复检查、migration、Compose、health、ready、Web health 和运行 SHA；数据库重建 passed 回执另外只证明 fresh 物理代、一次性管理员和空业务基线。客户配置、九岗位数据、51 项浏览器/PDF、带凭据岗位矩阵与客户 UAT 仍是独立未证明项，必须在真实发布后补齐，不能由基础 smoke 替代。

## 单一真源规则

1. 同一环境只保留一条主路径，不额外再并存另一套同级部署脚本
2. 发布、迁移、烟测、文档都围绕 `server/deploy/compose/prod` 收口
3. 若引入新的部署方式，必须同步更新：
   - `README.md`
   - `docs/当前真源与交接顺序.md`
   - `server/deploy/README.md`
   - 对应脚本和验证命令

## 变更流程

涉及部署改动时，按这个顺序做：

1. 先确认改动是否属于 `server/deploy/compose/prod`
2. 优先修改 `.env.example`、`compose.yml`、发布脚本和对应 README
3. 再执行最小校验
4. 最后同步更新文档

## 最小校验

```bash
cd /Users/simon/projects/plush-toy-erp/server/deploy/compose/prod
docker compose -f compose.yml config

cd /Users/simon/projects/plush-toy-erp
bash scripts/project-scan.sh --strict
```

如改动触达发布脚本或应用运行配置，再补：

```bash
bash /Users/simon/projects/plush-toy-erp/scripts/qa/fast.sh
node --test /Users/simon/projects/plush-toy-erp/scripts/deploy/release-artifact-bundle.test.mjs \
  /Users/simon/projects/plush-toy-erp/scripts/deploy/release-artifact-verify.test.mjs \
  /Users/simon/projects/plush-toy-erp/scripts/deploy/local-release-rehearsal.test.mjs
```

## 不要做的事

- 不要在没有明确需求时补回 K8s、Helm 或 `lab-ha`
- 不要把真实密码、JWT 密钥或远端主机写死进仓库
- 不要只改 live 机器或手工命令，而不回收脚本和文档

# 状态字典与生命周期索引 / Status Dictionary And Lifecycle Index

本文是 plush-toy-erp 状态体系的目标设计与统一入口。状态树只描述系统最终认可的对象、canonical key 和合法流转；旧 AI 草稿、本地实验、未发布字段及过渡方案不进入状态树，也不成为运行时合同。

判断 Workflow、Process 与 Fact 的责任边界，继续阅读 [`状态工作流事实边界.md`](状态工作流事实边界.md)。判断当前状态、状态历史和运行轨迹能否形成真实链，阅读 [`业务链与运行轨迹边界.md`](业务链与运行轨迹边界.md)。判断角色、菜单和流程投影，阅读 [`../product/多甲方角色能力与流程编排.md`](../product/多甲方角色能力与流程编排.md)。

## 设计规则 / Design Rules

1. 先确认状态属于 Source Document、Workflow task、Process Runtime、Fact / Ledger、MasterData lifecycle 还是派生投影；不同层即使同名也不得共用状态机。
2. canonical key、允许集合和流转判断必须有唯一代码或 Schema 真源；前端标签、Workflow payload、seed、fixture 和文档不能反向定义状态。
3. 状态树不表达实现成熟度。代码、versioned migration、目标环境发布和客户验收分别取证，不能混写成一个“已完成”。
4. 数据库 CHECK 只约束落库形状；权限、来源、并发、幂等、跨表事实和终态规则仍由 usecase / repo 守住。
5. 未知状态一律 fail closed：不映射成其他正式状态、不提供动作、不建立别名或双读分支。
6. 当前状态不是状态历史；没有持久化 transition / event 证据时，不从当前值、时间戳、页面标签或 payload 倒推历史。
7. 不建立万能 `business_status`。MasterData、Source Document、Workflow task、ProcessRuntime、Business Projection 与 Fact / Ledger 分别使用自己的 canonical 合同。

## 状态字典树 / Status Dictionary Tree

以下两棵树结构一一对应。箭头只表示已经存在精确合同的合法转换，花括号表示已登记状态集合但转换仍由对象专属 usecase 或多记录事务决定，斜杠表示并列终点或分类；所有节点都是目标设计的一部分，不附带历史或计划标签。

### 中文状态字典树

```text
plush-toy-erp 状态体系
├─ 源单据（业务承诺）
│  ├─ 销售订单：草稿 → 已提交 → 生效 → 已关闭 / 已取消
│  ├─ 采购订单：草稿 → 已提交 → 已批准 → 已关闭 / 已取消
│  ├─ 加工合同：草稿 → 已提交 → 已确认 → 已关闭 / 已取消
│  ├─ 生产订单：草稿 → 已发布 → 已关闭；草稿 / 已发布 → 已取消（受事实门禁约束）
│  └─ 销售 / 采购 / 委外明细：开放 → 已关闭 / 已取消
├─ 工作流（岗位协同）
│  ├─ 协同任务：创建 → 可执行；可执行 → 阻塞 / 已完成 / 已退回；阻塞 → 可执行
│  └─ 业务进度投影：来源专属阶段词汇，不构成统一线性生命周期
├─ 流程运行时（流程编排）
│  ├─ 流程实例：运行中 → 已完成 / 已阻塞
│  ├─ 流程节点：等待中 → 运行中 → 已完成 / 已阻塞
│  └─ 处理结果：节点或白名单领域命令的结果，不是通用单据状态
├─ 业务事实与台账
│  ├─ 收发与库存
│  │  ├─ 采购入库 / 退货 / 调整：草稿 → 已过账；草稿 / 已过账 → 已取消
│  │  ├─ 库存作业：草稿 → 已过账；草稿 / 已过账 → 已取消
│  │  ├─ 出货：草稿 → 已出货；草稿 / 已出货 → 已取消
│  │  ├─ 库存预留：生效 → 已释放 / 已消耗 / 已取消
│  │  └─ 库存批次：可用 ↔ 冻结；冻结 ↔ 拒收；可用 / 冻结 / 拒收 → 停用
│  ├─ 质量、退货与异常
│  │  ├─ 质检：草稿 → 已提交 → 通过 / 不通过；草稿 / 已提交 → 已取消
│  │  ├─ 采购拒收 / 委外回货处置：草稿 → 已过账；草稿 / 已过账 → 已取消
│  │  ├─ 生产异常决策：已提交 → 已批准 / 已拒绝 / 已取消
│  │  └─ 生产异常执行：待执行 → 已生效 → 已冲正
│  ├─ 生产执行
│  │  ├─ 生产 / 委外事实：草稿 → 已过账 → 已取消
│  │  ├─ 在制批次：待安排 → 已拆分 / 本厂加工 / 委外加工 / 已取消；加工 → 待质检 / 已接收；待质检 → 已接收 / 不通过
│  │  └─ 包材确认：待确认 → 已确认
│  └─ 财务与放行
│     ├─ 业务财务：草稿 → 已过账 ↔ 已结清；已过账 → 已取消
│     ├─ 收付款：草稿 → 已过账 → 已冲正
│     ├─ 核销 / 红冲：{已过账, 反向记录}；反向事实是新记录，不改写原行
│     └─ 出货财务放行：{待放行, 已放行, 已拒绝}；任何取值都不等于真实已出货
├─ 主数据生命周期
│  ├─ 物料清单：草稿 → 生效；草稿 / 生效 → 已归档；已归档 → 生效
│  └─ 其他对象：对象专属的启用 / 停用 / 归档；停用不删除正式业务引用
└─ 客户配置控制面
   └─ Revision：{构建中, 已发布, 已激活, 已被替代}；激活 / 回滚是同一客户多 revision 事务切换
```

### English Status Dictionary Tree

```text
plush-toy-erp status system
├─ Source Document (business commitment)
│  ├─ sales_order: draft → submitted → active → closed / canceled
│  ├─ purchase_order: draft → submitted → approved → closed / canceled
│  ├─ outsourcing_order: draft → submitted → confirmed → closed / canceled
│  ├─ production_order: DRAFT → RELEASED → CLOSED; DRAFT / RELEASED → CANCELLED (fact-gated)
│  └─ sales / purchase / outsourcing item: open → closed / canceled
├─ Workflow (role collaboration)
│  ├─ task: create → ready; ready → blocked / done / rejected; blocked → ready
│  └─ business projection: source-specific phase keys, not a universal linear lifecycle
├─ Process Runtime (orchestration)
│  ├─ instance: active → completed / blocked
│  ├─ node: waiting → active → completed / blocked
│  └─ outcome: result of a node or allowlisted domain command, not a universal document status
├─ Fact / Ledger (business fact)
│  ├─ Receipt, shipment and inventory
│  │  ├─ purchase receipt / return / adjustment: DRAFT → POSTED; DRAFT / POSTED → CANCELLED
│  │  ├─ inventory_operation: DRAFT → POSTED; DRAFT / POSTED → CANCELLED
│  │  ├─ shipment: DRAFT → SHIPPED; DRAFT / SHIPPED → CANCELLED
│  │  ├─ stock_reservation: ACTIVE → RELEASED / CONSUMED / CANCELLED
│  │  └─ inventory_lot: ACTIVE ↔ HOLD; HOLD ↔ REJECTED; ACTIVE / HOLD / REJECTED → DISABLED
│  ├─ Quality, returns and exceptions
│  │  ├─ quality: DRAFT → SUBMITTED → PASSED / REJECTED; DRAFT / SUBMITTED → CANCELLED
│  │  ├─ purchase rejection / outsourcing return disposition: DRAFT → POSTED; DRAFT / POSTED → CANCELLED
│  │  ├─ production exception decision: SUBMITTED → APPROVED / REJECTED / CANCELLED
│  │  └─ production exception execution: PENDING → APPLIED → REVERSED
│  ├─ Production execution
│  │  ├─ production / outsourcing fact: DRAFT → POSTED → CANCELLED
│  │  ├─ WIP batch: PLANNED → SPLIT / IN_PROGRESS / OUTSOURCED / CANCELLED; execution → WAITING_QUALITY / ACCEPTED; WAITING_QUALITY → ACCEPTED / REJECTED
│  │  └─ packaging confirmation: PENDING → CONFIRMED
│  └─ Finance and release
│     ├─ finance_fact: DRAFT → POSTED ↔ SETTLED; POSTED → CANCELLED
│     ├─ finance_payment: DRAFT → POSTED → REVERSED
│     ├─ allocation / credit note: {POSTED, REVERSED}; reversal is a new immutable entry
│     └─ shipment finance release: {PENDING, APPROVED, REJECTED}; none means SHIPPED
├─ MasterData lifecycle
│  ├─ BOM: DRAFT → ACTIVE; DRAFT / ACTIVE → ARCHIVED; ARCHIVED → ACTIVE
│  └─ other objects: object-specific active / inactive / archived states; deactivation preserves formal references
└─ Customer configuration control plane
   └─ revision: {building, published, active, superseded}; activate / rollback is a multi-revision transaction
```

读树时必须守住三条边界：Workflow task `done` 不等于 Fact `POSTED`；业务投影 `shipping_released` 不等于 shipment `SHIPPED`；Process node `completed` 只表示节点按合同结算，领域事实仍以对应 usecase 和事实表为准。

财务事实的 `SETTLED` 是余额结清状态，不是不可逆终态。仅 `RECEIVABLE / PAYABLE` 在收付款冲正或反向红冲后重新出现未结余额时，由对应领域事务执行 `SETTLED → POSTED` 并清空 `settled_at / settled_by`；页面不提供通用“重开”写入口，`RECONCILIATION / INVOICE` 也不能借这条边绕过各自合同。

## 精确合同索引 / Canonical Contract Index

| 层级与对象                   | 目标状态集合                                                                                    | 创建与转换真源                                                                                                                                                                          | 约束与显示入口                                                                           |
| ---------------------------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| Workflow task                | `ready / blocked / done / rejected`                                                             | `server/internal/biz/workflow_metadata.go`；公共创建固定为 `ready`；`ready → blocked / done / rejected`；`blocked → ready`                                                              | Workflow usecase / repo / JSON-RPC action、task / event schema、共享页面映射及直接 tests |
| Workflow business projection | 有受控生产者的来源专属阶段 key                                                                  | 受控任务动作、Process Runtime 或领域 usecase 内部副作用；各来源不共享统一转换图                                                                                                         | `workflow_metadata.go` taxonomy、`workflow_business_states` 与前后端 key 合同测试        |
| Process instance / node      | instance：`active / completed / blocked`；node：`waiting / active / completed / blocked`        | Process Runtime 的创建、推进、claim、outcome 与幂等合同                                                                                                                                 | process schema、repo 事务 / CAS 与直接 tests                                             |
| BOM                          | `DRAFT / ACTIVE / ARCHIVED`                                                                     | 创建固定为 `DRAFT`；activate / archive 的显式转换                                                                                                                                       | `server/internal/biz/inventory.go`、BOM repo、schema 与直接 tests                        |
| 销售 / 采购订单与订单行      | 对应 lifecycle helper 的精确集合                                                                | 对应 usecase 与 repo 的正式动作                                                                                                                                                         | `server/internal/core/status/`、order / item schema CHECK 与直接 tests                   |
| 加工合同与合同明细           | 对应 outsourcing lifecycle 精确集合                                                             | outsourcing order usecase / repo 的正式动作                                                                                                                                             | `server/internal/biz/outsourcing_order.go`、schema CHECK 与直接 tests                    |
| 生产订单 Source Document     | `DRAFT / RELEASED / CLOSED / CANCELLED`                                                         | `ProductionOrderUsecase`、repo CAS、receipt 与事实关联门禁                                                                                                                              | production order / event schema 与直接 tests                                             |
| 基础 Fact / Ledger           | 入库、退货、调整、库存作业、质检、出货、生产、委外、库存预留与财务事实的精确集合                | 对应领域 usecase 的 post / cancel / settle / ship；应收 / 应付结清后若因收付款冲正或反向红冲恢复未结余额，由同一事务执行 `SETTLED → POSTED`；其它已生效影响通过 reversal 或受控取消纠正 | 各事实 schema、库存事务、页面共享映射与直接 tests                                        |
| 生产执行与异常               | WIP batch、包材确认、异常 decision 与 execution 的精确集合                                      | `production_wip.go`、`production_exception_decision.go` 及对应 repo 的命名动作、version、receipt 与质量门禁                                                                             | WIP / packaging / exception schema、repo 事务、JSON-RPC 与直接 tests                     |
| 采购 / 委外处置              | 采购拒收与委外回货处置使用各自精确集合                                                          | 采购 / 委外处置由各自命名 usecase 执行；库存与质检影响仍由领域事务承接                                                                                                                  | 对应 biz、repo、schema、JSON-RPC 与直接 tests                                            |
| 收付款、核销与红冲           | payment：`DRAFT / POSTED / REVERSED`；allocation / credit note：不可变 `POSTED / REVERSED` 记录 | payment post / reverse 与不可变反向记录 usecase                                                                                                                                         | `finance_payment.go`、finance payment repo、schema 与直接 tests                          |
| 出货财务放行                 | `PENDING / APPROVED / REJECTED` 状态集合；商业出货走批准路径                                    | shipment finance release 命名领域命令；不得用通用状态写入补造拒绝路径                                                                                                                   | shipment process command、shipment schema 与直接 tests                                   |
| 客户配置 Revision            | `building / published / active / superseded` 状态集合                                           | publish 事务与 activate / rollback 多 revision 事务切换                                                                                                                                 | customer config usecase / transition / repo / schema 与直接 tests                        |

## Workflow 任务合同 / Workflow Task Contract

公共创建只产生 `ready`。任务状态与合法转换只有：

```text
ready   → blocked / done / rejected
blocked → ready
```

- `done / rejected` 是终态；终态不重开。返工或重新协同时创建新任务或新节点 attempt。
- 同状态写入不是幂等；网络重试必须重放同一个 `idempotency_key` 对应的 receipt。
- `blocked / rejected` 必须提供非空原因；恢复要求新的解除说明并清理任务上的旧阻塞原因。
- `resume` 固定执行 `blocked → ready`。任务的 `business_status_key` 与 `workflow_business_states` 投影继续保持 `blocked`，后续业务阶段只能由来源 usecase 的受控动作显式推进。
- 任务看板只接受 `all / ready / blocked / rejected / done / overdue / dueSoon`。`overdue / dueSoon` 是到期派生筛选，不是 canonical 状态；不提供聚合状态别名。
- break-glass 只能绕过受审计的人员范围，不能绕过状态转换、版本、幂等、事务、Process Runtime 结算或 Fact 边界。

## 实现、迁移与发布证据 / Implementation, Migration And Release Evidence

| 证据层              | 结论                                                                                                                                                                                                           |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 中央状态合同        | `workflow_metadata.go` 的 registry / transition 已按四个 Workflow task key 收口；它只证明中央允许集合与转换图。                                                                                                |
| 跨层调用方与生成物  | **本地 Product Core 已收口**。Workflow repo / service / API / UI、seed / fixture / tests 与 Ent generated 共同消费四个目标 key；该结论仍不外推为具体数据库已迁移或目标环境已发布。                             |
| versioned migration | **版本链已收口**。`20260714055504_migrate.sql` 已把四状态 CHECK 纳入正式 Atlas 链；migration 文件及本地 fresh / upgrade 结果仍不能证明任一目标数据库已经 apply，必须以该数据库的 migration status 与读回为准。 |
| 目标环境            | **未发布**。本地代码与定向测试不能证明目标环境已执行 migration、加载对应制品或完成业务验收。                                                                                                                   |

### 一次性数据边界 / One-time Data Boundary

- 正式运行代码只识别目标集合；遇到集合外 key 必须明确失败，不读取、映射、双写或静默降级。
- 迁移前先做数据审计。非生产数据优先重置或重新 seed；若发现必须保留的正式业务记录，单独评审可审计的一次性转换，明确映射、人工确认、回滚点和失败条件。
- 一次性转换完成后删除临时工具或关闭入口，不把转换逻辑留在 usecase、repo、API、UI 或查询筛选中。
- 当前只能说“中央 registry / transition、跨层调用方、Ent generated 与 versioned migration 已在本地 Product Core 收口；具体数据库是否已迁移和目标环境是否已发布仍分别取证”。

## 分层语义 / Layer Semantics

| 层级                         | 状态表达什么                                               | 不表达什么                                 |
| ---------------------------- | ---------------------------------------------------------- | ------------------------------------------ |
| Source Document              | 订单、合同或计划从草稿到生效、关闭、取消的业务承诺生命周期 | 不证明库存、质检、出货或财务事实已经发生   |
| Workflow task                | 某个责任角色的协同任务是否可执行、阻塞、完成或退回         | `done` 不等于任何 Fact 已过账              |
| Workflow business projection | 某个来源对象投影到哪个协同阶段                             | 不是统一状态机，也不是库存、出货或财务真源 |
| Process instance / node      | 编排实例和节点 attempt 的运行、阻塞、结算与 outcome        | 节点完成不替代领域命令的事实结果           |
| Fact / Ledger                | 已发生并可审计、取消、冲正或调整的业务事实                 | 不由页面显示状态或 Workflow payload 补造   |
| MasterData lifecycle         | 主数据是否可继续用于新业务、是否停用或归档                 | 停用不删除正式业务引用                     |

## 前端显示合同 / UI Projection Contract

- 页面消费共享状态映射或模块配置，不在单页复制另一套允许集合。
- 未知 key 显示业务可理解的“状态不可用”，同时隐藏动作；不得映射成最近似状态。
- 客户配置可以调整低风险显示标签，不能改变 canonical key、流转、权限或事实含义。
- Workflow task 动作按精确合同投影：`ready` 可完成、阻塞、退回或催办，`blocked` 只可恢复或催办，`done / rejected` 只读。

## 开发观察台 / Developer Observatory

本地开发构建通过 `/__dev/status-flows` 提供只读“业务链与运行观察台”。页面采用“一张业务链总图 + Workflow 协同 + ProcessRuntime 运行 + Fact / Ledger 事实 + 状态规则”的五视图结构：Workflow 管人，ProcessRuntime 管路，Fact / Ledger 管账，状态机管规则，业务链负责把它们串起来。业务链目录覆盖当前 27 个业务状态对象和全部 7 个 ProcessRuntime variant；6 个跨链控制面对象保留在状态目录中，不伪装成独立业务链。

观察台不是新的运行真源或配置执行器：

1. `devFlowStateCatalog.mjs` 汇总状态机和流程 variant，`devBusinessChainCatalog.mjs` 是业务链唯一配置目录，`devFactLedgerCatalog.mjs` 是事实定义唯一配置目录。页面只消费这些目录；canonical key、合法边、权限、guard、领域动作和事实副作用仍由本表“创建与转换真源”列出的代码、Schema 和测试决定。
2. 目录构建时校验 key 唯一、引用完整、状态机 / variant / Fact Key 覆盖和图可达；未知引用、缺失覆盖、重复 key 或不可达节点必须 fail closed。业务链 Tab 默认以 11 个链级节点和明确关系展示只读总图，不展开各链内部节点；点击后一次只展示一条详细业务链。总图是独立设计投影，不作为伪造的第 12 条业务链；横切对象只说明职责，不伪装成线性步骤。
3. Workflow、ProcessRuntime、Source Document、Fact / Ledger 与 MasterData lifecycle 保持分层。任务 `done`、节点 `completed`、业务投影和事实 `POSTED / SHIPPED / SETTLED` 不共用“已完成”语义。
4. 页面不提供任意状态写入、拖拽改图、脚本、SQL 或客户自定义执行器。证据和内部 key 次级展示，代码引用默认折叠；中文业务名称和恢复入口优先。
5. 运行轨迹只能消费后端授权且按任务可见性过滤的 task-scoped ProcessRuntime 上下文；总图最多高亮该任务所属的一条业务链，单链明细最多高亮对应的一个流程节点。当前没有通用 ProcessRuntime 实例 ID 或 Fact 凭证 ID 查询，页面会明确说明能力缺口，不根据标题、payload、fixture 或目录定义补造运行结果。
6. `view / chain / node / flow / state / process / fact / task_id` 写入 URL；默认入口归一为 `view=chain&chain=all`，刷新、前进后退和跨视图下钻保留上下文。定义搜索文字只留在当前页面，不写入 URL。未知、重复或过期参数停止加载，并提供恢复到业务总图的入口；`chain=all` 携带单链 `node` 时同样失败关闭。

新增状态对象时只在对应唯一真源落地；随后补观察目录证据与合同测试，不在 JSX 或多份文档复制目录清单。

## 新增或修改状态 / Change Checklist

1. 明确 owning layer 与业务含义，避免复用同名状态掩盖 Workflow / Fact 或 Source Document / Fact 边界。
2. 在唯一代码常量、Schema enum / CHECK 或 catalog 中登记 canonical key，不新增平行字典。
3. 在后端 usecase / repo 中锁住合法迁移、RBAC、owner / assignee、来源、事务、幂等、并发和终态。
4. 同步 versioned migration、共享显示映射和真正对应风险的 unit / repo / service / PostgreSQL / browser tests。
5. 分别记录代码、迁移、发布和验收证据；任一层缺失都不得外推。

## 明确不做 / Non-goals

- 不建立跨所有领域的万能 `business_status` 表或允许客户上传任意状态机的配置系统。
- 不让前端标签、Workflow payload、seed、fixture 或旧文档成为状态真源。
- 不在状态树中登记旧 AI 方案、实现成熟度或临时技术草案。
- 不为目标集合外的 key 建立别名、双读、双写、聚合筛选或永久 fallback。
- 不把本地测试绿色写成已迁移、已发布或客户已验收。
- Source Document 与 Fact 中正式定义的关闭、取消、冲正语义属于其目标生命周期，不得与 Workflow task 状态混用。

# 正式产品入口与菜单配置计划 / Formal Menu Entry Plan

- 文档类型：产品路线配套计划
- 状态：规划输入；runtime 真源为当前菜单配置、路由、RBAC、代码和测试
- 对应 roadmap 能力：`正式产品入口与旧入口退出`
- 对应建议路线：`013 v1-formal-menu-and-legacy-entry-exit`
- 对应原型：`docs/product/prototypes/formal-menu-candidate-v1/index.html`（To Implement，用于评审正式菜单候选）
- 对应评审表：`docs/product/菜单映射评审表.md`（用于对齐当前运行时菜单、正式候选主入口和 51 个内部覆盖细项）
- 对应实施拆分：`docs/product/正式菜单运行时实施拆分清单.md`（用于拆分真实菜单、RBAC 和测试影响）

## 0. 文档目的

本文承接正式产品入口、行业默认菜单候选、客户菜单配置和旧入口退出的规划细节。产品总路线图看 `docs/product/产品完成路线图.md`；当前菜单 runtime、seedData、权限码、路由和前端实现真源回到当前代码、配置、RBAC 和测试。当前产品内 docs registry 已下线，实际修改按具体实现任务拆分。

## 1. 产品目标

正式产品入口与旧入口退出的目标是把 V1 正式页面从旧通用入口、直链或兼容入口切到正式产品入口，并让不同客户通过配置控制可见菜单，而不是复制菜单代码。当前仍处于开发阶段，尚未进入甲方正式测试；与正式 V1 能力重叠的旧入口不保留产品内可见兼容页面、只读兼容页、旧路径重定向或权限别名。

必须满足：

- 客户、供应商、联系人、销售订单等 V1 正式能力有稳定入口。
- 旧 `business_records` 重叠入口退出正式业务写路径。
- 已由 V1 承接的旧 `partners / project-orders` 入口不保留旧页面、兼容只读页、旧路径重定向或权限别名；普通 `business` API 已冻结这两个旧模块写操作；旧数据迁移、归档和删除另行评审。
- 菜单隐藏只影响入口可见性，不替代后端动作权限、状态机和事实校验。
- 岗位任务入口按任务和职责投影，不复制桌面菜单树。
- 帮助文档入口跟随产品能力和客户启用状态；打印入口只在已有客户交付样本范围内展示，不代表当前要做通用打印模板内核。

## 1.1 当前实现状态

当前已完成：

- `客户档案` 指向正式 `customers` 页面：`/erp/master/partners/customers`。
- `供应商档案` 指向正式 `suppliers` 页面：`/erp/master/partners/suppliers`。
- `销售订单` 指向正式 `sales_orders` 页面：`/erp/sales/project-orders/sales-orders`。
- `产品档案` 已从正式入口壳切换为产品基础信息 / 产品规格 V1 页面，接入 `products / product_skus`、`masterdata` JSON-RPC 和 `product.* / product_sku.*` RBAC；`BOM 管理` 已从正式入口壳切换为 BOM Version V1 页面，接入 `bom_headers / bom_items`、`bom` JSON-RPC 和 `bom.*` RBAC；`工序档案` 已接入 MasterData / Engineering Data V1 页面，接入 `processes`、`masterdata` JSON-RPC 和 `process.*` RBAC，用可委外 / 可内制 / 需质检标记表达工序属性，不把工序类别写成固定枚举；`采购订单` 已从正式入口壳切换为采购承诺 V1 页面，接入 `purchase_orders / purchase_order_items`、`purchase_order` JSON-RPC 和最小状态流转；`入库管理` 已从正式入口壳切换为采购入库 V1 页面，接入 `purchase_receipts / purchase_receipt_items`、`purchase` JSON-RPC、草稿 / 加行 / 过账 / 取消已过账冲正主路径；`来料质检` 已从正式入口壳切换为 Quality Fact V1 页面，接入 `quality_inspections`、`quality` JSON-RPC、草稿 / 提交 / 合格或让步 / 不合格 / 取消主路径；`库存台账` 已从正式入口壳切换为 Inventory Fact 只读 V1 页面，接入 `inventory_balances / inventory_lots / inventory_txns`、ACTIVE `stock_reservations` 可用量 read model、`inventory` JSON-RPC 和 `warehouse.inventory.read` RBAC。
- `委外订单` 已从正式入口壳切换为加工合同源单 V1 页面，接入 `outsourcing_orders / outsourcing_order_items`、`outsourcing_order` JSON-RPC 和 `outsourcing.order.*` RBAC，只维护加工合同、工序明细和打印快照，不自动写库存、质检、应付、发票、付款或 Workflow 完成。
- `生产进度`、`出库管理`、`对账管理`、`应付管理`、`应收管理`、`发票管理` 已从正式入口壳切换为收窄 Operational Fact V1 页面，复用 `production_facts / shipments / stock_reservations / finance_facts`、`operational_fact` JSON-RPC 和既有 fact usecase；当前只承接可由现有事实表支撑的发料 / 出货 / 预留 / 财务事实，不代表完整源单、放行、核销、税控或总账能力。
- `生产排程`、`生产异常`、`出货放行` 仍是正式入口壳，只承接列表、筛选、列设置、导出、显式详情、新建 / 编辑只读业务表单弹窗。
- `出货单` 已新增为正式 Shipment Fact V1 入口：`/erp/warehouse/shipments`，复用 `shipments / shipment_items / inventory_txns` 和 `operational_fact` JSON-RPC，支持列表、明细、草稿、加行、确认出货和已出货取消冲正；`出货放行`、`出库管理`、`出货单` 同属出货管理但不互相替代。
- 桌面菜单、dashboard 模块入口、前端菜单权限选项、yoyoosun 客户菜单、毛绒行业模板候选和后端内置菜单均已使用上述正式入口。
- 旧 `/erp/master/partners` 与 `/erp/sales/project-orders` 路径不再渲染旧通用业务页、兼容只读页或旧路径重定向；前后端菜单权限也不再把旧路径归一到正式 V1 入口；普通 `business` API 只保留 `dashboard_stats`，旧记录方法已退出运行时。

当前未完成：

- 删除前 JSONL evidence 是否需要进入客户可见历史归档的单独评审；当前不恢复旧 `business_records` 表族。
- 生产排程、生产异常、出货放行仍需逐模块评审和实现对应领域 usecase、schema、API、RBAC、审计和测试；Operational Fact V1 页面仍不包含采购订单余额、采购退货 / 调整、质检明细 / 缺陷字典 / 抽检方案、质检与 Workflow 自动闭环、采购退货自动生成、完整预留明细、库存盘点调整、委外合同审批 / 报价 / 发料回货带值 / 质检结算联动、生产订单 / 排程、出货放行自动扣库、收付款核销、税控、总账或完整报表。
- 普通业务用户版产品内帮助入口。

当前已部分实现：

- 客户菜单 runtime config loader 已接入前端桌面菜单生成：`web/src/erp/config/customerMenuConfig.mjs` 可读取构建环境 `VITE_ERP_CUSTOMER_KEY`、页面预置 `window.__PLUSH_ERP_CUSTOMER_KEY__` 或 `window.__PLUSH_ERP_CUSTOMER_CONFIG__`，当前已登记 `config/customers/yoyoosun/menuConfig.mjs`。
- 该 loader 只控制桌面菜单分组、排序、显隐和文案；后端 RBAC action permission、Workflow / Fact usecase、schema、migration、真实导入和 SaaS tenant 均不在本阶段实现。
- 永绅 yoyoosun 试用培训说明草案已新增到 `docs/customers/yoyoosun/试用培训说明.md`，用于说明正式入口、旧入口退出、菜单配置边界和销售订单边界；它不是产品内帮助中心。

## 2. 菜单目标结构

| 层级 | 目标 | 说明 |
| --- | --- | --- |
| Product capability baseline | 定义产品内核真实能力 | 只描述产品具备哪些通用能力，不写客户差异 |
| Default industry menu | 行业默认菜单 | 从产品能力形成毛绒玩具行业默认菜单候选 |
| Customer menu config | 客户菜单配置 | 控制每客户启用 / 隐藏 / 排序 / 文案 / 默认入口 |
| RBAC action permission | 后端动作权限 | 决定用户能否执行动作，不能被菜单隐藏替代 |
| Mobile task entry | 手机端岗位入口 | 从任务和职责投影，不直接复制桌面菜单 |
| Docs / print entry | 帮助文档和客户打印样本入口 | 帮助文档跟随产品能力展示；打印样本入口只按客户交付范围展示 |
| Legacy removal marker | 旧入口退出标记 | 只说明旧入口已退出正式产品入口，不承诺客户可见归档页 |

菜单调整后的结果应是：

- 同一套产品代码支持不同客户菜单。
- 不同客户通过配置隐藏、启用、排序和改显示文案。
- 默认行业菜单可以作为初始模板，但不是每个客户必须完全一致。
- 菜单配置不改变库存、出货、财务、审计等核心事实规则。
- 菜单隐藏只影响可见入口，不替代后端权限校验。
- 旧 `business_records` 重叠入口不再作为正式业务菜单；如需保留历史数据，必须另做只读归档模型评审，不恢复旧三表。

## 3. 入口颗粒度规则

菜单命名和入口颗粒度必须稳定，不应把多个流程节点用 `/` 绑成一个长期菜单名。这里不要求把左侧菜单改成可展开的多级树；当前“分组标题 + 功能入口”的形态可以继续存在，但功能入口本身必须表达稳定业务对象或管理页。流程步骤应放在页面内 Tab、按钮、待办、岗位任务或工作流节点里。

| 层级 | 应表达什么 | 不应表达什么 |
| --- | --- | --- |
| 菜单分组 | 稳定业务域，如销售、采购、仓储、质检、生产、委外、出货、财务、系统 | 多个流程节点拼接名 |
| 功能入口 | 稳定业务对象或管理页，如销售订单、入库管理、库存台账、出货单、应收管理 | 一个名字塞入库通知 / 检验 / 入库等多个动作 |
| 页面 Tab / 分段 | 同一对象下的状态视图或流程段，如待质检、待入库、已入库 | 替代后端状态机或事实判断 |
| 按钮 / 动作 | 提交、审核、放行、确认入库、确认出库、作废、冲正 | 作为菜单权限或事实真源 |
| 任务 / 岗位任务端入口 | 按职责投影待办，如质检确认、仓库入库、出货放行 | 复制桌面菜单树 |

## 4. 默认行业菜单候选

下面是长期产品方向上的默认行业菜单候选，不代表当前 UI 已全部实现，也不代表每个客户都必须启用。客户菜单配置只能在这套能力基线之上启用、隐藏、排序或改文案，不应重新发明一套业务对象。

| 菜单分组 | 正式功能入口 | 能力归类 | 当前路线状态 | 颗粒度说明 |
| --- | --- | --- | --- | --- |
| 工作台 | 任务中心 | Workflow / Work Queue | 必须保留可达入口 | 展示待办和职责任务，不替代业务事实 |
| 工作台 | 经营看板 | Dashboard / Reporting | 默认可见，可隐藏 | 看板只读汇总，不作为事实真源 |
| 基础资料 | 客户档案 | MasterData | yoyoosun MVP 必须 | 单一主数据对象 |
| 基础资料 | 供应商档案 | MasterData | yoyoosun MVP 必须 | 单一主数据对象 |
| 基础资料 | 联系人 | MasterData | yoyoosun MVP 必须 | 可作为客户 / 供应商详情区块或独立管理入口 |
| 基础资料 | 产品档案 | MasterData | yoyoosun MVP 必须 | 不把 SKU / 颜色尺寸强塞进未评审模型 |
| 基础资料 | 材料档案 | MasterData | 行业默认 | 材料主数据，不等于采购或库存事实 |
| 基础资料 | 单位档案 | MasterData | 行业默认 | 单位主数据和后续换算规则 |
| 基础资料 | 仓库档案 | MasterData | 行业默认 | 仓库主数据，库位后续评审 |
| 销售 | 款式立项 | Source Document / Product Planning | 后续评审 | 不和销售订单塞成一个菜单名 |
| 销售 | 销售订单 | Source Document | yoyoosun MVP 必须 | 表达业务承诺，不生成出货、库存、财务事实 |
| 产品工程 | BOM 管理 | Product Structure | V1 最小页面已落 | BOM 是工程资料，不直接写库存事实 |
| 产品工程 | 加工环节 | MasterData / Engineering Data | V1 最小页面已落 | 维护可扩展工序主数据和委外 / 内制 / 质检标记，不代表完整委外源单、生产路线或质检事实 |
| 产品工程 | SKU / 规格 | Product Structure | 后续评审 | 色号、尺寸、版本按独立模型评审 |
| 采购 | 采购需求 | Purchase Planning | 后续评审 | 不等于采购订单或入库事实 |
| 采购 | 采购订单 | Source Document | V1 最小页面已落 | 表达采购承诺，不等于入库、库存或应付 |
| 采购 | 采购合同 | Source Document / Print | 后续评审 | 合同模板不等于采购事实 |
| 质检 | 来料质检 | Quality Fact | V1 最小页面已落 | 质检判定和批次状态由 QualityUsecase 管；当前不写库存流水，不自动生成采购退货 |
| 质检 | 成品质检 | Quality / Production | 后续评审 | 不等于成品入库事实 |
| 仓储 | 入库管理 | Purchase / Warehouse | V1 最小页面已落 | 入库通知、质检、确认入库放页面内状态或任务；当前页面只做采购入库事实，不替代来料质检、库存台账或财务应付 |
| 仓储 | 库存台账 | Inventory Fact | V1 只读页面已落 | 读取库存流水、批次、余额和 ACTIVE 预留可用量 read model；不提供库存写入、调整或出库确认 |
| 仓储 | 出库管理 | Shipment / Inventory Outbound | V1 收窄页面已落 | 复用出货单和库存预留 facts；出货放行不等于出库，只有出货单 SHIPPED 才写库存 OUT |
| 仓储 | 盘点调整 | Inventory Adjustment | 后续评审 | 盘点和调整走冲正 / 调整事实 |
| 委外 | 委外订单 | Outsourcing Source Document | V1 加工合同源单页面已落 | 维护加工合同、工序明细和打印快照，不自动写库存、质检或应付 |
| 委外 | 委外发料 | Outsourcing / Inventory Fact | V1 收窄页面已落 | 发料过账写正式库存事实，不从 workflow 直接扣库存 |
| 委外 | 委外回货 | Outsourcing Fact | V1 收窄页面已落 | 回货过账写正式库存事实；质检和结算仍分开 |
| 委外 | 委外结算 | Outsourcing / Finance | 后续评审 | 不从 workflow 直接生成应付 |
| 生产 | 生产订单 | Production Source Document | 后续评审 | 生产承诺 / 任务，不等于领料或入库 |
| 生产 | 生产排程 | Production Planning | 后续评审 | 排程不等于生产事实完成 |
| 生产 | 生产进度 | Production Fact | V1 收窄页面已落 | 当前承接生产发料、成品入库和返工 facts，不代表生产订单 / 排程 |
| 生产 | 生产领料 | Production / Inventory Fact | V1 收窄页面已落 | 领料过账写库存事实 |
| 生产 | 成品入库 | Production / Inventory Fact | V1 收窄页面已落 | 成品入库不等于 workflow done |
| 出货 | 出货计划 | Shipment Source Document | 后续评审 | 计划不扣库存 |
| 出货 | 出货放行 | Workflow / Shipment Release | 行业默认可选 | `shipping_released != shipped` |
| 出货 | 出货单 | Shipment Fact | V1 最小页面已落 | shipped 后才是出货事实 |
| 财务 | 应收管理 | Finance Fact | V1 收窄页面已落 | 只承接 `finance_facts.RECEIVABLE`，至少基于真实出货或对账评审 |
| 财务 | 应付管理 | Finance Fact | V1 收窄页面已落 | 只承接 `finance_facts.PAYABLE`，来源仍需回到采购、委外或对账 |
| 财务 | 发票管理 | Finance Fact | V1 收窄页面已落 | 只承接 `finance_facts.INVOICE`，不替代税控和查验 |
| 财务 | 收款管理 | Finance Fact | 后续评审 | 收款事实关联应收 |
| 财务 | 付款管理 | Finance Fact | 后续评审 | 付款事实关联应付 |
| 财务 | 对账管理 | Finance / Reconciliation | V1 收窄页面已落 | 只承接 `finance_facts.RECONCILIATION`，不自动写付款或总账 |
| 报表 | 业务报表 | Reporting | 后续评审 | 报表从事实和派生状态读取 |
| 打印 | 打印样本与合同输出 | Customer Delivery / Deferred | 客户交付样本，当前不进产品内核 | 只保留已有合同输出和客户样本记录；不做通用模板系统 |
| 系统 | 用户与角色 | RBAC | 行业默认 | 角色可配置，动作权限仍由产品定义 |
| 系统 | 权限配置 | RBAC / Responsibility | 行业默认 | 菜单权限不替代动作权限 |
| 系统 | 客户配置 | Customer Config | 后续评审 | 单客户配置不进入 Product Core |
| 系统 | 字典配置 | Customer Config | 后续评审 | 仅低风险字典可客户维护 |
| 系统 | 审计日志 | Audit | 后续评审 | 审计不能被客户关闭 |

## 5. 当前组合入口拆分输入

当前 UI 中把多个业务对象绑在一起的组合入口只作为“待拆依据”，不作为正式菜单名，也不作为后续客户交付入口。后续应拆成稳定产品能力，再由默认行业菜单和客户菜单配置组合出可见菜单。

| 现有组合入口（待拆，不作为正式菜单名） | 拆分后的正式入口方向 | 能力归类 | 产品默认 | 客户菜单配置 | 业务约束 |
| --- | --- | --- | --- | --- | --- |
| 任务看板 | 任务中心 | Work Queue / Workflow | 必须有可达入口 | 可按角色换位置或改名，不应全局关闭 | 如果有任务流转，必须至少有任务入口或岗位任务入口 |
| 业务看板 | 经营看板 | Dashboard / Reporting | 默认可见 | 可隐藏 | 不能作为业务事实或状态机真源 |
| 客户与供应商组合入口 | 客户档案 | MasterData | yoyoosun MVP 必须 | 可对非相关角色隐藏 | 订单、导入依赖时不能禁用对应后端能力 |
| 客户与供应商组合入口 | 供应商档案 | MasterData | yoyoosun MVP 必须 | 可对非相关角色隐藏 | 采购、导入依赖时不能禁用对应后端能力 |
| 产品 | 产品档案 | MasterData | yoyoosun MVP 必须 | 可对非相关角色隐藏 | 订单、BOM、库存依赖时不能禁用对应后端能力 |
| 订单与款式立项组合入口 | 销售订单 | Source Document | yoyoosun MVP 必须 | 无销售链路客户可隐藏 | 只是源单据，不自动生成出货、库存、财务事实 |
| 订单与款式立项组合入口 | 款式立项 | Product Planning | 后续评审 | 未启用产品工程客户可隐藏 | 款式资料、打样和评审状态不和销售订单共用正式菜单名 |
| 材料 BOM | BOM 管理 | Product Structure | 行业默认 | 可在未启用 BOM 客户隐藏 | 若启用生产、采购需求或成本核算，需先保证 BOM 入口或导入路径 |
| 产品工程基础资料 | 加工环节 | MasterData / Engineering Data | 行业默认 | 可在未启用生产 / 委外客户隐藏 | 加工环节是可扩展工序主数据；可委外标记不等于委外订单、生产任务、质检事实或库存流水 |
| 辅材与包材采购组合入口 | 材料档案 | MasterData | 行业默认 | 可对非相关角色隐藏 | 材料主数据不等于采购事实 |
| 辅材与包材采购组合入口 | 采购订单 | Source Document | V1 最小页面已落 | 可按客户是否使用采购模块隐藏 | 隐藏后不得创建只能通过该入口处理的采购任务；采购订单不等于采购入库 |
| 加工合同与委外下单组合入口 | 委外订单 | Outsourcing Source Document | V1 加工合同源单页面已落 | 无委外客户可隐藏 | 维护加工合同、工序明细和打印快照，不自动写库存、质检或应付 |
| 加工合同与委外下单组合入口 | 委外发料 | Outsourcing / Inventory Fact | V1 收窄页面已落 | 无委外客户可隐藏 | 发料必须写正式库存事实，不从 workflow 直接扣库存 |
| 加工合同与委外下单组合入口 | 委外回货 | Outsourcing Fact | V1 收窄页面已落 | 无委外客户可隐藏 | 回货 fact 已有最小入口；质检和结算仍分开 |
| 入库通知、检验与入库组合入口 | 入库管理 | Purchase / Warehouse | V1 最小页面已落 | 可按客户收货流程隐藏部分状态视图 | 入库通知、确认入库放页面内状态或任务，不写进菜单名；当前页面只做采购入库事实，不替代来料质检、库存台账或财务应付 |
| 入库通知、检验与入库组合入口 | 来料质检 | Quality Fact | V1 最小页面已落 | 无质检客户可隐藏 | 上游启用质检时，必须保留任务入口或移动端处理入口；当前页面只做 `quality_inspections` 判定和批次状态，不写库存流水或自动采购退货 |
| 库存 | 库存台账 | Inventory Fact | V1 只读页面已落 | 可对非仓库角色隐藏 | 库存事实仍由后端 `inventory_txns / inventory_balances / inventory_lots` 保证，可用量由 ACTIVE `stock_reservations` 只读派生，不能靠菜单显隐控制；当前页面只读，不提供调整或出库确认 |
| 待出货与出货放行组合入口 | 出货计划 | Shipment Source Document | 后续评审 | 未启用出货链路客户可隐藏 | 计划不扣库存 |
| 待出货与出货放行组合入口 | 出货放行 | Workflow / Shipment Release | 行业默认可选 | 未启用出货链路客户可隐藏 | `shipping_released != shipped`，隐藏入口时不得自动把放行当出库 |
| 待出货与出货放行组合入口 | 出货单 | Shipment Fact | V1 最小页面已落 | 未启用出货链路客户可隐藏 | shipped 后才是出货事实 |
| 出库 | 出库管理 | Shipment / Inventory Outbound | V1 收窄页面已落 | 可在出库事实未启用时隐藏 | 当前复用出货单和库存预留 facts，不从 workflow 直接扣库存 |
| 生产排单 | 生产排程 | Production Planning | 后续评审 | 无生产排程客户可隐藏 | 隐藏后不得创建只能由生产排程入口处理的任务 |
| 生产进度 | 生产事实 | Production Execution | V1 收窄页面已落 | 无生产进度客户可隐藏 | 当前承接生产发料、成品入库和返工 facts；生产订单 / 排程仍后续评审 |

## 6. 菜单隐藏与权限关闭后的业务保证

菜单隐藏、客户未启用能力、用户无权限是三种不同状态，不能混用。

| 状态 | 含义 | 后端要求 | Workflow / 业务保证 |
| --- | --- | --- | --- |
| 菜单隐藏 | 客户或角色看不到入口 | 不改变后端事实规则和动作权限 | 任务仍可通过任务看板、移动端或其他被授权入口处理 |
| 功能未启用 | 客户配置未启用某能力 | 对应 API / usecase 不应作为正常业务入口开放 | 上游流程不得生成只能由该能力处理的任务；必须停在已定义的 deferred / disabled / blocked 状态并给出原因 |
| 用户无权限 | 当前用户没有动作权限 | 后端拒绝动作，前端隐藏按钮只是体验 | 任务不能派给无职责角色；已派错时应进入可审计的改派 / 阻塞 / 退回路径 |
| 旧入口移除 / 隐藏 | 旧入口退出产品入口 | 禁止写入正式业务事实 | 不再作为客户可见能力；历史数据如需保留，只用于迁移、审计或回滚 |

核心规则：

- 客户可以组合菜单，但不能组合出破坏事实层的业务规则。
- 隐藏某个菜单前，必须确认没有 active workflow task 只能通过该入口处理。
- 关闭某个能力前，必须确认上游状态机不会继续派生该能力的待办任务。
- 如果任务已存在但入口被隐藏，系统必须提供任务看板、移动端或管理员改派 / 退回 / 阻塞路径。
- 后端 usecase、RBAC action permission、owner_role_key、assignee_id 和 task_status_key 才是状态机与业务事实的保护层。
- Workflow 可以派生协同任务和业务状态，但不能因为菜单存在或隐藏而直接写库存、出货或财务事实。

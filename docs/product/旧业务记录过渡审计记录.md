Doc Type / 文档类型: Business Records Transition Audit / business_records 过渡审计
Status / 状态: Superseded By Deletion / 已由删除闭环替代
Runtime Implemented / 运行时已实现: Yes / 是
Ent Schema Implemented / Ent Schema 已实现: Yes / 是
Migration Implemented / Migration 已实现: Yes / 是
Current Implementation Source of Truth / 当前实现真源: No / 否

# 业务记录过渡审计 / business_records Transition Audit

本文原本记录 009 docs-only 过渡审计。当前它已被删除闭环替代：旧 `business_records / business_record_items / business_record_events` 表族已由 `20260612112337` migration 删除，普通 `business` JSON-RPC 只保留 `dashboard_stats`。

当前实现真源：

- `docs/当前真源与交接顺序.md`
- `docs/product/旧业务记录删除完成记录.md`
- Ent schema、Atlas migration、代码和测试

## 历史审计结论保留

以下结论仍有效：

- 旧通用记录不能作为正式客户、供应商、联系人、销售订单、产品、采购、库存、质检、出货或财务事实真源。
- Workflow task done 不能等同 fact posted。
- 旧快照字段、旧页面、旧 JSONL evidence 和客户样本都不能自动升级为 Product Core schema。
- 若未来需要历史归档，必须另做只读归档模型评审，不能恢复旧三表。

## 已过期的旧结论

以下旧结论不再作为当前状态：

| 旧结论 | 当前状态 |
| --- | --- |
| `business_records` 作为兼容层 / demo / seed / source snapshot 保留 | 已删除，不再作为运行时来源 |
| 普通 `business` API 写入冻结为 archive read-only | 旧记录查询和写入方法已整体退出运行时 |
| 旧页面只读或 demo 化 | `BusinessModulePage` 只保留退出提示 |
| 不能删除 `business_records` | 当前开发库已完成删除；目标库仍需单独备份和迁移验证 |

## 删除后停止条件

- Stop if a new change reintroduces `business_records` or semantic equivalents as generic fact tables.
- Stop if old JSONL evidence is wired into runtime API, seed, menu, RBAC, Workflow or Fact usecase.
- Stop if target environment migration skips backup evidence or migration status verification.

# 后台工作台与看板原型 / Admin Command Center Prototype

本目录保存后台首页、任务看板、业务看板、模板打印中心和异常 / 阻塞闭环的整套交互原型。

## 文件

| 文件 | 用途 |
| --- | --- |
| `index.html` | 单文件可交互 HTML 原型，覆盖工作台、任务看板、业务看板、模板打印中心和异常 / 阻塞闭环五个视图。 |

## 设计结论

- 后台首页 / 工作台作为登录后第一屏，聚合“今日必须处理、跨角色阻塞、业务状态摘要、打印队列和常用入口”，不替代具体业务模块。
- 任务看板是协同层总览，按角色、状态、到期时间和业务来源聚合 Workflow 任务；它只表达协同任务，不把任务完成写成库存、出货、财务或发票事实。
- 业务看板只展示业务记录和协同状态的运营摘要，不作为 `inventory_txns`、`shipments`、`finance_facts` 等事实层真源。
- 模板打印中心独立成轻量工作台，覆盖模板选择、预览、字段映射、示例记录和打印 / 导出动作；正式实现仍以现有打印模板文档和运行时代码为准。
- 异常 / 阻塞闭环定义阻塞原因、转派、催办、处理、验证和关闭的用户动作；真实状态流转仍应回到 WorkflowUsecase 和 RBAC 边界。

## 待实现吸收状态

截至 2026-06-11，本原型仍按 `待吸收实现 / To Implement` 登记。当前运行时已经吸收后台首页、任务看板、业务看板、模板打印中心和异常 / 阻塞闭环的主要页面骨架和交互路径；工作台和业务看板保留运营中枢导航，模板打印中心已按原型收回为左侧模板导航、中间纸面预览、右侧字段映射的轻量三栏工作台。未获用户明确确认前，仍不能把本资产改成 `当前实现 / Current`。

| 原型视图 | 当前运行时吸收路径 | 尚待人工确认点 | 边界 |
| --- | --- | --- | --- |
| 后台首页 / 工作台 | `/erp/dashboard` 承接工作台首屏、今日焦点、业务状态摘要、常用入口、角色提醒和运营工具。 | 是否接受当前把工作台和任务看板拆成正式独立路由。 | 只做跨模块聚合和入口分发，不替代具体业务模块。 |
| 任务看板 | `/erp/task-board` 承接 Workflow 任务筛选、泳道、任务详情、完成、阻塞和催办入口。 | 是否接受 URL query 保存筛选作为当前运行时主路径。 | 不直接写库存、出货、应收、开票、付款或其他事实；不替代岗位任务端。 |
| 业务看板 | `/erp/business-dashboard` 承接业务记录摘要、状态分布、业务预警、模块健康明细和模块跳转。 | 是否接受当前只做运营摘要，不接事实层钻取。 | 不作为 `inventory_txns`、`shipments`、`finance_facts` 等事实真源。 |
| 模板打印中心 | `/erp/print-center` 承接正式打印模板目录、左侧模板导航、纸面预览、字段映射、模板核对和可编辑打印窗口入口。 | 是否接受当前用现有打印模板能力承接原型轻量三栏，不新增模板引擎。 | 业务带值仍从对应业务页进入；纸面预览固定浅色交付口径；样品确认单只作为禁用候选入口，不进入正式模板目录。 |
| 异常 / 阻塞闭环 | `/erp/operations/exceptions` 承接阻塞记录、责任分派、处理跟进、验证恢复和关闭归档的用户路径。 | 是否接受当前只展示和处理 Workflow 协同异常。 | 不把 workflow done 当 fact posted，不绕过 WorkflowUsecase / RBAC 边界。 |

明确不直接吸收：原型里的 dev-only 侧栏、单页五视图切换、固定数字、静态任务和 mock 脚本不进入正式运行时；候选模板只能以禁用候选入口展示，不能在缺少正式模板真源时被注册为可打印模板。

## 边界

- 本原型自身不接后端、不接权限、不进入 ERP 正式菜单、不进入生产构建。
- 本原型不替代 `docs/current-source-of-truth.md`、Workflow / Fact 架构文档、schema、migration、API 或测试。
- 真实实现时应优先复用后台布局、业务列表共享组件、任务详情面板和打印模板组件，不把 HTML 直接复制进 `web/src`。
- 如果后续继续扩展任务看板、业务看板或工作台菜单，需要同步评审菜单配置、RBAC 权限码、seed、帮助文档和浏览器回归。

## 验收关注点

- 五个视图可以在同一页面内切换。
- 任务详情、阻塞原因面板、模板切换和异常步骤切换有基础交互。
- 桌面宽屏和移动窄屏不出现横向溢出。
- 文案避免把 Workflow 任务状态误写成事实层过账结果。

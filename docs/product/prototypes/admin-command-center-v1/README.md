# 后台工作台样板 / Admin Workbench Pattern

本目录保存后台首页 / 工作台的 To Implement 标准样板，用于重新确认“工作台到底做什么”和“哪些入口不应继续并列放大”。

## 文件

| 文件 | 用途 |
| --- | --- |
| `index.html` | 单文件可交互 HTML 原型，把工作台收敛为今日处理台：今日焦点、优先队列、当前任务上下文、当前任务关联记录入口和交接边界。 |

## 设计结论

- 工作台的意义是登录后第一屏判断：今天先处理什么、为什么重要、应该进入哪个业务对象。
- 工作台不应继续展示成“工作台 / 任务看板 / 业务看板 / 打印中心 / 异常闭环”五个并列视图；详细任务、经营看板、打印编辑和异常处理可以从工作台跳转，但不应在工作台里重复铺开。
- 工作台只保留能继续处理当前事项的关联记录入口；不再单独铺一块“常用入口 / 快捷入口”列表。业务对象整体状态回到业务看板，正式菜单仍由客户配置和 RBAC 决定。
- 任务完成只代表 Workflow 协同状态变化，不代表库存、出货、财务或发票事实过账。

## 待实现吸收状态

截至 2026-06-18，本原型仍按 `待实现 / To Implement` 登记；运行时 `/erp/dashboard` 已吸收低密度方向，把工作台收敛为 3 个可点击队列筛选入口、优先处理队列和当前任务上下文。原型里的固定数字、静态任务、mock 脚本、完整演示结构和通用快捷入口列表仍不进入正式运行时；是否进一步合并、隐藏或改名仍需后续正式菜单评审。未获用户明确确认前，不能把本资产改成 `当前实现 / Current`。

| 原型区域 | 当前运行时参考路径 | 尚待人工确认点 | 边界 |
| --- | --- | --- | --- |
| 今日焦点 | `/erp/dashboard` 的 3 个队列筛选入口 | 已吸收“少入口、先判断”的方向；顶部数字本身就是筛选动作，不再额外放第二套筛选控件。 | 只做协同摘要，不替代任务处理页。 |
| 优先队列 | `/erp/dashboard` 的优先处理队列 | 已保留待我处理 / 阻塞或逾期 / 等待交接 3 个队列；当前队列为空时不兜底展示其他队列任务。 | 任务完成不写事实层。 |
| 当前任务上下文 | `/erp/dashboard` 的当前任务上下文 / 任务抽屉 | 已把当前队列选中任务作为右侧上下文核心，原因、到期和关联对象不再散落多处。 | 不在工作台内保存业务事实。 |
| 关联记录入口 | `/erp/dashboard` 当前任务上下文里的“关联记录”按钮 | 只跟随当前任务出现，不再复制客户 / 订单 / 供应商等通用入口列表。 | 跳转不替代正式菜单、RBAC 和后端 usecase。 |

明确不直接吸收：原型里的固定数字、静态任务、mock 脚本、“把所有治理视图塞进工作台”的演示结构和通用快捷入口列表不进入正式运行时；关联记录入口也不反推正式菜单。

## 边界

- 本原型自身不接后端、不接权限、不进入 ERP 正式菜单、不进入生产构建。
- 本原型不替代 `docs/当前真源与交接顺序.md`、Workflow / Fact 架构文档、schema、migration、API 或测试。
- 真实实现时应优先复用后台布局、业务列表共享组件、任务上下文面板和打印模板组件，不把 HTML 直接复制进 `web/src`。
- 如果后续继续扩展任务看板、业务看板或工作台菜单，需要同步评审菜单配置、RBAC 权限码、seed、帮助文档和浏览器回归。

## 验收关注点

- 优先队列筛选可切换，当前任务上下文随选择变化。
- 优先队列筛选需要使用真实可点击控件，当前选中项应有明确的 `aria-selected` 或 `aria-pressed` 状态；如果使用 `tablist / tab / tabpanel`，当前选中项应同步 `aria-selected`。
- 当前任务关联记录入口数量克制，并随当前任务上下文出现；首屏不重复铺开完整菜单或通用快捷入口，且明确不是新的正式菜单结构。
- 桌面宽屏和移动窄屏不出现横向溢出。
- 文案避免把 Workflow 任务状态误写成事实层过账结果。

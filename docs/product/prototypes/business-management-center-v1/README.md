# 业务管理中心样板 / Business Management Center Pattern

本目录保存后台“业务管理”入口的 To Implement 标准样板，用于重新确认业务管理菜单的意义。

## 文件

| 文件 | 用途 |
| --- | --- |
| `index.html` | 单文件可交互 HTML 原型，把业务管理入口收敛为业务对象总控：按链路选择对象、查看当前风险、进入标准业务页或详情页。 |

## 设计结论

- 业务管理菜单的意义不是把销售、采购、库存、生产、出货和财务全部堆成一个大菜单；它应回答“当前要管理哪条业务链路、哪类对象、下一步进入哪个标准页”。
- 业务管理中心只做对象选择、链路摘要、风险聚合和跳转；真实字段、保存、导出、过账、冲正和状态机仍回到各业务模块标准页、详情页、表单页和动作浮层。
- 业务管理不能成为第二套 `business_records` 或第二套事实写入入口；它也不能因为页面上出现对象就承诺对应 API、schema 或 RBAC 已完成。
- 未实现或只恢复入口壳的领域必须明确标注“待接入”，避免菜单看起来比能力更成熟。

## 待实现吸收状态

截至 2026-07-15，本原型仍按 `待实现 / To Implement` 登记；运行时 `/erp/business-dashboard` 已吸收低密度和对象总控方向，但没有把本原型晋级为 Current。当前页顶部按“主数据 / 工程、业务源单、事实记录、协同风险”四类独立口径展示只读摘要，四类不能相加；对象概览继续按对象族排版，但实际逐项展示后端 `dashboard_stats` 的 20 个模块数量和各自入口，不再用“推进中、状态分布、当前风险”等前端本地汇总代替领域真源。

协同概览独立读取 `workflow.get_task_board`：即使请求只取每栏 1 条代表事项，`total / counts` 仍是当前账号可见范围内的全量计数，四个 Workflow 泳道互斥。业务模块统计与协同概览并行、独立失败；一侧暂不可用时显示明确不可用状态，另一侧已成功的数据和对象入口继续保留，不用 0 掩盖请求失败。本样板仍只说明对象总控方向，不替代正式菜单、API、schema、RBAC、Workflow / Fact 边界或能力台账。

| 原型区域 | 当前运行时参考路径 | 尚待人工确认点 | 边界 |
| --- | --- | --- | --- |
| 业务链路切换 | 现有业务菜单和正式入口壳 | 本轮未新增业务管理总入口，仍由当前正式菜单承接链路切换。 | 不替代正式菜单评审。 |
| 当前链路对象 | `/erp/business-dashboard` 核心对象概览 | 8 个对象族内逐项展示 20 个后端模块；每项保留自己的 `available / total` 和标准页入口，不跨 MasterData、Source Document、Fact、Workflow 口径合计。 | 不新增第二套看板事实或未评审 API。 |
| 四类只读摘要 | `/erp/business-dashboard` 顶部摘要 | 主数据 / 工程、业务源单、事实记录只在所属模块全部可用时聚合；协同风险来自 `get_task_board` 当前可见的阻塞与到期全量计数。四类语义不同，不得相加。 | 摘要只读，不把 Workflow 完成写成 Fact。 |
| 协同概览 | `/erp/business-dashboard` 协同概览 | 使用 `get_task_board` 的四个互斥泳道全量计数，每栏最多展示 1 条代表事项；不再从有界任务列表或前端状态分组推导总量。 | 继续受 `workflow.task.read`、revision、owner / assignee 可见性约束。 |
| 进入标准页 | 各领域正式页面 | 已保留 20 个模块各自入口；字段、动作、状态筛选和权限仍由目标页负责。 | 字段、动作和权限仍由目标页负责。 |

明确不直接吸收：原型里的固定数字、静态对象、mock 脚本、链路名称和模块顺序不进入正式运行时；真实实现必须重新核对当前菜单配置、RBAC、路由和领域能力成熟度。

## 验收关注点

- 链路切换可用，当前链路对象和详情说明同步变化。
- 页面明确区分“可进入的标准页”和“待接入的事实动作”。
- 桌面和移动视口不出现页面级横向溢出。
- 文案不能把业务管理中心写成第二套菜单、第二套 API 或第二套事实真源。

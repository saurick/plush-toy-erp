# 产品能力进度台账 / Product Capability Ledger

本文是 `plush-toy-erp` 唯一的全局产品能力状态真源。它只记录产品能力做到哪一层、当前边界和下一步；yoyoosun 是否可见、可试用或已交付，统一看[客户交付矩阵](../customers/yoyoosun/客户交付矩阵.md)。

## 怎么查

1. 先在“能力状态”中定位业务能力和成熟度。
2. 需要确认 Workflow、Fact、字段来源或实现边界时，进入对应专题文档、代码、migration 和测试；本台账不复制详细证据清单。
3. 需要判断客户状态时，进入客户交付矩阵；产品成熟度不能直接推出客户已发布或已验收。

代码、schema、migration、测试和运行态证据与本文冲突时，以前者为准并修正本文。完整阅读顺序见[当前真源与交接顺序](../当前真源与交接顺序.md)。

## 成熟度口径

| 等级 | 名称 | 含义 | 是否可对客户承诺 |
| ---: | --- | --- | --- |
| L0 | Not Started | 未开始，只在路线或待办中 | 否 |
| L1 | Discussed | 已讨论，有初步口径 | 否 |
| L2 | Reviewed | 已完成架构或业务评审 | 否 |
| L3 | Drafted | 有 schema、配置或数据映射草案 | 否 |
| L4 | Schema Ready | schema、migration 和生成物完成 | 否 |
| L5 | Runtime Ready | repo/usecase 完成，后端核心逻辑可测 | 否 |
| L6 | API Ready | API 与 RBAC 完成，可内部联调 | 否 |
| L7 | UI Ready | 正式页面已接，可作为试用候选 | 否，仍需客户矩阵与环境证据 |
| L8 | Delivery Ready | 页面、真源、权限、审计、测试、部署、培训和客户验收均闭环 | 是 |

L0-L8 表示产品技术成熟度，不表示某个客户环境的发布状态。当前没有任何能力仅凭本表即可写成“客户已交付”。

## 能力状态

| 能力 | 所属层 | 成熟度 | 当前结论 / 下一步 |
| --- | --- | ---: | --- |
| 产品架构与 Workflow / Fact 边界 | Product Core | L7 | Workflow、ProcessRuntime、Source Document 与 Fact 已分层；runtime approval 按服务端登记的 approval capability 集合识别，并由固定审批事项、客户 active revision entitlement、责任池成员和流程冻结 revision 共同授权。七条正式 ProcessRuntime 已登记：销售、采购和 Shipment 财务放行的责任可在“权限管理 → 审批责任”维护，客户退货、收付款、库存人工调整和生产异常使用流程合同固定责任池。桌面 / Drawer / 移动端统一显示 ProcessRuntime “业务轨迹”和单条任务“本任务处理记录”；单任务记录最多返回最近 100 条，服务端与页面用 `truncated` 明确局部范围，仍不冒充跨节点完整审批链。所有 approval 只推进相应来源决定，独立领域命令才写库存、出货、核销或生产事实。目标环境证据待补。 |
| customers / suppliers / contacts | Product Core | L7 | 主数据、联系人、API、RBAC 和正式页面已接；真实客户数据导入、客户字段确认和目标岗位验收仍走客户矩阵。 |
| products / materials / units / warehouses | Product Core | L5-L7 | 主数据主路径已接，产品与 SKU 可选单重已贯通 schema、API、页面和 CSV；不自动做历史回填或单位换算。 |
| product_skus | Product Core | L7 | 显式 SKU 已贯通销售订单、批次、库存、生产、委外、出货和预留；不从 Excel、颜色或尺寸自动建 SKU，BOM SKU 粒度和旧库存重分类仍待评审。 |
| BOM 版本 | Product Core | L7 | BOM schema、repo/usecase、API/RBAC 和正式页面已接；SKU 粒度、采购需求快照与真实导入仍未闭环。 |
| processes / 固定生产路线 | Product Core | L7 | 工序档案保持独立主数据；固定 `PLUSH_SEW_HAND_V1` v1 只按标准位置绑定并在生产订单发布时冻结，不把名称、类别或排序当路线真源。 |
| sales_orders / items | Product Core | L7 | 草稿聚合 CAS、ProcessRuntime 提交、销售管理主办 / 老板升级 approval 和审批后自动激活已接；自动领域命令以同一事务写 `SUBMITTED → ACTIVE` 与 durable result，公开 submit / activate 旁路已移除。 |
| purchase_orders | Product Core | L7 | 正式页从 `DRAFT` 启动 `material_supply`，先显式执行 `purchase_order.submit`，再由采购审批责任池办理；批准后 `PurchaseOrderUsecase.ApprovePurchaseOrderForProcessCommand` 以同一事务写唯一批准状态和 durable result。公开直提 / 直批 API 均已移除。收货、IQC、入库继续走独立领域页面；采购订单行入库进度已有服务端权威读投影，但 BOM 采购需求、已采购、在途和剩余采购量综合 read model 因 grain 与扣减合同不明继续阻塞。 |
| purchase_receipts / returns / adjustments | Product Core | L7 | 入库、来源退货、调整、取消冲正和应付入口已有幂等与事务闭环；采购订单生成入库草稿前按同一读快照返回采购数量、有效已收、DRAFT 草稿占用、剩余可收 / 可生成和不可生成原因，页面不做浮点重算，写入仍在事务内重验。首次 IQC 支持精确行、多笔部分退回供应商或补换；采购退货当前不重开可收量。目标 apply、发布、岗位 smoke 和 UAT 仍未完成。 |
| quality_inspections | Product Core | L7 | 五类正式来源已接；首次 IQC 精确行部分处置、委外返厂 / 返工、生产报废 / WIP 让步均有正式办理页与领域执行边界，无已生效下游时可更正结果；仍缺目标 apply、发布与客户岗位验收。 |
| inventory_txns / balances / lots | Product Core | L7 | 流水不可直接改写，余额按同一库存 grain 派生；盘点、仓间调拨和人工调整均有正式源单生命周期，只有人工调整进入 `inventory_adjustment_approval`，批准不改库存，仓库显式 post 才写交易与余额。真实库位、完整 WMS、真实导入和客户验收不在当前承诺内。 |
| stock_reservations | Product Core | L7 | 销售订单行来源预留、释放、可用量和真实出货事务消费已接；服务端列表在同一读快照中按权限返回来源订单 / 行、产品 / SKU、仓库、单位和批次可读引用，并覆盖分页、筛选与精确六位小数。当前单条记录只有一项不可变预留数量；原始 / 已消费 / 剩余三数量、部分消费、消费 Shipment 链接、取消 / 恢复、单行可用量和正式仓库数据范围尚无合同，不重建第二套预留系统。 |
| shipments / shipment_items | Product Core | L7 | 来源候选、成品质检、财务 approval、正式 `SHIPPED`、库存 `OUT` 和取消冲正已分层；`finished_goods_delivery` 只负责财务审批和版本化放行门禁，质检判定、出货过账及应收创建继续走各自领域 usecase。普通与流程启动后的出货都 fail closed；`finance release != shipped`。目标 migration、岗位 smoke 和客户 UAT 待补。 |
| sales_returns / RMA | Product Core | L7 | 已出货来源、累计退货量、`sales_return_acceptance` 审批 / 拒绝、仓库收货、取消 CAS、隔离 HOLD 批次、退货质检和冲正已接；列表 / 详情已读回来源出货、产品 / SKU、仓库、单位、退货隔离批次及精确的来源已出货量、累计有效退货量、剩余可退量，来源参考资料失败不再清空退货记录。批准不写库存，显式 receive 才写退货库存与 RMA 待检。退款、换货、永绅需求确认、目标发布和 UAT 仍未闭环。 |
| AR / AP / invoice / payment / reconciliation | Product Core | L7 | 已接来源应收应付、发票、真实收付款、多单核销、部分核销、冲正和红冲 / 反向红冲；CreditNote 只接受应收 / 应付来源。FinancePayment 已接 `finance_payment_approval`，批准 / 拒绝只改收付款 Source，财务显式 post 才写分配和结清读模型，reverse 单独冲正。收付款与红冲列表已接服务端分页 / 筛选、精确六位小数未核销金额、来源投影和完整详情，核销合计及红冲上限在前后端均按精确小数校验。通用 `settle_finance_fact` 只允许对账事实；银行直连、总账、税控、永绅需求确认、目标发布和客户财务验收未闭环。 |
| production_orders | Product Core | L7 | 生产订单、BOM 需求快照、发布、排程任务和来源领料 / 完工 / 返工入口已接；订单页可查看成品返工进度，CLOSED 只允许来源返工链继续工序和补完工。固定路线使用前仍须显式绑定工序标准位置。 |
| 生产路线 / WIP / 分段质检 | Product Core | L7（本地源码） | 固定路线、WIP 批次 / 事件、数量守恒、分段质检、包材确认和内制 / 外发切换已接；成品返工以 `origin_rework_fact_id` 建立 HANDWORK 根批次并让后代继承来源，确切包装验收批次才可补完工，已开始补制的来源返工不可取消。当前 HEAD 仍需目标 migration apply、重新发布、读回和客户 UAT。 |
| production facts | Product Core | L7 | 物料需求领料、确切 WIP 批次完工、成品返工补制及 `production_exception_approval` 已接；REWORK 过账原子创建根 WIP / 事件 / 异常任务，生产记录与订单页可查看权威进度。报废 / WIP 让步批准后仍须生产显式执行；超领批准只形成额度并由正式领料事务消费。`20260730161955_migrate.sql` 已生成并通过本地守卫，目标 apply / 发布 / UAT 仍待补。 |
| outsourcing_orders / facts | Product Core | L7 | 加工合同生命周期、来源发料 / 回货、回货质检 / 应付和取消依赖已接；不合格返厂写 OUT 并以 REVERSAL 冲正，返工保存可追溯结果 WIP 且仅在无活动下游时可取消；正式 Web 办理页已接，仍缺目标发布 / UAT。 |
| business_attachments / audit | Reporting / Audit / Integration | L6 | 受权限控制的业务附件和控制面操作记录已有基础入口；Workflow task 附件读取按任务可见性，上传 / 撤销要求 frozen revision、非终态、精确 owner / assignee 和 `expected_version`，super admin 不能虚构业务岗位。普通证据已接带原因、操作者和时间的一次性撤销：列表保留原上传与撤销审计，普通预览 / 下载失败关闭，产品图片仍只走固定槽位替换 / 清空；下载会在授权后按持久化 `file_size + SHA-256` 校验内容，不一致失败关闭。运行审计从服务端请求上下文写入非敏感 `request_id`，调用方不能伪造。模块级业务审计覆盖、撤销后特权审计调阅 / 保管策略、对象存储运维、扫码与外部集成仍需按真实需求评审；目标 migration / 发布 / UAT 尚未执行。 |
| mobile task entry | Industry Template | L7 | 多岗位切换、任务、动作锁、受控附件和异常留痕已接；进入岗位端必须同时具备账号 `mobile.<role>.access` 与 active revision 对该岗位入口的有效动作投影，super admin 也不能凭管理身份冒充业务岗位。任务端不写业务事实，扫码和现场使用仍待真实反馈。 |
| 客户数据导入 tooling | Delivery | L5 | 只读 extract、manifest、freeze 和 dry-run 可运行；当前没有真实客户数据和写入执行器，不得把模拟 fixture 写成真实导入。 |
| Customer Config | Customer Config | L3-L7 | validate、publish、activate、rollback、active revision 和 effective session 已运行；runtime manifest 可选择七条注册流程，权限中心“审批责任”只以固定三类模板维护销售、采购和 Shipment 财务放行的启停、主办 / 备用 / 升级角色或具名成员，另外四条异常流程的责任池由注册合同固定。常规保存使用 active revision/hash CAS，在一个后端事务内写入 immutable revision、完整投影、active 切换和单条审计；通用配置发布流水线仍保留独立 publish / activate。具名员工、责任层级、账号状态和冻结 revision 在预览、保存、列表与办理动作上使用同一合同。编号、导入、`businessFlows`、`stateMachines`、`processPolicies` 仍是 draft/preview，不驱动事实，也不构成 tenant 或插件系统。 |
| yoyoosun 资料治理与私有化部署包 | Delivery | L4-L6 | 客户原件继续由专属 Private 仓库受控，Product Core 只留配置、工具和脱敏分类；当前产品 HEAD、固定目标版本、真实导入和客户签收必须分别取证。 |
| 行业模板与多客户私有化复制包 | Industry Template / Delivery | L3-L4 | 可作为内部候选和新增客户包模板，不代表第二真实客户、SaaS、多租户或已交付。 |
| Customer Extension | Customer Extension | L1 | 只有真实且无法由 Core、行业模板或配置承接的客户专属逻辑才评审 extension；当前没有 runtime extension 层或真实落地需求。 |
| 业务帮助 / Reporting / Integration 增强 | Help / Reporting | L0-L2 | 业务帮助已有单一岗位入口；通用报表、扫码和外部集成在事实层稳定并有真实需求后再评审。 |
| SaaS / 多租户 / license / billing | Productization | L1 | 当前明确不实现；继续按单客户私有化部署和客户配置包管理。 |

## 流程运行时状态

这张表只保留流程能力的差异状态，避免 preview 配置被误读为已执行 runtime。详细边界见[状态、工作流与事实边界](../architecture/状态工作流事实边界.md)、[业务链与运行轨迹边界](../architecture/业务链与运行轨迹边界.md)和[当前真源与交接顺序](../当前真源与交接顺序.md)。

| 流类型 | 当前状态 | 领域写入边界 |
| --- | --- | --- |
| Workflow task | runtime 已接 | 通用 `workflow.task.approve` 与四项领域审批 capability 通过同一服务端注册表进入统一审批集合。办理同时要求 RBAC、任务 revision 的 entitlement、精确 owner pool/role 或具名成员、assignee、状态/version 与幂等；同角色但属于另一责任池也拒绝。三项可配置审批按最低可用责任层级解析，四项异常流程按固定合同责任池解析，没有候选人即失败关闭。`boss` 的受控转交不使 super admin 自动成为岗位候选人，任务完成也不直接写 Fact。 |
| ProcessRuntime / sales_order_acceptance | runtime 已接，目标证据待补 | 销售提交后由销售审批责任池主办、老板仅作升级，批准后自动执行唯一激活命令；失败恢复依赖 durable command evidence，不开放直连 submit / activate 旁路。 |
| ProcessRuntime / material_supply | runtime 已接，目标证据待补 | `submit_purchase_order → purchase_order_approval → approve_purchase_order → end` 只提交并批准采购 Source Document；收货、IQC 和入库由独立领域页面 / usecase 执行，流程任务完成不生成这些事实。 |
| ProcessRuntime / finished_goods_delivery | runtime 已接，目标证据待补 | `shipment_finance_approval → shipment_finance_release → end` 只写 Shipment 财务放行门禁；启动前校验已有成品质检，正式出货、库存 OUT、应收、发票和收付款继续由各自领域用例执行。 |
| ProcessRuntime / sales_return_acceptance | runtime 已接，目标证据待补 | 老板 approval 只批准 / 拒绝客户退货 Source；仓库独立收货任务后的显式 receive 才写退货库存和 RMA 待检，取消与冲正继续走领域命令。 |
| ProcessRuntime / finance_payment_approval | runtime 已接，目标证据待补 | 老板 approval 只批准 / 拒绝 FinancePayment Source；财务独立执行任务后的显式 post 才写多来源分配和结清读模型，reverse 单独冲正。 |
| ProcessRuntime / inventory_adjustment_approval | runtime 已接，目标证据待补 | 只覆盖人工调整；提交和 approval 不改库存，仓库独立执行任务后的显式 post 才写 `inventory_txns` / `inventory_balances`。 |
| ProcessRuntime / production_exception_approval | runtime 已接，目标证据待补 | 老板 approval 只记录报废、WIP 让步或超领决定；报废 / 让步须生产独立执行，超领额度只由正常领料 Fact 消费。 |
| 首次 IQC 退厂 / 补换 | 领域 usecase 已接，目标证据待补 | 按精确行和累计数量登记；补换生成新待收 / 待检链，不写入库事实。 |
| 委外返厂 / 返工 | 领域 usecase 已接，目标证据待补 | 返厂写库存 OUT / REVERSAL；返工写可追溯 WIP 子批并受下游取消门禁。 |
| 生产报废 / 超领 / WIP 让步 | 决定与执行分离，目标证据待补 | 批准只记录决定或额度；明确执行才写 SCRAP / WIP，超领由正式领料消费额度。 |
| 财务红冲 | 领域 usecase 已接，目标证据待补 | 仅正式应收 / 应付可生成红冲；反向红冲保留原记录并支持结果读回。 |
| `businessFlows` / `stateMachines` / `processPolicies` | preview-only | 只作配置预览和 lint，不覆盖 usecase 状态机、权限或 Fact 写入规则。 |
| 打印默认值 | effective session 投影 | 只提供买方/委托方默认展示值，不覆盖供应商、加工方或源单业务快照。 |

## 当前 V1 主链审计汇总

本表按 Source Document → Workflow / ProcessRuntime → Fact / Ledger → 派生读模型重新核对当前代码。状态只表示 Product Core 当前工作树中的正式 V1 边界；不表示目标 migration 已 apply、客户配置已激活、目标岗位已 smoke 或客户 UAT 已通过。

| 状态 | 数量 | 业务切片 | 尚缺内容 |
| --- | ---: | --- | --- |
| 已闭环 | 8 | 销售；采购提交 / 审批；收货 / IQC / 入库；生产 / WIP / 分段质检；委外；出货 / 出库；应收应付 / 发票；异常 / 取消 / 冲正 | 仍需按固定版本补目标发布、真实岗位 smoke 与 UAT，不能由本地代码状态外推 |
| 部分 | 4 | BOM / 工程；库存 / 预留；客户退货 / RMA；收付款 / 核销 / 红冲 | 采购订单行入库进度和预留可读引用投影已接；BOM SKU 与采购需求 / 已采购 / 在途 / 剩余采购量 grain、预留数量拆分 / 部分消费 / 取消恢复和仓库数据范围仍待正式确认。库存人工调整审批已经接入，不再列为缺口。RMA 的退款 / 换货 / 补发及其财务关系、收付款客户口径仍待正式确认；目标发布、岗位实登和客户 UAT 未闭环 |
| 阻塞 | 1 | 真实客户数据写入导入 | 当前没有已批准真实数据和正式写入执行器；必须 fail closed，不能用 fixture、mock 或只读 extract 代替 |
| 范围外 | 1 | 银行直连 / 银行流水自动导入、总账、税控 | 不在当前 FinancePayment V1，需独立业务与架构评审 |

合计 14 个主链审计切片：8 个本地代码边界已闭环，4 个部分，1 个因真实数据 / 导入授权缺失阻塞，1 个属于当前 V1 范围外。本轮核对没有改变数量，但修正了库存人工调整审批的过时理由，并把已落的采购入库进度与预留可读引用从未实现项中移除；该汇总不包含审计 / 附件专项，也不是对每个“已闭环”切片的目标环境重新认证。部分、阻塞和范围外共 6 个，不得计入“完整业务闭环”。

## 高风险判定口径

- 库存闭环必须同时能解释 `IN / OUT / REVERSAL` 流水、余额与可用量、ACTIVE 预留、批次/质检状态、来源单据和取消/冲正；页面存在或任务完成不能单独证明闭环。
- 显式 SKU 在批次、流水、余额、生产、委外、出货和预留间保持同一 grain；`product_sku_id = NULL` 是独立的未分规格库存池，不能当作 SKU 不足时的兜底。
- 客户配置 runtime 只消费已发布、激活并经 catalog、依赖与 RBAC 交集校验的 revision；preview 内容不得执行、提升权限或改写业务事实。
- 异常审批只形成决定或额度；报废、让步、返厂、返工、补换和红冲仍由各自领域动作形成 Fact / WIP / 来源链，并可按其下游依赖独立拒绝或冲正。
- 本地代码、测试或较早目标版本绿色不等于当前 HEAD 已发布，也不等于客户 UAT 或签收完成。

## 证据怎么查

| 要确认什么 | 正式入口 |
| --- | --- |
| 当前代码、schema、migration、测试与环境证据的优先级 | [当前真源与交接顺序](../当前真源与交接顺序.md) |
| 业务来源、状态、字段和上下游事实 | [业务主链路数据流向与字段来源规则](业务主链路数据流向与字段来源规则.md) |
| Workflow、ProcessRuntime、Source Document 与 Fact | [状态、工作流与事实边界](../architecture/状态工作流事实边界.md) |
| 业务链、ProcessRuntime 业务轨迹、任务记录、审批链与审计链 | [业务链与运行轨迹边界](../architecture/业务链与运行轨迹边界.md) |
| 测试形态和验证层级 | [自动化测试策略](自动化测试策略.md) |
| Product Core、行业模板、客户配置与客户扩展 | [客户差异策略](客户差异策略.md)、[客户实例策略](客户实例策略.md) |
| yoyoosun 可见、试用、交付、差异与待确认项 | [客户交付矩阵](../customers/yoyoosun/客户交付矩阵.md) |

## 维护规则

- 只有产品能力成熟度、当前边界或下一步发生实质变化时才更新对应一行；不要把测试输出、每轮补丁历史或完整证据清单复制进来。
- 客户是否启用、是否可试用、目标环境是否发布、客户差异或确认事项，只更新对应客户交付矩阵。
- 代码、migration、测试或运行态事实与本表冲突时，先按正式真源核对，再修正文档；不得反向修改实现来迎合台账。
- 新增客户时复用一份客户交付矩阵，不复制本全局台账。

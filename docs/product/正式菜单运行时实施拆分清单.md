# 正式菜单运行时实施拆分清单 / Formal Menu Runtime Implementation Plan v1

- 文档类型：产品实现拆分计划
- 状态：已部分实施；当前 runtime 真源仍以代码、测试和 `docs/当前真源与交接顺序.md` 为准
- 对应计划：`docs/product/正式产品入口与菜单配置计划.md`
- 对应评审表：`docs/product/菜单映射评审表.md`
- 对应原型：`docs/product/prototypes/formal-menu-candidate-v1/index.html`

## 0. 文档目的

本文把正式菜单候选、当前运行时菜单和后端 RBAC 菜单拆成当前检查结果、剩余问题和下一步单页闭环，避免把 12 个候选主入口或 51 个内部覆盖细项一次性写进左侧导航。

2026-06-13 已执行一轮运行时吸收：前端默认菜单、yoyoosun 客户菜单、毛绒行业候选模板、后端内置菜单、菜单权限预设、路由和正式页面壳已同步。该轮不新增权限码、API、schema、migration、WorkflowUsecase 或 Fact usecase，不恢复旧 `business_records`。

2026-06-18 已完成前后端菜单真源一致性检查：后端内置菜单已包含 `工作台`、`任务看板`、`业务看板`、`异常 / 阻塞闭环`，并把应收 / 发票入口对齐为 `应收管理`、`发票管理`。该检查不改变前端导航、客户菜单显隐、权限码、schema、migration、Workflow / Fact usecase 或页面结构。

2026-06-19 已完成菜单命名一致性检查：当前前端业务模块、前端菜单测试、后端内置菜单和后端菜单测试均已使用 `产品档案` 与 `BOM 管理`。该检查不改 route、客户菜单显隐、权限码、schema、migration、Workflow / Fact usecase 或页面结构；后续不要再把 `产品` / `材料 BOM` 当成待改菜单名。

2026-06-19 已完成 yoyoosun 菜单显隐检查：当前 yoyoosun 客户菜单保留 `工作台` 和 `任务看板`，隐藏 `业务看板`、`异常 / 阻塞闭环`、`生产排程`、`生产异常` 和 `出货放行`。Workflow 处理主路径仍可从 `任务看板`、岗位任务端和具体业务页协同入口进入；隐藏客户侧左侧入口不改变后端 RBAC 安全边界、Workflow / Fact usecase、route、schema、migration 或页面结构，并由 `seedData.test.mjs` 覆盖。

## 1. 当前运行时真源

| 面 | 当前文件 | 作用 | 实施注意 |
| --- | --- | --- | --- |
| 前端默认导航 | `web/src/erp/config/seedData.mjs` | 定义工作台、任务看板、业务看板、打印中心、异常闭环、权限管理等默认入口，并组合业务模块导航 | 改 key / label / path / section 都会影响前端菜单、权限项、测试和截图 |
| 前端业务模块 | `web/src/erp/config/businessModules.mjs` | 定义业务页分组、模块标题、路由、摘要和页面口径 | 改 title / route 会影响侧边栏、权限项、业务页标题和文档 |
| 客户菜单配置 | `web/src/erp/config/customerMenuConfig.mjs`、`config/customers/yoyoosun/menuConfig.mjs` | 控制客户侧显示、隐藏、排序和文案 | 只影响入口可见性，不是 RBAC 或事实层安全边界 |
| 前端菜单权限项 | `web/src/erp/config/menuPermissions.mjs` | 从当前导航生成菜单权限分组和角色预设 | 改菜单会影响 `ERP_MENU_PERMISSION_GROUPS`、默认角色预设和旧路径归一化 |
| 后端内置菜单 | `server/internal/biz/rbac.go` 的 `BuiltinAdminMenus()` | 登录返回 menus、RBAC 菜单可见性和 menu_options | 必须和前端正式入口保持 path / label / required permissions 一致 |
| 后端菜单测试 | `server/internal/biz/rbac_test.go`、`server/internal/service/jsonrpc_*` | 锁定管理员可见菜单、旧入口退出和 JSON-RPC 返回结构 | 前端菜单变更若影响后端内置菜单，必须同步这些测试 |

当前实现已经不是“只有前端菜单”。如果只改 `seedData.mjs` 或客户配置而不改后端 `BuiltinAdminMenus()`，登录返回菜单、权限管理选项和前端导航会漂移。

当前仍是正式入口壳的入口只剩：`生产排程`、`生产异常`、`出货放行`。这些入口只恢复页面结构和交互，不读取或写入旧 `business_records`，也不代表对应领域写入 API 已完成。`产品档案`、`BOM 管理`、`采购订单`、`入库管理`、`来料质检`、`库存台账`、`出货单`、`委外订单`、`生产进度`、`出库管理`、`对账管理`、`应付管理`、`应收管理` 和 `发票管理` 已有对应 V1 或收窄 V1 页面，仍需按各自边界区分主数据、工程资料、采购承诺、采购入库事实、质检事实、含 ACTIVE 预留可用量 read model 的库存只读台账、出货事实、委外加工合同源单、生产 operational facts、库存预留和财务 facts。

## 2. 已完成检查项

下表记录已经收敛的前后端菜单口径。若这些项再次漂移，应先修代码和测试，再更新本文；不要把下表重新当成待办清单。

| key / 路由 | 前端默认导航 | 后端内置菜单 | 当前结论 | 后续边界 |
| --- | --- | --- | --- | --- |
| `global-dashboard` / `/erp/dashboard` | `工作台` | `工作台` | 已一致 | 不和 `/erp/task-board` 混名 |
| `task-board` / `/erp/task-board` | `任务看板` | `任务看板` | 已一致 | 后端 required permission 当前为 `workflow.task.read` |
| `business-dashboard` / `/erp/business-dashboard` | `业务看板` | `业务看板` | 已一致 | 只读看板，不替代业务事实 |
| `exception-flow` / `/erp/operations/exceptions` | `异常 / 阻塞闭环` | `异常 / 阻塞闭环` | 已一致 | 后端 required permission 当前为 `workflow.task.read` |
| `receivables` / `/erp/finance/receivables` | `应收管理` | `应收管理` | 已一致 | 只承接 `finance_facts.RECEIVABLE` |
| `invoices` / `/erp/finance/invoices` | `发票管理` | `发票管理` | 已一致 | 不替代税控、查验或总账 |
| `products` / `/erp/master/products` | `产品档案` | `产品档案` | 已一致 | 只维护产品 / SKU 主数据，不写库存、BOM、订单、生产或出货事实 |
| `material-bom` / `/erp/purchase/material-bom` | `BOM 管理` | `BOM 管理` | 已一致 | BOM 是工程资料，不生成采购需求、采购订单、库存流水、生产任务或成本 |
| `print-center` / `/erp/print-center` | `模板打印中心`，yoyoosun 分组为 `单据模板` | `模板打印中心` | 客户分组差异是配置预期，不是后端菜单漂移 | 保留 |

## 3. 当前剩余问题

后续正式菜单治理不再按统一编号推进，而是按具体页面做单页闭环：

1. 库存台账：检查余额、批次、流水 tab 和页面动作是否仍保持只读台账语义，不误导库存写入、盘点调整、出库确认、批次状态变更或未完整实现的库存预留能力。验收时至少覆盖默认态、tab 切换、筛选、空状态、异常态、暗色和相邻区域。
2. 财务事实页：检查应收、应付、发票、对账入口是否只表达当前收窄 finance facts，不误导完整核销、税控、查验、纳税申报、总账或多账簿能力。验收时至少覆盖各入口标题、主要动作、空状态、详情 / 筛选和 Workflow / Shipment 边界文案。
3. formal-shell：检查 `生产排程`、`生产异常`、`出货放行` 是否仍只表达待接入预览或 Workflow 协同入口，不误导真实创建、导出、删除、打印、回收站或领域事实写入。`出货放行` 只能表示 Workflow 协同处理，不能写成 `SHIPPED`。
4. 新能力入口：报表、数据导入、审计日志、客户配置 UI、系统配置 UI、备份恢复 UI 等不得直接进入左侧菜单；进入前必须先有设计、API / RBAC / schema / migration 影响评审、测试策略、数据真源和回滚路径。

## 4. 已完成检查项的边界

前后端菜单真源一致性检查当前只做后端内置菜单与当前前端默认导航对齐：

1. 后端 `global-dashboard` label 为 `工作台`，path 为 `/erp/dashboard`。
2. 后端 `task-board` path 为 `/erp/task-board`，label 为 `任务看板`，required permission 为 `workflow.task.read`。
3. 后端 `business-dashboard` label 为 `业务看板`，path 为 `/erp/business-dashboard`。
4. 后端 `exception-flow` path 为 `/erp/operations/exceptions`，label 为 `异常 / 阻塞闭环`，required permission 为 `workflow.task.read`。
5. 后端 `receivables` label 为 `应收管理`。
6. 后端 `invoices` label 为 `发票管理`。

已完成检查项不做：

- 不改 `web/src/erp/config/seedData.mjs`、`businessModules.mjs` 或 yoyoosun 客户菜单。
- 不新增路由、页面、权限码、API、schema、migration。
- 不把任务看板或异常闭环写成 Fact 入口。
- 不改变客户菜单显隐；当前客户菜单显隐以 `config/customers/yoyoosun/menuConfig.mjs` 为准。

## 5. 验收命令

前后端菜单一致性或文档再次变化时至少执行：

```bash
cd /Users/simon/projects/plush-toy-erp/server && go test ./internal/biz ./internal/data
cd /Users/simon/projects/plush-toy-erp/web && node --test src/erp/config/seedData.test.mjs src/erp/config/menuPermissions.test.mjs
```

如果触达前端菜单或客户配置，需要升级执行：

```bash
cd /Users/simon/projects/plush-toy-erp/web && pnpm lint && pnpm css && pnpm test
cd /Users/simon/projects/plush-toy-erp/web && STYLE_L1_SCENARIOS=erp-dashboard-desktop,erp-business-dashboard-desktop,dev-testing-dark-desktop pnpm style:l1
```

如果只改后端菜单 label / options 且不改页面样式，可以不跑浏览器视觉回归，但最终回复必须明确说明没有触达前端运行时页面。

## 6. 停止条件

遇到以下情况必须停止并重新评审，不继续顺手扩范围：

- 需要新增或修改权限码。
- 需要改路由、页面路径或旧路径兼容策略。
- 需要新增 API、schema、migration 或 seed。
- 需要改变 yoyoosun 菜单显隐。
- 需要把报表、数据导入、审计日志、客户配置 UI、备份恢复 UI 等候选能力变成正式入口。
- 发现 active workflow task 只能通过被隐藏或将删除的入口处理。

## 7. 剩余风险

- 本文是实施拆分清单，不是完整自动化守卫。后端菜单守卫已补，但前后端跨语言快照比对仍未建立。
- 页面内 tab / 动作吸收仍需按具体页面验收默认态、交互态、恢复态、暗色、移动视口和相邻区域；不要一次性把多个事实域塞进同一轮。

# yoyoosun 客户交付矩阵 / yoyoosun Delivery Matrix

本文只回答一个问题：永绅 yoyoosun 当前能看到、试用、验收或暂不承诺哪些能力。

它是客户交付台账，不是 Product Core 真源。产品通用能力成熟度看 `../../product/产品能力进度台账.md`，客户差异看 `客户差异台账.md`。后续新增客户时，新建 `docs/customers/<customer-key>/客户交付矩阵.md`，不要追加到本文。

## 状态口径

| 状态 | 含义 |
| --- | --- |
| 未开始 | 当前不对该客户承诺，也没有可用入口 |
| 内部可用 | 代码或本地验证已有基础，但不直接对客户承诺 |
| 可演示 | 可用于演示或内部说明，但仍需明确不包含内容 |
| 可试用 | 可交给客户试用，必须配套边界说明和本地 / 试用环境验收证据；不等同于目标环境发布、真实客户导入或客户签收 |
| 已交付 | 已完成客户侧验收或交付确认 |

## 交付矩阵

| 模块 / 能力 | 当前状态 | 客户可见承诺 | 验收证据 | 风险 / 下一步 |
| --- | --- | --- | --- | --- |
| 客户档案 | 可试用 | 可创建、编辑、查看、启停客户；当前只使用模拟客户数据 | V1 页面、菜单、RBAC、本地测试和试用模拟记录 | 真实客户数据导入当前不可执行；客户编码、简称、税号等字段仍需确认 |
| 供应商档案 | 可试用 | 可创建、编辑、查看、启停供应商；当前只使用模拟供应商数据 | V1 页面、菜单、RBAC、本地测试和试用模拟记录 | 是否区分材料供应商、加工厂等分类仍需确认 |
| 联系人 | 可试用 | 可在客户 / 供应商详情中维护主联系人和普通联系人 | MasterData usecase / repo / API 测试 | 手机、微信、联系人角色等字段不直接升级为 Product Core |
| 销售订单 | 可试用 | 可录入、提交、激活、关闭、取消销售订单；销售订单只是客户订单承诺 | V1 页面、菜单、RBAC、销售订单 usecase / repo / API 测试 | 不代表出货、库存、应收、发票或收付款已完成 |
| 销售订单明细 | 可试用 | 可维护订单行，并可从已维护 SKU 选择带出产品 / 单位 / 编号 / 名称 / 颜色快照；出货、预留和库存事实保持同一显式 SKU | 销售订单行 usecase / repo / API 测试、V1 销售订单页面、SKU 库存与出货来源一致性测试 | 不自动从订单颜色 / 尺寸 / 客户款号创建 SKU；财务不按 SKU 建立事实 grain |
| 采购订单 | 内部可用 | 可录入草稿、提交、审批、关闭和取消采购承诺，并打印采购合同；不承诺客户已验收 | schema、repo/usecase、JSON-RPC、RBAC、正式页面和本地测试 | 工程/BOM采购需求生成、在途/剩余量展示、应付/发票自动联动和客户流程验收未完成 |
| 采购入库 | 内部可用 | 可从采购订单创建入库草稿，逐行待检；首次来料不合格可登记退厂 / 补换处置且不伪造已入库退货流水；已过账入库支持取消冲正、来源退货、调整和应付入口 | 正式采购入库 / 质检页面、repo/usecase/API、幂等 / CAS / 事务测试 | 仍需客户确认收货、IQC、退厂和补换口径，并补目标环境证据 |
| BOM / 产品 / SKU | 内部可用 | BOM Version 已有内部 V1 页面和 API；产品基础信息 / 产品规格已接维护页面，销售订单行可选用 SKU，显式 SKU 已贯通批次、库存流水 / 余额、生产 / 委外、出货和预留 | BOM JSON-RPC / repo / 页面测试，Product / SKU API 与页面测试，SKU inventory PostgreSQL 隔离 / 并发测试 | BOM SKU 粒度、采购需求生成、导入受控创建 SKU、旧库存重分类和真实客户验收仍需评审 |
| 工序档案 | 可演示 | 可维护可委外 / 可内制 / 需质检工序；固定 `PLUSH_SEW_HAND_V1` 路线发布时冻结机裁、车缝、手工和包装工序快照，不把工序排序当路线设计器 | `processes` schema / API / RBAC / V1 页面、生产路线 / WIP 本地源码与定向合同 | 当前只落固定产品路线，不承诺客户自由编排路线；WIP migrations 尚未 apply，目标环境部署和客户岗位验收未完成 |
| 质量检验 | 内部可用 | 采购入库、委外回货、生产 WIP、出货前成品和客户退货按正式来源办理；支持首次来料退厂、委外返厂 / 返工、WIP 让步审批及无下游事实时的结果更正 | 正式页面、来源 usecase / API / RBAC、并发 / 下游依赖 / 结果更正合同 | 不逐件登记缺陷，不开放有下游事实的结果撤销；本地 migration 未对目标客户库 apply，也未完成岗位 UAT |
| 库存事实 | 内部可用 | 可查看库存流水、余额、批次、预留和可用量，并办理盘点、仓间调拨及带审批引用的受控人工调整；过账和冲正保持审计 | 正式库存台账、schema / repo / API、CAS / 幂等 / 防负数测试和 Style L1 | 不承诺完整 WMS；`NULL` 规格库存不自动归入 SKU，真实库位与客户规则仍需确认 |
| 出货放行 | 内部可用 | 可说明 workflow 中 `shipping_released` 只表示已放行 | Workflow usecase / repo / JSON-RPC 测试 | 不等于 `shipped`，不能写成已出库或已出货 |
| 出货单 / 出货事实 | 可试用 | 可办理来源出货、取消冲正，并从已出货行发起客户退货；RMA 审批 / 收货后进入独立 HOLD 批次和客户退货质检，不回写原出货批次 | 来源候选、并发出货、RMA 累计数量 / CAS / 隔离质检 / 冲正和正式页面测试 | 未发布目标环境；不代表物流签收、真实退货导入或客户验收完成 |
| 库存预留 | 内部可用 | 销售订单行可显式创建预留、释放并查看可用量；真实出货会在同一事务内消费同来源行和精确 SKU grain 的完整预留，页面与 API 不提供独立消费动作 | 销售订单来源预留页面 / API 定向测试、SKU 可用量与 PostgreSQL 预留 / 释放 / 出库竞争测试 | 新来源门禁尚未发布到目标环境；单条预留不支持部分消费，`NULL` 规格库存不与 SKU 共池，客户岗位尚未验收 |
| 财务应收 / 应付 / 收付款 | 内部可用 | 本地可从正式来源生成应收 / 应付 / 发票，并登记真实收付款、按同往来方同币种多单核销、冲正和红冲 | schema / repo / API / RBAC、核销与红冲事务测试、正式收付款页面 | V1 不含银行直连、支付审批、总账、税控；未发布目标环境，也未完成客户财务验收 |
| 生产事实 | 可演示 | 本地 Product Core 可在生产订单发布时冻结 BOM 物料需求，并从需求行领料、订单行完工、已过账完工发起返工；领料只选既有批次，完工可选既有或新批次 | production order / material requirement / production fact usecase、API、页面与定向测试 | yoyoosun 最小生产订单菜单和生产岗位能力已纳入当前预览配置；seed、active revision、部署和岗位验收未接或未证明；不包含自动 MRP、排产、成本归集、完整报工或返工完成自动入库 |
| 加工合同 / 委外订单 | 内部可用 | 可维护草稿、提交、确认、关闭、取消和打印；固定路线先车缝再手工，两段分别由生产经理决定本厂或外发。FABRIC 正常流只能整单外发，且 MATERIAL 合同行必须逐条精确覆盖显式 `FABRIC_PROCESSING` 冻结材料需求 | schema / migrations、repo / usecase / API / RBAC、正式页面、WIP allocation 与来源动作定向测试 | 合同确认不自动写发料、回货、质检、应付或付款；WIP migrations 尚未 apply，客户真实合同、岗位和目标环境未验收 |
| 委外事实 | 可演示 | 已把来源发料 / 回货与 WIP 绑定；委外回货不合格可受控返厂或创建返工 WIP，返厂写 OUT 并可冲正，已生效返工不允许伪取消 | 委外来源、处置 schema / repo / API、CAS / 幂等 / 库存 / 财务依赖测试 | 不包含自动结算或完整委外成本；未 apply 目标客户库、未发布、未完成岗位 UAT |
| 岗位任务端 | 可试用 | 可按角色进入岗位任务页，查看任务、上传/下载业务附件、记录现场留痕和异常；已上传证据不提供普通物理删除 | 单入口路由 smoke、试用账号 RBAC、本地模拟 workflow 与附件边界测试 | 不代表真实客户账号、手机拍照和扫码已签收；任务端不自动过账事实 |
| 私有化部署资料包 | 可演示 | yoyoosun 部署资料、runbook、checklist 和 evidence 模板可作为当前客户交付资料包候选 | `deployments/yoyoosun/`、部署 / release readiness 模板和受控命令入口 | 目标环境发布 evidence、部署 smoke、首次发布前备份 evidence、客户使用确认和真实数据导入仍需单独确认 |
| 客户配置包 | 内部可用；有限试用 | 已支持配置校验、发布、激活、回滚和 effective session；yoyoosun 可投影模块、角色、页面、动作、责任池、3 个字段可见 surface、采购 / 委外甲方打印默认值和批准的流程 manifest；当前预览配置已同步销售、质量、财务、生产来源办理所需的最小菜单和岗位能力，质量岗位的产品只读能力不开放产品维护菜单 | customer config biz/repo/JSON-RPC 测试、effective session 前端测试、`config/customers/yoyoosun/*`、customer package 边界脚本 | 目标环境 authenticated active revision / rollback readback 未完成；字段编号 / 导入、businessFlows / stateMachines / processPolicies 仍按 preview / blocked 状态；不代表 SaaS tenant、动态插件或真实导入批准 |
| 行业默认模板候选 | 内部可用 | 可作为内部复用候选，不直接对 yoyoosun 承诺为已交付能力 | `config/industry-templates/plush/*` 和边界脚本 | 单客户样本不能直接升级成行业默认或 Product Core |
| 多客户私有化复制包模板 | 内部可用 | 可作为内部新增客户包模板候选，不代表真实第二客户已交付 | `config/private-deployment-template/*` 和边界脚本 | 不新增 SaaS、多租户、license、billing 或 runtime tenant |
| SaaS / 多租户 | 未开始 | 当前不对 yoyoosun 承诺 SaaS、多租户、license 或 billing | `../../product/软件即服务进入门禁评审.md` | 继续按单客户私有化部署和客户配置包管理 |
| 审计 / 附件 / 集成增强 | 内部可用 | 可查看受权限控制的审计页并在已支持对象上管理业务附件；不承诺完整报表、扫码或外部系统集成 | runtime audit / business attachment 页面、API、RBAC 和测试 | 模块级审计覆盖、对象存储运维、扫码与外部集成仍需评审和客户验收 |

本表新增的来源办理能力只记录当前 Product Core 本地实现与定向验证。yoyoosun 当前预览配置包已同步来源办理所需的最小菜单和岗位能力投影；客户私有资料、流程 manifest、seed 和部署资料未改，也没有 active revision、目标环境发布、真实客户数据导入或客户签收证据，因此各行状态不升级为“已交付”。

## 试用 / 手动回归数据口径

手动回归先使用 `scripts/qa/manual-regression-data-plan.mjs` 汇总 Product Core 中性 seed、永绅 preview fixture、仍受支持的试用模拟入口、旧业务事实只读计划和岗位任务模拟入口。当前永绅 preview fixture 只证明候选数据覆盖；旧业务事实通用 apply 已退役，不能据此声称生产、委外、库存或财务事实已写入。

这些数据只服务本地 / 试用环境回归和培训演示；它们不是客户真实导入、不是目标环境发布证据，也不能替代真实 import approval、备份、恢复计划或客户签收。

## 维护规则

- 客户是否能试用、是否能交付承诺、是否被阻塞发生变化时，更新本文。
- 更新时只写业务模块名，不使用 CAP、DELTA、MATRIX、EVIDENCE 等内部编号作为主索引。
- “已交付”必须有客户侧验收或交付确认；内部模拟、目标环境 smoke、本地测试只能支撑“内部可用 / 可演示 / 可试用”。
- 客户交付状态不能反向定义 Product Core；客户差异是否进入产品内核，仍看 `客户差异台账.md` 和产品评审。

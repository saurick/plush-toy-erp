# plush-toy-erp progress

本文件只保留当前活跃事项、最近完成记录和归档索引；历史流水已归档到 `docs/archive/`。`progress.md` 是过程交接线索，不是正式需求、数据模型或部署真源。

## 归档索引

- `docs/archive/progress-2026-06-28-before-runtime-manifest.md`：归档 2026-06-28 之前至客户配置版本运行时最小闭环、`/__dev` 页面治理等过程记录。
- `docs/archive/progress-2026-06-29-before-release-evidence-hardening.md`：归档 2026-06-28 客户配置 runtime manifest、发布执行器、dev-only 页面治理、Workflow action 合同、导入与发布证据前置门禁等过程记录。
- `docs/archive/progress-2026-06-29-before-target-evidence-binding.md`：归档 2026-06-29 release evidence、真实导入 recovery plan、文档清单、备份恢复和回滚演练门禁早期硬化过程记录。
- `docs/archive/progress-2026-06-29-before-priority-audit-closeout.md`：归档 2026-06-29 target evidence binding 之后到 release evidence runner 脱敏报告与 URL 前置拦截的过程记录。
- `docs/archive/progress-2026-06-29-before-process-runtime-minimum.md`：归档 2026-06-29 adminProfileSync 菜单投影文档纠偏、P2 explain / entitlement / break-glass 中段过程记录。
- `docs/archive/progress-2026-06-29-before-linked-task-idempotency.md`：归档 2026-06-29 P3 ProcessRuntime expected_version 守卫之前至 linked task 幂等闭环前的过程记录。
- `docs/archive/progress-2026-06-30-before-inventory-post-inbound.md`：归档 2026-06-30 P3 / P4 前段、adminProfileSync 菜单投影文档多轮纠偏、sales_order_acceptance、material_supply definition evidence、`purchase_receipt.create` 和 `quality_inspection.decide` 领域命令 handler 过程记录。
- `docs/archive/progress-2026-06-30-before-p5-release-input-checklist.md`：归档 2026-06-30 P4-2 / P4-3、adminProfileSync 文档纠偏和进入 P5 release closeout 输入清单前的过程记录。
- `docs/archive/progress-2026-06-30-before-p5-input-checklist-followup.md`：归档 2026-06-30 P5 release closeout report-only、release evidence hardening、菜单投影纠偏和 input checklist JSON 自定义路径只读合同之前的过程记录。
- `docs/archive/progress-2026-06-30-before-outsourcing-order-api-gate.md`：归档 2026-06-30 P5 input checklist、adminProfileSync 文档纠偏、purchase / quality / shipment / stock reservation / production / outsourcing fact / sales / purchase order moduleStates 门禁等过程记录。
- `docs/archive/progress-2026-07-01-before-action-projection-l1.md`：归档 2026-06-30 outsourcing order API 门禁之后至 2026-07-01 动作投影浏览器回归扩展的过程记录。
- `docs/archive/progress-2026-07-01-before-progress-archive-and-next-no-write.md`：归档 2026-07-01 动作投影浏览器回归扩展之后至 Purchase Receipt Browser E2E 输入模板的过程记录。
- `docs/archive/progress-2026-07-01-before-readme-preflight-sync-closeout.md`：归档 2026-07-01 Purchase Receipt Browser E2E 输入模板之后至 Web README mobile preflight 口径同步的过程记录。
- `docs/archive/progress-2026-07-01-before-bom-inline-editor.md`：归档 2026-07-01 Web README preflight 口径同步之后至 BOM 明细弹窗内联编辑前的过程记录。
- `docs/archive/progress-2026-07-02-before-trial-real-smoke.md`：归档 2026-07-01 BOM 明细内联编辑前至 2026-07-01 qa:full 生命周期收口的过程记录。
- `docs/archive/progress-2026-07-02-before-effective-session-diagnostic-smoke.md`：归档 2026-07-02 试用真实 smoke 前归档说明至旧兼容接口 / 旧 mobile 运行时清理的过程记录。
- `docs/archive/progress-2026-07-02-before-next-no-write-closure.md`：归档 2026-07-02 effective session 诊断 smoke 前归档说明至移动端 workflow smoke 文档边界守卫的过程记录。
- `docs/archive/progress-2026-07-02-before-dashboard-source-label.md`：归档 2026-07-02 __dev/testing 错误提示与技术字段预设补齐至 mobile-workflow-smoke 测试入口补齐的过程记录。
- `docs/archive/progress-2026-07-02-before-sku-source-field-linkage.md`：归档 2026-07-02 Dashboard 来源标签收口之后至 yoyoosun asset 验证命令防误判收口的过程记录。
- `docs/archive/progress-2026-07-02-before-next-local-closure.md`：归档 2026-07-02 SKU 来源字段链路收口之后至移动端任务端 no-write preflight 复核的过程记录。
- `docs/archive/progress-2026-07-02-before-post-fast-local-closure.md`：归档 2026-07-02 客户配置控制台 no-write 读回预检之后至 L1 与移动端登录回跳本地回归复核的过程记录。
- `docs/archive/progress-2026-07-02-before-simulated-report-only-closure.md`：归档 2026-07-02 下一本地闭环前归档说明至 active module state / Workflow 写门禁复核的过程记录。
- `docs/archive/progress-2026-07-02-before-review-driven-config-contract.md`：归档 2026-07-02 模拟 report-only 后至测试业务数据隔离守卫收口的过程记录。
- `docs/archive/progress-2026-07-02-before-p5-release-candidate.md`：归档 2026-07-02 P5 本地前置验证、server 镜像构建守卫、133 baseline 与 fast 门禁复核后的过程记录。
- `docs/archive/progress-2026-07-03-before-dev-customer-visible-labels.md`：归档 2026-07-02 P5 release candidate 前归档说明至 2026-07-03 客户配置 compiled catalog 可见标签收口的过程记录。
- `docs/archive/progress-2026-07-03-before-collaboration-submit-guard.md`：归档 2026-07-03 客户配置可见标签前归档说明至移动任务端岗位标签投影收口的过程记录。

## 当前活跃事项

- 多甲方角色能力流程编排以 `docs/product/多甲方角色能力流程编排优先级.md` 和 `node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 的 `implementationOrder` 为本地优先级入口；GPT/reference 资料只作输入，当前真源仍回到代码、migration、测试和正式文档。
- 当前审计显示 P0-P4 本地证据为 ready；P5 测试部署 / 导入 / 第二客户验证仍为 `target-evidence-required`，第一条 blocked release action 是 `immutable-version`。
- P5 当前只允许 report-only、input template、preflight 和 checklist 准备；没有真实目标环境、镜像 digest、migration 前后版本、backup id、目标管理员凭据和用户确认时，不写 `deployments/**/evidence/**`，不执行 `--execute`，不把本地 ready 写成目标 release evidence。
- 真实客户数据导入、正式生产发布、目标环境 smoke、目标 migration、备份恢复、回滚 / 前向修复演练、客户配置激活和签收仍未执行，不能被本地 dry-run、manifest 编译、status、gate、audit、runner report、input template 或 preflight 替代。
- 当前可继续推进的本地闭环优先落在：试用账号 / 菜单 / 岗位任务端前置检查、客户配置控制台 no-write 诊断、字段链路守卫、错误提示和 README / `__dev/testing` 入口口径一致性。

## 2026-07-03 本页协同提交 guard 前归档说明

- 完成：归档 `progress.md` 超阈值历史记录到 `docs/archive/progress-2026-07-03-before-collaboration-submit-guard.md`，根入口继续保留归档索引、当前活跃事项和本轮最新本地闭环。归档内容覆盖 2026-07-03 客户配置可见标签前归档说明至移动任务端岗位标签投影收口，当前目标仍保持 review findings 驱动，不把 release evidence、真实生产部署、真实客户导入、签收、备份恢复或回滚演练升为本轮主任务。
- 验证：归档前 `progress.md` 已超过 80KB；归档文件保留完整旧流水，后续本轮完成后继续运行 docs inventory 和 `git diff --check`。
- 下一步：继续按 review findings 推进本地可验证切片，优先处理 Workflow action / RBAC / usecase parity、客户配置规则 / 策略 / 扩展点、任务端真实造数 smoke、字段链路和打印字段剩余缺口。
- 阻塞/风险：归档只整理过程记录，不改变 runtime、schema、RBAC、Workflow / Fact usecase、真实账号登录、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练；当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 本页协同提交前后端 explain guard 收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理共享 `CollaborationTaskPanel` 提交路径只依赖抽屉加载态 / allowedModes、没有在提交瞬间统一跑后端 `explain_action_access` 的缺口。现在本页协同抽屉在调用页面传入的 complete / block / reject / urge handler 前，会通过 `verifyWorkflowTaskActionAccessBeforeSubmit` 重新提交 `task_id + action_key` 给后端 explain，并把当前 reason 一并传入；缺少原因、后端拒绝或 explain 失败时只展示中文提示，不再委托外层 handler。未改后端 WorkflowUsecase、JSON-RPC 合同、RBAC 权限码、事实落账、真实账号登录或 release evidence。
- 验证：`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs web/src/erp/utils/workflowTaskActionAccess.test.mjs web/src/erp/utils/businessCollaborationTasks.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过，69 个断言，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/components/business-list/CollaborationTaskPanel.jsx src/erp/utils/workflowTaskActionSubmitGuard.mjs src/erp/utils/workflowTaskActionSubmitGuard.test.mjs` 通过；`STYLE_L1_SCENARIOS=erp-effective-session-action-projection-business-pages /usr/local/bin/pnpm --dir web style:l1` 通过 1 个业务页 action 投影场景；`go test ./internal/biz ./internal/service -run 'Workflow|workflow'` 通过；`git diff --check` 通过。
- 下一步：继续按 review findings 推进下一个本地可验证闭环，优先处理任务端真实造数 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实浏览器带值回归和字段链路缺值回补。
- 阻塞/风险：本轮只证明共享前端协同面板提交前 guard、Workflow action guard 单测、业务协同模型、用户可见 raw 字段守卫、一个业务页 L1 场景和后端 Workflow 定向测试；不证明真实账号登录、真实浏览器任务动作、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 销售订单接单流程节点版本合同补强

- 完成：继续按 review findings 复核 Workflow action / RBAC / usecase parity，补强销售订单接单流程 start -> execute 的 JSON-RPC 合同测试。`start_sales_order_acceptance_process` 返回的 `started_node.version` 现在被定向断言为正数，避免前端 `submitSalesOrderAcceptanceProcess` 后续依赖 `expected_version` 的合同被改坏；同时保留同一幂等键重试返回同一流程 / 节点、不同幂等键重复启动同一销售订单被拒绝的测试口径。实现侧已确认 `processNodeInstanceToMap` 原本返回 `version`，本轮只补测试守住前后端合同，不改 schema、RBAC、ProcessRuntime 语义或 Workflow / Fact 边界。
- 验证：`go test ./internal/service -run 'CustomerConfigJSONRPC(StartSalesOrderAcceptanceProcess|ExecuteSalesOrderAcceptanceSubmit)'` 通过；`go test ./internal/service -run 'Workflow|CustomerConfigJSONRPC(StartSalesOrderAcceptanceProcess|ExecuteSalesOrderAcceptanceSubmit)'` 通过；`node --test web/src/erp/api/customerConfigApi.test.mjs scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs web/src/erp/utils/workflowTaskActionAccess.test.mjs` 通过 35 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续找行为层真实缺口，优先看真实浏览器 / mock 运行下销售订单提交流程、任务端动作和客户配置 effective session 投影是否能形成端到端闭环，而不是只补静态断言。
- 阻塞/风险：本轮只补本地 JSON-RPC / 前端合同测试，不证明真实账号登录、真实页面点击销售订单提交、真实后端写入完整接单链、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练；当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 移动任务端 IQC 结果可见标签收口

- 完成：继续按 review findings 处理用户可见层 raw key 泄露缺口。移动任务详情事实行的 `IQC 结果` 不再直接展示 `payload.qc_result` 原始值，已收口为中文业务标签：`pass / passed / qualified` 显示“合格”，`fail / failed / reject / rejected` 显示“不合格”，`rework` 显示“返工”，`pending` 显示“待检”；未知值只显示“质检已记录”，避免把客户或后端内部 key 透给一线移动端用户。未改后端质检事实、WorkflowUsecase、JSON-RPC 合同、RBAC、真实写入或打印链路。
- 验证：`node --test web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 64 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/mobile/utils/mobileRoleTaskModel.mjs src/erp/mobile/utils/mobileRoleTaskModel.test.mjs src/erp/utils/mobileTaskView.mjs src/erp/utils/mobileTaskView.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=mobile-tasks-dark /usr/local/bin/pnpm --dir web style:l1` 通过 1 个移动任务端场景。
- 下一步：继续按 review findings 推进下一个本地可验证闭环，优先补任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印字段 mapper / 采集 / 导出 / 历史缺值链路和菜单投影一致性。
- 阻塞/风险：本轮只证明移动端模型、可见技术字段守卫和一个移动端 L1 场景；不证明真实账号登录、真实浏览器任务动作、真实后端写入、质检事实闭环、打印模板闭环、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 加工合同打印预览字段链路收口

- 完成：继续按 review findings 处理打印字段链路缺值。`/erp/print-center/processing-contract` 现在对正式模板详情页也渲染只读纸面预览，不再只显示“预览入口”；`PrintTemplateRenderer` 的加工合同预览改为读取加工合同草稿真源字段 `buyerSigner / buyerSignDateText / supplierSigner / supplierSignDateText`，并从明细快照计算合计数量和金额，不再依赖不存在的 `totalQuantity / totalAmount` 或采购合同专用 `signDateText`。打印中心右侧摘要也兼容加工合同的 `buyerSignDateText`。未新增销售订单打印模板，未改后端 PDF 生成、业务事实、客户配置发布或 Excel 母版回写。
- 验证：`node --test web/src/erp/config/printTemplates.test.mjs web/src/erp/data/processingContractTemplate.test.mjs web/src/erp/utils/materialPurchaseContractEditor.test.mjs web/src/erp/utils/printPdf.test.mjs` 通过 48 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/components/print/PrintTemplateRenderer.jsx src/erp/pages/PrintTemplatePreviewPage.jsx src/erp/pages/PrintCenterPage.jsx src/erp/config/printTemplates.test.mjs scripts/style-l1/scenarios.mjs` 通过；`STYLE_L1_SCENARIOS=print-preview-processing /usr/local/bin/pnpm --dir web style:l1` 通过 1 个加工合同预览场景；相邻回归 `STYLE_L1_SCENARIOS=print-center-desktop /usr/local/bin/pnpm --dir web style:l1` 和 `STYLE_L1_SCENARIOS=print-preview-material /usr/local/bin/pnpm --dir web style:l1` 串行通过。曾尝试并行跑两个 L1 场景，因共用临时端口 `4173` 产生端口冲突，确认端口无残留后已串行重跑通过。
- 下一步：继续按 review findings 推进打印真实业务带值入口、客户配置打印模板默认值预检、导出 / 列表字段策略和历史缺值回补；销售订单打印仍需 mapper / 表单 / 导出 / 打印 / 历史缺值测试完整评审后另做，不能用本轮加工合同修复替代。
- 阻塞/风险：本轮只证明两套正式模板的本地静态预览、打印模板合同测试、一个加工合同预览浏览器场景和两个相邻 L1 场景；不证明真实采购 / 委外订单带值打开工作台、服务端 PDF 坐标审阅、Excel 母版回写、销售订单打印、真实账号登录、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 客户配置打印模板字段可见投影收口

- 完成：继续按 review findings 处理 `__dev/customer-config` 的用户可见 raw key 缺口。客户配置控制台的打印模板字段面板不再把 `material-purchase-contract` / `processing-contract` 作为页面标签直出，改为“模板锚点已登记”的业务可读提示；打印默认方信息面板不再展示 `buyerCompany / buyerContact / buyerPhone / buyerAddress / buyerSigner` 等默认字段 raw key，改为“N 个默认方字段已登记”。底层 summary 仍保留机器 key 供 manifest / 测试合同使用，页面和 L1 只检查业务可读投影。未改客户配置 schema、发布 / 激活 usecase、正式打印工作台接入、销售订单打印模板或真实客户导入。
- 验证：`node --test web/src/erp/config/devCustomerConfig.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/multi-client-role-workflow-priority-audit.test.mjs web/src/erp/config/printTemplates.test.mjs` 通过 41 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/config/devCustomerConfig.mjs src/erp/config/devCustomerConfig.test.mjs src/erp/pages/DevCustomerConfigPage.jsx scripts/style-l1/scenarios.mjs` 通过；`STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个客户配置控制台场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。曾把打印默认方 L1 断言放在切到“菜单字段 / Assets”之后导致面板不存在，已改回“包预检”视图内验证并重跑通过。
- 下一步：继续按 review findings 推进客户配置规则 / 策略 / 扩展点运行时边界、真实造数任务端动作 smoke、菜单投影一致性和打印真实业务带值入口；销售订单打印仍需完整 mapper / 表单 / 导出 / 打印 / 历史缺值测试后再评审。
- 阻塞/风险：本轮只证明开发态客户配置控制台的打印模板字段和默认方信息不直出 raw key，以及对应单测 / L1；不证明正式打印工作台已经消费客户配置默认值、不证明真实账号登录、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 移动任务端状态标签 fallback 收口

- 完成：继续按 review findings 处理岗位任务端可用体验缺口。移动端任务模型新增 `resolveMobileTaskStatusLabel`，列表已办标签、详情最近动态和详情事实行状态都改用同一中文状态 fallback；当上游只传 `task_status_key`、没有 `task_status_label` 时仍显示“可执行 / 未知状态”等业务可读状态，不出现空标签，也不把未知 `task_status_key` 直出给一线用户。未改后端 WorkflowUsecase、JSON-RPC 合同、RBAC、任务写入动作、真实登录脚本或业务事实。
- 验证：`node --test web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 78 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/mobile/utils/mobileRoleTaskModel.mjs src/erp/mobile/utils/mobileRoleTaskModel.test.mjs src/erp/mobile/components/MobileTaskDetailScreen.jsx src/erp/mobile/components/MobileTaskListScreen.jsx` 通过；`STYLE_L1_SCENARIOS=mobile-tasks-dark /usr/local/bin/pnpm --dir web style:l1` 通过 1 个移动任务端场景。
- 下一步：继续按 review findings 推进任务端真实造数 / 真实动作 smoke、Workflow action / RBAC / usecase parity、菜单投影一致性、客户配置规则 / 策略 / 扩展点运行时边界和打印真实业务带值入口。
- 阻塞/风险：本轮只证明移动端本地模型、任务端 no-write smoke 边界、用户可见技术字段守卫和一个移动端 L1 场景；不证明真实账号登录、真实浏览器任务动作、真实后端写入、Workflow task done 触发事实、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 事实页动作投影直连守卫收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理正式事实页写动作门禁可被页面私有 wrapper 弱化的缺口。`ShipmentsPage`、`V1PurchaseReceiptsPage`、`V1QualityInspectionsPage` 的 create / post / ship / cancel / update 权限判断改为直接调用共享 `hasActionPermission`，不再保留页面局部 `hasPermission` 包装；`workflow-ui-action-boundary` 守卫同步要求事实页直接消费共享 action projection，并禁止重新引入页面私有 wrapper。该改动只收口前端投影门禁和静态防回归，不改变后端 RBAC、JSON-RPC、WorkflowUsecase、Fact usecase、状态机或真实写入。
- 验证：`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/masterDataOrderView.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 98 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`node --check scripts/qa/workflow-ui-action-boundary.test.mjs` 通过；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/V1PurchaseReceiptsPage.jsx src/erp/pages/V1QualityInspectionsPage.jsx src/erp/pages/ShipmentsPage.jsx` 通过；`STYLE_L1_SCENARIOS=business-formal-module-shells-desktop,business-formal-shipping-release-readonly-actions-desktop,purchase-receipts-table-control-columns-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 3 个场景。曾尝试使用已失效的 `business-v1-quality-inspections` / `business-v1-shipments` 场景名，runner 拒绝并列出当前有效场景，已改用现有正式业务页场景重跑通过。
- 下一步：继续按 review findings 推进菜单权限投影、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明三个事实页前端写动作使用共享 action projection、静态守卫和相关 L1 场景；不证明真实账号登录、真实浏览器点击写动作、后端 RBAC 拒绝路径、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 权限中心角色 key 可见 fallback 收口

- 完成：继续按 review findings 处理角色 / 菜单投影的用户可见 raw key 缺口。权限中心现在通过 `getRoleVisibleName` 统一把角色表格、角色选择项、角色模板卡片和角色详情标题收口到 `role.name` 或共享 `getRoleDisplayName`；当后端角色缺少中文名时只显示“已配置角色”，不再把 `sales / warehouse / quality` 等 `role_key` 作为用户可见名称 fallback。内部 `role_key` 仍保留为选择值、React key、提交参数和筛选条件，不改变后端 RBAC、角色模板 schema、权限码或 effective session 投影合同。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/roleKeys.test.mjs web/src/erp/config/menuPermissions.test.mjs web/src/erp/utils/adminProfileSync.test.mjs` 通过 64 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/PermissionCenterPage.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs src/erp/utils/roleKeys.mjs src/erp/utils/roleKeys.test.mjs` 通过；`STYLE_L1_SCENARIOS=permission-center-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个权限中心场景。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 的实际页面入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明权限中心角色名称 fallback、角色 helper、菜单 / effective session 定向测试和一个权限中心 L1 场景；不证明真实账号登录、真实后端 RBAC 拒绝路径、角色权限真实保存、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 订单状态 raw key fallback 收口

- 完成：继续按 review findings 处理正式订单列表的用户可见 raw key 缺口。`masterDataOrderView.statusText` 现在对未知非空状态统一返回“业务状态”，空状态仍返回 `-`；销售订单、采购订单、委外订单列表和导出继续复用该 helper，因此未知 `lifecycle_status / line_status` 不再直接显示底层枚举 key。`userVisibleTechnicalFields` 守卫同步覆盖订单列和 `statusText` 真源，防止 `labels[key] || key` 回退重新出现。未改后端状态机、生命周期动作、JSON-RPC、RBAC、订单保存逻辑或真实数据。
- 验证：`node --test web/src/erp/utils/masterDataOrderView.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/components/operational-facts/OperationalFactForms.test.mjs` 通过 82 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/utils/masterDataOrderView.mjs src/erp/utils/masterDataOrderView.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs src/erp/components/sales-orders/salesOrderColumns.jsx src/erp/components/purchase-orders/purchaseOrderColumns.jsx src/erp/components/outsourcing-orders/outsourcingOrderColumns.jsx` 通过；`STYLE_L1_SCENARIOS=sales-order-acceptance-submit-action-desktop,purchase-order-date-filter-desktop,business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 3 个场景。首次新增静态守卫时只扫描列文件、未扫描 `masterDataOrderView.mjs`，已修正守卫范围后重跑通过。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明前端订单 read model 的状态展示 fallback、相关静态守卫和 3 个本地 L1 场景；不证明真实账号登录、真实后端状态迁移、历史数据库异常状态回补、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 审计日志变化值 raw key 投影收口

- 完成：继续按 review findings 处理后台用户可见层 raw key / raw id 泄露缺口。`AuditLogsPage` 的 before/after 变化摘要现在对 `id / *_id / *_key / owner_role_key / task_status_key / source_type / payload` 等技术字段值只显示“已记录”，不再把底层枚举、内部引用或 payload 值直接转成用户可见字符串；已知业务字段如账号状态、手机号、角色、权限仍按既有业务口径展示。未改后端审计事件结构、JSON-RPC、RBAC、账号管理 usecase 或真实审计写入。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs web/src/common/utils/errorMessage.test.mjs` 通过 50 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/AuditLogsPage.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=system-audit-logs-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个审计日志场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明审计日志前端变化摘要投影、用户可见技术字段守卫、错误提示守卫和一个审计日志 L1 场景；不证明后端审计写入语义、真实账号操作审计、真实登录、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 审计日志下一步文案技术词收口

- 完成：继续按 review findings 处理后台审计日志用户可见文案的技术口径泄漏。`AuditLogsPage` 中“下一步”建议不再显示 `before/after role_keys`、`before/after permission_keys`、`after.disabled` 或 `BOOTSTRAP_ADMIN_ONCE`，改为“角色变更前后差异”“权限变更前后差异”“账号当前状态”“一次性管理员初始化”等业务可读口径；字段映射表仍保留 `role_keys / permission_keys` 作为内部翻译锚点，不改变审计 payload 解析。未改后端审计写入、账号管理 usecase、RBAC、JSON-RPC 或真实审计事件结构。
- 验证：首次新增守卫时把内部 `after.disabled` 判断也判为失败，已收窄为只拦截 `next:` 用户可见文案；修正后 `node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs web/src/common/utils/errorMessage.test.mjs` 通过 50 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/AuditLogsPage.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=system-audit-logs-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个审计日志场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明审计日志前端建议文案、用户可见技术字段守卫、错误提示守卫和一个审计日志 L1 场景；不证明后端审计写入语义、真实账号操作审计、真实登录、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 协同任务责任岗位投影收口

- 完成：继续按 review findings 处理 Workflow / 业务页责任岗位投影残缺。`CollaborationTaskPanel`、`WorkflowTaskActionDrawer` 和 `WorkflowBusinessModulePage` 的用户可见责任岗位统一改用 `getWorkflowTaskOwnerRoleLabel`；采购订单、加工合同和 Formal 业务页不再向协同面板传入页面级 `roleLabelMap`；采购 / 加工订单页面配置中移除了未使用的 `*_WORKFLOW_ROLE_LABELS` 旧扩展点。该改动只收口前端责任岗位显示真源，不改变后端 `owner_role_key`、RBAC、JSON-RPC、WorkflowUsecase、任务动作提交或真实写入。
- 验证：首次运行 `node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs web/src/erp/utils/businessCollaborationTasks.test.mjs web/src/erp/utils/workflowTaskActionAccess.test.mjs` 时发现 `workflow-ui-action-boundary` 仍要求旧的 `roleLabels.get(...) || getWorkflowTaskOwnerRoleLabel(...)` 口径，已同步收紧为只允许共享 helper；修正后同命令通过 77 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/components/business-list/CollaborationTaskPanel.jsx src/erp/components/workflow/WorkflowTaskActionDrawer.jsx src/erp/pages/WorkflowBusinessModulePage.jsx src/erp/pages/FormalBusinessModulePage.jsx src/erp/pages/V1PurchaseOrdersPage.jsx src/erp/pages/V1OutsourcingOrdersPage.jsx src/erp/components/purchase-orders/purchaseOrderPageConfig.mjs src/erp/components/outsourcing-orders/outsourcingOrderPageConfig.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=erp-effective-session-action-projection-business-pages,business-collaboration-purchase-selected-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 2 个场景。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明本地前端责任岗位投影、Workflow action 静态边界和 2 个本地 L1 场景；不证明真实账号登录、真实浏览器任务动作、后端 RBAC 拒绝路径、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 库存台账批次状态 raw key fallback 收口

- 完成：继续按 review findings 处理用户可见 raw key 投影缺口。`V1InventoryLedgerPage` 的库存批次选中标签不再使用 `LOT_STATUS_LABELS[row.status] || row.status`，改为复用 `lotStatusText(row.status)`；未知非空批次状态显示“批次状态”，空值显示 `-`，避免底层批次状态 key 出现在选中操作条。`userVisibleTechnicalFields` 守卫同步补上 `LOT_STATUS_LABELS[row.status] || row.status` 禁止项，覆盖此前静态测试没有拦住的写法。未改库存 repo、JSON-RPC、事实表、批次状态机、库存写入或真实数据。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/masterDataOrderView.test.mjs` 通过 81 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/V1InventoryLedgerPage.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；首次尝试 `STYLE_L1_SCENARIOS=inventory-ledger-source-filter-desktop /usr/local/bin/pnpm --dir web style:l1` 时发现该场景名不存在，runner 拒绝并列出有效场景，已改用现有有效场景；`STYLE_L1_SCENARIOS=business-menu-groups-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个正式业务壳场景并覆盖库存台账批次 / 流水视图。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明库存台账前端选中标签、用户可见技术字段守卫和本地 L1 场景；不证明真实库存数据异常状态迁移、真实后端写入、批次状态变更 usecase、真实账号登录、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 正式业务边界文案去工程化收口

- 完成：继续按 review findings 处理正式业务 / 后台可见层的工程术语泄漏。权限中心、Formal 业务壳、业务事实页、采购入库、委外订单、Workflow 协同页、出货弹窗和正式模块配置中的 `RBAC / API / usecase / finance_facts / RECEIVABLE / PAYABLE / INVOICE / RECONCILIATION / Source Document / Operational Fact` 可见文案改为“角色权限模型 / 后台接口 / 权限控制 / 后端业务规则 / 后端财务规则 / 后端采购入库规则 / 业务事实 / 源单”等业务可读口径；dev-only 治理页和内部变量、内部配置 key 不在本轮修改范围。`userVisibleTechnicalFields` 新增正式业务边界文案守卫，`businessFormalScenarios` 同步改为断言中文文案并反向断言旧 raw 文案缺席。未改后端 RBAC、JSON-RPC、WorkflowUsecase、Fact usecase、schema、权限码或真实数据。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/config/menuPermissions.test.mjs` 通过 52 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/PermissionCenterPage.jsx src/erp/pages/FormalBusinessModulePage.jsx src/erp/pages/V1OperationalFactPage.jsx src/erp/pages/OperationalFactsPage.jsx src/erp/pages/V1OutsourcingOrdersPage.jsx src/erp/pages/V1PurchaseReceiptsPage.jsx src/erp/pages/WorkflowBusinessModulePage.jsx src/erp/config/businessModules.mjs src/erp/components/operational-facts/operationalFactPageConfig.mjs src/erp/components/shipments/ShipmentBusinessModal.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`node --check web/scripts/style-l1/businessFormalScenarios.mjs` 通过；旧词 `rg` 扫描目标正式文件无命中。首次跑 L1 时 `business-formal-module-shells-desktop` 仍在找旧 `Source Document：加工合同`，更新场景后又发现仍在找 `finance_facts / RECEIVABLE`，已同步为中文断言并反向断言旧 raw 文案缺席；随后 `STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景，`STYLE_L1_SCENARIOS=permission-center-desktop,purchase-receipts-table-control-columns-desktop,purchase-receipt-add-item-inline-draft-desktop,shipment-date-filter-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 4 个场景。L1 仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明正式业务 / 后台前端可见边界文案、静态守卫和本地 L1 场景；不证明真实账号登录、真实后端 RBAC 拒绝路径、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 正式业务壳和来料质检表名 / 状态 key 投影收口

- 完成：继续按 review findings 处理用户可见 raw key 泄漏。Formal 业务壳的“主事实 / 真源、来源表、领域表待评审、business_records”等用户可见字段改为“当前业务范围、关联业务、后端规则待接入、旧业务记录壳”等业务口径，正式模块配置中的表名式入口、英文状态机说明和 `warehouse_inbound done / purchase_receipt posted / REVERSAL` 等工程文案改为业务可读说明；来料质检页头不再展示 `quality_inspections` 或 `SUBMITTED / HOLD / ACTIVE / REJECTED`，改为“质检判定、批次冻结 / 可用 / 不可用”。内部 `primaryEntity / factSource / sourceRefs`、API 返回字段和测试 mock 仍保留机器 key，不改变 schema、后端 usecase、JSON-RPC、RBAC 或质检事实写入。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/config/menuPermissions.test.mjs` 通过 53 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/FormalBusinessModulePage.jsx src/erp/pages/V1QualityInspectionsPage.jsx src/erp/config/businessModules.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/style-l1/businessFormalScenarios.mjs` 通过；`STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个正式业务壳场景，覆盖库存、来料质检、委外、财务等相邻可见文案。首次 L1 发现来料质检页仍可见 `quality_inspections`，已修正页头并补静态守卫后重跑通过；L1 仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明 Formal 业务壳、正式模块配置和来料质检页头的前端用户可见投影、静态守卫和本地 L1 场景；不证明真实账号登录、真实后端 RBAC 拒绝路径、真实后端写入、质检事实闭环、打印模板闭环、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 出货 / 入库正式页头 raw key 收口

- 完成：继续按 review findings 处理正式业务页头的用户可见 raw key 泄漏。`ShipmentsPage` 页头不再展示 `shipments / shipment_items`、`SHIPPED` 或 `inventory_txns.OUT`，改为“出货单据 / 出货明细 / 确认出货 / 库存出库事实”；`V1PurchaseReceiptsPage` 页头不再展示 `purchase_receipts / purchase_receipt_items`、`Workflow：协同入库`、`PurchaseReceipt：入库事实` 或 `inventory_txns`，改为“采购入库草稿 / 入库明细 / 协同任务 / 入库单 / 库存流水”。静态守卫新增两个正式页头专项断言，只拦截用户可见描述和标签，不误伤内部 `primaryEntity`、API 字段、状态判断或测试 mock。未改后端出货 / 入库 usecase、JSON-RPC、RBAC、库存事实写入或 Workflow / Fact 边界。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/config/menuPermissions.test.mjs` 通过 54 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/ShipmentsPage.jsx src/erp/pages/V1PurchaseReceiptsPage.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；页头旧词 `rg` 扫描无命中；`STYLE_L1_SCENARIOS=business-formal-module-shells-desktop,purchase-receipts-table-control-columns-desktop,shipment-date-filter-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 3 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。首次把禁词放进组合守卫时误伤内部 `businessModules.mjs` 机器锚点，已改为页面页头专项守卫后重跑通过。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明出货 / 入库两个正式页头的前端用户可见投影、静态守卫和 3 个本地 L1 场景；不证明真实账号登录、真实后端 RBAC 拒绝路径、真实后端写入、库存事实完整闭环、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 BOM / 出货状态 fallback 收口

- 完成：继续按 review findings 处理正式业务列表状态 raw key fallback。`BOMVersionColumns` 新增 `bomStatusText`，BOM 列展示、导出、排序、`BOMVersionsPage` CSV 导出和选中摘要统一未知状态为“BOM 状态”，不再把底层 `status` key 直出；`shipmentColumns` 新增 `shipmentStatusText`，出货列展示、导出和排序统一未知状态为“出货状态”；未使用的 `StatusPill` 也从 `STATUS_LABELS[status] || status` 改为“业务状态” fallback。`userVisibleTechnicalFields` 守卫纳入 BOM 页面和 `StatusPill`，禁止 `BOM_STATUS_LABELS[...] || rawStatus`、`SHIPMENT_STATUS_LABELS[...] || rawStatus` 和 `STATUS_LABELS[status] || status` 回流。未改后端 BOM / 出货状态机、JSON-RPC、RBAC、schema、库存事实或真实数据。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/config/menuPermissions.test.mjs web/src/erp/utils/masterDataOrderView.test.mjs` 通过 104 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/BOMVersionsPage.jsx src/erp/components/bom/BOMVersionColumns.jsx src/erp/components/shipments/shipmentColumns.jsx src/erp/components/StatusPill.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；raw fallback `rg` 扫描无命中；`STYLE_L1_SCENARIOS=business-formal-module-shells-desktop,shipment-date-filter-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 2 个场景。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明 BOM / 出货前端状态展示、导出、排序 fallback、静态守卫和 2 个本地 L1 场景；不证明真实账号登录、真实后端状态迁移、历史数据库异常状态回补、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 客户配置 dev 控制台状态 fallback 收口

- 完成：继续按 review findings 处理用户可见 raw key fallback。`DevCustomerConfigPage` 的 `StatusTag` 新增 `statusText`，已登记中文状态继续显示原文，已登记机器状态映射为中文，未知英文 / 下划线状态只显示“状态”，不再使用 `STATUS_LABELS[status] || status` 把底层状态 key 直出。`userVisibleTechnicalFields` 守卫同步纳入 `DevCustomerConfigPage`，防止 dev-only 客户配置诊断页重新退回 raw status fallback。未改客户配置包 schema、runtime manifest、后端 `customer_config` API、发布 / 激活 / 回滚门禁或真实客户数据。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/config/devCustomerConfig.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 65 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/DevCustomerConfigPage.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`rg -n "STATUS_LABELS\\[(status|normalizedStatus)\\]\\s*\\|\\|\\s*(status|normalizedStatus)" web/src/erp/pages/DevCustomerConfigPage.jsx web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 无命中；`STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印真实业务带值入口和字段链路缺值回补。
- 阻塞/风险：本轮只证明 dev-only 客户配置控制台状态标签、用户可见技术字段守卫、dev 入口边界和 1 个本地 L1 场景；不证明真实账号登录、真实后端 customer config active revision 读回、真实客户导入、目标环境激活、生产发布、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 移动任务到期状态 raw key fallback 收口

- 完成：继续按 review findings 处理岗位任务端用户可见 raw key。`mobileTaskView` 新增 `getMobileTaskDueStatusLabel`，移动任务模型的 `due_status_label` 不再使用 `DUE_STATUS_LABELS[dueStatus] || dueStatus`；未知非空到期状态统一显示“到期状态”，空值显示 `-`，避免岗位任务列表把未知 `due_status` key 展示给一线用户。`userVisibleTechnicalFields` 守卫同步纳入 `mobileTaskView.mjs` 并禁止该 raw fallback 回流。未改后端 Workflow due 计算、JSON-RPC、RBAC、任务动作、真实造数或移动端真实登录。
- 验证：`node --test web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 83 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/utils/mobileTaskView.mjs src/erp/utils/mobileTaskView.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`rg -n "DUE_STATUS_LABELS\\[dueStatus\\]\\s*\\|\\|\\s*dueStatus|getMobileTaskDueStatusLabel\\('unknown_due_status_key'\\)" web/src/erp/utils/mobileTaskView.mjs web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 只命中测试中的未知值断言；`STYLE_L1_SCENARIOS=mobile-tasks-dark /usr/local/bin/pnpm --dir web style:l1` 通过 1 个移动端任务场景。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明移动任务模型、可见技术字段守卫、任务端 no-write smoke 边界和 1 个本地 L1 场景；不证明真实账号登录、真实浏览器任务动作、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Formal 业务壳状态导出 fallback 收口

- 完成：继续按 review findings 处理正式业务壳用户可见 raw key。`FormalBusinessModulePage` 新增 `statusText`，状态 Tag 和导出值统一走中文业务状态投影；未知非空 `business_status` 只显示“业务状态”，不再通过 `STATUS_OPTIONS.find(... )?.label || record.business_status` 把底层状态 key 写入导出。`userVisibleTechnicalFields` 守卫同步禁止该导出 fallback 回流。未改后端业务模块、Workflow / Fact usecase、JSON-RPC、RBAC、真实导出能力或生产数据。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/config/menuPermissions.test.mjs` 通过 54 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/FormalBusinessModulePage.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`rg -n "record\\.business_status|statusText\\(" web/src/erp/pages/FormalBusinessModulePage.jsx web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 剩余命中均为筛选 / 比较或已走 `statusText` 的导出；`STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个正式业务壳场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明 Formal 业务壳前端状态展示 / 导出 fallback、用户可见技术字段守卫和 1 个本地 L1 场景；不证明真实业务导出、真实后端写入、真实账号登录、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 应付协同质检结果 raw key 投影收口

- 完成：继续按 review findings 处理岗位任务 / 后台协同用户可见 raw key。`payableReconciliationFlow` 的应付登记 / 对账任务相关单据不再直接拼接 `payload.iqc_result` 或 `payload.qc_result`；已知结果映射为“合格 / 不合格 / 返工 / 放行 / 待检”，未知非空结果统一显示“质检已记录”，避免客户配置、上游导入或历史数据里的底层 key 出现在财务任务、移动端任务详情和协同说明里。`userVisibleTechnicalFields` 新增静态守卫，防止直拼 raw 结果回流。未改后端 WorkflowUsecase、JSON-RPC、RBAC、真实任务动作、真实写入或打印模板。
- 验证：`node --test web/src/erp/utils/payableReconciliationFlow.test.mjs web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 82 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 13 个 no-write / input-template / redaction 边界测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/utils/payableReconciliationFlow.mjs src/erp/utils/payableReconciliationFlow.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=mobile-tasks-dark /usr/local/bin/pnpm --dir web style:l1` 通过 1 个移动端任务场景；生产代码 `rg` 扫描旧的 `IQC 结果：${payload.iqc_result}` / `检验结果：${payload.qc_result}` 直拼无命中。
- 下一步：继续按 review findings 推进成品入库 / 出货 / 财务任务相关单据里的结果类字段投影、菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明应付协同任务派生的前端 / 移动端可见结果投影、静态守卫、no-write 移动任务脚本边界和 1 个本地 L1 场景；不证明真实账号登录、真实浏览器任务动作、后端 RBAC 拒绝路径、真实后端写入、完整打印链路、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 成品抽检结果 raw key 投影收口

- 完成：继续按 review findings 处理成品任务相关单据的结果类 raw key。`finishedGoodsFlow` 的成品入库 / 返工任务仍保留 `payload.qc_result` 机器值作为 Workflow 合同，但 `related_documents` 不再把 `pass / fail / approval_result` 或未知 key 直接拼进“成品抽检结果”；已知结果映射为“合格 / 不合格 / 返工 / 放行 / 待检”，未知非空结果显示“抽检已记录”。`userVisibleTechnicalFields` 新增成品任务专项守卫。顺手把 `workflow-ui-action-boundary` 中仍要求用户可见 `usecase` 的采购入库守卫改为当前“后端采购入库规则”口径，避免测试继续驱动页面回退到工程术语。未改后端 WorkflowUsecase、JSON-RPC、RBAC、真实任务动作、库存事实写入或出货事实。
- 验证：`node --test web/src/erp/utils/finishedGoodsFlow.test.mjs web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 83 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；首次合并跑 `scripts/qa/workflow-ui-action-boundary.test.mjs` 时发现采购入库测试仍要求旧 `usecase` 文案，已改为“后端采购入库规则”后重跑 `node --test scripts/qa/workflow-ui-action-boundary.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 31 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/utils/finishedGoodsFlow.mjs src/erp/utils/finishedGoodsFlow.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`node --check scripts/qa/workflow-ui-action-boundary.test.mjs` 通过；`STYLE_L1_SCENARIOS=mobile-tasks-dark /usr/local/bin/pnpm --dir web style:l1` 通过 1 个移动端任务场景；生产代码 `rg` 扫描旧的 `成品抽检结果：${options.qcResult}`、`成品抽检结果：pass/fail/custom` 无命中。
- 下一步：继续按 review findings 推进出货 / 财务任务相关单据里的结果类字段投影、菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明成品任务派生的前端 / 移动端可见结果投影、静态守卫、Workflow action 边界测试口径、no-write 移动任务脚本边界和 1 个本地 L1 场景；不证明真实账号登录、真实浏览器任务动作、后端 RBAC 拒绝路径、真实后端写入、库存 / 出货事实闭环、完整打印链路、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 移动任务 related_documents 结果 raw key 防护

- 完成：继续按 review findings 处理岗位任务端历史 / 外部 payload 可见层漏点。`mobileTaskView.normalizeRelatedDocuments` 不再原样透传结果类相关单据文案；`IQC 结果`、`检验结果`、`委外回货检验结果` 和 `成品抽检结果` 中的 `pass / failed / custom_*` 等机器值会在移动任务 view model 层投影为“合格 / 不合格 / 质检已记录 / 抽检已记录”，普通订单号、入库记录、出货记录等业务单号保持原样。该改动是岗位端展示防护，承接历史任务或外部 payload；不替代上游任务派生源头修复，也不改变后端 payload 合同。
- 验证：`node --test web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/payableReconciliationFlow.test.mjs web/src/erp/utils/finishedGoodsFlow.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 96 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 13 个 no-write / input-template / redaction 边界测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/utils/mobileTaskView.mjs src/erp/utils/mobileTaskView.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=mobile-tasks-dark /usr/local/bin/pnpm --dir web style:l1` 通过 1 个移动端任务场景；生产代码 `rg` 扫描原样透传 `map(String)` 和结果类 raw fixture 无命中，剩余 raw 结果命中仅为测试输入样本。
- 下一步：继续按 review findings 推进出货 / 财务任务相关单据字段、菜单权限投影与 effective session 实际入口一致性、任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明移动端 view model 对结果类 `related_documents` 的本地展示防护、静态守卫、no-write 移动任务脚本边界和 1 个本地 L1 场景；不证明真实账号登录、真实浏览器任务动作、后端 RBAC 拒绝路径、真实后端写入、全部历史 payload 已清洗、完整打印链路、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 业务事实列内部 ID 排序 / 导出投影收口

- 完成：继续按 review findings 处理业务事实列表的 raw id 投影缺口。`operationalFactPageConfig` 中对象、仓库 / 批次 / 单位、供应商、客户和财务往来方列统一改为 `subjectColumnText / stockContextText / supplierColumnText / customerColumnText / counterpartyColumnText`；列展示、排序值和导出值都走“已关联”或业务名称文案，不再把 `supplier_id / customer_id / counterparty_id / subject_id / warehouse_id / lot_id / unit_id` 拼成候选业务值。该改动只收口前端列配置和导出/排序口径，不改变后端事实 schema、查询参数、JSON-RPC、库存或财务事实写入。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/components/operational-facts/OperationalFactForms.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs` 通过 48 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/components/operational-facts/operationalFactPageConfig.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个正式业务壳场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；目标 `rg` 扫描 `record.supplier_name || record.supplier_id`、`record.customer_snapshot || record.customer_id`、`counterparty_id` 拼接和仓库 / 批次 / 单位 ID 拼接无命中。
- 下一步：继续按 review findings 推进菜单权限投影与 effective session 实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、任务端真实造数 / 真实动作 smoke、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明业务事实前端列配置、排序 / 导出值静态守卫、错误提示边界和 1 个本地 L1 场景；不证明真实后端查询排序、真实导出文件内容、真实账号登录、后端 RBAC 拒绝路径、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 菜单权限标签 raw key fallback 收口

- 完成：继续按 review findings 处理菜单权限投影。`menuPermissions` 的 `getPermissionLabel` 和 `getMobileRolePermissionLabel` 未命中时不再返回原始 path / role key，改为“菜单权限”和“岗位入口”；未知空值仍返回空字符串。权限归一、旧入口别名、有效权限过滤和移动角色端口顺序不变。该改动只收口前端菜单 / 岗位权限标签展示，不改变后端 RBAC、权限码真源、菜单显隐安全边界或真实账号权限。
- 验证：`node --test web/src/erp/config/menuPermissions.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs` 通过 57 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/config/menuPermissions.mjs src/erp/config/menuPermissions.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=permission-center-desktop,business-menu-groups-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 2 个权限 / 菜单场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`node --test web/src/erp/config/devTesting.test.mjs web/src/erp/config/seedData.test.mjs` 通过 15 个 dev 测试入口和菜单 seed 合同测试；目标 `rg` 扫描 `return matched?.label || key` 无生产代码命中，剩余未知 key 命中仅为测试样本。
- 下一步：继续按 review findings 推进 effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、任务端真实造数 / 真实动作 smoke、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明前端菜单权限 label fallback、dev/customer-config 边界、seed/devTesting 合同和 2 个本地 L1 场景；不证明真实账号登录、后端 RBAC 拒绝路径、真实 effective session 数据、真实客户配置 active revision、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Workflow 未知动作 key 提交边界收口

- 完成：继续按 review findings 处理 Workflow action / RBAC / usecase parity 和用户可见 raw key 边界。`normalizeWorkflowActionMode` 未识别动作时不再返回原始 action key，改为空动作；后端 explain 返回的未知 `action_key` 不进入 `byAction`，前端 submit guard 收到未知 actionKey 时不调用后端 explain，直接提示“当前任务动作缺少必要参数，请刷新后重试”。该改动保留 complete / block / reject / urge 正式动作合同，不新增 Workflow 动作，不改后端 WorkflowUsecase、RBAC、JSON-RPC 或真实任务写入。
- 验证：`node --test web/src/erp/utils/workflowTaskActionAccess.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 73 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/utils/workflowTaskActionAccess.mjs src/erp/utils/workflowTaskActionAccess.test.mjs src/erp/utils/workflowTaskActionSubmitGuard.mjs src/erp/utils/workflowTaskActionSubmitGuard.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 13 个 no-write / input-template / redaction 边界测试；`STYLE_L1_SCENARIOS=mobile-tasks-dark,erp-effective-session-action-projection-business-pages /usr/local/bin/pnpm --dir web style:l1` 通过 2 个场景；目标 `rg` 扫描 `return ACTION_MODE_ALIASES[key] || key` 无生产代码命中，未知 action key 仅保留为测试输入样本。
- 下一步：继续按 review findings 推进后端 JSON-RPC / RBAC action parity、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、任务端真实造数 / 真实动作 smoke、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明前端 Workflow action mode 归一、submit guard 本地拒绝、Workflow UI action 边界、no-write 移动任务脚本边界和 2 个本地 L1 场景；不证明真实账号登录、真实浏览器任务动作、后端 RBAC 拒绝路径、真实后端写入、后端 WorkflowUsecase 动作矩阵、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Workflow 前端 raw 写 wrapper 退出收口

- 完成：继续按 review findings 处理 Workflow action / RBAC / usecase parity。正式 UI 已由 `complete_task_action` / `block_task_action` / `reject_task_action` / `urge_task` 和后端 explain submit guard 承接，`web/src/erp/api/workflowApi.mjs` 不再导出未使用的 `createWorkflowTask` 和 `upsertWorkflowBusinessState`，前端 API wrapper 也不再保留 `create_task` / `upsert_business_state` raw 写入口。`workflow-ui-action-boundary.test.mjs` 取消对 `workflowApi.mjs` 的跳过，后续把 raw workflow 写入口放回前端 API client 会被同一守卫拦截。未改后端 WorkflowUsecase、JSON-RPC 方法、RBAC、模块状态门禁、真实任务写入或任何 Fact 过账逻辑。
- 验证：`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionAccess.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs` 通过 33 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/api/workflowApi.mjs src/erp/utils/workflowTaskActionAccess.mjs src/erp/utils/workflowTaskActionSubmitGuard.mjs` 通过；`node --check scripts/qa/workflow-ui-action-boundary.test.mjs` 通过；`rg -n "createWorkflowTask|upsertWorkflowBusinessState|'create_task'|\"create_task\"|'upsert_business_state'|\"upsert_business_state\"" web/src/erp --glob '!**/*.test.*'` 无命中；`cd server && go test ./internal/service -run 'TestJsonrpcDispatcher_Workflow(CompleteTaskAction|ControlledTaskActions|UrgeTask|Explain(ActionAccess|TaskAssignment))|TestJsonrpcDispatcher_WorkflowWriteAPIRequiresEnabledModule'` 通过；`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 53 个测试；`STYLE_L1_SCENARIOS=erp-effective-session-action-projection-business-pages /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景。
- 下一步：继续按 review findings 推进后端 JSON-RPC / RBAC action parity、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、任务端真实造数 / 真实动作 smoke、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明前端 API wrapper 不再暴露 raw workflow 写入口、正式 UI action boundary 静态守卫、前端 action access / submit guard、后端 Workflow JSON-RPC 定向合同、移动任务 no-write 脚本边界和一个本地 L1 场景；不证明真实账号登录、真实浏览器任务动作、后端 RBAC 全矩阵、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 客户配置控制台边界 label fallback 收口

- 完成：继续按 review findings 处理客户配置包规则 / 策略 / 扩展点控制台和用户可见 raw key 边界。`DevCustomerConfigPage` 的字段编号边界和客户配置包边界列表不再使用 `item.label || item.key`，改为 `guardItemLabel`，缺 label 时分别显示“字段编号边界”和“客户配置包边界”；已有 summary 层的 `key` 仍作为 React key 和内部审计锚点保留，不进入可见文案 fallback。该改动只收口 `/__dev/customer-config` 页面渲染 fallback，不改变客户包 schema、runtime manifest 编译、后端 validate / publish / activate、moduleStates、策略实现、扩展点实现或 release evidence。
- 验证：`node --test web/src/erp/config/devCustomerConfig.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs` 通过 65 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/DevCustomerConfigPage.jsx src/erp/config/devCustomerConfig.mjs src/erp/config/devCustomerConfig.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；目标 `rg` 扫描 `item.label || item.key` 无生产页面命中，只剩守卫断言和 `guardItemLabel` 使用；`node --test scripts/qa/docs-inventory.test.mjs`、`git diff --check` 通过；`multi-client-role-workflow-priority-audit --json` 仍为 `ok=true`、`releaseReady=false`、`blockingCategory=external-release-evidence-required`。
- 下一步：继续按 review findings 推进客户配置规则 / 策略 / 扩展点运行时边界、effective session 与菜单实际入口一致性、任务端真实造数 / 真实动作 smoke、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明 dev-only 客户配置控制台边界列表的可见 label fallback、dev/customer-config 合同、正式前端客户配置边界、一个本地 L1 场景和文档 / diff 静态检查；不证明真实客户配置 active revision、真实账号登录、真实后端写入、目标环境发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 移动任务到期状态 raw label 防护

- 完成：继续按 review findings 处理岗位任务端可见 raw key。`mobileRoleTaskModel` 新增 `resolveMobileTaskDueLabel`，到期时间优先展示 `due_at_label`，否则按 `due_status` 通过 `getMobileTaskDueStatusLabel` 投影；只有已是中文业务文案的 `due_status_label` 才可作为兜底，未知 `unknown_due_status_key` 这类 raw label 不再展示。`MobileTaskListScreen` 改为调用该 helper，`getTaskQueueTone` 也不再用 `task.due_status_label` 作为风险语气 fallback。未改后端 Workflow 任务、JSON-RPC、RBAC、真实任务动作、真实写入或移动端真实登录流程。
- 验证：`node --test web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 93 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/mobile/components/MobileTaskListScreen.jsx src/erp/mobile/utils/mobileRoleTaskModel.mjs src/erp/mobile/utils/mobileRoleTaskModel.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=mobile-tasks-dark /usr/local/bin/pnpm --dir web style:l1` 通过 1 个移动任务端场景；目标 `rg` 扫描 `task.due_at_label || task.due_status_label` 和 `task.alert_label || task.due_status_label` 无命中；`node --test scripts/qa/docs-inventory.test.mjs`、`git diff --check` 通过；`multi-client-role-workflow-priority-audit --json` 仍为 `ok=true`、`releaseReady=false`、`blockingCategory=external-release-evidence-required`。
- 下一步：继续按 review findings 推进任务端真实造数 / 真实动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、effective session 与菜单实际入口一致性、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明移动任务端模型和列表页对到期状态的本地投影、防 raw label 静态守卫、mobile workflow no-write 脚本边界和 1 个本地 L1 场景；不证明真实账号登录、真实浏览器任务动作、后端 RBAC 拒绝路径、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 移动任务最近动态 raw action label 防护

- 完成：继续按 review findings 处理岗位任务端可见 raw key。`mobileTaskView` 对历史 `payload.mobile_action.action_label` 增加展示投影：已知 `action_key` 优先映射为“完成 / 阻塞 / 退回 / 催办”，未知技术 label 统一回落为“任务处理”；`mobileRoleTaskModel` 新增 `resolveMobileActionDisplayLabel`，`MobileTaskDetailScreen` 的“最近动态”不再直接展示 `latestMobileAction.action_label || action_key`，避免旧 payload 中的 `unknown_action_key` 出现在业务用户界面。未改后端 Workflow 任务、JSON-RPC、RBAC、真实任务动作、真实写入或移动端真实登录流程。
- 验证：`node --test web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 94 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/mobile/components/MobileTaskDetailScreen.jsx src/erp/mobile/utils/mobileRoleTaskModel.mjs src/erp/mobile/utils/mobileRoleTaskModel.test.mjs src/erp/utils/mobileTaskView.mjs src/erp/utils/mobileTaskView.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=mobile-tasks-dark /usr/local/bin/pnpm --dir web style:l1` 通过 1 个移动任务端场景；目标 `rg` 扫描 `latestMobileAction.action_label ||` 无命中，`unknown_action_key` 只保留在测试输入样本。
- 下一步：继续按 review findings 推进任务端真实造数 / 真实动作 smoke、后端 JSON-RPC / RBAC action parity、客户配置规则 / 策略 / 扩展点运行时边界、effective session 与菜单实际入口一致性、打印字段 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明移动任务 view model 和详情页对最近动作标签的本地投影、防 raw action label 静态守卫、mobile workflow no-write 脚本边界和 1 个本地 L1 场景；不证明真实账号登录、真实浏览器任务动作、后端 RBAC 拒绝路径、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 客户配置控制台打印字段要求预检收口

- 完成：`/__dev/customer-config` 打印模板字段区不再只展示字段真源计数；`buildPrintTemplateFieldSummary` 将两套正式模板的 `fieldRequirements` 转成业务可读 `fieldRequirementItems`，页面展示要求 label、来源和边界，并只显示“字段要求锚点已登记”，不展示 requirement key。未新增销售订单打印模板，未发布 `sales_order_items.default`，未改 PDF 生成、后端门禁或真实打印带值 mapper。
- 验证：`node --test web/src/erp/config/devCustomerConfig.test.mjs web/src/erp/config/printTemplates.test.mjs scripts/qa/sales-order-field-chain-boundary.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 32 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/config/devCustomerConfig.mjs src/erp/config/devCustomerConfig.test.mjs src/erp/pages/DevCustomerConfigPage.jsx src/erp/config/printTemplates.test.mjs` 通过；`STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景。
- 下一步：继续按 review findings 推进打印业务带值 mapper / 历史缺值回补、客户配置规则 / 策略 / 扩展点运行时边界、effective session 与菜单实际入口一致性、任务端真实造数 / 真实动作 smoke。
- 阻塞/风险：本轮只证明 dev-only 控制台的正式两套模板字段要求预检更具体，仍不证明销售订单打印、真实业务带值打印、目标环境发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

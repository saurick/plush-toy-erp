# progress 归档：2026-07-02 模拟 report-only 前

本文件从 `progress.md` 归档，保留 2026-07-02 下一本地闭环前归档说明至 active module state / Workflow 写门禁复核的过程记录。它只作为历史追溯，不作为当前正式需求、runtime、schema、部署或客户交付真源。

## 2026-07-02 progress 归档到下一本地闭环前

- 完成：归档前 `progress.md` 为 245 行 / 83,864 bytes，已超过 80KB 归档阈值；已将 2026-07-02 `客户配置控制台 no-write 读回预检收口` 至 `移动端任务端 no-write preflight 复核` 的过程记录复制归档到 `docs/archive/progress-2026-07-02-before-next-local-closure.md`。
- 完成：根 `progress.md` 保留归档索引、当前活跃事项、本次归档说明和最近三条 no-write / preflight 记录，避免过程流水继续膨胀为隐藏真源。
- 下一步：继续按 goal 选择一个本地最高优先级闭环；无演示密码时优先字段链路剩余项、客户配置控制台 no-write 读回缺口或移动端任务端本地证据；有演示密码后再跑真实 RBAC / 浏览器 smoke 报告。
- 阻塞/风险：本轮归档本身不改变 runtime、schema、RBAC、Workflow / Fact、客户配置、测试脚本、部署、真实客户数据、目标环境 evidence、提交或推送；归档文件只用于追溯，不作为当前真源。

## 2026-07-02 progress 归档到 qa:fast 本地闭环后

- 完成：归档前 `progress.md` 为 216 行 / 82,378 bytes，已超过 80KB 归档阈值；已将 2026-07-02 `客户配置控制台 no-write 读回预检收口` 至 `L1 与移动端登录回跳本地回归复核` 的过程记录归档到 `docs/archive/progress-2026-07-02-before-post-fast-local-closure.md`。
- 完成：根 `progress.md` 保留归档索引、当前活跃事项、上一轮归档说明和最新 `qa:fast` 本地闭环复核记录，避免过程流水继续膨胀为隐藏真源。
- 下一步：继续按 goal 选择一个本地最高优先级闭环；无演示密码时只推进 no-write / mock 可证明缺口或做复核，有演示密码后再运行真实 RBAC / 浏览器 smoke 报告。
- 阻塞/风险：本轮归档本身不改变 runtime、schema、RBAC、Workflow / Fact、客户配置、测试脚本、部署、真实客户数据、目标环境 evidence、提交或推送；归档文件只用于追溯，不作为当前真源。

## 2026-07-02 qa:fast 本地闭环复核

- 完成：继续当前本地试用闭环，不重启 goal、不扩张 release evidence。本轮未改业务代码，补跑 `scripts/qa/fast.sh` 作为更宽的本地守卫证据，覆盖错误码同步、Workflow / Fact 边界、Workflow UI action 合同、正式前端 customer config 投影、无凭据 effective session 探针、adminProfileSync、角色菜单 / seed / workflowStatus、`__dev` 入口、试用账号 RBAC / 浏览器 smoke 输入模板、移动端登录回跳边界、采购入库 real-write 输入模板、销售订单字段链路、错误提示、文档清单、客户配置包 / runtime manifest、导入 no-write、模拟闭环、前端 lint / css 和服务端 Go tests。
- 验证：追加前 `progress.md` 为 209 行 / 80,462 bytes，未达 600 行或 80KB=81,920 bytes 归档阈值；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过，结束标记为 `[qa:fast] 完成`。该命令包含 `web` 的 `pnpm lint` / `pnpm css` 和 `server` 的 `go test ./internal/... ./pkg/...`；过程中仍有既有 Node `MODULE_TYPELESS_PACKAGE_JSON` warning，但未导致失败。
- 下一步：本次追加后 `progress.md` 预计超过 80KB，下一次写入前应先归档旧流水；无演示密码时继续选择 no-write / mock 可证明缺口或仅做复核；有演示密码后再运行真实 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`。
- 阻塞/风险：`qa:fast` 不读取密码 / token，不真实登录，不执行真实客户数据导入，不发布 / 激活 / 回滚客户配置，不做生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；本轮验证不证明真实账号、真实浏览器、后端 authenticated active revision 读回、真实岗位任务动作或外部 release evidence 已完成。

## 2026-07-02 style:l1 全量本地浏览器回归复核

- 完成：继续当前本地试用闭环，不重启 goal、不扩张 release evidence。全量 `style:l1` 首轮停在 `system-audit-logs-desktop`：场景仍期待可见 `assistant-admin`，但审计日志页当前真源已把 raw target key 收口为业务可读兜底。已将该场景改为验证 `目标已关联`，并显式断言审计页不展示 `assistant-admin`、原始 event key、`actor` / `target` 和原始 payload。
- 验证：追加前 `progress.md` 为 56 行 / 10,072 bytes，未达 600 行或 80KB 归档阈值；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=system-audit-logs-desktop STYLE_L1_PORT=4174 /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=4174 /usr/local/bin/pnpm --dir web style:l1` 全量通过 77 个场景。过程中仍有既有 Node `MODULE_TYPELESS_PACKAGE_JSON` warning，但未导致失败。
- 下一步：无演示密码时继续选择 no-write / mock 可证明缺口或做定向复核；有演示密码后再运行真实 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`。
- 阻塞/风险：`style:l1` 是本地 mock 浏览器回归，不读取密码 / token，不真实登录，不证明真实 RBAC、真实岗位任务动作、后端 authenticated active revision 读回、客户配置正式发布 / 激活 / 回滚、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收或外部 release evidence 已完成。

## 2026-07-02 yoyoosun 本地入口与客户配置投影诊断

- 完成：按当前 goal 的最高优先级补充，完成 yoyoosun 本地入口 no-write 诊断，不重启 goal、不扩张 release evidence。当前端口归属为：`5175` 是本仓库 Product Core Vite dev，`5176` 是 `/Users/simon/projects/openai-oauth-api-service/web`，`5177` 是本仓库已运行的 yoyoosun 入口，`5178/5179` 当前未监听；`start:yoyoosun --print-plan` 和 `preview:yoyoosun --print-plan` 因 `5176` 被占用都会计划顺延到 `5178`，但当前已可打开的 yoyoosun 入口是 `http://localhost:5177/erp`。
- 验证：追加前 `progress.md` 为 63 行 / 11,648 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false / firstBlockedAction=immutable-version`；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web start:yoyoosun -- --print-plan` 与 `preview:yoyoosun -- --print-plan` 均输出计划端口 `5178` 和 yoyoosun config / asset 验证命令；`pnpm --dir web --silent audit:yoyoosun-entry -- --json` 输出 `yoyoosunPorts=[5177] / productCorePlaceholderPorts=[5175] / htmlFallbackPorts=[5175,5176] / backendHealth.ok=true`；`curl http://localhost:5177/customer-config.js` 命中 `customerKey: "yoyoosun"`，`curl -I http://localhost:5177/customer-assets/yoyoosun/favicon-yoyoosun.svg` 命中 `content-type: image/svg+xml`；`node scripts/qa/customer-config-effective-session-probe.mjs --json` 按预期停在 `40302 未登录`，`blockers=[missing-authenticated-admin-session]`；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview` 通过，`modules=15 / roles=9 / entitlements=346`。
- 下一步：若继续推进同一切片，需要有本地演示密码或 token 后再运行真实 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 或 `mobileWorkflowRuntimeBrowserSmoke --report`，用普通账号和 super admin 分别读回 effective session 来源、菜单投影、字段策略和岗位任务端访问；无凭据时下一步应转向 no-write / mock 可证明的下一个本地阻断项。
- 阻塞/风险：本轮只证明前端端口、静态 customer-config 注入、yoyoosun asset、后端 health、无凭据 effective-session 边界和 runtime manifest 编译；不证明当前浏览器登录账号是否为 super admin，不证明 active customer config revision 来源、普通客户角色投影、真实 RBAC、真实岗位任务动作、真实客户数据导入、客户配置 publish / activate / rollback、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 真实登录与移动端 smoke no-write preflight 复核

- 完成：在没有演示密码 / token 的前提下，继续同一 goal 的 no-write 证据闭环，不重启 goal、不扩张 release evidence。复核三组真实登录 / 浏览器 / 移动端 smoke 前置：试用账号 RBAC、试用账号浏览器 smoke、移动端 workflow runtime browser smoke。三组均正确停在缺演示密码，不读取密码、不登录、不调用 JSON-RPC、不启动浏览器、不写数据库；其中两组浏览器 preflight 显式传入 `http://localhost:5177`，并确认该外部前端地址匹配 yoyoosun，未误接 `5175` Product Core 或 `5176` 其他项目。
- 验证：追加前 `progress.md` 为 70 行 / 14,446 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false / firstBlockedAction=immutable-version`；`TRIAL_ACCOUNT_BACKEND_URL=http://127.0.0.1:8300 node scripts/qa/trial-account-rbac.mjs --preflight-report output/trial-account-rbac/preflight.json` 写入 `ready=false` 报告，阻断为 `missing-trial-account-password-env`；`TRIAL_BROWSER_SMOKE_BASE_URL=http://localhost:5177 TRIAL_BROWSER_SMOKE_BACKEND_HEALTH_URL=http://127.0.0.1:8300/healthz node web/scripts/trialDemoAccountBrowserSmoke.mjs --preflight-report output/trial-demo-account-browser-smoke/preflight.json` 写入 `ready=false` 报告，阻断为 `missing-demo-password-env`，且 `externalBaseURLMatchesYoyoosun=true / yoyoosunPorts=[5177]`；`MOBILE_WORKFLOW_BROWSER_SMOKE_BASE_URL=http://localhost:5177 MOBILE_WORKFLOW_BROWSER_SMOKE_BACKEND_URL=http://127.0.0.1:8300 node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report output/mobile-workflow-runtime-browser-smoke/preflight.json` 写入 `ready=false` 报告，阻断为 `missing-demo-password-env`，且 `externalBaseURLMatchesYoyoosun=true / yoyoosunPorts=[5177]`。
- 下一步：如要证明普通账号菜单投影、active revision 来源、真实 RBAC 或移动端任务动作，必须提供本地演示密码 / token 后运行真实 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 或 `mobileWorkflowRuntimeBrowserSmoke --report`；无凭据时继续选择不需要真实登录的 mock / static / style:l1 / docs 口径缺口。
- 阻塞/风险：本轮报告写入 `output/`，该目录被 `.gitignore` 忽略，只作为本机 no-write 证据；不证明真实登录、真实浏览器渲染、真实 RBAC、普通账号菜单投影、后端 authenticated active revision、移动端 done / blocked / rejected / urge 动作、真实客户数据导入、客户配置 publish / activate / rollback、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 no-write 入口与试用手册守卫串行复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务代码，只复核 no-write / preflight 入口的脚本与文档守卫：试用账号 RBAC、试用账号浏览器 smoke、移动端 workflow runtime browser smoke、yoyoosun 本地入口审计、无凭据 effective session 探针、`/__dev/testing` 入口和试用角色文档口径。
- 验证：追加前 `progress.md` 为 77 行 / 17,279 bytes，未达 600 行或 80KB 归档阈值；先直接运行 `node --test scripts/qa/trial-account-rbac.test.mjs web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/scripts/yoyoosunEntryPlan.test.mjs scripts/qa/customer-config-effective-session-probe.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/trial-role-entry-docs.test.mjs`，其中两个 live 端口探针场景在默认并发下没有稳定读到 `yoyoosunPorts=[5177]`；单独复跑 `node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 和 `node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs` 均通过；`pnpm --dir web --silent audit:yoyoosun-entry -- --json` 当前仍识别 `5177` 为 yoyoosun，`5175` 为 Product Core placeholder，`5176` 为其他项目 html fallback；最终按 runtime 探针串行口径运行 `node --test --test-concurrency=1 scripts/qa/trial-account-rbac.test.mjs web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/scripts/yoyoosunEntryPlan.test.mjs scripts/qa/customer-config-effective-session-probe.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 47 个测试。
- 下一步：无演示密码时继续只推进 mock / static / no-write 可证明缺口；如果要证明真实 RBAC、普通账号菜单投影、authenticated active revision 或移动端真实 done / blocked / rejected / urge 动作，需要提供本地演示密码或 token 后再跑真实报告命令。
- 阻塞/风险：本轮串行测试证明的是本地脚本、文档和 live 端口 preflight 守卫，不读取密码、不登录、不调用真实业务 JSON-RPC、不写数据库、不启动浏览器，不证明真实账号、真实 RBAC、普通账号菜单投影、authenticated active revision、目标环境 release evidence、生产部署、真实客户导入、备份恢复、回滚演练、客户签收、提交或推送。默认并发批量跑 live 端口探针可能出现不稳定，后续复核这组 runtime preflight 测试优先使用 `--test-concurrency=1` 或按文件拆跑。

## 2026-07-02 yoyoosun effective-session no-write 证据复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务代码，只补当前运行态证据：`git fetch --prune` 后本地 `main` 与 `origin/main` 无 ahead / behind；priority audit 仍为 `ok=true`，完成状态阻断类别是 `external-release-evidence-required`，第一条 blocked release action 是 `immutable-version`，但本轮不把 release evidence 升级为主任务；`start:yoyoosun --print-plan` 和 `preview:yoyoosun --print-plan` 因 `5176` 被占用均计划顺延到 `5178`，当前实际已运行的 yoyoosun 入口仍是 `http://localhost:5177/erp`。
- 验证：追加前 `progress.md` 为 84 行 / 19,982 bytes，未达 600 行或 80KB 归档阈值；`pnpm --dir web --silent audit:yoyoosun-entry -- --json` 输出 `backendHealth.ok=true / yoyoosunPorts=[5177] / productCorePlaceholderPorts=[5175] / htmlFallbackPorts=[5175,5176]`；`curl -fsS http://localhost:5177/customer-config.js` 命中 `customerKey: "yoyoosun"`，`curl -fsSI http://localhost:5177/customer-assets/yoyoosun/favicon-yoyoosun.svg` 命中 `content-type: image/svg+xml`；`node scripts/qa/customer-config-effective-session-probe.mjs --json --report output/customers/yoyoosun/customer-config-effective-session-probe/current.json` 写入 ignored 本地报告，并按预期停在 `resultCode=40302 / status=auth_required / activeRevisionVerified=false / blockers=[missing-authenticated-admin-session]`；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview` 通过，`modules=15 / roles=9 / entitlements=346`；`node --test --test-concurrency=1 web/scripts/yoyoosunEntryPlan.test.mjs scripts/qa/customer-config-effective-session-probe.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs scripts/qa/customer-config-boundaries.test.mjs scripts/qa/multi-client-role-workflow-priority-audit.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 48 个测试。
- 下一步：无演示密码 / token 时继续推进 no-write / mock 可证明缺口，优先字段链路、错误提示、移动端任务端 mock / preflight 或 `__dev/customer-config` 口径守卫；如要证明真实普通账号菜单投影、后端 authenticated active revision、super admin / 普通 admin 差异、真实岗位任务端 done / blocked / rejected / urge 动作，需要提供本地演示密码或 token 后再跑真实 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 或 `mobileWorkflowRuntimeBrowserSmoke --report`。
- 阻塞/风险：本轮只证明本机端口归属、静态 customer-config 注入、客户资产、后端 health、无 Authorization 的 effective session 鉴权边界、runtime manifest 编译和对应守卫；不证明真实登录、真实 RBAC、active revision 来源、普通账号菜单 / 字段 / 动作投影、移动端真实任务动作、客户配置 publish / activate / rollback、真实客户数据导入、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 L1 与字段 / 错误 / 移动端守卫复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务代码，只用当前工作区重新确认上一轮之后的下一个本地阻断：`git fetch --prune` 后本地 `main` 与 `origin/main` 无 ahead / behind；priority audit 当前 `ok=true / readOnly=true / releaseReady=false`，69 项本地 check 全部通过，剩余 completion 阻断仍是 `external-release-evidence-required`。上一轮记录里提到的 `business-formal-module-shells-desktop` / `business-v1-suppliers` 焦点失败不是当前活跃阻断，本轮已用真实 L1 复核确认该场景通过。
- 验证：追加前 `progress.md` 为 91 行 / 23,205 bytes，未达 600 行或 80KB 归档阈值；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=business-formal-module-shells-desktop STYLE_L1_PORT=4188 /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`node --test --test-concurrency=1 scripts/qa/sales-order-field-chain-boundary.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs web/src/common/utils/errorMessage.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs` 通过 77 个测试，覆盖销售订单字段策略 / 导出 / mapper / SKU stale snapshot 清理、销售订单打印未接通边界、用户可见中文错误、raw id / source_type / owner_role_key / task_status_key 等技术字段隐藏、移动端任务模型、reason / 催办 / 完成反馈和 no-write mobile smoke 边界。过程中仍有既有 Node `MODULE_TYPELESS_PACKAGE_JSON` warning，但未导致失败。
- 下一步：无演示密码 / token 时继续选择 no-write / mock 可证明缺口，可优先复核 `__dev/customer-config` 页面 L1、正式前端 effective session action projection L1，或继续缩小字段链路剩余未证明面；如果要证明真实普通账号菜单投影、authenticated active revision、真实 RBAC 或移动端真实任务动作，仍需要本地演示密码或 token 后再跑真实报告命令。
- 阻塞/风险：本轮是本地 mock / static / no-write 证据，不读取密码、不真实登录、不调用真实业务写 JSON-RPC、不写数据库、不证明 authenticated active revision、普通账号真实菜单 / 字段 / 动作投影、移动端真实 done / blocked / rejected / urge 动作、真实客户数据导入、客户配置 publish / activate / rollback、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 dev customer-config 与 effective-session 投影 L1 复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务代码，只复核上一轮建议的 no-write / mock 可证明缺口：`__dev/customer-config` 页面、正式前端 effective session action projection、super admin 产品核心可见、无可见菜单空态，以及对应 helper / 边界测试。`git fetch --prune` 后本地 `main` 与 `origin/main` 无 ahead / behind；priority audit 当前仍为 `ok=true / readOnly=true / releaseReady=false`，69 项本地 check 全部通过，剩余 completion 阻断仍是外部 release evidence。
- 验证：追加前 `progress.md` 为 98 行 / 25,918 bytes，未达 600 行或 80KB 归档阈值；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop,erp-effective-session-action-projection-business-pages,erp-effective-session-super-admin-product-core,erp-no-visible-menu-blocks-outlet STYLE_L1_PORT=4192 /usr/local/bin/pnpm --dir web style:l1` 通过 4 个场景；`node --test --test-concurrency=1 web/src/erp/config/devCustomerConfig.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs web/src/erp/utils/adminProfileSync.test.mjs web/src/erp/config/menuPermissions.test.mjs scripts/qa/multi-client-role-workflow-priority-audit.test.mjs` 通过 64 个测试，覆盖 dev-only 客户配置控制台 preview/gated apply 边界、正式前端不读 raw 客户包、页面 / action / field 投影接入 admin profile、普通账号按 RBAC 与 effective session 取交集、sync failure 不退回 RBAC-only、super admin 产品核心看全、菜单权限不包含开发验收入口，以及 release evidence 仍为目标环境缺口。
- 下一步：无演示密码 / token 时，可继续选择 no-write / mock 可证明缺口，例如移动端认证回跳 L1 / preflight、正式业务页 action projection 更窄场景，或 `__dev/testing` 入口与测试索引复核；如果要证明真实普通账号菜单投影、authenticated active revision、真实 RBAC 或移动端真实任务动作，必须提供本地演示密码或 token 后再跑真实报告命令。
- 阻塞/风险：本轮仍是本地 mock / static / no-write 证据，不读取密码、不真实登录、不调用真实业务写 JSON-RPC、不写数据库、不证明 authenticated active revision、普通账号真实菜单 / 字段 / 动作投影、移动端真实 done / blocked / rejected / urge 动作、真实客户数据导入、客户配置 publish / activate / rollback、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 __dev/testing 与移动登录 no-write preflight 复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务代码，只复核 `__dev/testing` 测试入口、docs 白名单、移动端认证回跳输入模板和 preflight 边界；priority audit 当前仍为 `ok=true / readOnly=true / releaseReady=false`，69 项本地 check 全部通过，剩余 completion 阻断仍是外部 release evidence。
- 验证：追加前 `progress.md` 为 105 行 / 28,657 bytes，未达 600 行或 80KB 归档阈值；`node --test --test-concurrency=1 web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs web/scripts/mobileAuthLoginRouteSmoke.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 21 个测试，覆盖 `__dev/testing` 仅开发态暴露、只索引当前维护测试文档、排除 `docs/reference/**` 与 `docs/archive/**`、移动登录 input template / preflight 不启动浏览器或后端；`node web/scripts/mobileAuthLoginRouteSmoke.mjs --print-input-template` 输出 no-write 输入模板；`node web/scripts/mobileAuthLoginRouteSmoke.mjs --preflight-report output/mobile-auth-login-route-smoke/preflight.json` 写入 ignored 本地报告，报告标记 `writesDatabase=false / callsBackend=false / startsBrowser=false / startsDevServer=false`，视口覆盖 phone / iPad；`STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=4196 pnpm --dir web style:l1` 通过 1 个场景。过程中仍有既有 Node `MODULE_TYPELESS_PACKAGE_JSON` warning，但未导致失败。
- 下一步：无演示密码 / token 时继续选择 no-write / mock 可证明缺口，可优先复核正式业务页 action projection 更窄场景、移动端任务端 mock 回归或字段链路剩余守卫；如果要证明真实普通账号菜单投影、authenticated active revision、真实 RBAC 或移动端真实任务动作，必须提供本地演示密码或 token 后再跑真实报告命令。
- 阻塞/风险：本轮只证明 dev-only 测试入口、文档命令来源白名单、移动登录脚本 no-write preflight 和本地 mock 浏览器 L1；不证明真实登录、真实 RBAC、普通账号菜单 / 字段 / 动作投影、authenticated active revision、移动端真实 done / blocked / rejected / urge 动作、真实客户数据导入、客户配置 publish / activate / rollback、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 移动端认证回跳 mock 浏览器 smoke 复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务代码，只把上一轮移动登录 no-write preflight 升级为本地 mock 浏览器 smoke，覆盖岗位任务端认证回跳、mock auth / workflow RPC、任务 / 预警 / 通知 / 进度展示、退出登录、phone / iPad 视口和生产单端口 `/m/<role>/tasks` 路由。
- 验证：追加前 `progress.md` 为 112 行 / 31,167 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false`，69 项本地 check 全部通过，剩余 completion 阻断仍是外部 release evidence；`PATH=/usr/local/bin:$PATH pnpm --dir web smoke:mobile-auth-login-route` 通过，已验证 9 个岗位任务端角色：boss、sales、purchase、production、warehouse、finance、pmc、quality、engineering。过程中仍有既有 Node `MODULE_TYPELESS_PACKAGE_JSON` warning，但未导致失败。
- 下一步：无演示密码 / token 时继续选择 no-write / mock 可证明缺口，可优先复核正式业务页 action projection 更窄场景、移动端 workflow runtime mock / preflight、字段链路剩余守卫或客户配置控制台 no-write 读回；如果要证明真实普通账号菜单投影、authenticated active revision、真实 RBAC 或移动端真实 done / blocked / rejected / urge 动作，必须提供本地演示密码或 token 后再跑真实报告命令。
- 阻塞/风险：本轮 smoke 使用 mock auth / workflow RPC 和本地浏览器运行时，不读取密码 / token，不真实登录，不调用真实后端业务 JSON-RPC，不写数据库；不证明 authenticated active revision、普通账号真实菜单 / 字段 / 动作投影、移动端真实任务写动作、真实客户数据导入、客户配置 publish / activate / rollback、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 移动端任务页 action / reason / L1 本地复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务代码，只复核移动端任务页的本地 mock / static / L1 证据：done / blocked / rejected / urge 动作模型、reason 必填与反馈、催办 / 预警 / 通知展示、移动端事实行不透出技术字段、以及 phone 窄屏 dark 场景和浏览器返回仍停留移动端。
- 验证：追加前 `progress.md` 为 119 行 / 33,248 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false`，69 项本地 check 全部通过，剩余 completion 阻断仍是外部 release evidence；`PATH=/usr/local/bin:$PATH node --test --test-concurrency=1 web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 53 个测试；`STYLE_L1_SCENARIOS=mobile-tasks-dark,mobile-tasks-browser-back-stays-mobile STYLE_L1_PORT=4201 pnpm --dir web style:l1` 通过 2 个场景。过程中仍有既有 Node `MODULE_TYPELESS_PACKAGE_JSON` warning，但未导致失败。
- 下一步：无演示密码 / token 时继续选择 no-write / mock 可证明缺口，可优先复核客户配置控制台 no-write 读回、正式业务页 action projection 更窄场景或字段链路剩余守卫；如果要证明真实普通账号菜单投影、authenticated active revision、真实 RBAC 或移动端真实 done / blocked / rejected / urge 动作，必须提供本地演示密码或 token 后再跑真实报告命令。
- 阻塞/风险：本轮只证明移动端任务页本地模型、mock 浏览器 L1 和真实 smoke 的 no-write / password boundary；不读取密码 / token，不真实登录，不调用真实后端业务 JSON-RPC，不写数据库；不证明 authenticated active revision、普通账号真实菜单 / 字段 / 动作投影、移动端真实任务写动作、真实客户数据导入、客户配置 publish / activate / rollback、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 qa:strict 本地闭环复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只把当前工作区推进到一轮 `qa:strict` 广口径本地验证：覆盖 core / Workflow-Fact / Workflow UI action、正式前端客户配置投影、无凭据 effective session 探针、菜单 / 角色 / `__dev` 入口、试用账号与浏览器 smoke no-write 边界、销售订单字段链路、前端错误提示、文档清单、客户配置包 / runtime manifest / 发布门禁、导入和模拟闭环、web test/build、server test/build、shellcheck / shfmt / govulncheck。
- 验证：追加前 `progress.md` 为 126 行 / 35,552 bytes，未达 600 行或 80KB 归档阈值；`PATH=/usr/local/bin:$PATH bash scripts/qa/strict.sh` 通过，结束标记为 `[qa:strict] 全部通过`。过程中 `gen-error-codes` 按脚本主路径重新生成 `web/src/common/consts/errorCodes.generated.js`，并仍有既有 Node `MODULE_TYPELESS_PACKAGE_JSON` warning，但未导致失败。
- 下一步：无演示密码 / token 时继续选择 no-write / mock / static 可证明缺口；如果要证明真实普通账号菜单投影、authenticated active revision、真实 RBAC 或移动端真实 done / blocked / rejected / urge 动作，必须提供本地演示密码或 token 后再跑真实 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 或 `mobileWorkflowRuntimeBrowserSmoke --report`。
- 阻塞/风险：本轮是本地严格验证闭环，不读取密码 / token，不真实登录，不执行真实客户数据导入，不发布 / 激活 / 回滚客户配置，不做生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；不证明外部 release evidence 已完成。

## 2026-07-02 qa:full 本地 pre-push 门禁复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只补齐 `qa:full` 本地 pre-push 质量门禁证据：先运行 `fast.sh`，再补充 secrets、govulncheck、web test/build、server `go test ./...` 和 `make build`。
- 验证：追加前 `progress.md` 为 133 行 / 37,381 bytes，未达 600 行或 80KB 归档阈值；`PATH=/usr/local/bin:$PATH bash scripts/qa/full.sh` 通过，结束标记为 `[qa:full] 全部通过`。其中 web test 通过 510 个用例，Vite build 成功，server 全量 `go test ./...` 与 `make build` 成功；过程中 `gen-error-codes` 按脚本主路径重新生成错误码文件，仍有既有 Node `MODULE_TYPELESS_PACKAGE_JSON` warning，但未导致失败。
- 下一步：无演示密码 / token 时继续选择 no-write / mock / static 可证明缺口；如果要证明真实普通账号菜单投影、authenticated active revision、真实 RBAC 或移动端真实 done / blocked / rejected / urge 动作，必须提供本地演示密码或 token 后再跑真实 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 或 `mobileWorkflowRuntimeBrowserSmoke --report`。
- 阻塞/风险：`qa:full` 是本地 pre-push 质量门禁，不读取密码 / token，不真实登录，不执行真实客户数据导入，不发布 / 激活 / 回滚客户配置，不做生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；按 `scripts/README.md`，它也不替代浏览器里的样式 / box 模型回归或外部 release evidence。

## 2026-07-02 style:l1 全量浏览器回归复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只在上一轮 `qa:full` 后补齐它不覆盖的浏览器级 L1 回归证据，覆盖当前工作区正式页面、开发入口、移动端、暗色 / 窄屏、交互态、恢复态和相邻区域的 mock runtime 检查。
- 验证：追加前 `progress.md` 为 140 行 / 39,049 bytes，未达 600 行或 80KB 归档阈值；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=4212 /usr/local/bin/pnpm --dir web style:l1` 通过，结束标记为 `[style:l1] 通过，共验证 77 个场景`。过程中仍有既有 Node `MODULE_TYPELESS_PACKAGE_JSON` warning，但未导致失败。
- 下一步：无演示密码 / token 时继续选择 no-write / mock / static 可证明缺口；如果要证明真实普通账号菜单投影、authenticated active revision、真实 RBAC 或移动端真实 done / blocked / rejected / urge 动作，必须提供本地演示密码或 token 后再跑真实 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 或 `mobileWorkflowRuntimeBrowserSmoke --report`。
- 阻塞/风险：`style:l1` 是本地 mock 浏览器回归，不读取密码 / token，不真实登录，不调用真实后端业务写 JSON-RPC，不写数据库；不证明 authenticated active revision、普通账号真实菜单 / 字段 / 动作投影、移动端真实任务写动作、真实客户数据导入、客户配置 publish / activate / rollback、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 customer-config active readback no-write preflight 复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只把客户配置 active revision 读回的前置缺口落成 ignored 本地报告：重新编译 yoyoosun runtime manifest，并运行 `customer-config-release-readiness` 的 `--readback-preflight-report`，确认当前只能证明 manifest / release report 文件存在，仍不能证明目标环境 active effective session 或真实普通账号投影。
- 验证：追加前 `progress.md` 为 147 行 / 40,712 bytes，未达 600 行或 80KB 归档阈值；`git fetch --prune` 完成后本地 `main` 仍对齐 `origin/main`，但工作区仍有大量既有未提交改动；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要为 `ok=true / readOnly=true / releaseReady=false / total=69 / passed=69 / firstBlockedAction=immutable-version`；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node scripts/deploy/customer-config-release-readiness.mjs --manifest output/customers/yoyoosun/customer-config-runtime-manifest.json --evidence-dir deployments/yoyoosun/evidence/releases/2026-06-29 --release-report output/customers/yoyoosun/customer-config-release/customer-config-release-report.json --readback-preflight-report output/customers/yoyoosun/customer-config-readback-preflight.json --json` 写入 no-write 报告并返回 `readyForReadinessGate=false`，blockers 为 `missing-effective-session-verification`、`smoke-report-missing-backend-endpoint-alias`、`missing-smoke-effective-session-check`；`git check-ignore -v` 确认两份 `output/` 报告均被 `.gitignore` 忽略；`node --test scripts/deploy/customer-config-release-readiness.test.mjs scripts/deploy/customer-config-release-execute.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs scripts/qa/multi-client-role-workflow-priority-audit.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 75 个测试；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview` 通过。
- 下一步：无演示密码 / token 时继续选择 no-write / mock / static 可证明缺口；若要证明 active revision 来源、普通账号菜单 / 字段 / 动作投影或目标 smoke 读回，必须有真实执行报告、目标 smoke backend alias、customer config admin token 或本地演示密码后再运行真实读回 / smoke 命令。
- 阻塞/风险：本轮只生成本地 ignored preflight 报告，不调用后端 publish / activate / rollback，不读取 token，不登录，不写数据库，不写 release evidence；不证明 authenticated active revision、普通账号真实 RBAC、移动端真实任务写动作、真实客户数据导入、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 ERP MVP no-write 闭环报告复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只按当前 `scripts/README.md` 的主路径运行 `mvp-closure` no-write 汇总入口，把试用模拟数据、业务事实模拟和移动端 Workflow 模拟闭环的 report-only 证据收口到同一份 ignored 本地报告。
- 验证：追加前 `progress.md` 为 154 行 / 43,832 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false`，本地 69 项 ready，第一条 release 缺口仍是 `immutable-version`；`node --test scripts/qa/mvp-closure.test.mjs scripts/qa/trial-simulated-data.test.mjs scripts/qa/operational-fact-simulated-closure.test.mjs scripts/qa/mobile-workflow-simulated-closure.test.mjs` 通过 28 个测试，锁住 `mvp-closure` 不支持 `--apply / --execute` 且子工具 input template / report-only 不写库；`node scripts/qa/mvp-closure.mjs --run-report-tools --product-id 1 --unit-id 1 --warehouse-id 1 --out output/customers/yoyoosun/mvp-closure` 生成 `mvp-closure-report.json` 和 `mvp-closure-report.md`，报告显示 `mode=report-with-no-write-tools / simulatedOnly=true / writesDatabase=false / callsBackend=false / realCustomerImport=false / missingRequiredPaths=[]`，三条 no-write 子工具 `trial-simulated-data`、`operational-fact-simulated-closure`、`mobile-workflow-simulated-closure` 均为 `PASS`；`git check-ignore -v output/customers/yoyoosun/mvp-closure/mvp-closure-report.json output/customers/yoyoosun/mvp-closure/mvp-closure-report.md` 确认报告在 `.gitignore` 的 `output/` 边界内。
- 下一步：无演示密码 / token 时继续选择 no-write / mock / static 可证明缺口；若要把该 MVP 计划升级为真实本地或试用环境证据，必须分别运行对应脚本的显式 `--apply` 路径并提供确认环境变量、已有模拟主数据 ID 和后端目标，不允许通过 `mvp-closure` 汇总入口直接写入。
- 阻塞/风险：本轮 `mvp-closure` 只证明当前仓库具备本地 report-only 验收计划和三组 no-write 子报告，不连接后端、不写数据库、不真实登录、不证明真实 RBAC、authenticated active revision、普通账号菜单 / 字段 / 动作投影、移动端真实任务写动作、真实客户数据导入、客户配置 publish / activate / rollback、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 采购入库真实写入链路 no-write 前置复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只按 `T7 业务事实 / E2E` 当前主路径复核采购入库第一条真实写入链路的服务层和浏览器前置入口；服务层 preflight 已 ready，浏览器 preflight 被正确挡在真实写入前置条件，不把 no-write 前置报告误写成真实过账证据。
- 验证：追加前 `progress.md` 为 161 行 / 46,437 bytes，未达 600 行或 80KB 归档阈值；`git fetch --prune` 通过；`git status -sb --untracked-files=all` 确认当前工作区仍有大量未提交改动，本轮未清理、未回退、未 stash；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / total=69 / passed=69 / firstBlockedAction=immutable-version`；`node scripts/qa/purchase-receipt-real-write-e2e.mjs --print-input-template` 只输出服务层真实写入输入模板，不跑 Go test、不连 PostgreSQL、不写报告、不写库；`node scripts/qa/purchase-receipt-real-write-e2e.mjs --preflight-report output/qa/purchase-receipt-real-write-e2e/preflight.json` 生成 ignored 本地脱敏报告，`callsBackend=false` 且无 blocker；`node web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs --print-input-template` 只输出浏览器真实写入输入模板，不读本地配置、不启动 Vite / Playwright、不登录、不写库；`node web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs --preflight-report output/purchase-receipt-real-write-browser-e2e/preflight.json` 生成 ignored 本地前置报告，`writesDatabase=false / startsBrowser=false / startsDevServer=false`，并因缺少 `missing-persistent-test-data-acceptance` 与 `missing-admin-credential-env` 保持 `ready=false`；`node --test scripts/qa/purchase-receipt-real-write-e2e.test.mjs web/scripts/purchaseReceiptRealWriteBrowserE2E.test.mjs` 通过 13 个测试；`git check-ignore -v output/qa/purchase-receipt-real-write-e2e/preflight.json output/purchase-receipt-real-write-browser-e2e/preflight.json` 确认报告仍在 `.gitignore` 的 `output/` 边界内。
- 下一步：若要升级到真实本地 / 试用模拟事实写入，必须提供本地管理员账号密码环境变量，并显式接受 `PR-BROWSER-*` 持久测试数据；服务层真实链路再运行 `node scripts/qa/purchase-receipt-real-write-e2e.mjs --out output/qa/purchase-receipt-real-write-e2e`，浏览器真实链路再运行 `pnpm --dir web smoke:purchase-receipt-real-write` 或脚本等价命令。无凭据时继续选择 no-write / mock / static 可证明缺口。
- 阻塞/风险：本轮只证明采购入库真实写入链路的前置入口、no-write 边界和本地脚本合同未坏；没有登录、没有调用真实 JSON-RPC、没有写采购入库事实、没有过账库存、没有取消冲正、没有证明真实 RBAC、authenticated active revision、客户配置 publish / activate / rollback、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 行业模板模拟闭环 no-write 复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只复核行业模板候选的本地模拟闭环，确认 yoyoosun 只能作为种子客户样本参与行业模板候选，不把单客户字段、菜单、编号或导入资料直接升成 Product Core 默认。
- 验证：追加前 `progress.md` 为 168 行 / 49,615 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / total=69 / passed=69 / firstBlockedAction=immutable-version`；`node scripts/qa/industry-template-boundaries.mjs` 通过，输出 `industry template boundaries ok: plush, roles=9, menuSections=13, importMode=dry_run_only`；`node --test scripts/qa/industry-template-closure.test.mjs` 通过 1 个测试；`node scripts/qa/industry-template-closure.mjs --out output/customers/yoyoosun/industry-template-closure` 生成 ignored 本地报告，报告显示 `templateKey=plush / status=candidate / runtimeEnabled=false`，`roles=9 / desktopMenuSections=13 / desktopMenuItems=28 / mobileTaskPatterns=6 / fieldModules=7 / numberingRules=7 / importSources=5 / deferredItems=8`，并且 `noRealImport / dryRunOnly / noTenant / noSchemaChange / noMigrationChange / noRuntimeMenuLoaderChange / noWorkflowFactChange / noBusinessRecordWrite / noFactWrite` 均为 `true`；`git check-ignore -v output/customers/yoyoosun/industry-template-closure/industry-template-report.json output/customers/yoyoosun/industry-template-closure/industry-template-report.md` 确认报告仍在 `.gitignore` 的 `output/` 边界内。
- 下一步：无演示密码 / token 时继续选择 no-write / mock / static 可证明缺口，可优先复核多客户私有化复制包模拟闭环、客户导入 dry-run / freeze 本地证据或现有字段链路守卫；有演示密码后再运行真实 RBAC / 浏览器 / 移动端 smoke 报告。
- 阻塞/风险：本轮只证明行业模板候选配置和本地报告生成边界，不启用 runtime 模板，不导入真实客户数据，不创建 tenant，不改 schema / migration，不改运行时菜单 loader，不改变 Workflow / Fact 规则，不写 `business_records` 或事实表；不证明第二客户真实部署、客户配置 publish / activate / rollback、真实账号、真实 RBAC、authenticated active revision、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 客户导入 dry-run / freeze no-write 复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只复核 yoyoosun 客户导入前的 source manifest、fixture dry-run 和 freeze evidence 工具，确认当前仍是 T6 导入前证据链，不进入真实导入、不写正式表、不写 `business_records`。
- 验证：追加前 `progress.md` 为 175 行 / 52,209 bytes，未达 600 行或 80KB 归档阈值；`git fetch --prune` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / total=69 / passed=69 / firstBlockedAction=immutable-version`；`node --test scripts/import/customerSourceManifestCheck.test.mjs scripts/import/customerSourceExtract.test.mjs scripts/import/customerImportDryRun.test.mjs scripts/import/customerSourceSnapshotFreezeCheck.test.mjs` 通过 35 个测试，覆盖 no DB / no server / no web runtime、敏感字段不输出原始值、`shipping_released != shipped`、Workflow done 不等于 Fact posted、forbidden / deferred / blocker 等导入边界；`node scripts/import/customerSourceManifestCheck.mjs --manifest docs/customers/yoyoosun/source-manifest.json --raw-dir docs/customers/yoyoosun/raw-source-files` 输出 `sourceCount=11 / structuredExtractCount=5 / canExecuteRealImport=false`；`node scripts/import/customerImportDryRun.mjs --source scripts/import/fixtures/customers/yoyoosun/source-snapshot.freeze.sample.json --existing scripts/import/fixtures/customers/yoyoosun/existing-v1.freeze.sample.json --out output/customers/yoyoosun/real-dry-run-evidence` 生成 ignored 本地 dry-run package，`validation-summary.json` 显示 `normalizedRows=20 / canProceedToManualReview=true / canExecuteRealImport=false / blockerCount=32 / forbiddenCount=15`；`node scripts/import/customerSourceSnapshotFreezeCheck.mjs --source scripts/import/fixtures/customers/yoyoosun/source-snapshot.freeze.sample.json --existing scripts/import/fixtures/customers/yoyoosun/existing-v1.freeze.sample.json --out output/customers/yoyoosun/source-snapshot-freeze` 生成 ignored freeze evidence，`freezeId=customer-freeze-b31e577e283e-4caf9a2483b8 / valid=false / canExecuteRealImport=false`，`freeze-check-summary.json` 显示 `blockerCount=17 / warningCount=10 / forbiddenFieldCount=11 / deferredFieldCount=5 / shippingBoundaryRiskCount=4 / workflowFactBoundaryRiskCount=2`；`git check-ignore -v output/customers/yoyoosun/real-dry-run-evidence/validation-summary.json output/customers/yoyoosun/real-dry-run-evidence/dry-run-report.md output/customers/yoyoosun/source-snapshot-freeze/freeze-metadata.json output/customers/yoyoosun/source-snapshot-freeze/freeze-check-summary.json output/customers/yoyoosun/source-snapshot-freeze/freeze-check-report.md` 确认报告仍在 `.gitignore` 的 `output/` 边界内。
- 下一步：无演示密码 / token 时继续选择 no-write / mock / static 可证明缺口，可优先复核多客户私有化复制包模拟闭环、部署资料包 lint 或现有字段链路守卫；如要进入真实客户导入，必须另开数据治理评审，先具备客户确认、备份 evidence、reviewed recovery plan、目标环境信息和显式确认，且仍只能走受控 JSON-RPC V1 API。
- 阻塞/风险：本轮只证明导入前 source manifest / fixture dry-run / freeze 工具和边界测试，不解析成正式导入批准，不执行真实客户数据导入，不连接数据库，不读取 server config，不调用 web runtime，不写正式表、不写 `business_records`、不生成 SQL / migration、不创建出货、库存或财务事实；不证明真实账号、真实 RBAC、authenticated active revision、客户配置 publish / activate / rollback、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 私有化复制包 no-write 复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只复核多客户私有化复制包的本地模拟闭环和 yoyoosun 部署资料包结构，确认当前只是 template / package candidate，不创建真实客户目录、不执行目标环境部署、不在服务器构建。
- 验证：追加前 `progress.md` 为 182 行 / 56,132 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / total=69 / passed=69 / firstBlockedAction=immutable-version`；`node scripts/qa/private-deployment-boundaries.mjs` 通过，输出 `private deployment boundaries ok: private-customer-package, roots=3, simulatedKey=SIM-PRIVATE-DEPLOYMENT`；`node scripts/deploy/deployment-package-lint.mjs --customer yoyoosun` 通过，输出 `required=41 / files=56`；`node --test scripts/deploy/deployment-package-lint.test.mjs scripts/qa/private-deployment-package-closure.test.mjs` 通过 12 个测试，覆盖资料包敏感文件 / raw customer files 拒绝、release evidence / backup / migration / signoff / rollback / smoke 模板字段守卫和私有化复制模拟边界；`node scripts/qa/private-deployment-package-closure.mjs --out output/customers/yoyoosun/private-deployment-package-closure` 生成 ignored 本地报告，报告显示 `reviewMilestone=private-deployment-package-candidate / templateKey=private-customer-package / status=template_candidate / runtimeEnabled=false / simulatedCustomerKey=SIM-PRIVATE-DEPLOYMENT`，`packageRoots=3 / customerDocs=10 / customerConfigFiles=3 / deploymentDocs=23 / reusableSources=4 / industryRoles=9 / industryMenuSections=13`，并且 `noTenant / noSaas / noSchemaChange / noMigrationChange / noRbacChange / noWorkflowFactChange / noRuntimeLoaderChange / noCodeFork / noRealImport / noBusinessRecordWrite / noFactWrite / noTargetBuild / industryTemplateStillCandidate / simulatedKeyNotRuntime` 均为 `true`；`git check-ignore -v output/customers/yoyoosun/private-deployment-package-closure/private-deployment-package-report.json output/customers/yoyoosun/private-deployment-package-closure/private-deployment-package-report.md` 确认报告仍在 `.gitignore` 的 `output/` 边界内。
- 下一步：无演示密码 / token 时继续选择 no-write / mock / static 可证明缺口，可优先复核部署资料包之外的字段链路守卫、产品内帮助 / 培训口径或现有客户配置控制台读回缺口；若要进入真实新客户私有化交付，必须另行确认稳定 customer key、客户资料入仓边界、导入 dry-run / unresolved queue、部署地址、备份恢复和验收清单。
- 阻塞/风险：本轮只证明私有化复制包模板和 yoyoosun 部署资料包 lint 的本地 no-write 边界，不创建 `docs/customers/SIM-PRIVATE-DEPLOYMENT`、`config/customers/SIM-PRIVATE-DEPLOYMENT` 或 `deployments/SIM-PRIVATE-DEPLOYMENT`，不创建 tenant，不做 SaaS、多租户、license / billing，不改 schema / migration / RBAC / Workflow / Fact / runtime loader，不导入真实客户数据，不写 `business_records` 或事实表，不在目标服务器构建；不证明真实第二客户部署、真实账号、authenticated active revision、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 产品内帮助 / 培训口径 no-write 复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑和正式文档，只复核产品内帮助 / 培训口径是否仍与当前真源一致：正式前端不恢复旧产品内文档中心、帮助中心、高级文档或开发与验收运行时入口；`/__dev/testing`、`/__dev/docs`、`/__dev/capability-ledger` 和 yoyoosun 试用 runbook 仍作为本地开发 / 试用验收说明入口，不进入正式客户菜单或 RBAC 安全边界。
- 验证：追加前 `progress.md` 为 189 行 / 59,622 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / total=69 / passed=69 / statuses.ready=67 / statuses.guarded=1 / statuses.evidence-required=1 / blockingCategory=external-release-evidence-required`，并明确仍不证明目标环境 release、migration、smoke、备份恢复、回滚演练、客户配置目标环境激活 / 回滚、super admin break-glass 或真实客户导入；`rg` 复核旧 `/erp/docs`、`/erp/qa`、`/erp/help-center`、`/erp/source-readiness`、`/erp/mobile-workbenches` 和 `docRegistry` 命中均停留在 README / 当前真源说明或测试守卫中，没有发现恢复为正式运行时入口；`node --test scripts/qa/trial-role-entry-docs.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/docs-inventory.test.mjs web/src/erp/config/devHub.test.mjs web/src/erp/config/devDocs.test.mjs web/src/erp/config/devTesting.test.mjs web/src/erp/config/devCapabilityLedger.test.mjs` 通过 37 个测试，覆盖试用账号 / 岗位路径 / yoyoosun runbook 口径、dev-only 导航、测试入口只索引当前维护文档、reference / archive 不作为可复制命令来源、dev docs 不恢复产品内文档 registry、能力台账只读解析产品能力和 yoyoosun 交付矩阵；`git diff --check` 通过。
- 下一步：无演示密码 / token 时继续选择 no-write / mock / static 可证明缺口，可优先复核字段链路剩余守卫或客户配置控制台读回缺口；若要证明培训口径对应真实账号可用，必须提供本地演示密码或 token 后再运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 或移动端真实 smoke 报告。
- 阻塞/风险：本轮只证明帮助 / 培训 / dev-only 测试入口的本地静态与单测口径，不真实登录、不调用后端业务写 JSON-RPC、不写数据库、不更新正式客户菜单、不发布或激活客户配置、不导入真实客户数据；不证明 authenticated active revision、普通账号真实 RBAC、移动端真实任务写动作、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 字段链路与打印边界 no-write 复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只刷新字段链路覆盖报告并复核销售订单受理字段链、打印边界、客户配置 runtime manifest 和技术字段可见性：字段链路覆盖报告从旧的 8/30 覆盖刷新为 30/30 覆盖；销售订单字段策略仍同时控制列表和 CSV 导出，表单字段仍走共享 mapper，SKU 来源切换会清理残值；销售订单打印边界仍保持显式未接通，不发布 `sales_order_items.default`，不补销售订单打印模板。
- 验证：追加前 `progress.md` 为 196 行 / 62,509 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / total=69 / passed=69 / statuses.ready=67 / statuses.guarded=1 / statuses.evidence-required=1`，唯一 `evidence-required` 仍是 `release-evidence-target-remains-evidence-required`，唯一 `guarded` 仍是 `domain-command-entry-remains-guarded`；`node scripts/qa/erp-field-linkage.mjs` 通过 108 个测试并生成 `web/public/qa/erp-field-linkage-coverage.latest.json`，报告摘要为 `totalFields=30 / coveredFields=30 / partialFields=0 / missingFields=0 / totalCases=49 / passedCases=49 / totalScenarios=53 / passedScenarios=53`；`node --test scripts/qa/sales-order-field-chain-boundary.test.mjs web/src/erp/config/printTemplates.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 46 个测试；`node --test scripts/qa/docs-inventory.test.mjs` 通过，`markdownFiles=263`；`git diff --check` 通过；`git check-ignore -v output/qa/field-linkage/node-test.tap` 确认 TAP 输出仍在 ignored `output/` 边界内。
- 下一步：无演示密码 / token 时继续选择 no-write / mock / static 可证明缺口，可优先复核客户配置控制台 readback、真实运行入口 preflight 或 raw id / 错误提示剩余静态守卫；若要证明 sales order 打印完整链路，必须同时补 mapper / 表单 / 导出 / 打印 / 历史缺值测试，不能只发布模板。
- 阻塞/风险：本轮只证明本地字段链路、打印未接通边界、客户配置 runtime manifest 和技术字段可见性守卫，不真实登录、不调用后端业务写 JSON-RPC、不写数据库、不发布或激活客户配置、不导入真实客户数据、不补销售订单打印 mapper / 模板；不证明 authenticated active revision、普通账号真实 RBAC、移动端真实任务写动作、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 yoyoosun 本地入口当前运行态复核

- 完成：继续当前 plush goal 的最高优先级切片，不重启旧计划、不扩大 release evidence。本轮不改业务代码，只按当前真实运行态复核 yoyoosun 本地入口、端口归属、静态客户配置注入、后端 `get_effective_session` 无凭据边界和 runtime manifest。当前 `start:yoyoosun --print-plan` 与 `preview:yoyoosun --print-plan` 都因 `5176` 被其他项目占用而计划顺延到 `5178`；但当前已经实际运行的 yoyoosun 入口仍是 `http://localhost:5177/erp`。端口归属为：`5175` 是本仓库 Product Core Vite dev，占位 `customer-config.js`；`5176` 是 `/Users/simon/projects/openai-oauth-api-service/web`，只命中 HTML fallback；`5177` 是本仓库 yoyoosun Vite dev，命中 `customerKey: "yoyoosun"` 和 yoyoosun SVG asset；`5178/5179` 当前未监听。
- 验证：追加前 `progress.md` 为 203 行 / 65,360 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / total=69 / passed=69 / statuses.ready=67 / statuses.guarded=1 / statuses.evidence-required=1 / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web start:yoyoosun -- --print-plan` 和 `preview:yoyoosun -- --print-plan` 均输出计划端口 `5178`、backend `http://127.0.0.1:8300`、且明确不执行 customer_config publish / activate；`lsof` / `ps` 确认 `5175`、`5177` 的 cwd 都是本仓库 `web`，`5176` 的 cwd 是 `openai-oauth-api-service/web`；手工 `curl -4` 验证 `5175/customer-config.js` 是中性 Product Core placeholder，`5176/customer-config.js` 是 HTML fallback，`5177/customer-config.js` 是 yoyoosun 配置，`5177/customer-assets/yoyoosun/favicon-yoyoosun.svg` 返回 `content-type: image/svg+xml`；复跑 `pnpm --dir web --silent audit:yoyoosun-entry -- --json` 得到 `yoyoosunPorts=[5177] / productCorePlaceholderPorts=[5175] / htmlFallbackPorts=[5175,5176] / backendHealth.ok=true`；`node scripts/qa/customer-config-effective-session-probe.mjs --json` 返回 `health.ok=true` 且 `resultCode=40302 / message=未登录 / status=auth_required / expectedUnauthenticatedBoundary=true / blockers=[missing-authenticated-admin-session]`；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview` 通过，`modules=15 / roles=9 / entitlements=346`；`node --test --test-concurrency=1 web/scripts/yoyoosunEntryPlan.test.mjs scripts/qa/customer-config-effective-session-probe.test.mjs web/devCustomerConfigPlugin.test.mjs web/scripts/localPort.mjs web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 36 个测试；`node --test scripts/qa/customer-config-runtime-manifest.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 21 个测试。
- 下一步：如果要继续证明“为什么页面看起来仍像 Product Core”，必须提供本地演示密码或 token 后跑真实 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 或 `mobileWorkflowRuntimeBrowserSmoke --report`，分别区分 super_admin 产品核心看全、普通客户角色菜单 / 页面 / 字段策略收窄和岗位任务端真实动作；无凭据时只能继续推进 no-write / mock / static 可证明缺口，不能声称 active revision、普通账号投影或真实 RBAC 已证明。
- 阻塞/风险：本轮只证明当前本机端口归属、静态 yoyoosun 注入、后端 health、无凭据 effective-session 鉴权边界、runtime manifest 编译和相关脚本合同；不读取密码 / token，不真实登录，不证明当前浏览器账号是否为 super_admin，不证明 active customer config revision 来源、普通客户角色投影、真实 RBAC、移动端真实任务动作、客户配置 publish / activate / rollback、真实客户数据导入、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 本地闭环 completion audit 复核

- 完成：继续当前 plush goal 的本地收口，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只复核多甲方角色 / Workflow 优先级 audit 的 completion 状态：P0 到 P4 本地均为 `ready`，P5 仍是 `target-evidence-required`；本地可继续用 priority map 作为执行队列，但不能把目标环境 release evidence 缺口升级成本轮主任务或本地阻断。
- 验证：追加前 `progress.md` 为 210 行 / 69,583 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要为 `ok=true / readOnly=true / releaseReady=false / total=69 / passed=69 / statuses.ready=67 / statuses.guarded=1 / statuses.evidence-required=1 / blockingCategory=external-release-evidence-required`。`completionAudit.state=target-evidence-required`，`canCompleteLocally=false`；唯一 `evidence-required` 仍是 `release-evidence-target-remains-evidence-required`，唯一 `guarded` 仍是 `domain-command-entry-remains-guarded`。未证明项仍限于目标环境 release、migration、smoke、备份恢复、回滚演练、客户配置目标环境激活 / 回滚、super admin break-glass 和真实客户导入。
- 下一步：无演示密码 / token 时继续选择 no-write / mock / static 可证明缺口；若要证明普通账号菜单 / 字段 / 动作投影、authenticated active revision 或移动端真实任务写动作，必须先提供本地演示密码或 token 后再运行真实登录 / 浏览器 / 移动端 smoke 报告。目标环境 release evidence、生产部署、真实导入、签收、备份恢复和回滚演练继续保持低优先级，只保留受控命令入口和缺口说明。
- 阻塞/风险：本轮是只读 completion audit 与过程记录，不读取密钥，不真实登录，不调用后端写 JSON-RPC，不写数据库，不发布 / 激活 / 回滚客户配置，不执行生产部署、目标环境 migration、真实客户导入、备份恢复、回滚演练、客户签收、提交或推送；不证明全局 goal 已完成。

## 2026-07-02 错误提示与技术字段可见性守卫复核

- 完成：继续当前 plush goal 的本地闭环，不重启旧计划、不扩大 release evidence。本轮不改业务逻辑，只复核错误提示、审计日志、任务来源、业务页面 raw id / 内部字段和底层英文异常的用户可见层守卫，确认正式前端仍通过共享 helper 和静态 / 浏览器负断言收口，不把 `id / *_id / source_type / owner_role_key / task_status_key / payload` 等技术实现暴露给业务用户。
- 验证：追加前 `progress.md` 为 217 行 / 71,717 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false / total=69 / passed=69 / statuses.ready=67 / statuses.guarded=1 / statuses.evidence-required=1`；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/common/utils/errorMessage.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/dashboardTaskDisplay.test.mjs` 通过 39 个测试；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=system-audit-logs-desktop STYLE_L1_PORT=4175 /usr/local/bin/pnpm --dir web style:l1` 通过 1 个浏览器场景；`node --test scripts/qa/docs-inventory.test.mjs` 通过，`markdownFiles=263`。
- 下一步：无演示密码 / token 时继续选择 mock / static / no-write 可证明缺口；若要证明真实账号菜单投影、authenticated active revision、移动端真实任务动作或真实错误返回在浏览器中的收口，必须提供本地演示密码或 token 后再跑真实登录 / 浏览器 / 移动端 smoke 报告。
- 阻塞/风险：本轮只证明本地静态扫描、单元测试和一个 mock 浏览器场景，不登录、不调用真实业务写 JSON-RPC、不写数据库、不发布 / 激活 / 回滚客户配置、不导入真实客户数据；不证明真实账号、真实 RBAC、真实后端错误链路、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 active module state / Workflow 写门禁复核

- 完成：继续当前 plush goal 的 P0 本地边界，不重启计划、不扩大 release evidence。本轮未改业务逻辑，只复核 Workflow 写入口、解释合同、角色 / 菜单投影和前端正式 UI action 守卫：正式按钮继续走 `complete_task_action` / `block_task_action` / `reject_task_action` 等 JSON-RPC action 合同，不回退 `update_task_status`；`workflow_tasks` active module state 仍拦截写方法，历史 `list` / `explain` 读取保持可用；前端不把 Workflow task done 写成 Fact posted。
- 验证：追加前 `progress.md` 为 224 行 / 73803 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过，69/69 passed，`completionState=target-evidence-required`，`releaseReady=false` 仅来自目标环境 evidence 缺口；`go test ./internal/service -run 'TestJsonrpcDispatcher_WorkflowWriteAPIRequiresEnabledModule|TestJsonrpcDispatcher_WorkflowCompleteTaskActionUsesDoneAndServerActorRole|TestJsonrpcDispatcher_WorkflowControlledTaskActionsUseServerStatusAndActorRole|TestJsonrpcDispatcher_WorkflowUrgeTaskRejectsEmptyReason|TestJsonrpcDispatcher_WorkflowUpdateTaskStatusRemoved|TestJsonrpcDispatcher_WorkflowExplain'` 通过；`go test ./internal/biz -run 'TestBuiltinRoleWorkflowPermissionMatrix|TestMobileRoleAccessPermissionIncludesEngineering|TestNormalizeAdminMobileRolePermissionsUsesCurrentRoleKeys|TestAdminMobileRolePermissionOptionsIncludeEngineering'` 通过；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs scripts/qa/multi-client-role-workflow-priority-audit.test.mjs web/src/erp/config/menuPermissions.test.mjs web/src/erp/utils/adminProfileSync.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs` 通过，73/73 passed。
- 下一步：继续按 mock / static / no-write 切片收口本地试用闭环；若要证明真实普通账号 action、真实 mobile writes 或目标环境 active revision，必须提供密码 / token 和对应目标环境，并单独跑真实登录 / 浏览器 / 写入回归。
- 阻塞/风险：本轮只证明本地 T3/T4/T5 合同与静态守卫，不登录、不写 DB、不执行真实 JSON-RPC 写请求、不证明目标环境；Node `MODULE_TYPELESS_PACKAGE_JSON` warning 仍是现有测试运行警告，未在本轮处理。

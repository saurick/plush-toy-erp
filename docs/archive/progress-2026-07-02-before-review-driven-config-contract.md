# progress 归档：2026-07-02 review-driven config contract 前

本文件归档 `progress.md` 中 2026-07-02 模拟 report-only 后至测试业务数据隔离守卫收口的过程记录。归档内容只用于追溯，不作为当前正式需求、数据模型或部署真源。

## 2026-07-02 progress 归档到模拟 report-only 后

- 完成：归档前 `progress.md` 为 252 行 / 83,853 bytes，已超过 80KB 归档阈值；已将 2026-07-02 `progress 归档到下一本地闭环前` 至 `active module state / Workflow 写门禁复核` 的过程记录复制归档到 `docs/archive/progress-2026-07-02-before-simulated-report-only-closure.md`。
- 完成：根 `progress.md` 保留归档索引、当前活跃事项和最近三条本地 no-write / report-only 复核记录，避免过程流水继续膨胀为隐藏真源。
- 下一步：继续按当前 goal 选择一个本地最高优先级闭环；无演示密码 / token 时只推进 mock / static / no-write 可证明缺口，有凭据后再跑真实 RBAC、浏览器和移动端 smoke 报告。
- 阻塞/风险：本轮归档不改变 runtime、schema、RBAC、Workflow / Fact、客户配置、测试脚本、部署、真实客户数据、目标环境 evidence、提交或推送；归档文件只用于追溯，不作为当前正式真源。

## 2026-07-02 qa:fast 当前工作区本地门禁复核

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未主动改业务代码，只在当前大范围 dirty worktree 上复跑 `scripts/qa/fast.sh`，确认当前本地 T0-T6 / 部分 T7-no-write 门禁仍能完整通过，覆盖错误码、Workflow / Fact 边界、Workflow UI action、正式前端客户配置投影、无凭据 effective-session 探针、角色菜单 / 入口、`__dev`、试用账号 / 浏览器 smoke 输入模板、移动端认证回跳、采购入库真实写入 preflight、销售订单字段链路、错误提示 / 技术字段、客户配置包 / runtime manifest、导入 tooling、模拟闭环、web lint / css 和 server Go tests。
- 验证：追加前 `progress.md` 为 71 行 / 18,092 bytes，低于归档阈值；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 完整通过并输出 `[qa:fast] 完成`；后续 `git diff --check` 通过。过程中仍有既有 Node `MODULE_TYPELESS_PACKAGE_JSON` warning，但未导致失败。
- 下一步：无演示密码 / token 时继续推进 mock / static / no-write 可证明缺口；若要证明普通账号真实菜单投影、authenticated active revision、移动端真实任务动作、真实业务写链路或目标环境发布状态，必须分别提供本地凭据、真实后端前置或目标环境 evidence 后运行对应真实 smoke / E2E / release gate。
- 阻塞/风险：`qa:fast` 是本地门禁，不读取密码 / token，不执行真实客户导入，不发布 / 激活 / 回滚客户配置，不做生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；本轮不证明全量 `qa:full`、`qa:strict`、完整 `style:l1`、真实账号浏览器回归、真实 DB 写入 E2E 或目标环境 release evidence 已完成。

## 2026-07-02 移动端单端口入口与 no-write preflight 复核

- 完成：继续当前 plush goal 的本地闭环，不重启计划、不扩大 release evidence。本轮未改业务代码，只复核岗位任务端单端口 `/m/<role>/tasks`、`__dev/testing` 可复制入口和旧 mobile 多实例入口退出边界；静态搜索确认运行入口不再引用 `startMobileApps`、`vite.mobile-*`、旧 `mobile-*` 多实例 `/tasks` 或 `web/src/erp/mobile/router.jsx`，命中项均为“已退出 / 不再生成”的边界说明。
- 验证：追加前 `progress.md` 为 64 行 / 14,920 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false`，目标环境 release evidence 仍是低优先级缺口；`node --test web/src/erp/config/devTesting.test.mjs web/src/erp/config/devCustomerConfig.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 28 个测试；`node --test web/scripts/mobileAuthLoginRouteSmoke.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs` 通过 40 个测试；`node web/scripts/mobileAuthLoginRouteSmoke.mjs --print-input-template` 和 `node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --print-input-template` 均只打印 no-write 输入模板；`node web/scripts/mobileAuthLoginRouteSmoke.mjs --preflight-report output/mobile-auth-login-route-smoke/preflight.json` 生成 no-write 报告，`writesDatabase=false / callsBackend=false / startsBrowser=false / startsDevServer=false / blockers=[]`；`node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report output/mobile-workflow-runtime-browser-smoke/preflight.json` 生成 no-write 报告，`preflightOnly=true / writesDatabase=false / startsBrowser=false / startsDevServer=false / blockers=[missing-demo-password-env]`，模拟动作计划覆盖 `block / complete / reject / urge-only`、reason、完成反馈、异常上报和内部通知线索；`pnpm --dir web --silent audit:yoyoosun-entry -- --json` 当前仍显示 `5177` 是 yoyoosun 入口、`5175` 是 Product Core placeholder、`5176` 是其他项目 HTML fallback；两个 `output/` 报告均由 `.gitignore` 忽略；`node --test scripts/qa/docs-inventory.test.mjs` 通过。
- 下一步：无演示密码 / token 时继续选择 mock / static / no-write 可证明缺口；若要证明真实账号菜单投影、authenticated active revision、移动端真实 done / blocked / rejected / urge 动作或真实通知反馈，必须提供本地演示密码或 token 后运行真实登录 / 浏览器 / 移动端 smoke 报告。
- 阻塞/风险：本轮只证明本地静态入口、单元测试和 no-write preflight 报告；不启动真实浏览器、不登录、不调用真实后端写 JSON-RPC、不写数据库、不创建真实 workflow 任务、不导入真实客户数据、不发布 / 激活 / 回滚客户配置、不证明目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 __dev/customer-config 控制台 no-write 复核

- 完成：继续当前 plush goal 的本地闭环，不重启计划、不扩大 release evidence。本轮未改业务逻辑，只复核 `__dev/customer-config` 控制台、moduleStates、打印模板字段预检、UI dry-run 边界、publish / activate / rollback 输入模板和 active revision 读回 preflight：控制台仍是 dev-only，本地报告仍写 ignored `output/`，release executor / readiness 模板仍区分 no-write、报告模式、显式执行、激活、回滚和目标 smoke 读回。
- 验证：追加前 `progress.md` 为 231 行 / 76231 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过，69/69 passed，`completionState=target-evidence-required`，`releaseReady=false` 仅来自目标环境 evidence 缺口；`node --test web/src/erp/config/devCustomerConfig.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/multi-client-role-workflow-priority-audit.test.mjs web/src/erp/config/printTemplates.test.mjs scripts/deploy/customer-config-release-execute.test.mjs scripts/deploy/customer-config-release-readiness.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs` 通过，92/92 passed；`node scripts/deploy/customer-config-release-execute.mjs --print-input-template` 和 `node scripts/deploy/customer-config-release-readiness.mjs --print-input-template` 通过，均声明不调用后端、不写库、不证明 active revision；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop STYLE_L1_PORT=4176 /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node scripts/deploy/customer-config-release-readiness.mjs --manifest output/customers/yoyoosun/customer-config-runtime-manifest.json --evidence-dir deployments/yoyoosun/evidence/releases/2026-06-29 --release-report output/customers/yoyoosun/customer-config-release/customer-config-release-report.json --readback-preflight-report output/customers/yoyoosun/customer-config-readback-preflight.json --json` 生成 no-write 报告，`readyForReadinessGate=false`，blockers 为 `missing-effective-session-verification`、`smoke-report-missing-backend-endpoint-alias`、`missing-smoke-effective-session-check`；`git check-ignore -v output/customers/yoyoosun/customer-config-runtime-manifest.json output/customers/yoyoosun/customer-config-readback-preflight.json` 确认输出仍在 `.gitignore` 的 `output/` 边界内。
- 下一步：无演示密码 / token / customer config admin token 时继续选择 mock / static / no-write 可证明缺口；若要证明 active revision、普通账号菜单 / 字段 / 动作投影、目标 smoke 读回或 rollback 生效，必须提供真实执行报告、目标 smoke backend alias、customer config admin token 或本地演示密码后再运行真实读回 / smoke / executor 命令。
- 阻塞/风险：本轮只证明 dev-only 控制台和客户配置发布链路的本地 T1/T5/T8-no-write 合同，不调用 `publish_customer_config / activate_customer_config / rollback_customer_config`，不登录、不写 DB、不上传 raw 包、不执行真实导入、不写 release evidence；不证明 authenticated active revision、普通账号真实 RBAC、移动端真实任务动作、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 生命周期 / 纠错路径本地守卫复核

- 完成：继续当前 plush goal 的本地闭环，不重启计划、不扩大 release evidence。本轮未改业务逻辑，只复核业务对象生命周期和纠错边界：源单关闭 / 取消不等同事实写入或冲正，事实 / 库存纠错继续走取消、冲正或只读查询，正式业务列表不提供通用删除、回收站或还原删除壳能力。
- 验证：追加前 `progress.md` 为 238 行 / 79896 bytes，低于归档阈值；源单生命周期、库存冲正 / 不可删除、采购入库 / 退货 / 调整、质检、出货取消和 status 定向 Go 测试通过；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/businessModuleNavigation.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 21 个测试；`git diff --check` 通过。
- 下一步：继续优先选择不需要密码、token、目标环境或真实客户数据的 no-write / mock / static 缺口；如要证明真实账号生命周期动作或真实后端错误原因，必须提供本地演示凭据后再跑真实登录 / 浏览器 / JSON-RPC 回归。
- 阻塞/风险：本轮只证明本地定向测试和静态守卫，不登录、不调用真实业务写 JSON-RPC、不写数据库、不做生产部署、不导入真实客户数据、不证明目标环境 release evidence、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 模拟数据 report-only 证据复核

- 完成：继续当前 plush goal 的试用账号 / seed / fixture 与移动端模拟闭环，不重启计划、不扩大 release evidence。本轮只生成 ignored `output/` report-only 证据：试用模拟数据、业务事实模拟闭环和移动端 workflow 模拟闭环均保持 `simulatedOnly=true / realCustomerImport=false`。
- 验证：追加前 `progress.md` 为 245 行 / 81334 bytes，未达 600 行或 80KB 归档阈值；`git fetch --prune` 后当前分支为 `main...origin/main [ahead 1]`；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / completionState=target-evidence-required / firstBlockedAction=immutable-version`；`node scripts/qa/trial-simulated-data.mjs --out output/customers/yoyoosun/trial-simulated-data` 生成 report-only 报告；`node scripts/qa/operational-fact-simulated-closure.mjs --product-id 1 --unit-id 1 --warehouse-id 1 --run-id 20260702-GOAL --out output/customers/yoyoosun/operational-fact-simulated-closure` 生成 report-only 报告；`node scripts/qa/mobile-workflow-simulated-closure.mjs --run-id 20260702-GOAL --out output/customers/yoyoosun/mobile-workflow-simulated-closure` 生成 report-only 报告；报告摘要确认 `writesDatabase=false / callsBackend=false`，移动端报告额外确认 `factPosting=false`；`git check-ignore -v` 确认 5 个 JSON / Markdown 输出均由 `.gitignore` 的 `output/` 忽略；`node --test scripts/qa/trial-simulated-data.test.mjs scripts/qa/operational-fact-simulated-closure.test.mjs scripts/qa/mobile-workflow-simulated-closure.test.mjs` 通过 25 个测试；`git diff --check` 通过。
- 下一步：下次写入 `progress.md` 前需要重新检查大小，若已达到 80KB 阈值则先归档；无演示密码 / token 时继续推进 mock / static / no-write 可证明缺口，有凭据后再跑真实 RBAC、浏览器和移动端 smoke 报告。
- 阻塞/风险：本轮不执行 `--apply`，不读取密码 / token，不登录，不调用后端 JSON-RPC，不写数据库，不导入真实客户数据，不创建真实 workflow 任务，不写库存、出货、财务或其它 Fact；`operational-fact` report-only 使用占位 `product-id/unit-id/warehouse-id=1` 只证明脚本合同和报告形状，不证明这些 ID 在目标环境真实有效，也不证明目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 qa:strict 本地门禁与移动端 preflight 稳定化

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮先复跑 priority audit，确认 P0-P4 本地 ready、P5 仍为 `external-release-evidence-required`；随后运行 `qa:strict`，发现 `mobile workflow runtime browser smoke preflight report is no-write and redacted` 单测把实时 `5177` 端口当成固定事实，已改为通过可注入的 yoyoosun entry audit runtime 做确定性单测，CLI 真实路径仍继续只读探测当前端口、`/customer-config.js` 和 yoyoosun asset。
- 验证：追加前 `progress.md` 为 78 行 / 19,976 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；首次 `PATH=/usr/local/bin:$PATH bash scripts/qa/strict.sh` 在移动端 preflight 单测失败；修复后 `node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 13/13，`PATH=/usr/local/bin:$PATH bash scripts/qa/strict.sh` 全部通过，覆盖 strict 本地边界、web build、server `go test ./...` 和 `make build`；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint --ext .js --ext .jsx --ext .mjs scripts/mobileWorkflowRuntimeBrowserSmoke.mjs` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：无演示密码 / token 时继续推进 mock / static / no-write 可证明缺口；若要证明真实账号菜单投影、authenticated active revision、移动端真实 done / blocked / rejected / urge 动作或真实通知反馈，必须提供本地演示密码或 token 后运行真实登录 / 浏览器 / 移动端 smoke 报告。
- 阻塞/风险：本轮不启动真实浏览器、不登录、不调用真实后端写 JSON-RPC、不写数据库、不导入真实客户数据、不发布 / 激活 / 回滚客户配置、不写 release evidence、不做生产部署、备份恢复、回滚演练、客户签收、提交或推送；根目录 `scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 `web` ESLint 时被 base path 规则忽略，已由 Node 定向测试和 `qa:strict` 覆盖。

## 2026-07-02 yoyoosun 本地入口与无凭据读回复核

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未改业务代码，只复核 yoyoosun 本地入口真实端口、移动端 workflow no-write preflight、客户配置边界、runtime manifest 和无凭据 `get_effective_session` 探针：当前 `5177` 是 yoyoosun dev/static 入口，`5175` 是 Product Core placeholder，`5176` 是 `openai-oauth-api-service` 的 HTML fallback；`start:yoyoosun` / `preview:yoyoosun` 的 print-plan 因 `5176` 被占用建议使用 `5178`，端口顺延属于预期。
- 验证：追加前 `progress.md` 为 85 行 / 22,277 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web start:yoyoosun -- --print-plan` 和 `preview:yoyoosun -- --print-plan` 均输出实际计划端口 `5178`、验证 `customer-config.js` 与 yoyoosun asset 的 curl 命令，并声明不 publish / activate 客户配置；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web --silent audit:yoyoosun-entry -- --json --report output/yoyoosun-local-entry-audit/current-after-strict.json` 生成 ignored 只读报告，backend health ok，`summary.yoyoosunPorts=["5177"]`；`PATH=/usr/local/bin:$PATH node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report output/mobile-workflow-runtime-browser-smoke/preflight-after-strict.json` 生成 ignored no-write 报告，`readyForRealSmoke=false` 且 blocker 只有 `missing-demo-password-env`；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest-after-strict.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node scripts/qa/customer-config-effective-session-probe.mjs --json --report output/customers/yoyoosun/customer-config-effective-session-probe/after-strict.json` 返回预期 `40302 未登录 / status=auth_required / activeRevisionVerified=false`；上述 `output/` 报告均由 `.gitignore` 忽略。
- 下一步：无演示密码 / token 时继续选择 mock / static / no-write 可证明缺口；如要证明普通账号真实菜单投影、authenticated active revision、移动端真实任务动作或完成反馈，必须提供本地演示密码或 token 后运行 `trial-account-rbac`、`trialDemoAccountBrowserSmoke` 或 `mobileWorkflowRuntimeBrowserSmoke` 的真实报告路径。
- 阻塞/风险：本轮不登录、不读取密码 / token、不调用真实写 JSON-RPC、不写数据库、不启动真实浏览器、不发布 / 激活 / 回滚客户配置、不导入真实客户数据、不写 release evidence、不做生产部署、备份恢复、回滚演练、客户签收、提交或推送；no-auth 探针只证明本地后端健康和未登录边界，不证明 active revision、普通账号 RBAC、客户配置投影或目标环境发布状态。

## 2026-07-02 effective session 菜单投影 L1 回归

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未改业务代码，只在 yoyoosun 本地入口 / qa:strict 已通过后补跑 targeted `style:l1`，覆盖 super admin 产品核心看全、普通账号 action projection 收窄和无可见菜单阻断业务 Outlet 三个浏览器场景。
- 验证：追加前 `progress.md` 为 92 行 / 25,437 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false`，P0-P4 本地 ready，P5 仍是外部 release evidence 缺口；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=5197 STYLE_L1_SCENARIOS=erp-effective-session-super-admin-product-core,erp-effective-session-action-projection-business-pages,erp-no-visible-menu-blocks-outlet /usr/local/bin/pnpm --dir web style:l1` 通过，共验证 3 个场景。
- 下一步：无演示密码 / token 时继续选择 mock / static / no-write 可证明缺口；如要证明普通账号真实菜单投影、authenticated active revision、移动端真实任务动作或完成反馈，必须提供本地演示密码或 token 后运行真实登录 / 浏览器 / 移动端 smoke 报告。
- 阻塞/风险：本轮 L1 是本地浏览器 / mock 运行时回归，不读取密码 / token，不登录，不调用真实后端写 JSON-RPC，不写数据库，不证明 active revision、真实 RBAC、目标环境 smoke、release evidence、真实客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送；完整 `style:l1` 全场景本轮未跑。

## 2026-07-02 effective session 诊断态 L1 回归

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未改业务代码，只补跑 remaining effective-session 诊断类浏览器场景，覆盖本地 DEV direct URL、sync failure 和空 pages 三种诊断态，确认这些入口仍只是本地排障可见性，不把普通正式账号退回 RBAC-only 或绕过后端授权。
- 验证：追加前 `progress.md` 为 99 行 / 27,107 bytes，低于归档阈值；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=5198 STYLE_L1_SCENARIOS=erp-effective-session-direct-url-local-dev-diagnostic,erp-effective-session-sync-failure-local-dev-diagnostic,erp-effective-session-empty-pages-local-dev-diagnostic /usr/local/bin/pnpm --dir web style:l1` 通过，共验证 3 个场景；`node --test web/src/erp/utils/adminProfileSync.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs` 通过 21 个测试；priority audit 摘要仍为 `ok=true / readOnly=true / releaseReady=false / completionState=target-evidence-required / blockingCategory=external-release-evidence-required / firstBlockedAction=immutable-version`。
- 下一步：无演示密码 / token 时继续选择 mock / static / no-write 可证明缺口；如要证明普通账号真实菜单投影、authenticated active revision、移动端真实任务动作或完成反馈，必须提供本地演示密码或 token 后运行真实登录 / 浏览器 / 移动端 smoke 报告。
- 阻塞/风险：本轮仍只证明本地浏览器 / mock / helper 合同，不读取密码 / token，不登录，不调用真实后端写 JSON-RPC，不写数据库，不证明 active revision、真实 RBAC、目标环境 smoke、release evidence、真实客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送；完整 `style:l1` 全场景本轮未跑。

## 2026-07-02 移动端认证回跳浏览器 smoke 回归

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未改业务代码，只在 effective-session L1 后补跑移动端认证回跳切片：no-write preflight 确认脚本输入和边界，真实浏览器 smoke 使用 mock auth / workflow RPC 覆盖 9 个岗位任务端的 `/m/<role>/tasks` 生产单端口路由、登录回跳、任务展示、预警 / 通知、退出登录和 phone / iPad 布局。
- 验证：追加前 `progress.md` 为 106 行 / 29,006 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false`，目标环境仍是 `target-evidence-required`；`node --test web/scripts/mobileAuthLoginRouteSmoke.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs` 通过 34 个测试；`node web/scripts/mobileAuthLoginRouteSmoke.mjs --preflight-report output/mobile-auth-login-route-smoke/preflight-after-effective-session.json` 生成 ignored no-write 报告；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web smoke:mobile-auth-login-route` 通过，验证 9 个岗位任务端角色。
- 下一步：无演示密码 / token 时继续选择 mock / static / no-write 可证明缺口；如要证明真实账号 RBAC、authenticated active revision、移动端真实 done / blocked / rejected / urge 动作或后端完成反馈，必须提供本地演示密码或 token 后运行真实登录 / 浏览器 / 移动端 smoke 报告。
- 阻塞/风险：本轮浏览器 smoke 使用 mock RPC，不调用真实后端写 JSON-RPC，不写数据库，不创建真实 workflow 任务，不证明真实 RBAC、customer config active revision、目标环境 smoke、release evidence、真实客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送；完整 `style:l1` 全场景本轮未跑。

## 2026-07-02 错误提示 / 技术字段 / 字段链路守卫复核

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未改业务逻辑，只复核用户可见错误提示、业务界面技术字段隐藏、任务来源业务文案、字段链路覆盖和 `__dev/testing` 当前入口白名单，确认正式前端仍不直接透传底层英文错误、raw id、`owner_role_key`、`source_type`、payload、内部编号 fallback 或过期 reference/archive 测试入口。
- 验证：追加前 `progress.md` 为 113 行 / 31,006 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required / firstBlockedAction=immutable-version`；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/common/utils/errorMessage.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/dashboardTaskDisplay.test.mjs web/src/erp/utils/workflowDashboardStats.test.mjs` 通过 50 个测试；`node --test web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 15 个测试；`node scripts/qa/erp-field-linkage.mjs` 通过 108 个测试并刷新 `web/public/qa/erp-field-linkage-coverage.latest.json`，报告显示 `totalFields=30 / coveredFields=30 / missingFields=0 / totalCases=49 / passedCases=49 / totalScenarios=53 / passedScenarios=53`。
- 下一步：无演示密码 / token 时继续选择 mock / static / no-write 可证明缺口；如要证明真实账号下的字段策略、真实后端错误码、authenticated active revision、真实导出 / 打印 / 移动端写动作或目标环境 release evidence，必须提供对应凭据、真实后端前置或目标环境证据后再运行真实 smoke / E2E / release gate。
- 阻塞/风险：本轮只证明本地静态扫描、单元测试、字段链路 QA 报告和 dev-only 测试入口合同；不登录、不调用真实后端写 JSON-RPC、不写数据库、不导入真实客户数据、不发布 / 激活 / 回滚客户配置、不证明真实 RBAC、真实客户字段策略、目标环境 smoke、release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送；完整 `style:l1`、`qa:full` 和 `qa:strict` 本轮未跑。

## 2026-07-02 试用账号 / 菜单 / 岗位入口 no-write 前置复核

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未改业务逻辑，只复核试用账号 RBAC 静态投影、桌面菜单 / 岗位任务端入口配置、旧入口清理、输入模板和 no-write preflight：当前 10 个 `demo_*` 账号计划、9 个移动端岗位路径、`demo_admin` 移动端拒绝态、旧帮助 / 开发验收 / 高级文档入口清理和 yoyoosun 菜单收窄计划在本地配置与文档入口中保持一致。
- 验证：追加前 `progress.md` 为 120 行 / 33,449 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required / firstBlockedAction=immutable-version`；`node --test scripts/qa/trial-account-rbac.test.mjs web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 17 个测试；`node --test web/src/erp/config/entryConfig.test.mjs web/src/erp/config/menuPermissions.test.mjs web/src/erp/config/seedData.test.mjs web/src/erp/config/workflowStatus.test.mjs` 通过 28 个测试；`node scripts/qa/trial-account-rbac.mjs --print-input-template` 和 `node web/scripts/trialDemoAccountBrowserSmoke.mjs --print-input-template` 均只打印 no-write 输入模板；`node scripts/qa/trial-account-rbac.mjs --preflight-report output/customers/yoyoosun/trial-account-rbac/preflight-after-guards.json` 写出 ignored 报告，`preflightOnly=true / writesDatabase=false / blockers=[missing-trial-account-password-env]`；`node web/scripts/trialDemoAccountBrowserSmoke.mjs --preflight-report output/trial-demo-account-browser-smoke/preflight-after-guards.json` 写出 ignored 报告，`preflightOnly=true / writesDatabase=false / startsBrowser=false / blockers=[missing-demo-password-env]`；两个报告均由 `.gitignore` 的 `output/` 忽略。
- 下一步：如要证明真实 RBAC、普通账号桌面菜单投影、岗位任务端真实入口、`demo_admin` 移动端真实拒绝态或 DEV-only effective session 脱敏读回，必须提供本地演示密码 / token 并运行 `trial-account-rbac.mjs --report` 或 `trialDemoAccountBrowserSmoke.mjs --report` / `pnpm --dir web smoke:trial-demo-browser`。
- 阻塞/风险：本轮只证明本地静态合同、输入模板和 no-write preflight；不读取密码、不登录、不调用 `admin_login / me`、不启动浏览器、不启动 Vite、不写数据库、不创建真实 workflow 任务、不证明真实 RBAC、customer config active revision、目标环境 smoke、release evidence、真实客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送；完整 `style:l1`、`qa:full` 和 `qa:strict` 本轮未跑。

## 2026-07-02 销售订单字段链路与客户配置字段策略复核

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未改业务代码，只复核销售订单受理字段链路、列表 / 导出字段策略、表单保存共享 mapper、明细 SKU 来源切换清残值、打印模板边界和客户配置 runtime manifest：当前只发布 `sales_orders.default` 的 `order_no / source_no / expected_ship_date`，不发布 `sales_order_items.default`，也不新增销售订单打印模板。
- 验证：追加前 `progress.md` 为 127 行 / 36,330 bytes，低于归档阈值；`node --test scripts/qa/sales-order-field-chain-boundary.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs web/src/erp/utils/masterDataOrderView.test.mjs` 通过 67 个测试；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest-sales-order-field-chain.json` 生成 ignored preview manifest，摘要为 `revision=yoyoosun-customer-package-v1.runtime-manifest-v1 / roles=9`，字段策略 surfaces 仅 `customers.default / sales_orders.default / suppliers.default`，`hasSalesOrderItems=false`；`node --test scripts/qa/customer-config-boundaries.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 6 个测试；`git check-ignore -v output/customers/yoyoosun/customer-config-runtime-manifest-sales-order-field-chain.json` 确认输出由 `.gitignore` 的 `output/` 忽略。
- 下一步：如要继续把销售订单字段链路升级到真实打印或明细字段策略，必须同时实现 mapper / 表单 / 导出 / 打印 / 历史缺值回补和对应测试；否则继续保持 `sales_order_items.default` 未发布、销售订单打印未接通的受控边界。
- 阻塞/风险：本轮只证明本地静态守卫、共享 mapper 单测和客户配置 preview manifest，不登录、不调用真实后端写 JSON-RPC、不写数据库、不发布 / 激活 / 回滚客户配置、不证明真实客户字段策略 active revision、不做销售订单打印模板、不证明目标环境 release evidence、生产部署、真实客户导入、备份恢复、回滚演练、客户签收、提交或推送；完整 `style:l1`、`qa:full` 和 `qa:strict` 本轮未跑。

## 2026-07-02 业务页 L1 与 Workflow 写门禁复核

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未改业务代码，只复核上轮 goal 提到的 `business-formal-module-shells-desktop` L1 候选阻断和 P0 active module state / Workflow action boundary：供应商等正式业务页弹窗焦点回归当前未复现失败；Workflow 写动作、explain、客户配置投影和正式前端按钮仍通过后端 JSON-RPC / RBAC / usecase / module state 合同约束。
- 验证：追加前 `progress.md` 为 134 行 / 38,731 bytes，低于归档阈值；`git fetch --prune` 后当前分支仍是 `main...origin/main` 且工作区有大量未提交改动；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`，本地 ready 侧未出现新的 implementation blocker；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=5201 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`cd server && go test ./internal/service -run 'TestJSONRPC.*(Workflow|CustomerConfig|Module|Task|Explain|Complete|Block|Reject|SalesOrder)|TestWorkflow|TestCustomerConfig'` 通过；`cd server && go test ./internal/biz -run 'Test.*(Workflow|CustomerConfig|Module|ProcessRuntime|SalesOrder|Active|BreakGlass|Explain)'` 通过；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs web/src/erp/utils/adminProfileSync.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs` 通过 47 个测试；`node --test web/src/erp/config/menuPermissions.test.mjs web/src/erp/config/entryConfig.test.mjs web/src/erp/config/workflowStatus.test.mjs web/src/erp/api/customerConfigApi.test.mjs` 通过 26 个测试。
- 下一步：无演示密码 / token 时继续选择 mock / static / no-write 可证明缺口；如要证明真实账号普通角色投影、authenticated active revision、真实移动端写动作、真实业务写链路或目标环境状态，必须提供对应凭据、后端前置或目标环境 evidence。
- 阻塞/风险：本轮只证明本地 T3/T4/T5 定向测试和一个浏览器 L1 场景；不登录、不调用真实后端写 JSON-RPC、不写数据库、不发布 / 激活 / 回滚客户配置、不证明真实 RBAC、active revision、目标环境 smoke、release evidence、真实客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送；完整 `style:l1`、`qa:full` 和 `qa:strict` 本轮未跑。

## 2026-07-02 完整 style:l1 本地页面回归

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未改业务代码，只补跑此前多条记录留下的完整 `style:l1` 盲区，覆盖本地开发态、正式业务页、菜单 / 入口、打印、移动端、客户配置、effective session 诊断和相邻页面布局场景。
- 验证：追加前 `progress.md` 为 141 行 / 41,391 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`，release gap 仍只来自目标环境证据缺口；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=5202 /usr/local/bin/pnpm --dir web style:l1` 通过，共验证 77 个场景。
- 下一步：继续优先处理无需密码、token、目标环境或真实客户数据的本地缺口；如要证明真实账号 RBAC、authenticated active revision、真实移动端写动作、目标环境 smoke 或 release evidence，必须先提供对应凭据、后端前置或目标环境证据。
- 阻塞/风险：本轮 `style:l1` 使用本地 DEV / mock / browser 回归，不登录、不调用真实后端写 JSON-RPC、不写数据库、不导入真实客户数据、不发布 / 激活 / 回滚客户配置、不证明真实 RBAC、active revision、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送；`qa:full` 本轮仍未跑。

## 2026-07-02 qa:full 本地门禁收口

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮补跑 `qa:full`，首次在 `trialDemoAccountBrowserSmoke.test.mjs` 失败，原因是试用账号浏览器 smoke preflight 单测把实时 5177 yoyoosun 端口当成固定前置；已把 CLI preflight 保持真实端口探测、但不要求本机常驻 5177，并新增可注入 mock runtime 的确定性 yoyoosun entry audit 单测，真实 smoke 路径仍要求密码、后端和受审计 yoyoosun 前端入口。
- 验证：追加前 `progress.md` 为 148 行 / 42,933 bytes，低于归档阈值；`git fetch --prune` 后当前分支仍为 `main...origin/main` 且工作区存在大量未提交改动；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；首次 `PATH=/usr/local/bin:$PATH bash scripts/qa/full.sh` 在 `trial demo account browser smoke CLI preflight writes sanitized report` 失败，实际 `yoyoosunPorts=[]`、旧断言期望 `['5177']`；修复后 `node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过 7/7；`node --check web/scripts/trialDemoAccountBrowserSmoke.mjs && node --check web/scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint --ext .js --ext .jsx --ext .mjs scripts/trialDemoAccountBrowserSmoke.mjs scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过；第二次 `PATH=/usr/local/bin:$PATH bash scripts/qa/full.sh` 全部通过，覆盖 fast、secrets、govulncheck、web test 510/510、web build、server `go test ./...` 和 `make build`。
- 下一步：若继续推进本地闭环，优先选择真实账号 / authenticated active revision / 移动端真实写动作中仍能通过受控凭据或 no-write 前置证明的最小切片；无密码 / token 时不要把真实 RBAC 或 active revision 写成已证明。
- 阻塞/风险：本轮只证明本地 T0/T3/T4/T5/T8-pre-push 门禁和试用账号 smoke preflight 可测试性，不登录、不读取密码 / token、不调用真实后端写 JSON-RPC、不写数据库、不导入真实客户数据、不发布 / 激活 / 回滚客户配置、不证明真实 RBAC、authenticated active revision、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 MVP closure no-write 总报告收口

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未改业务代码，只运行 `mvp-closure` report-only 总入口，把试用模拟数据、业务事实模拟闭环和移动端任务模拟闭环三个 no-write 报告串成一个本地索引，确认当前本地 ready 与目标环境 evidence-required 的边界仍分开。
- 验证：追加前 `progress.md` 为 155 行 / 45,419 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok=true / readOnly=true / releaseReady=false`，其中 release / target smoke / backup restore / rollback / customer sign-off 仍明确是未证明项；`node scripts/qa/mvp-closure.mjs --run-report-tools --product-id 1 --unit-id 1 --warehouse-id 1 --out output/customers/yoyoosun/mvp-closure-current` 通过并生成 ignored 报告；报告摘要为 `mode=report-with-no-write-tools / simulatedOnly=true / writesDatabase=false / callsBackend=false / realCustomerImport=false / changesRuntime=false / missingRequiredPaths=[]`，三个子工具 `trial-simulated-data`、`operational-fact-simulated-closure`、`mobile-workflow-simulated-closure` 均为 `PASS`；`git check-ignore -v output/customers/yoyoosun/mvp-closure-current/mvp-closure-report.json output/customers/yoyoosun/mvp-closure-current/mvp-closure-report.md` 确认输出由 `.gitignore` 的 `output/` 忽略；`node --test scripts/qa/mvp-closure.test.mjs` 通过 3 个测试。
- 下一步：继续把无需密码 / token / 目标环境的本地闭环缺口收口为可复跑命令；如要进入真实账号 RBAC、authenticated active revision、真实移动端写动作或目标环境 release evidence，必须先具备对应凭据、后端前置和明确授权。
- 阻塞/风险：本轮使用 `product-id/unit-id/warehouse-id=1` 只证明 report-only 工具链和报告形状，不证明这些 ID 在本地或目标环境真实有效；不执行 `--apply`，不登录，不读取密码 / token，不调用后端 JSON-RPC，不写数据库，不导入真实客户数据，不创建真实 workflow 任务，不写库存、出货、财务或其它 Fact，不证明真实 RBAC、active revision、目标环境 smoke、release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 采购入库真实写入链路 no-write 前置收口

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未执行采购入库真实写入，只把 `purchase-receipt-real-write-e2e` 的输入模板和本地 no-write preflight 跑通，确认第一条采购入库真实写入链路的服务层测试锚点、PostgreSQL 防呆脚本和 Make target 存在，可作为后续受控真实写入前的前置证据。
- 验证：追加前 `progress.md` 为 162 行 / 47,805 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`，本轮不升级 release evidence；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --print-input-template` 输出 `writesReports=false / runsGoTests=false / writesTestDatabase=false / writesProductionDatabase=false / callsBackend=false / realCustomerImport=false / changesRuntime=false`；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --preflight-report output/qa/purchase-receipt-real-write-e2e/current-preflight.json` 通过，报告摘要为 `readyForRequestedCommand=true / withPostgres=false / runsGoTests=false / invokesMakeTargets=false / connectsPostgres=false / writesTestDatabase=false / writesProductionDatabase=false / callsBackend=false / realCustomerImport=false / storesDbUrlValue=false / blockers=[]`，并确认两个服务层测试锚点存在；`node --test scripts/qa/purchase-receipt-real-write-e2e.test.mjs` 通过 7 个测试；`git check-ignore -v output/qa/purchase-receipt-real-write-e2e/current-preflight.json output/qa/purchase-receipt-real-write-e2e-test-preflight/preflight.json` 确认报告由 `.gitignore` 的 `output/` 忽略。
- 下一步：若要真正证明采购入库服务层真实写入链路，下一轮必须明确进入真实本地测试库写入命令，并按脚本边界运行 `node scripts/qa/purchase-receipt-real-write-e2e.mjs --out output/qa/purchase-receipt-real-write-e2e`；如需 PostgreSQL 防呆集成测试，再显式加 `--with-postgres` 并确认本地测试库可达。
- 阻塞/风险：本轮不运行 Go test、不调用 `make`、不连接 PostgreSQL、不读取或保存 DSN、不调用后端、不写测试库或生产库、不导入真实客户数据、不发布 / 激活 / 回滚客户配置、不证明采购入库事实链、真实 RBAC、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 采购入库服务层真实写入链路回归

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮执行 `purchase-receipt-real-write-e2e` 默认服务层真实写入链路，跑通 JSON-RPC 采购入库用例，确认采购入库 done 仍通过 Purchase / Inventory 事实 usecase 落本地测试库证据，不把 Workflow task done 误写成 Fact posted。
- 验证：追加前 `progress.md` 为 169 行 / 50,446 bytes，低于归档阈值；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --out output/qa/purchase-receipt-real-write-e2e/current-service` 通过，报告摘要为 `scenario=purchase-receipt-real-write-e2e / summary.status=PASS / total=2 / passed=1 / skipped=1 / withPostgres=false / writesTestDatabase=true / writesProductionDatabase=false / realCustomerImport=false / changesRuntime=false`；内部命令 `cd server && go test ./internal/service -run TestJsonrpcDispatcher_PurchaseReceiptAPIClosesInboundInventoryFact|TestJsonrpcDispatcher_PurchaseReceiptAPIRequiresDomainPermissions -count=1` 通过，PostgreSQL 防呆分支按预期跳过并提示需要显式 `--with-postgres`；`git check-ignore -v output/qa/purchase-receipt-real-write-e2e/current-service/purchase-receipt-real-write-e2e.json output/qa/purchase-receipt-real-write-e2e/current-service/purchase-receipt-real-write-e2e.md` 确认报告由 `.gitignore` 的 `output/` 忽略。
- 下一步：如要把采购入库证据从服务层本地测试库升级到本地 PostgreSQL 防呆集成测试，下一轮需显式运行 `node scripts/qa/purchase-receipt-real-write-e2e.mjs --with-postgres` 并先确认本地测试库前置；如要证明真实账号 / 浏览器 / 目标环境链路，则必须提供对应凭据、后端前置和目标环境授权。
- 阻塞/风险：本轮只证明本地 service-layer Go 测试和 ignored 报告，未运行 `--with-postgres`，不连接真实 PostgreSQL 目标环境，不登录，不调用运行中后端，不读取密码 / token，不导入真实客户数据，不发布 / 激活 / 回滚客户配置，不证明真实 RBAC、浏览器交互、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 采购入库本地 PostgreSQL 防呆集成回归

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮在上一条服务层真实写入链路基础上显式加入 `--with-postgres`，跑通采购入库本地 PostgreSQL 防呆集成测试，确认本地测试库路径仍由 `scripts/purchase-receipt-pg.sh` 守住 host、库名和脱敏输出。
- 验证：追加前 `progress.md` 为 176 行 / 52,739 bytes，低于归档阈值；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --preflight-report output/qa/purchase-receipt-real-write-e2e/current-postgres-preflight.json --with-postgres` 通过，摘要为 `readyForRequestedCommand=true / blockers=[] / withPostgres=true / connectsPostgres=false / writesTestDatabase=false / writesProductionDatabase=false / target=127.0.0.1:55432/plush_erp_purchase_receipt_test / reachable=true`；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --with-postgres --out output/qa/purchase-receipt-real-write-e2e/current-postgres` 通过，报告摘要为 `summary.status=PASS / total=2 / passed=2 / skipped=0 / withPostgres=true / writesTestDatabase=true / writesProductionDatabase=false / realCustomerImport=false / changesRuntime=false`；内部 `go test ./internal/service ...` 与 `make purchase_receipt_pg_test` 均通过，PG stdout 中 DSN 已脱敏为 `<redacted-postgres-url>`；`git check-ignore -v` 确认 preflight、JSON 和 Markdown 报告均由 `.gitignore` 的 `output/` 忽略。
- 下一步：采购入库事实链路本地证据已覆盖 service-layer 和本地 PostgreSQL guard；若继续推进真实页面 / 真实账号链路，下一切片应转到浏览器级 `purchaseReceiptRealWriteBrowserE2E` 的 no-write preflight 或在具备演示密码、后端前置和持久测试数据确认后跑真实 smoke。
- 阻塞/风险：本轮只证明本地测试 PostgreSQL，未连接生产或目标环境 PostgreSQL，不登录，不调用运行中后端，不读取密码 / token，不导入真实客户数据，不发布 / 激活 / 回滚客户配置，不证明真实浏览器交互、真实账号 RBAC、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 采购入库浏览器真实写入 no-write 前置复核

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未进入真实浏览器写入，只复核 `purchaseReceiptRealWriteBrowserE2E` 的输入模板、前置报告和测试边界：真实 smoke 会写本地 / 开发库 `PR-BROWSER-*` 模拟采购入库事实，必须显式接受持久测试数据并提供管理员凭据后才能运行。
- 验证：追加前 `progress.md` 为 183 行 / 55,048 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `P0-P4 localState=ready / P5 target-evidence-required / releaseReady=false / firstBlockedAction=immutable-version`；`PATH=/usr/local/bin:$PATH node web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs --print-input-template` 通过，确认模板为 `writesDatabase=false / callsBackend=false / startsBrowser=false / startsDevServer=false / readsLocalConfig=false / downstreamWritesDatabase=true / requiresPersistentTestDataAcceptance=true / generatedRecordPrefix=PR-BROWSER-*`；`PATH=/usr/local/bin:$PATH node web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs --preflight-report output/purchase-receipt-real-write-browser-e2e/current-preflight.json` 生成 ignored no-write 报告，摘要为 `readyForRealSmoke=false / blockers=[missing-persistent-test-data-acceptance, missing-admin-credential-env] / backendHealth.ok=true / backendHealth.status=200 / baseURL=http://127.0.0.1:4196 / safeTarget.allowed=true / writesDatabase=false / callsJSONRPC=false / startsBrowser=false / readsPasswordValue=false / storesAccessToken=false`；`node --test web/scripts/realLoginSmokeShared.test.mjs web/scripts/purchaseReceiptRealWriteBrowserE2E.test.mjs` 通过 12 个测试；`git check-ignore -v output/purchase-receipt-real-write-browser-e2e/current-preflight.json output/purchase-receipt-real-write-browser-e2e/preflight-test.json` 确认报告由 `.gitignore` 的 `output/` 忽略。
- 下一步：如要真正证明采购入库浏览器写入链路，必须显式提供本地管理员账号密码，并确认可接受保留 `PR-BROWSER-*` 持久测试记录后再运行 `pnpm --dir web smoke:purchase-receipt-real-write`；若缺核心演示主数据，再追加 `-- --seed-core-demo`。
- 阻塞/风险：本轮只证明 no-write 前置和脚本防线，不登录、不启动 Vite / Playwright、不调用 JSON-RPC、不创建采购入库草稿、不过账、不取消冲正、不写数据库、不读取或保存密码 / token / Authorization header，不证明真实浏览器交互、真实账号 RBAC、目标环境 release evidence、生产部署、真实客户导入、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 真实登录共享前置 no-write 复核

- 完成：继续当前 plush goal 的本地试用闭环，不重启计划、不扩大 release evidence。本轮未执行真实登录，只复核 `realLoginSmokeShared` 的输入模板、后端 health、凭据来源候选和脱敏报告边界，为采购合同、加工合同、移动端认证回跳和采购入库浏览器真实写入 smoke 共享前置收口。
- 验证：追加前 `progress.md` 为 190 行 / 57,824 bytes，低于归档阈值；`git fetch --prune` 已完成；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `P0-P4 localState=ready / P5 target-evidence-required / releaseReady=false / firstBlockedAction=immutable-version`；`PATH=/usr/local/bin:$PATH node web/scripts/realLoginSmokeShared.mjs --print-input-template` 通过，模板声明 `writesDatabase=false / callsBackend=false / startsBrowser=false / readsLocalConfig=false`；`PATH=/usr/local/bin:$PATH node web/scripts/realLoginSmokeShared.mjs --preflight-report output/real-login-smoke-shared/current-preflight.json` 生成 ignored no-write 报告，摘要为 `backendHealth.ok=true / status=200 / baseURL=http://127.0.0.1:4174 / credentialEnvPresent=false / configCredentialCandidates=[server/configs/dev/config.local.yaml, server/configs/dev/config.yaml] / readyForCredentialedSmokeCandidate=true / blockers=[] / callsAuthEndpoint=false / callsJSONRPCAuth=false / validatesCredentials=false / readsPasswordValue=false / storesAccessToken=false`；`node --test web/scripts/realLoginSmokeShared.test.mjs` 通过 6 个测试；`git check-ignore -v output/real-login-smoke-shared/current-preflight.json output/real-login-smoke-shared/preflight-test.json` 确认报告由 `.gitignore` 的 `output/` 忽略。
- 下一步：如要进入真实登录 smoke，必须在受控 shell 中提供 `REAL_LOGIN_ADMIN_USERNAME / REAL_LOGIN_ADMIN_PASSWORD`，或确认可使用本地 dev config 中的管理员凭据，并按具体 smoke 的写入边界执行；采购入库浏览器写入还必须额外显式接受 `PR-BROWSER-*` 持久测试记录。
- 阻塞/风险：本轮只证明本地后端 health 和凭据来源候选，不读取 config 内容、不读取或校验密码、不调用 auth JSON-RPC、不启动 Vite / Playwright、不登录、不写数据库、不证明真实账号可用、真实 RBAC、authenticated active revision、目标环境 release evidence、生产部署、真实客户导入、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 yoyoosun 本地入口当前态 no-write 复核

- 完成：继续当前 plush goal 的 yoyoosun 本地入口与客户配置投影诊断，不重启计划、不扩大 release evidence。本轮未改运行代码，只重新复核 `start:yoyoosun`、`preview:yoyoosun`、当前端口归属、静态 `customer-config.js` 注入、客户资产、后端无凭据 `get_effective_session` 探针和 runtime manifest preview：当前已运行的 yoyoosun 入口是 `5177`，`5175` 是本仓库 Product Core placeholder，`5176` 是 `openai-oauth-api-service`，下一次 print-plan 因 `5176/5177` 被占用会顺延到 `5178`。
- 验证：追加前 `progress.md` 为 197 行 / 60,359 bytes，低于归档阈值；`git fetch --prune` 后当前分支仍为 `main...origin/main` 且工作区存在大量未提交改动；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false`，P5 继续是 target evidence 缺口；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web start:yoyoosun -- --print-plan` 和 `preview:yoyoosun -- --print-plan` 均输出当前请求端口占用并建议 `5178`，且明确不发布 / 激活客户配置；`pnpm --dir web --silent audit:yoyoosun-entry -- --report output/yoyoosun-local-entry-audit/current.json --json` 生成 ignored no-write 报告，确认 `5177` 命中 `customerKey=yoyoosun` 和 svg asset，`5175` 为 neutral placeholder，`5176` 为其他项目 HTML fallback；`curl -fsS http://localhost:5177/customer-config.js` 命中 `customerKey: "yoyoosun"`，`curl -fsSI http://localhost:5177/customer-assets/yoyoosun/favicon-yoyoosun.svg` 返回 `content-type: image/svg+xml`；`node scripts/qa/customer-config-effective-session-probe.mjs --json --report output/customers/yoyoosun/customer-config-effective-session-probe/current.json` 只得到 `40302 未登录`，按预期记录 `missing-authenticated-admin-session`；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node --test web/scripts/yoyoosunEntryPlan.test.mjs scripts/qa/customer-config-effective-session-probe.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs` 通过 28 个测试；三个 `output/` 报告均由 `.gitignore` 忽略。
- 下一步：若继续证明普通账号菜单投影、authenticated active revision 或移动端真实任务动作，必须提供受控本地演示账号密码 / token 后运行对应真实 RBAC、trial browser 或 mobile workflow smoke；无凭据时继续只能推进 mock / static / no-write 可证明缺口。
- 阻塞/风险：本轮只证明当前本机端口和静态 yoyoosun 注入、后端健康、无凭据 auth_required 边界与 runtime manifest preview；不登录、不读取密码 / token、不验证 `get_effective_session.source`、不证明 active revision、普通账号 RBAC、super_admin 实际菜单、移动端真实入口、真实客户导入、发布 / 激活 / 回滚客户配置、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 试用入口真实 smoke no-write 前置复核

- 完成：继续当前 plush goal 的角色 / 菜单 / 岗位任务端本地闭环，不重启计划、不扩大 release evidence。本轮复用已确认的 `http://localhost:5177` yoyoosun 前端入口，只生成试用账号 RBAC、桌面试用浏览器 smoke 和移动端 workflow browser smoke 的 no-write 前置报告；三个报告均保持不读密码、不登录、不启动浏览器、不写数据库，当前剩余真实 smoke 阻断收敛为缺少本地演示密码。
- 验证：追加前 `progress.md` 为 204 行 / 63,635 bytes，低于归档阈值；精简 `multi-client-role-workflow-priority-audit` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`，P0-P4 local ready、P5 evidence-required；`pnpm --dir web --silent audit:yoyoosun-entry -- --json` 继续确认 `5177` 为 yoyoosun、`5175` 为 Product Core placeholder、`5176` 为其他项目 HTML fallback；`TRIAL_ACCOUNT_BACKEND_URL=http://127.0.0.1:8300 node scripts/qa/trial-account-rbac.mjs --preflight-report output/customers/yoyoosun/trial-account-rbac/current-yoyoosun-entry-preflight.json` 生成 ignored 报告，`preflightOnly=true / writesDatabase=false / callsJSONRPC=false / backendHealth.ok=true / blockers=[missing-trial-account-password-env]`；`TRIAL_BROWSER_SMOKE_BASE_URL=http://localhost:5177 TRIAL_BROWSER_SMOKE_BACKEND_HEALTH_URL=http://127.0.0.1:8300/healthz node web/scripts/trialDemoAccountBrowserSmoke.mjs --preflight-report output/trial-demo-account-browser-smoke/current-yoyoosun-entry-preflight.json` 生成 ignored 报告，`readyForStaticYoyoosunPreview=true / readyForRealSmoke=false / blockers=[missing-demo-password-env]`；`MOBILE_WORKFLOW_BROWSER_SMOKE_BASE_URL=http://localhost:5177 MOBILE_WORKFLOW_BROWSER_SMOKE_BACKEND_HEALTH_URL=http://127.0.0.1:8300/healthz node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report output/mobile-workflow-runtime-browser-smoke/current-yoyoosun-entry-preflight.json` 生成 ignored 报告，`readyForStaticYoyoosunPreview=true / readyForRealSmoke=false / blockers=[missing-demo-password-env]`；`node --test scripts/qa/trial-account-rbac.test.mjs web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/scripts/mobileAuthLoginRouteSmoke.test.mjs` 通过 36 个测试；`node --test web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs web/src/erp/config/entryConfig.test.mjs web/src/erp/config/menuPermissions.test.mjs web/src/erp/config/seedData.test.mjs web/src/erp/config/workflowStatus.test.mjs` 通过 49 个测试；三个报告均由 `.gitignore` 的 `output/` 忽略。Node 仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning，未导致失败。
- 下一步：若要证明真实账号 RBAC、普通账号桌面菜单投影、岗位任务端真实入口、`demo_admin` 移动端拒绝态、DEV-only effective session 脱敏读回或移动端 done / blocked / rejected / urge 动作，必须提供受控本地演示密码后运行 `trial-account-rbac.mjs --report`、`trialDemoAccountBrowserSmoke.mjs --report` 或 `mobileWorkflowRuntimeBrowserSmoke.mjs --report`；无凭据时继续只能推进 mock / static / no-write 可证明缺口。
- 阻塞/风险：本轮只证明 yoyoosun 入口下的真实 smoke 前置和本地配置 / 模型 / 脚本合同；不读取密码 / token，不调用 `admin_login / me`，不启动 Vite / Playwright，不创建 workflow 任务，不提交 done / blocked / rejected / urge，不写数据库，不证明真实 RBAC、authenticated active revision、普通账号菜单投影、移动端真实动作、目标环境 release evidence、生产部署、真实客户导入、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 测试业务数据隔离守卫收口

- 完成：按测试治理和 seed / import 治理边界新增 `scripts/qa/test-data-isolation-boundary.mjs` 与单测，建立四个测试业务数据桶的只读守卫：Product Core demo seed、yoyoosun 试用 / 业务事实 / 岗位任务模拟数据、真实导入 dry-run / freeze 预检、真实导入 execute 门禁；同步接入 `fast.sh`、`strict.sh`，并更新 `scripts/README.md`、`scripts/qa/README.md` 和 `docs/product/自动化测试策略.md`。
- 验证：追加前 `progress.md` 为 211 行 / 67,509 bytes，低于归档阈值；`node --test scripts/qa/test-data-isolation-boundary.test.mjs` 通过 3 个测试；`node scripts/qa/test-data-isolation-boundary.mjs --json` 返回 `ok=true / readOnly=true / writesDatabase=false / executesImport=false / realCustomerImport=false / checkCount=7 / violationCount=0`，四个 bucket 均无 violation；`bash -n scripts/qa/fast.sh scripts/qa/strict.sh` 通过；`node --test scripts/import/customerImportDryRun.test.mjs scripts/import/customerSourceSnapshotFreezeCheck.test.mjs` 通过 29 个测试；`node --test scripts/qa/docs-inventory.test.mjs` 通过 1 个测试；`git diff --check -- scripts/qa/test-data-isolation-boundary.mjs scripts/qa/test-data-isolation-boundary.test.mjs scripts/qa/fast.sh scripts/qa/strict.sh scripts/qa/README.md scripts/README.md docs/product/自动化测试策略.md` 通过。
- 下一步：后续任何 seed、fixture、模拟数据或客户导入工具改动，先跑 `node --test scripts/qa/test-data-isolation-boundary.test.mjs`；需要查看机器可读分桶时跑 `node scripts/qa/test-data-isolation-boundary.mjs --json`。若要写入模拟数据，仍必须单独进入对应 `--apply` 路径和确认短语；若要真实客户导入，仍必须走 dry-run / approval / backup / recovery / execute 门禁。
- 阻塞/风险：本轮只新增只读守卫和说明文档，不登录、不读取密码 / token、不调用后端 JSON-RPC、不连接 PostgreSQL、不写数据库、不导入真实客户数据、不创建真实 workflow 任务、不发布 / 激活 / 回滚客户配置、不证明真实账号 RBAC、authenticated active revision、目标环境 release evidence、生产部署、备份恢复、回滚演练、客户签收、提交或推送；完整 `fast.sh`、`strict.sh`、`full.sh` 本轮未整包跑。

# plush-toy-erp progress

本文件只保留当前活跃事项、最近完成记录和归档索引；历史流水已归档到 `docs/archive/`。`progress.md` 是过程交接线索，不是正式需求、数据模型或部署真源。

## 归档索引

- `docs/archive/progress-2026-06-28-before-runtime-manifest.md`：归档 2026-06-28 之前至客户配置版本运行时最小闭环、`/__dev` 页面治理等过程记录。
- `docs/archive/progress-2026-06-29-before-release-evidence-hardening.md`：归档 2026-06-28 客户配置 runtime manifest、发布执行器、dev-only 页面治理、Workflow action 合同、导入与发布证据前置门禁等过程记录。
- `docs/archive/progress-2026-06-29-before-target-evidence-binding.md`：归档 2026-06-29 release evidence、真实导入 recovery plan、文档清单、备份恢复和回滚演练门禁早期硬化过程记录。
- `docs/archive/progress-2026-06-29-before-priority-audit-closeout.md`：归档 2026-06-29 target evidence binding 之后到 release evidence runner 脱敏报告与 URL 前置拦截的过程记录。
- `docs/archive/progress-2026-06-29-before-process-runtime-minimum.md`：归档 2026-06-29 adminProfileSync 菜单投影文档纠偏、P2 explain / entitlement / break-glass 中段过程记录。
- `docs/archive/progress-2026-06-29-before-linked-task-idempotency.md`：归档 2026-06-29 P3 ProcessRuntime expected_version 守卫之前至 linked task 幂等闭环前的过程记录。
- `docs/archive/progress-2026-06-30-before-inventory-post-inbound.md`：归档 2026-06-30 P3 / P4 前段、adminProfileSync 菜单投影文档多轮纠偏、sales_order_acceptance、material_supply definition evidence、`purchase_receipt.create` 和 `quality_inspection.decide` 领域命令 handler 过程记录。
- `docs/archive/progress-2026-06-30-before-p5-release-input-checklist.md`：归档 2026-06-30 P4-2 / P4-3、adminProfileSync 文档纠偏和进入 P5 release closeout 输入清单前的过程记录。
- `docs/archive/progress-2026-06-30-before-p5-input-checklist-followup.md`：归档 2026-06-30 P5 release closeout report-only、release evidence hardening、菜单投影纠偏和 input checklist JSON 自定义路径只读合同之前的过程记录。
- `docs/archive/progress-2026-06-30-before-outsourcing-order-api-gate.md`：归档 2026-06-30 P5 input checklist、adminProfileSync 文档纠偏、purchase / quality / shipment / stock reservation / production / outsourcing fact / sales / purchase order moduleStates 门禁等过程记录。
- `docs/archive/progress-2026-07-01-before-action-projection-l1.md`：归档 2026-06-30 outsourcing order API 门禁之后至 2026-07-01 动作投影浏览器回归扩展的过程记录。
- `docs/archive/progress-2026-07-01-before-progress-archive-and-next-no-write.md`：归档 2026-07-01 动作投影浏览器回归扩展之后至 Purchase Receipt Browser E2E 输入模板的过程记录。
- `docs/archive/progress-2026-07-01-before-readme-preflight-sync-closeout.md`：归档 2026-07-01 Purchase Receipt Browser E2E 输入模板之后至 Web README mobile preflight 口径同步的过程记录。
- `docs/archive/progress-2026-07-01-before-bom-inline-editor.md`：归档 2026-07-01 Web README preflight 口径同步之后至 BOM 明细弹窗内联编辑前的过程记录。
- `docs/archive/progress-2026-07-02-before-trial-real-smoke.md`：归档 2026-07-01 BOM 明细内联编辑前至 2026-07-01 qa:full 生命周期收口的过程记录。
- `docs/archive/progress-2026-07-02-before-effective-session-diagnostic-smoke.md`：归档 2026-07-02 试用真实 smoke 前归档说明至旧兼容接口 / 旧 mobile 运行时清理的过程记录。
- `docs/archive/progress-2026-07-02-before-next-no-write-closure.md`：归档 2026-07-02 effective session 诊断 smoke 前归档说明至移动端 workflow smoke 文档边界守卫的过程记录。
- `docs/archive/progress-2026-07-02-before-dashboard-source-label.md`：归档 2026-07-02 __dev/testing 错误提示与技术字段预设补齐至 mobile-workflow-smoke 测试入口补齐的过程记录。
- `docs/archive/progress-2026-07-02-before-sku-source-field-linkage.md`：归档 2026-07-02 Dashboard 来源标签收口之后至 yoyoosun asset 验证命令防误判收口的过程记录。
- `docs/archive/progress-2026-07-02-before-next-local-closure.md`：归档 2026-07-02 SKU 来源字段链路收口之后至移动端任务端 no-write preflight 复核的过程记录。
- `docs/archive/progress-2026-07-02-before-post-fast-local-closure.md`：归档 2026-07-02 客户配置控制台 no-write 读回预检之后至 L1 与移动端登录回跳本地回归复核的过程记录。
- `docs/archive/progress-2026-07-02-before-simulated-report-only-closure.md`：归档 2026-07-02 下一本地闭环前归档说明至 active module state / Workflow 写门禁复核的过程记录。
- `docs/archive/progress-2026-07-02-before-review-driven-config-contract.md`：归档 2026-07-02 模拟 report-only 后至测试业务数据隔离守卫收口的过程记录。
- `docs/archive/progress-2026-07-02-before-p5-release-candidate.md`：归档 2026-07-02 P5 本地前置验证、server 镜像构建守卫、133 baseline 与 fast 门禁复核后的过程记录。
- `docs/archive/progress-2026-07-03-before-dev-customer-visible-labels.md`：归档 2026-07-02 P5 release candidate 前归档说明至 2026-07-03 客户配置 compiled catalog 可见标签收口的过程记录。
- `docs/archive/progress-2026-07-03-before-collaboration-submit-guard.md`：归档 2026-07-03 客户配置可见标签前归档说明至移动任务端岗位标签投影收口的过程记录。
- `docs/archive/progress-2026-07-03-before-order-selection-status-fallback.md`：归档 2026-07-03 本页协同提交 guard 前归档说明至客户配置控制台打印字段要求预检收口的过程记录。
- `docs/archive/progress-2026-07-03-before-print-preview-missing-field.md`：归档 2026-07-03 订单选中摘要状态 fallback 至 trial demo browser smoke 移动入口投影收口的过程记录。
- `docs/archive/progress-2026-07-05-before-work-instruction-font-size.md`：归档 2026-07-03 yoyoosun flow / projection closure 之后至 2026-07-05 作业指导书续页编号行交互接入的过程记录。
- `docs/archive/progress-2026-07-06-before-color-card-padding.md`：归档 2026-07-05 作业指导书 Sheet1 行高、续页、图片标注、字体、分页、客户样例和默认黑字 / 居中纠偏等长流水，为 2026-07-06 色卡纸面留白调整前归档。

## 当前活跃事项

- 多甲方角色能力流程编排以 `docs/product/多甲方角色能力流程编排优先级.md` 和 `node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 的 `implementationOrder` 为本地优先级入口；GPT/reference 资料只作输入，当前真源仍回到代码、migration、测试和正式文档。
- 当前审计显示 P0-P4 本地证据为 ready；P5 的 133 内部验证证据已收口到 `deployments/yoyoosun/evidence/releases/2026-07-03/`，`release-evidence-status` 为 `ready`，closeoutSummary 为 `gateVerified=6/6`、`blockers=0`。
- 本次 133 发布验证已完成本地构建镜像、远端 `docker load`、运行时 `.env` 脱敏 preflight、migration before/after、pre-migration backup、隔离恢复 + migration 演练、真实库 migration、业务容器重建、目标 smoke、rollback / forward-fix evidence 和 internal-only sign-off。

## 2026-07-06 工程模板业务页带值打印字段链路

完成：核对现有入口后确认 BOM 管理已有“打印物料明细 / 打印色卡”，委外订单已有“作业指导书打印”。本轮在现有入口基础上增强三条业务页到打印草稿 mapper：BOM -> 物料明细带产品编号 / 名称 / 款号、BOM 版本、BOM 备注、材料类别 / 料号 / 规格 / 颜色 / 单位 / 部位 / 单位用量 / 损耗 / 备注；BOM -> 色卡带产品、材料规格 / 颜色 / 名称、料号和部位 / 做法；委外订单 -> 作业指导书带客户配置公司名、来源订单号 / 委外单号、产品编号 / 名称、工序、加工数量 / 单位、回货日期和备注，并过滤已取消明细。客户部署包存在时从 `window.__PLUSH_ERP_CUSTOMER_CONFIG__.brand.companyName` 带公司名；Product Core 不硬编码永绅公司名、源图片或作业文本，缺少业务真源的字段保持空白。

下一步：如果后续要让 yoyoosun 业务页也自动带 Sheet1 页眉图或客户作业文本，需要先把 `engineeringPrintSamples` 从“默认样例预览”评审为“客户部署模板默认正文”，再定义 business draft 与客户模板 patch 的合并优先级；本轮不改变该边界。

阻塞/风险：追加前 `progress.md` 为 208 行、48576 字节，未达到 600 行或 80KiB 归档阈值。本轮只改前端业务页入口上下文、工程打印 mapper、打印 workspace 客户公司名读取 helper、工程打印单测和进度记录；不改 schema、migration、RBAC、Workflow / Fact、服务端 PDF 门禁、客户配置激活、业务附件归档或 raw source。已通过 `cd web && node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm -v` = `10.13.1`、`cd web && /usr/local/bin/pnpm exec eslint --fix ...`（`.jsx` 两个业务页按当前 ESLint 配置提示 ignored warning，0 errors）、`git diff --check -- ...`。未跑浏览器截图 / style:l1，因为本轮没有调整布局/CSS/交互态外观；剩余视觉风险限定为已有打印窗口渲染，不在本轮字段 mapper 范围内。

## 2026-07-06 物料明细和色卡源文件比例收口

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 重新读取 `docs/customers/yoyoosun/source-manifest.json` 与 `docs/customers/yoyoosun/raw-source-files/26204#抱抱猴子材料明细表2026-4-10.xlsx`，用 manifest 校验、openpyxl 结构化解析、LibreOffice 转 PDF 和 PNG 截图比对确认：物料明细目标页是源 PDF 第 1 页，色卡目标页是源 PDF 第 4 页，作业指导书仍按第一个备注前模板处理；源文件其他汇总页、空白页、重复页、打印区外图片和重复拼接内容按噪点 / 非本轮模板排除。物料明细运行时收口为源文件比例的紧凑表格、底部审核/制表字段、下方汇总行和源比例页尾缩略图片区；色卡运行时按源文件双栏色卡块、8mm 左右行高和 13px 左右字体比例收口。三套工程模板统一支持选中文字“标红 / 取消标红”，默认样例文字保持黑色，默认样例只保留代表性行，不把客户源图或长清单硬编码进 Product Core。运行时复核截图输出到 `web/output/playwright/style-l1/engineering-template-review/runtime/material-detail-runtime-latest.png`、`color-card-runtime-latest.png`、`work-instruction-runtime-latest.png`。

下一步：后续如要继续提高视觉一致性，应优先补真实 PDF 导出链路和长清单 / 多图分页回归，而不是继续把源文件里的完整客户样例行塞进默认模板。若客户确认物料明细顶部产品图或色卡实物图需要作为固定客户样例资产，再走 customer-specific 资产 provenance 和配置入口，不进通用核心默认值。

阻塞/风险：追加前 `progress.md` 为 265 行、62319 字节，未达到 600 行或 80KB 归档阈值。本轮只改工程打印前端模板、纸面样式、编辑器交互回归和进度记录，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档或真实导入。`node --check` 不能直接检查 `.jsx` 扩展名，页面语法由项目 lint 覆盖。已通过 `node --check web/src/erp/data/engineeringPrintTemplates.mjs`、`node --check web/src/erp/utils/engineeringPrintEditor.mjs`、`node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm --dir web lint`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`STYLE_L1_SCENARIOS=engineering-print-workspace-yoyoosun-sheet1-assets /usr/local/bin/pnpm --dir web style:l1`、`git diff --check`。

## 2026-07-06 物料明细底部源噪点行移除

完成：根据用户纠偏，把物料明细源 Excel 底部的 `日期 / 订单 / 产品编号 / 产品名称 / 数量 / 备品 / 交期 / 审核` 判定为源文件噪点，而不是模板结构。已从 Product Core 默认物料明细样例、BOM 生成物料明细草稿、右侧纸面渲染、左侧字段编辑入口和 scoped CSS 中删除 `footerCells` / `summary-row` 实现；运行时保留真正的 `审核 / 制表` 页脚和可选补充图片区。L1 场景新增反向断言，防止这条噪点行再次出现在物料明细纸面，并重新生成 `web/output/playwright/style-l1/engineering-template-review/runtime/material-detail-runtime-latest.png`。

下一步：后续继续处理物料明细源文件时，底部 `日期 / 订单 / 产品编号...` 只作为 source evidence 里的被排除噪点，不再作为可编辑模板字段恢复。

阻塞/风险：追加前 `progress.md` 为 273 行、65040 字节，未达到 600 行或 80KB 归档阈值。本轮只改物料明细前端模板数据、纸面渲染、样式和 L1 断言，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、色卡或作业指导书。已通过 `node --check web/src/erp/data/engineeringPrintTemplates.mjs`、`node --check web/src/erp/utils/engineeringPrintEditor.mjs`、`node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm --dir web css`、`/usr/local/bin/pnpm --dir web lint`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`git diff --check`。

## 2026-07-06 工程模板 nbsp 转义全面回归

完成：按 `$plush-code-review-governance` 和 `$plush-print-template-source-governance` 全面复核三个新增工程模板的可编辑单元格，确认 `&amp;nbsp;` 问题不是源 Excel 内容，而是运行时空白占位 / 富文本清洗路径。已把普通文本和富文本编辑统一纳入 `nbsp` artifact 清洗：`&nbsp;`、`&amp;nbsp;`、`amp;amp;nbsp` 等进入编辑又离开时会归一为空白；rich contenteditable 在 input / keyUp / blur 都会清洗，普通 contenteditable 在 blur 时清洗并重写当前 DOM。同步修复文字标红取消：选区覆盖红色 span 时明确清除选区红色，不再依赖 `execCommand('foreColor', '#111827')` 覆盖。L1 对物料明细、色卡、作业指导书三张纸面分别新增空白单元格进入/离开和键盘输入 `&amp;nbsp;` 后离开的断言，同时继续覆盖标红 / 取消标红。

下一步：如果后续新增第四类工程模板或把现有模板接入 PDF 后端，应复用同一个 `EditableText` 清洗路径，不再复制一套编辑器。

阻塞/风险：追加前 `progress.md` 为 281 行、66771 字节，未达到 600 行或 80KB 归档阈值。本轮只改工程打印前端编辑器、三模板 L1 回归和进度记录，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档或源 Excel。已通过 `node --check web/src/erp/data/engineeringPrintTemplates.mjs`、`node --check web/src/erp/utils/engineeringPrintEditor.mjs`、`node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm --dir web css`、`/usr/local/bin/pnpm --dir web lint`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`STYLE_L1_SCENARIOS=engineering-print-workspace-yoyoosun-sheet1-assets /usr/local/bin/pnpm --dir web style:l1`、`git diff --check`。

## 2026-07-06 工程模板单元格编辑层铺满

完成：根据用户继续纠偏，把物料明细、色卡、作业指导书三个新增工程模板的纸面可编辑单元格统一改为类似采购 / 加工合同的 full-cell editor：可编辑层 `display:block / width:100% / box-sizing:border-box`，物料明细和色卡由单元格 padding 转移到 editable 层，作业指导书延续 td padding 为 0 并让 editable 占满单元格高度。L1 新增 `getBoundingClientRect()` 断言，分别量物料明细材料名单元格、色卡工艺单元格和作业指导书作业内容单元格，确认 editable 宽高、上下左右 gap 基本覆盖父单元格，不再只是一小段 inline span 可编辑。同步补强 `$plush-print-template-source-governance`：以后打印模板可编辑文本在表格 / 网格单元格内必须铺满单元格，并在验证里比较 editable layer 与 parent cell 的 bounding box。

下一步：后续如果新增第四类工程模板或把现有合同模板重构为共享编辑器，要继续用 full-cell editor 作为默认合同，不再单独做局部 inline 编辑 span。

阻塞/风险：追加前 `progress.md` 为 289 行、68780 字节，未达到 600 行或 80KB 归档阈值。本轮只改工程打印 scoped CSS、三模板 L1 断言、项目打印模板治理 skill 和进度记录，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档或源 Excel。已通过 skill frontmatter / `agents/openai.yaml` Ruby YAML 校验、`node --check web/src/erp/data/engineeringPrintTemplates.mjs`、`node --check web/src/erp/utils/engineeringPrintEditor.mjs`、`node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm --dir web css`、`/usr/local/bin/pnpm --dir web lint`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`STYLE_L1_SCENARIOS=engineering-print-workspace-yoyoosun-sheet1-assets /usr/local/bin/pnpm --dir web style:l1`、`git diff --check`。

## 2026-07-06 打印模板编辑焦点边框统一

完成：确认 5 个正式打印模板编辑态边框不一致的原因是各模板各自维护 `:focus` / `td:focus-within` 样式，编辑焦点和行 / 单元格选中态混用了 dashed 与 solid/inset 视觉语言。已把采购合同、加工合同、物料分析明细表、色卡、作业指导书的 active editable focus 统一为实线 inset 焦点样式；虚线只保留给占位、图片框或参考辅助线，不再表示正在编辑。L1 新增 `assertPrintEditableFocusBorderStyle`，用 computed style 校验 5 个模板的可编辑单元格获得焦点后不再出现 dashed。同步更新 `$plush-print-template-source-governance`、全局 `$erp-print-template-source-governance` 和 trade-erp `$trade-print-template-source-governance`，把“编辑焦点统一实线、选中态另行区分”写入长期规则。

下一步：后续如果继续统一打印模板编辑器，优先抽 shared focus token / helper，而不是在每个模板里复制新的焦点规则。虚线仍可用于未上传图片占位、drop zone 或 source reference，不能再作为 active editable 的主边框。

阻塞/风险：追加前 `progress.md` 为 297 行、70914 字节，未达到 600 行或 80KB 归档阈值。本轮只改打印模板编辑态样式、L1 回归和三个 print-template-source-governance skill，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel 或客户配置激活。已通过 `node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`/usr/local/bin/pnpm --dir web lint`、`STYLE_L1_SCENARIOS=print-workspace-material,print-workspace-processing,engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、skill frontmatter Ruby YAML 校验、项目 / trade `agents/openai.yaml` Ruby YAML 校验、metadata grep、`git diff --check`。`quick_validate.py` 因当前系统 Python 和 bundled Python 都缺少 `yaml` 模块未跑通；本轮用 YAML 解析与 metadata grep 作为替代校验。

## 2026-07-06 打印模板治理 skill 中文主体与文案门禁

完成：按 `$skill-creator` 继续修正三份打印模板治理 skill：全局 `$erp-print-template-source-governance`、项目 `$plush-print-template-source-governance` 和 trade-erp `$trade-print-template-source-governance`。本轮把通用版英文-only 主体改成中文主线 + English anchors；将 plush / trade 项目版的 Workflow、Validation、Output 和项目边界中大段英文改成中文主体；同步补齐 whole-cell editor / editable layer bounding box、active editable focus、职业任务文案、source provenance、source noise、coverage matrix、image / payload / runtime bound 等长期检查项。`agents/openai.yaml default_prompt` 也已加入“职业任务文案”，`display_name` 继续保持英文。

下一步：后续实际打印模板实现或 review 时，要按对应项目 skill 同时检查源文件版本链、真实模板本体、重复模块 / 噪点排除、整格编辑层、焦点样式、岗位可读文案、图片槽来源位置、PDF / print 输出和性能盲区。

阻塞/风险：追加前 `progress.md` 为 305 行、73016 字节，未达到 600 行或 80KB 归档阈值。本轮只改全局 / 项目 skill 和进度记录，不改 runtime、schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活或 docs 索引。

## 2026-07-06 工程打印模板编辑焦点保持

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 修复工程打印模板右侧纸面 `contenteditable` 的焦点保持问题。复现结论是：从一个编辑块切到另一个编辑块时，前一个编辑块 `blur` 提交草稿和后续窗口快照 / DOM 同步会让新编辑块短暂获得焦点后掉回 `body`，绿色 inset 边框随之消失。已在工程模板 `EditableText` 层记录本次明确点击的可编辑块，并在短延迟窗口内按当前文档最新节点恢复意外掉焦；非可编辑区域点击会清空恢复请求。选择行 / 选择单元格模式下会清空恢复请求并退出当前编辑态，确保选行不进入内容编辑。L1 场景新增物料明细、色卡和作业指导书三套工程模板“编辑 A 后点击编辑 B，600ms 后 B 仍保持焦点和 inset 边框”的回归断言。

下一步：后续如果把采购合同 / 加工合同也抽到同一套工程编辑器，应复用这套“明确点击目标 + 当前文档节点恢复 + 选择模式退出编辑态”的边界，不要回到单页私有焦点补丁。

阻塞/风险：追加前 `progress.md` 为 313 行、74441 字节，未达到 600 行或 80KB 归档阈值。本轮只改工程打印工作台前端编辑交互、L1 回归和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活或模板字段合同。已通过 `pnpm --dir web exec eslint --fix --ext .js --ext .jsx src/erp/pages/EngineeringPrintWorkspacePage.jsx`、`pnpm --dir web exec eslint --fix scripts/style-l1/scenarios.mjs`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`pnpm --dir web css`。`pnpm --dir web test` 仍有 1 个既有现场失败：`printTemplates.test.mjs` 期望作业指导书默认样例 `rows.length >= 8`，但当前未跟踪的 `web/src/erp/data/engineeringPrintTemplates.mjs` 样例数据不满足；该文件本轮未修改，未顺手调整样例数量。

## 2026-07-06 作业指导书单元格上下居中

完成：按 `$plush-print-template-source-governance` 修复作业指导书右侧纸面单元格内容上下不居中的问题。原因是 `td` 虽然设置了 `vertical-align: middle`，但单元格内的 `erp-engineering-print-editable` 仍是普通 block，表格高度下文字块会贴近上沿；本轮只在作业指导书纸面局部把 editable / readonly 内容层改成 `flex`、`align-items: center`、整格高度和 `border-box`，编号格额外保持水平居中。同步扩展 `engineering-print-workspace-row-buttons` L1 场景，用 DOM Range 和 `getBoundingClientRect()` 检查公司、产品编号、汇总值、段落标题、编号、正文和备注等单元格文字中心与父单元格中心偏移，避免只看截图或只检查单个作业行。

下一步：后续如果继续处理作业指导书视觉比例，应优先沿用当前纸面 DOM 和 scoped CSS；不要新增第二套 PDF HTML、截图型模板或客户硬编码。若要进一步对齐源 Excel 的全部行高和字体比例，应在源 workbook / PDF baseline 与运行时截图之间做单独视觉回归。

阻塞/风险：追加前 `progress.md` 为 321 行、76552 字节，未达到 600 行或 80KB 归档阈值。本轮只改 `web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活或模板字段合同。已通过 `node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec stylelint "src/erp/styles/app/engineering-print.css"`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`STYLE_L1_SCENARIOS=engineering-print-workspace-yoyoosun-sheet1-assets /usr/local/bin/pnpm --dir web style:l1`、`git diff --check -- web/src/erp/styles/app/engineering-print.css web/scripts/style-l1/scenarios.mjs`。窄范围 `node --test web/src/erp/utils/engineeringPrintEditor.test.mjs web/src/erp/config/printTemplates.test.mjs` 仍有 1 个既有现场失败：`printTemplates.test.mjs` 期望作业指导书默认样例 `rows.length >= 8`，当前样例数据为更少代表行；`engineeringPrintEditor` 相关 10 个用例通过。

## 2026-07-06 作业指导书说明行高与物料明细居中

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 继续修复工程打印纸面布局。作业指导书裁床 / 刺绣说明行不再保留 7.1 / 10.8 / 11.1 / 5mm 的局部 Excel 抽取行高，统一改为和车缝编号行一致的 11.6mm；默认 Product Core 样例和 yoyoosun 客户样例都同步改，避免客户配置下重新出现同类说明行高不一致。物料分析明细表的 `.erp-material-detail-table__editable` 补 `height: 100%` 和 `box-sizing: border-box`，让可编辑内容层真正铺满表头 / 明细单元格后再做 flex 垂直居中。

下一步：若后续还要进一步贴近源 Excel，应先重新确认源 workbook / PDF 中哪些高度是稳定模板结构，哪些只是客户样本内容导致的局部拉高；同类说明行默认继续保持一致，不再把个别源行高度当 Product Core 样例口径。

阻塞/风险：追加前 `progress.md` 为 329 行、78898 字节，未达到 600 行或 80KB 归档阈值。本轮只改工程打印默认数据、yoyoosun 客户样例、工程打印 scoped CSS、L1 回归和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活或模板字段合同。已通过 `node --check config/customers/yoyoosun/customer-config.example.js && node --check web/src/erp/data/engineeringPrintTemplates.mjs && node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec stylelint "src/erp/styles/app/engineering-print.css"`、`node --test web/src/erp/utils/engineeringPrintEditor.test.mjs`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`STYLE_L1_SCENARIOS=engineering-print-workspace-yoyoosun-sheet1-assets /usr/local/bin/pnpm --dir web style:l1`、`git diff --check -- config/customers/yoyoosun/customer-config.example.js web/src/erp/data/engineeringPrintTemplates.mjs web/src/erp/styles/app/engineering-print.css web/scripts/style-l1/scenarios.mjs`。`node --test web/src/erp/config/printTemplates.test.mjs` 仍有 1 个既有现场失败：作业指导书默认样例 `rows.length >= 8` 断言与当前代表行策略冲突。

## 2026-07-06 物料明细顶部信息格居中

完成：补齐物料分析明细表顶部产品编号 / 订单编号等 8 个信息格的垂直居中；L1 用 DOM Range 检查 label/value 中心。
下一步：继续按右侧纸面 DOM 验收打印视觉。
阻塞/风险：追加前 337 行、81201 字节；未改 schema、RBAC、Workflow / Fact、PDF 后端、客户配置或源文件。

## 2026-07-06 色卡页脚字段和值同行

完成：按 `$plush-print-template-source-governance` 修复色卡打印模板页脚 `制卡 / 日期 / 审核 / 复核` 字段和值分行的问题。原因是页脚字段值沿用通用 `.erp-engineering-print-editable` 块级样式，本轮只在 `.erp-color-card-paper__footer` 局部把字段组改为 flex 同行、值保持 `nowrap` 和稳定最小宽度；不改字段草稿、mapper、PDF 后端、表格单元格整格编辑、图片槽或客户配置。

下一步：后续继续调整色卡纸面比例时，优先复用现有右侧纸面 DOM 和 scoped CSS；如要把页脚字段做成更强的共享组件，再和物料明细页脚一起抽统一样式。

阻塞/风险：追加前 `progress.md` 为 343 行、81595 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/styles/app/engineering-print.css` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活或模板字段合同。已通过 `/usr/local/bin/pnpm --dir web css`、`STYLE_L1_BASE_URL=http://127.0.0.1:5175 STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、Playwright footer box 量测（4 个字段 label/value 中心差约 0.27px，`white-space: nowrap`，截图 `web/output/playwright/color-card-footer-inline.png`）和 `git diff --check -- web/src/erp/styles/app/engineering-print.css`。窄范围 `printTemplates / engineeringPrintEditor / printPdf / printWorkspace` 单测仍有 1 个既有现场失败：`printTemplates.test.mjs` 期望作业指导书默认样例 `rows.length >= 8`，与当前代表行策略冲突；其余 51 个用例通过。

## 2026-07-06 色卡纸面左右留白收窄

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 调整色卡打印模板右侧纸面布局。复核当前运行态后确认色卡纸面左右内边距各约 `10mm`，两侧表格各约 `92.25mm`；本轮只把 `.erp-color-card-paper` 左右 padding 收窄到 `6mm`，保留上下 padding、中间 `5.5mm` 窄隔栏、双栏结构、列比例、表格整格编辑、页脚同行和当前草稿 / mapper 不变。浏览器量测确认调整后 sheet 宽约 `198mm`，两侧表格各约 `96.25mm`，无横向溢出，单元格可容纳更多文字。

下一步：后续若继续压缩色卡纸面，应先对照源 Excel / PDF 判断是外侧留白、双栏中缝、列比例还是字体行高需要动；不要为了局部文字塞下去破坏源文件双栏结构或行选择 / 整格编辑合同。

阻塞/风险：本轮开始前 `progress.md` 已超过 80KiB，已先把 2026-07-05 作业指导书长流水归档到 `docs/archive/progress-2026-07-06-before-color-card-padding.md` 并更新归档索引。本轮只改 `web/src/erp/styles/app/engineering-print.css`、`progress.md` 和新增归档文件；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同或 Product Core 样例数据。已通过 `/usr/local/bin/pnpm --dir web css`、`STYLE_L1_BASE_URL=http://127.0.0.1:5175 STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、Playwright 色卡 box 量测（padding `6mm`、两侧表格约 `96.25mm`、sheet / paper 无横向溢出、页脚仍 `nowrap` 同行，截图 `web/output/playwright/color-card-narrow-side-padding.png`）和 `git diff --check -- web/src/erp/styles/app/engineering-print.css`。

## 2026-07-06 色卡空白行选择与下插

完成：按 `$plush-code-review-governance` 和 `$plush-print-template-source-governance` review 色卡行交互后确认问题在前端纸面行选择模型：色卡可见空白占位行在 DOM 上是完整行，但旧逻辑只有已有明细行才响应“选择色卡行”。本轮把所有可见色卡行纳入选择目标，并区分 `persisted` 明细行与 placeholder 空白位：空白位可上插 / 下插，删除保持禁用；下插后生成真实空白明细行并选中，不复制旧内容。重新进入“选择色卡行”时会清空旧行选择，避免 stale selection 让按钮提前可用。

下一步：后续如继续扩展色卡批量行操作或键盘选择，应继续复用这套“可见行 target + 真实明细行 persisted”的状态，不把占位行误当成可删除明细。

阻塞/风险：追加前 `progress.md` 为 136 行、30347 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/pages/EngineeringPrintWorkspacePage.jsx`、`web/src/erp/utils/engineeringPrintEditor.test.mjs`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活或模板字段合同。已通过 `/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm --dir web exec eslint src/erp/pages/EngineeringPrintWorkspacePage.jsx src/erp/utils/engineeringPrintEditor.test.mjs scripts/style-l1/scenarios.mjs`（该 JSX 文件按当前 ESLint 配置提示 ignored warning，命令退出 0）、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`git diff --check -- web/src/erp/pages/EngineeringPrintWorkspacePage.jsx web/src/erp/utils/engineeringPrintEditor.test.mjs web/scripts/style-l1/scenarios.mjs web/src/common/consts/errorCodes.generated.js`。完整 `/usr/local/bin/pnpm --dir web test` 跑到 620/621，通过本轮色卡相关测试，但仍有 1 个既有现场失败：`src/erp/config/printTemplates.test.mjs` 期望作业指导书默认样例 `rows.length >= 8`，当前工作区样例数据为更少代表行；该失败与本轮色卡行选择 / 下插改动无关。

## 2026-07-06 色卡行选择视觉与指定插入收口

完成：继续按用户截图复核色卡行选择，确认上一轮仍有两处未收口：`selectColorLine` 同时写入 `selectedColorBlockIndex`，导致整块继续套用块选中样式；选中第 1 行时该行包含 `rowSpan` 的色卡图片区，通用行选中样式又把跨行图片区染绿，看起来仍像选中了整块。本轮把行选择和块选择状态/视觉拆开：进入“选择色卡行”会清掉块选择；选中具体行或插入行后只保留 `selectedColorLine`；块选中 class 只在没有行选中时生效；色卡行选中时排除左侧跨行图片区，只高亮部位 / 做法单元格。L1 同步补上具体插入位置断言：选第 1 行下插后新空白行必须出现在第 1 行正下方并被选中；选第 2 行上插后新空白行必须出现在第 2 行正上方并被选中；空白占位行仍可上插 / 下插但不可直接移除。

下一步：后续如果给色卡增加键盘上下移动或批量行操作，应沿用“块选择只操作块、行选择只操作行”的分离状态，不让 `rowSpan` 图片区参与行高亮。

阻塞/风险：追加前 `progress.md` 为 144 行、32699 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/pages/EngineeringPrintWorkspacePage.jsx`、`web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活或模板字段合同。已通过 `/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`（连续跑通两轮，第二轮覆盖上插 / 下插位置断言）、`git diff --check -- web/src/erp/pages/EngineeringPrintWorkspacePage.jsx web/src/erp/styles/app/engineering-print.css web/scripts/style-l1/scenarios.mjs web/src/erp/utils/engineeringPrintEditor.test.mjs progress.md`。本轮截图证据已生成：`web/output/playwright/style-l1/engineering-template-review/runtime/color-card-line-selection-persisted.png`、`color-card-line-insert-after-selected-row.png`、`color-card-line-insert-before-selected-row.png`、`color-card-line-selection-placeholder.png`、`color-card-line-selection-inserted.png`。

## 2026-07-06 色卡空白占位行可见上插下插

完成：继续按用户反馈复核空白占位行上插 / 下插，确认上一轮仍只验证了 persisted 行数，没有证明纸面可见行数真的增加。根因是空白占位行由 `minRows` 补出来，旧 `insertColorCardLine` 在 placeholder 上插 / 下插时只追加真实空白行但不提高 `minRows`，当真实行数仍未超过 `minRows` 时，用户看到纸面行数不变，等同于“没插入”。本轮把色卡行 helper 改成按可见行语义处理 placeholder：空白位上插会在当前可见空白位处新增可见空白行；空白位下插会保留原空白位，并在其下新增可见空白行；插入后提高 `minRows`，移除新行时再收回可见行数。色卡 DOM 同步补 `data-color-card-block-index` / `data-color-line-index`，L1 可精确定位第 1 色卡块内的空白行。

下一步：后续如果把空白行持久化策略继续加强，应同步检查 localStorage / draft normalize 是否要保留用户刻意新增的全空白行；当前本轮只保证当前打印窗口内上插 / 下插的可见行为和行数恢复。

阻塞/风险：追加前 `progress.md` 为 152 行、35170 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/utils/engineeringPrintEditor.mjs`、`web/src/erp/utils/engineeringPrintEditor.test.mjs`、`web/src/erp/pages/EngineeringPrintWorkspacePage.jsx`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同或样例数据。已通过 `/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`git diff --check -- web/src/erp/utils/engineeringPrintEditor.mjs web/src/erp/utils/engineeringPrintEditor.test.mjs web/src/erp/pages/EngineeringPrintWorkspacePage.jsx web/scripts/style-l1/scenarios.mjs web/src/erp/styles/app/engineering-print.css progress.md`。本轮新增截图证据：`web/output/playwright/style-l1/engineering-template-review/runtime/color-card-placeholder-insert-before.png` 和 `color-card-placeholder-insert-after.png`，截图显示空白占位行上插 / 下插后纸面可见行数增加且新行被选中。

## 2026-07-06 skill 截图调试门禁

完成：补强全局 `$page-design-governance`、`$code-review-governance`、`$erp-print-template-source-governance`，以及项目 `$plush-page-design-governance`、`$plush-code-review-governance`、`$plush-print-template-source-governance`。本轮将“截图调试 / visual evidence”写入触发描述、UI default prompt 和正文门禁：只要触达用户可见页面、打印/PDF、布局、焦点、选择、插入/删除、移动端或暗色态，就必须用真实浏览器截图或 Playwright artifact 配合 DOM / box metrics、目标行/单元格/焦点断言验证，不能只靠代码、单测或默认态截图收口。

下一步：后续页面、打印模板和 UI 相关 code review 要按本门禁输出截图/浏览器证据名称、覆盖状态和剩余盲区；纯后端、schema、RBAC、docs-only 或 skill-only 任务不机械套用截图流程。

阻塞/风险：追加前 `progress.md` 为 160 行、37621 字节，未达到 600 行或 80KiB 归档阈值。本轮只改全局 / 项目 skill 和进度记录，不改 runtime、schema、migration、RBAC、Workflow / Fact、PDF 后端、源文件、客户配置或正式 docs 索引。全局 `~/.codex/skills` 不属于当前仓库 git 管理；项目版 skill 会随本仓库提交。

## 2026-07-06 色卡块解除 12 行限制

完成：按 `$plush-print-template-source-governance` 处理色卡块插入行时出现“每个色卡块最多支持 12 行”的阻断。根因是编辑器主路径把 `ENGINEERING_PRINT_LIMITS.colorBlockLines` 固定为 12，`insertColorCardLine` 同时按真实明细行数和可见 `minRows` 拦截，导致用户在色卡块内继续上插 / 下插时直接触发旧上限。本轮解除 12 行样例长度限制，把色卡块内行安全上限提升到与工程明细 / 作业行一致的 80 行；单测覆盖第 13 行可插入，同时保留 80 行 runtime bound，避免异常操作无限增大打印 DOM / PDF 负载。L1 浏览器回归新增真实纸面连续下插到 13 行以上，并断言旧的 12 行提示不再出现。

下一步：如果后续实际客户色卡需要超过 80 行，应先评估分页、PDF payload、打印窗口性能和操作体验，再调整 runtime bound；不要把所有保护直接删成无限增长。

阻塞/风险：追加前 `progress.md` 为 160 行、37621 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/utils/engineeringPrintEditor.mjs`、`web/src/erp/utils/engineeringPrintEditor.test.mjs`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同或样例数据。已通过 `/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`git diff --check -- web/src/erp/utils/engineeringPrintEditor.mjs web/src/erp/utils/engineeringPrintEditor.test.mjs web/scripts/style-l1/scenarios.mjs web/src/erp/pages/EngineeringPrintWorkspacePage.jsx web/src/erp/styles/app/engineering-print.css progress.md`。本轮新增截图证据：`web/output/playwright/style-l1/engineering-template-review/runtime/color-card-more-than-12-lines.png`。

## 2026-07-06 色卡打印页脚留在第一页

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 修复色卡打印时页脚 `制卡 / 日期 / 审核 / 复核` 被推到第二页的问题。截图对应的是 runtime product noise：第一页中间有大量空白，但 footer 跑到下一页。根因在色卡纸面布局而不是源文件：`.erp-color-card-paper__gutter` 的 `min-height: 100%` 会在打印分页下把 sheet 区撑到接近整页高，footer 只能排到第二页；同时色卡纸面没有局部声明 border-box / flex 底部布局。本轮把色卡纸面改为纵向 flex + border-box，sheet 保持自然表格高度，中缝只跟随两侧表格高度，footer 用 `margin-top: auto` 留在同一张 A4 纸面底部。

下一步：如果后续色卡块真实行数多到超过单页，需要另评审分页策略、重复页眉 / 页脚和 PDF 输出；本轮只修“内容很少但 footer 被空白挤到第二页”的错误分页。

阻塞/风险：追加前 `progress.md` 为 184 行、42784 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同或样例数据。已通过 `node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`。L1 新增 print-media box 断言：footer bottom 不超过 paper bottom，中缝高度不超过两侧表格高度，并生成截图 `web/output/playwright/style-l1/engineering-template-review/runtime/color-card-print-footer-first-page.png`。

## 2026-07-06 skill 截图尺度与噪点规则

完成：回答“截图到什么程度、噪点怎么处理”后，补强全局 `$page-design-governance`、`$code-review-governance`、`$erp-print-template-source-governance`，以及项目 `$plush-page-design-governance`、`$plush-code-review-governance`、`$plush-print-template-source-governance`。本轮将“截图够用标准 / Screenshot sufficiency”写成证据链口径：默认 2-4 张关键截图覆盖 baseline、exact changed / problem state、相关 boundary 和相邻区域；打印模板、PDF parity、图片槽、分页或多步编辑等高风险场景可扩到 5-8 张，但每张必须回答具体版式、交互或输出判断。同步补“噪点分级”：source noise、rendering noise 默认记录或排除；runtime product noise 必须修复或列为 remaining risk，不能照搬源噪点，也不能用“截图噪点”掩盖真实错位、遮挡、缺字、误高亮或错误焦点。

下一步：后续页面、打印模板和 UI review 按本规则输出截图/Playwright artifact 名称、DOM / box metrics、覆盖状态和剩余盲区；纯后端、schema、RBAC、docs-only 或 skill-only 任务不机械套用截图流程。

阻塞/风险：追加前 `progress.md` 为 176 行、41029 字节，未达到 600 行或 80KiB 归档阈值。本轮只改全局 / 项目 skill 正文和进度记录，不改 runtime、schema、migration、RBAC、Workflow / Fact、PDF 后端、源文件、客户配置或 `agents/openai.yaml`。全局 `~/.codex/skills` 不属于当前仓库 git 管理；项目版 skill 会随本仓库提交。已通过临时 venv 中的 `quick_validate.py`、`agents/openai.yaml` YAML / metadata 扫描、关键词扫描和 `git diff --check`。

## 2026-07-06 物料明细单位列与纸面留白收窄

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 修复物料分析明细表单位列 `PCS` 被拆成两行的问题。根因是物料明细纸面左右 padding 和单位列比例叠加后，单位列实际宽度过窄；本轮只收窄 `.erp-material-detail-paper` 左右 padding 到 `2.5mm`，把单位列从 `2.7%` 调到 `3.6%`，并在单位列关闭常规短英文单位的任意字母断行。其他列从宽列中小幅回收比例，保持表格总宽 100%、右侧纸面 DOM、整格编辑、图片槽和页脚结构不变。

下一步：后续如果继续压缩物料明细表格，应先区分外侧留白、列比例、字体/行高和源 Excel 结构，不用局部硬塞文字的方式破坏整格编辑和打印输出真源。

阻塞/风险：追加前 `progress.md` 为 192 行、44718 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/pages/EngineeringPrintWorkspacePage.jsx`、`web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同或样例数据。已通过 `git diff --check -- web/src/erp/pages/EngineeringPrintWorkspacePage.jsx web/src/erp/styles/app/engineering-print.css web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons STYLE_L1_PORT=5235 /usr/local/bin/pnpm --dir web style:l1`。误跑 `/usr/local/bin/pnpm --dir web test -- ...` 时项目脚本忽略追加参数并执行全量，仍有 1 个既有现场失败：`src/erp/config/printTemplates.test.mjs` 期望作业指导书默认样例 `rows.length >= 8`，与本轮物料明细版式改动无关。

## 2026-07-06 物料明细和作业指导底部留在第一页

完成：继续按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 修复用户截图里的同类打印分页问题。物料分析明细表的 `审核 / 制表` footer、作业指导书的 `备注` 都属于当前纸面底部内容，不应在第一页保留大块空白后被推到第二页。本轮把物料明细纸面和作业指导纸面都收口为纵向 flex + border-box：主表格保持自然高度，底部 footer / 备注用 `margin-top: auto` 留在同一张 A4 纸面底部。作业指导书的备注从主表格内部拆成同一纸面内的底部备注表，并继承整格可编辑层的 flex 垂直居中样式。

下一步：如果后续真实长清单或多图让任一工程模板确实超过单页，需要单独评审分页策略、重复页眉 / 页脚、续页备注和 PDF 输出；本轮只修“内容没占满第一页但底部内容被空白推到第二页”的错误分页。

阻塞/风险：追加前 `progress.md` 为 200 行、46621 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/pages/EngineeringPrintWorkspacePage.jsx`、`web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同或样例数据。已通过 `node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`。L1 新增通用 print-media 底部块断言，覆盖 `material-detail-print-footer-first-page.png`、`color-card-print-footer-first-page.png`、`work-instruction-print-remark-first-page.png` 三张截图证据。

## 2026-07-06 物料明细合并单元格口径确认

完成：按 `$plush-print-template-source-governance` 回答物料分析明细表是否需要合并功能。已用 `source-manifest.json` 和 openpyxl 只读核对 `docs/customers/yoyoosun/raw-source-files/26204#抱抱猴子材料明细表2026-4-10.xlsx`，确认 `材料分析明细表-1` 源页存在稳定合并区：`B1:G1`、`B2:G2`、`L4:M4`、`M21:N21`、`L23:N23`、`G25:L25`。运行时现有 `EngineeringPrintWorkspacePage.jsx` 已给物料明细暴露 `选择单元格 / 合并选区 / 拆分当前`，并复用 `engineeringPrintEditor.mjs` 与 `detailCellMerge.mjs` 的合并覆盖清值、行增删位移和纵向合并块删除主路径；本轮补正式文档说明，避免后续误判为只有采购 / 加工合同支持合并。

下一步：如后续继续做物料明细 PDF parity，可在 L1 / PDF 回归里补一个真实合并区视觉断言，覆盖合并后 `rowSpan / colSpan`、被覆盖格不渲染、插入 / 删除后合并块位置仍正确。

阻塞/风险：追加前 `progress.md` 为 208 行、48576 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `docs/打印模板字段与编辑行为清单.md`、`docs/打印模板实现原理.md` 和进度记录；不改 runtime、schema、migration、RBAC、Workflow / Fact、PDF 后端、源 Excel、客户配置激活、图片持久化、业务附件归档或 Product Core 默认样例。已通过 `node scripts/import/customerSourceManifestCheck.mjs --manifest docs/customers/yoyoosun/source-manifest.json --raw-dir docs/customers/yoyoosun/raw-source-files`、openpyxl 源结构只读解析和 `/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`。

## 2026-07-06 物料明细顶部信息值槽整格编辑

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 修复物料分析明细表顶部信息格编辑框只包住文字的问题。标签和值同格的区域继续保留“标签不可编辑、右侧值可编辑”的语义，但右侧 `.erp-engineering-print-editable` 现在在元信息格内 `align-self: stretch` 并铺满值槽高度，焦点态使用绿色实线内描边，避免像截图里的日期字段只出现一条小输入框。L1 同步新增 DOM / box 断言，量测 8 个顶部信息格的 label 与 editable：editable 必须是 flex、border-box、贴合右侧值槽、铺满内容高度，不能退回 inline 或只包文本。

下一步：后续如继续调整工程资料模板的单元格编辑，应区分“整格都是值”与“标签 + 值同格”的两类可编辑区域；前者编辑层铺满整格，后者编辑层铺满标签右侧值槽。

阻塞/风险：追加前 `progress.md` 为 208 行、48576 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs`、`docs/打印模板字段与编辑行为清单.md` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同、默认样例数据或客户配置。验证已通过 `node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`git diff --check -- web/src/erp/styles/app/engineering-print.css web/scripts/style-l1/scenarios.mjs docs/打印模板字段与编辑行为清单.md`。误用默认 `pnpm --dir web css` 时命中 Codex runtime 的 `pnpm 11.7.0` 被项目 engine 拦截，已改用项目锁定的 `/usr/local/bin/pnpm 10.13.x` 重跑通过。

## 2026-07-06 物料明细签核行贴近表格

完成：按 `$plush-print-template-source-governance` 修正上一轮对物料分析明细表 footer 的过度泛化。源 Excel 中 `审核 / 制表` 是紧贴明细表下方的签核行，不是像色卡页脚或作业指导备注那样贴页底的底部块；本轮移除物料明细 footer 的 `margin-top: auto`，让签核行回到表格下方，并保持底部补充图片上传后跟随签核行下方输出。L1 回归同步从“页底留在第一页”改成“签核行贴近明细表 + 补充图跟随签核行”的盒模型断言，截图证据更新为 `material-detail-print-footer-near-table.png`。

下一步：后续若物料明细真实长清单或多图超出单页，应单独评审分页、续页签核行和补充图位置；本轮只修默认 / 短表状态下签核行被错误推到页底的问题。

阻塞/风险：追加前 `progress.md` 为 224 行、52385 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同、默认样例数据或客户配置。已通过 `node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`。

## 2026-07-06 物料明细底部图片放大为页尾附件位

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 修复物料分析明细表底部上传图片显示过小的问题。核对源 workbook `docs/customers/yoyoosun/raw-source-files/26204#抱抱猴子材料明细表2026-4-10.xlsx` 的 xlsx drawing XML 后，确认 `材料分析明细表-1` 存在页尾宽幅图片锚点：`image4.png` 从第 27 行第 1 列附近跨到第 29 行第 14 列附近，另有第 41-58 行的大幅参考图；这类 end-of-template image slot 不应按 28mm 宽、8mm 高缩成小条。本轮把物料明细底部补充图片区改为加工合同同类页尾附件位：两列、占满纸面内容宽度、7mm 间距、单图 60mm 高，上传区仍留在左侧字段面板，右侧纸面只显示当前草稿图片。

下一步：如果后续客户确认物料明细底部只允许单张横向宽图而不是两张附件位，需要再把 slot 合并为单列宽幅布局；本轮先按现有两个底部上传槽和加工合同同类附件位收口。

阻塞/风险：追加前 `progress.md` 为 240 行、56100 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同、默认样例数据或客户配置。已通过 `node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`git diff --check -- web/src/erp/styles/app/engineering-print.css web/scripts/style-l1/scenarios.mjs progress.md`。新增截图证据：`web/output/playwright/style-l1/engineering-template-review/runtime/material-detail-bottom-image-uploaded.png`。

## 2026-07-06 打印窗口刷新保留编辑草稿

完成：按 `$plush-print-template-source-governance` 排查“打印模板编辑后刷新页面内容丢失”。根因是打印中心菜单入口为保证新窗口使用默认样例，会带 `draft=fresh` 打开工作台；旧实现把这个 URL 原样保存进窗口 state / 整窗快照恢复脚本，刷新后 React 工作台再次执行 fresh 重置，覆盖当前窗口已编辑草稿。本轮新增 `buildRestorablePrintWorkspaceURL`，让窗口 state、整窗 HTML 快照 bootstrap 和五套正式模板工作台持久化时都使用 restore URL；fresh 只保留为首次进入意图，不再进入刷新恢复主链。同步补强 L1 打印窗口刷新回归：采购合同 / 加工合同弹窗会先写入标记，再刷新，再断言刷新前编辑内容仍存在并可继续编辑。

下一步：工程三套模板当前复用同一 `buildRestorablePrintWorkspaceURL` 和窗口 state 主链，单测已覆盖通用链路；后续可补单独 L1 场景覆盖物料明细、色卡、作业指导书的“编辑后刷新仍保留”浏览器回归。

阻塞/风险：追加前 `progress.md` 为 240 行、56100 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/utils/printWorkspace.js`、`web/src/erp/utils/printWorkspace.test.mjs`、三套打印工作台入口页、`web/scripts/style-l1/printAssertions.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、业务附件、打印留档、服务端 PDF 门禁、客户配置激活、源文件或 Product Core 默认样例。已通过 `pnpm exec eslint --fix --ext .js --ext .jsx --ext .mjs src/erp/utils/printWorkspace.js src/erp/utils/printWorkspace.test.mjs src/erp/pages/MaterialPurchaseContractPrintWorkspacePage.jsx src/erp/pages/ProcessingContractPrintWorkspacePage.jsx src/erp/pages/EngineeringPrintWorkspacePage.jsx scripts/style-l1/printAssertions.mjs`、`pnpm exec node --test src/erp/utils/printWorkspace.test.mjs src/erp/utils/usePrintWorkspaceWindowSnapshot.test.mjs src/erp/utils/engineeringPrintEditor.test.mjs`、`STYLE_L1_SCENARIOS=print-workspace-material-shell-refresh,print-workspace-processing-shell-refresh pnpm style:l1`、`git diff --check -- web/src/erp/utils/printWorkspace.js web/src/erp/utils/printWorkspace.test.mjs web/src/erp/pages/MaterialPurchaseContractPrintWorkspacePage.jsx web/src/erp/pages/ProcessingContractPrintWorkspacePage.jsx web/src/erp/pages/EngineeringPrintWorkspacePage.jsx web/scripts/style-l1/printAssertions.mjs`。补跑 `src/erp/config/printTemplates.test.mjs` 时仍有一个既有现场失败：作业指导书默认样例 `rows.length >= 8` 的断言与当前紧凑样例不一致，和本轮刷新持久化无关。

## 2026-07-06 物料明细顶部信息区取消格线

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 将物料分析明细表顶部信息区的打印视觉改回源 Excel 口径。源文件 `材料分析明细表-1` 第 1-4 行是标题、字段和值的非格子排版，边框从第 5 行表头开始；本轮去掉 `.erp-engineering-print-meta-grid` 及子项的默认格线，只保留内部 grid 作为排版手段。顶部“日期”等标签 + 值区域继续保持标签不可编辑、右侧值槽整槽可编辑，焦点态绿色实线内描边不会进入打印 / PDF。

下一步：如果继续追源 Excel 做更细的顶部对齐，可再单独评审图片锚点、列坐标和字体比例；不要把内部排版 grid 误解成应打印出来的单元格边框。

阻塞/风险：追加前 `progress.md` 为 240 行、56100 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs`、`docs/打印模板字段与编辑行为清单.md` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同、默认样例数据或客户配置。已通过 `node scripts/import/customerSourceManifestCheck.mjs --manifest docs/customers/yoyoosun/source-manifest.json --raw-dir docs/customers/yoyoosun/raw-source-files`、`node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`。L1 新增无顶部格线 DOM / box 断言和 `material-detail-meta-focus-latest.png` 聚焦态截图证据。

## 2026-07-06 物料明细审核制表值槽放大

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 修复物料分析明细表底部 `审核 / 制表` 编辑框高度过小、不好点击的问题。签核行继续保持源 Excel 口径贴近明细表下方，不变成页底 footer；本轮只把签核字段组改为稳定高度的 label + value grid，右侧值槽提升到 `minmax(28mm, 1fr)` 和 `8mm` 最小高度，沿用顶部信息区的 flex 垂直居中和值槽整槽点击口径。焦点态仍是绿色实线内描边，不进入打印 / PDF。

下一步：后续若继续调整签核行位置，应先区分“贴近表格的签核行”和“页尾附件位”，不要为了放大点击区重新把签核行推到页底或改变源文件签核语义。

阻塞/风险：追加前 `progress.md` 为 264 行、62737 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs`、`docs/打印模板字段与编辑行为清单.md` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同、默认样例数据或客户配置。已通过 `node scripts/import/customerSourceManifestCheck.mjs --manifest docs/customers/yoyoosun/source-manifest.json --raw-dir docs/customers/yoyoosun/raw-source-files`、`node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`git diff --check -- web/src/erp/styles/app/engineering-print.css web/scripts/style-l1/scenarios.mjs docs/打印模板字段与编辑行为清单.md progress.md`。L1 新增/更新签核行 value slot 盒模型断言和 `material-detail-footer-focus-latest.png` 聚焦态截图证据。

## 2026-07-06 五套打印模板刷新恢复 L1 补齐

完成：继续按 `$plush-print-template-source-governance` 补齐上一条刷新恢复修复的浏览器回归盲区。此前采购合同、加工合同已有“编辑后刷新仍保留”L1，工程三套只复用共享恢复链路和单测。本轮在 `web/scripts/style-l1/scenarios.mjs` 新增三条工程模板刷新恢复场景：`print-workspace-engineering-material-detail-shell-refresh`、`print-workspace-engineering-color-card-shell-refresh`、`print-workspace-engineering-work-instruction-shell-refresh`。三条场景都从打印中心打开对应模板弹窗，向右侧纸面可编辑单元格写入标记，刷新弹窗，再断言刷新前编辑内容仍存在且刷新后还能继续编辑。这样五套正式模板都具备同组 L1 覆盖：采购合同、加工合同、物料分析明细表、色卡、作业指导书。

下一步：后续若新增第六套正式模板，必须同步接入 `printWorkspace` 恢复链路、PDF module gate、字段合同和同类刷新恢复 L1；不要只依赖共享 helper 单测。

阻塞/风险：追加前 `progress.md` 为 272 行、64757 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、业务附件、打印留档、服务端 PDF 门禁、客户配置激活、源文件、模板样例或 runtime 实现。已通过 `node --check web/scripts/style-l1/scenarios.mjs`、`pnpm exec eslint --fix --ext .mjs scripts/style-l1/scenarios.mjs scripts/style-l1/printAssertions.mjs`、`STYLE_L1_SCENARIOS=print-workspace-engineering-material-detail-shell-refresh,print-workspace-engineering-color-card-shell-refresh,print-workspace-engineering-work-instruction-shell-refresh pnpm style:l1`、`STYLE_L1_SCENARIOS=print-workspace-material-shell-refresh,print-workspace-processing-shell-refresh,print-workspace-engineering-material-detail-shell-refresh,print-workspace-engineering-color-card-shell-refresh,print-workspace-engineering-work-instruction-shell-refresh pnpm style:l1`、`git diff --check -- web/scripts/style-l1/scenarios.mjs web/scripts/style-l1/printAssertions.mjs web/src/erp/utils/printWorkspace.js web/src/erp/utils/printWorkspace.test.mjs web/src/erp/pages/MaterialPurchaseContractPrintWorkspacePage.jsx web/src/erp/pages/ProcessingContractPrintWorkspacePage.jsx web/src/erp/pages/EngineeringPrintWorkspacePage.jsx progress.md`。

## 2026-07-06 色卡签核行贴近表格

完成：按 `$plush-print-template-source-governance` 修复色卡 `制卡 / 日期 / 审核 / 复核` 被推到页底的问题。此前为避免打印分页把色卡 footer 留在第一页，错误地把 `.erp-color-card-paper__footer` 做成 `margin-top: auto` 页底块；用户截图确认源文件语义应是表格下方签核行。本轮把色卡 footer 改为自然跟随色卡表格，`margin-top: 3mm`、不再自动吃掉整页空白。L1 同步改成量测 footer 与 `.erp-color-card-paper__sheet` 的距离，要求打印态签核行贴近表格、仍在第一页内，且中缝不再把 sheet 撑成整页。

下一步：如果后续真实色卡块行数多到超出单页，应单独评审色卡分页、续页签核行和 PDF 输出；本轮只修短表状态下签核行被错误推到页底的问题。

阻塞/风险：追加前 `progress.md` 为 272 行、64757 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同、默认样例数据或客户配置。已通过 `node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`git diff --check -- web/src/erp/styles/app/engineering-print.css web/scripts/style-l1/scenarios.mjs progress.md`。新增截图证据：`web/output/playwright/style-l1/engineering-template-review/runtime/color-card-print-footer-near-table.png`。

## 2026-07-06 物料明细毛向移入顶部信息区

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 修复物料分析明细表 `毛向` 字段位置与编辑命中区。源 Excel 将 `毛向` 放在标题右侧、靠近产品图区的位置，本轮按用户纠偏将该位置判定为 source noise，不再照搬；运行时把 `毛向` 移入顶部业务信息区，和 `设计师` 同槽展示并可编辑。顶部标题区只保留公司、标题和右上产品图，`毛向` 不再出现在 header；`毛向` 值槽沿用顶部信息区整槽点击和绿色实线内描边焦点口径，避免回到小高度编辑框。

下一步：如果后续需要继续细调顶部信息区，可优先评审是否将 `设计师 / 毛向` 拆成独立槽或调整列数；不要再把源 Excel 顶部孤立 `毛向` 当作模板结构真源。

阻塞/风险：追加前 `progress.md` 为 288 行、69059 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/pages/EngineeringPrintWorkspacePage.jsx`、`web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs`、`docs/打印模板字段与编辑行为清单.md` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同、默认样例数据或客户配置。已通过 `node scripts/import/customerSourceManifestCheck.mjs --manifest docs/customers/yoyoosun/source-manifest.json --raw-dir docs/customers/yoyoosun/raw-source-files`、`node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx src/erp/pages/EngineeringPrintWorkspacePage.jsx`（仅既有 hooks dependency warning）、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`git diff --check -- web/src/erp/pages/EngineeringPrintWorkspacePage.jsx web/src/erp/styles/app/engineering-print.css web/scripts/style-l1/scenarios.mjs docs/打印模板字段与编辑行为清单.md progress.md`。L1 更新顶部信息区 value slot 盒模型断言，确认 header 不含 `毛向`、meta 区包含 `毛向`，并新增 `material-detail-hair-direction-focus-latest.png` 聚焦态截图证据。

## 2026-07-06 三套工程模板真实 PDF 空白第二页修复

完成：继续按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 修复用户截图里的真实打印分页问题。现象不是字段链路错误，而是三套工程资料模板的右侧纸面 DOM 虽然把 `审核 / 制表`、`制卡 / 日期 / 审核 / 复核`、`备注` 放在第一页内，但 Chromium A4 PDF 会因为打印态 `.erp-engineering-print-paper` 自身 `min-height: 297mm` 把底部块拆到第 2 页。本轮在 `@media print` 取消工程纸面自身 A4 `min-height`，让 A4 页面高度由浏览器 / PDF 页面负责；同时把作业指导书备注从 `margin-top: auto` 改回紧跟正文表格，贴近源 Excel 第一个备注行口径。

下一步：后续若真实长清单、多图或客户源文件需要多页，必须单独评审续页页眉、续页备注、签核行保留策略和 PDF 输出；不要再用单页 `min-height` 或 flex 空白撑页来模拟 A4。

阻塞/风险：追加前 `progress.md` 为 288 行、69059 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同、默认样例数据或客户配置。已通过 `node scripts/import/customerSourceManifestCheck.mjs --manifest docs/customers/yoyoosun/source-manifest.json --raw-dir docs/customers/yoyoosun/raw-source-files`、`pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`git diff --check -- web/src/erp/styles/app/engineering-print.css web/scripts/style-l1/scenarios.mjs`。额外用 Playwright 生成真实 A4 PDF 并用 `pypdf` 读页文本，确认 `engineering-material-detail.pdf`、`engineering-color-card.pdf`、`engineering-work-instruction.pdf` 均为 1 页，且对应底部文本都在第 1 页。截图证据更新为 `material-detail-print-footer-near-table.png`、`color-card-print-footer-near-table.png`、`work-instruction-print-remark-first-page.png`。

## 2026-07-06 作业指导书备注并入主表最后一行

完成：按 `$plush-print-template-source-governance` 修复作业指导书末尾备注被渲染成主表外独立备注表的问题。只读核对 yoyoosun 源 workbook `Sheet1` 后确认第一处备注是 `A24:I24` 合并行，属于同一张作业指导书表格；后续 `A62:I62`、`A77:I77`、`A90:I90` 是重复源模块。本轮把运行时备注 `<tr>` 移回 `.erp-work-instruction-paper__sheet` 的 `tbody` 最后一行，删除独立 `remark-table` 样式，L1 改为断言备注必须是主表最后一行且不能出现独立备注表。

下一步：后续继续做作业指导书源文件对齐时，仍以第一个备注行作为模板 body 结束线；备注后的重复模块只作为 source duplicate evidence，不进入 Product Core 默认样例。

阻塞/风险：追加前 `progress.md` 为 304 行、73687 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/pages/EngineeringPrintWorkspacePage.jsx`、`web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs`、`docs/打印模板字段与编辑行为清单.md` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同、默认样例数据或客户配置。已通过 `node scripts/import/customerSourceManifestCheck.mjs --manifest docs/customers/yoyoosun/source-manifest.json --raw-dir docs/customers/yoyoosun/raw-source-files`、`pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`STYLE_L1_SCENARIOS=engineering-print-workspace-yoyoosun-sheet1-assets pnpm --dir web style:l1`、`git diff --check`。补跑 `node --test src/erp/utils/engineeringPrintEditor.test.mjs src/erp/config/printTemplates.test.mjs src/erp/utils/printWorkspace.test.mjs` 时仍有一个既有现场失败：`printTemplates.test.mjs` 要求作业指导书默认作业行 `rows.length >= 8`，但当前 Product Core 紧凑样例是 5 行；这和本轮备注结构修复无关。

## 2026-07-06 五套打印模板编辑中刷新不丢失

完成：按 `$plush-code-review-governance` 和 `$plush-print-template-source-governance` 继续排查“五份模板编辑时刷新仍丢失”。根因不是模板源文件或字段映射，而是运行时恢复链路：右侧 `contentEditable` 原先主要在 `blur` 后才写入结构化草稿；用户保持焦点直接刷新时，静态 shell HTML 快照和 React draft 又会互相覆盖。最终将打印工作台打开与刷新主路径收口为直接可恢复 React 工作台 URL，保留 `state` 和 window state 兼容信息，但不再把地址栏改写到 `/print-window-shell.html`。五套模板的可编辑控件新增“输入时静默写草稿、停顿后正常提交 React state、退出前 flush 当前活动编辑器”的共享链路；采购合同、加工合同、物料分析明细表、色卡、作业指导书刷新 L1 均改为不按 Tab、不离焦，直接模拟编辑中刷新。

下一步：后续新增打印模板时必须接入同一 `usePersistentPrintWorkspaceDraft` 和刷新 L1；如果要重新启用静态 shell 作为编辑主路径，需要先单独评审脚本重放、按钮交互和结构化 draft 同步，不要再让 shell HTML 快照覆盖 React draft。

阻塞/风险：追加前 `progress.md` 为 312 行、75806 字节，未达到 600 行或 80KiB 归档阈值。本轮只改打印工作台恢复链路、五套模板编辑持久化、壳页兼容持久化、L1 断言/场景和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活或模板字段合同。已通过 `pnpm exec node --test src/erp/utils/printWorkspace.test.mjs src/erp/utils/usePrintWorkspaceWindowSnapshot.test.mjs src/erp/utils/engineeringPrintEditor.test.mjs`、`STYLE_L1_SCENARIOS=print-workspace-material-shell-refresh,print-workspace-processing-shell-refresh,print-workspace-engineering-material-detail-shell-refresh,print-workspace-engineering-color-card-shell-refresh,print-workspace-engineering-work-instruction-shell-refresh pnpm style:l1`。验证覆盖的是五份模板在右侧纸面编辑焦点仍停留时刷新；未覆盖服务端 PDF 生成、业务附件留档和真实客户源文件变更，因为本轮没有触达这些链路。

## 2026-07-06 物料明细毛向单独换行

完成：按 `$plush-print-template-source-governance` 和 `$plush-page-design-governance` 继续调整物料分析明细表顶部信息区。`毛向` 仍按 source noise 纠偏留在顶部业务信息区，不恢复到源 Excel 标题右侧；本轮将它从 `设计师 / 毛向` 横向复合格拆出，作为顶部信息区第 9 个字段单独换行展示，编辑焦点继续铺满标签右侧值槽，避免被挤成过窄点击区。

下一步：若后续还要压缩顶部区域高度，应优先评审顶部信息区整体列数、图片高度和表格起始位置，不再把 `毛向` 塞回 `设计师` 同一行横向复合格。

阻塞/风险：追加前 `progress.md` 为 312 行、75806 字节，未达到 600 行或 80KiB 归档阈值。本轮只改 `web/src/erp/pages/EngineeringPrintWorkspacePage.jsx`、`web/src/erp/styles/app/engineering-print.css`、`web/scripts/style-l1/scenarios.mjs`、`docs/打印模板字段与编辑行为清单.md` 和进度记录；不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片持久化、业务附件归档、源 Excel、客户配置激活、模板字段合同、默认样例数据或客户配置。已通过 `node scripts/import/customerSourceManifestCheck.mjs --manifest docs/customers/yoyoosun/source-manifest.json --raw-dir docs/customers/yoyoosun/raw-source-files`、`node --check web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx src/erp/pages/EngineeringPrintWorkspacePage.jsx`（仅既有 hooks dependency warning）、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`。截图证据更新为 `web/output/playwright/style-l1/engineering-template-review/runtime/material-detail-hair-direction-focus-latest.png`。

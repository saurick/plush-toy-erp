# plush-toy-erp progress

本文件只保留当前活跃事项、最近完成记录和归档索引；历史流水已归档到 `docs/archive/`。`progress.md` 是过程交接线索，不是正式需求、数据模型或部署真源。

## 归档索引

- `docs/archive/progress-2026-06-28-before-runtime-manifest.md`：归档 2026-06-28 之前至客户配置版本运行时最小闭环、`/__dev` 页面治理等过程记录。
- `docs/archive/progress-2026-06-29-before-release-evidence-hardening.md`：归档 2026-06-28 客户配置 runtime manifest、发布执行器、dev-only 页面治理、Workflow action 合同、导入与发布证据前置门禁等过程记录。
- `docs/archive/progress-2026-06-29-before-target-evidence-binding.md`：归档 2026-06-29 release evidence、真实导入 recovery plan、文档清单、备份恢复和回滚演练门禁早期硬化过程记录。
- `docs/archive/progress-2026-06-29-before-priority-audit-closeout.md`：归档 2026-06-29 target evidence binding 之后到 release evidence runner 脱敏报告与 URL 前置拦截的过程记录。
- `docs/archive/progress-2026-06-29-before-process-runtime-minimum.md`：归档 2026-06-29 adminProfileSync 菜单投影文档纠偏、P2 explain / entitlement / break-glass 中段过程记录。
- `docs/archive/progress-2026-06-29-before-linked-task-idempotency.md`：归档 2026-06-29 P3 ProcessRuntime expected_version 守卫之前至 linked task 幂等闭环前的过程记录。
- `docs/archive/progress-2026-06-30-before-inventory-post-inbound.md`：归档 2026-06-30 P3 / P4 前段、adminProfileSync 菜单投影文档多轮纠偏、sales_order_acceptance、material_supply definition evidence、`purchase_receipt.create` 和 `quality_inspection.decide` 领域命令 handler 过程记录。
- `docs/archive/progress-2026-06-30-before-p5-release-input-checklist.md`：归档 2026-06-30 P4-2 / P4-3、adminProfileSync 文档纠偏和进入 P5 release closeout 输入清单前的过程记录。
- `docs/archive/progress-2026-06-30-before-p5-input-checklist-followup.md`：归档 2026-06-30 P5 release closeout report-only、release evidence hardening、菜单投影纠偏和 input checklist JSON 自定义路径只读合同之前的过程记录。
- `docs/archive/progress-2026-06-30-before-outsourcing-order-api-gate.md`：归档 2026-06-30 P5 input checklist、adminProfileSync 文档纠偏、purchase / quality / shipment / stock reservation / production / outsourcing fact / sales / purchase order moduleStates 门禁等过程记录。
- `docs/archive/progress-2026-07-01-before-action-projection-l1.md`：归档 2026-06-30 outsourcing order API 门禁之后至 2026-07-01 动作投影浏览器回归扩展的过程记录。
- `docs/archive/progress-2026-07-01-before-progress-archive-and-next-no-write.md`：归档 2026-07-01 动作投影浏览器回归扩展之后至 Purchase Receipt Browser E2E 输入模板的过程记录。
- `docs/archive/progress-2026-07-01-before-readme-preflight-sync-closeout.md`：归档 2026-07-01 Purchase Receipt Browser E2E 输入模板之后至 Web README mobile preflight 口径同步的过程记录。
- `docs/archive/progress-2026-07-01-before-bom-inline-editor.md`：归档 2026-07-01 Web README preflight 口径同步之后至 BOM 明细弹窗内联编辑前的过程记录。
- `docs/archive/progress-2026-07-02-before-trial-real-smoke.md`：归档 2026-07-01 BOM 明细内联编辑前至 2026-07-01 qa:full 生命周期收口的过程记录。
- `docs/archive/progress-2026-07-02-before-effective-session-diagnostic-smoke.md`：归档 2026-07-02 试用真实 smoke 前归档说明至旧兼容接口 / 旧 mobile 运行时清理的过程记录。
- `docs/archive/progress-2026-07-02-before-next-no-write-closure.md`：归档 2026-07-02 effective session 诊断 smoke 前归档说明至移动端 workflow smoke 文档边界守卫的过程记录。
- `docs/archive/progress-2026-07-02-before-dashboard-source-label.md`：归档 2026-07-02 __dev/testing 错误提示与技术字段预设补齐至 mobile-workflow-smoke 测试入口补齐的过程记录。
- `docs/archive/progress-2026-07-02-before-sku-source-field-linkage.md`：归档 2026-07-02 Dashboard 来源标签收口之后至 yoyoosun asset 验证命令防误判收口的过程记录。
- `docs/archive/progress-2026-07-02-before-next-local-closure.md`：归档 2026-07-02 SKU 来源字段链路收口之后至移动端任务端 no-write preflight 复核的过程记录。
- `docs/archive/progress-2026-07-02-before-post-fast-local-closure.md`：归档 2026-07-02 客户配置控制台 no-write 读回预检之后至 L1 与移动端登录回跳本地回归复核的过程记录。
- `docs/archive/progress-2026-07-02-before-simulated-report-only-closure.md`：归档 2026-07-02 下一本地闭环前归档说明至 active module state / Workflow 写门禁复核的过程记录。
- `docs/archive/progress-2026-07-02-before-review-driven-config-contract.md`：归档 2026-07-02 模拟 report-only 后至测试业务数据隔离守卫收口的过程记录。
- `docs/archive/progress-2026-07-02-before-p5-release-candidate.md`：归档 2026-07-02 P5 本地前置验证、server 镜像构建守卫、133 baseline 与 fast 门禁复核后的过程记录。
- `docs/archive/progress-2026-07-03-before-dev-customer-visible-labels.md`：归档 2026-07-02 P5 release candidate 前归档说明至 2026-07-03 客户配置 compiled catalog 可见标签收口的过程记录。
- `docs/archive/progress-2026-07-03-before-collaboration-submit-guard.md`：归档 2026-07-03 客户配置可见标签前归档说明至移动任务端岗位标签投影收口的过程记录。
- `docs/archive/progress-2026-07-03-before-order-selection-status-fallback.md`：归档 2026-07-03 本页协同提交 guard 前归档说明至客户配置控制台打印字段要求预检收口的过程记录。
- `docs/archive/progress-2026-07-03-before-print-preview-missing-field.md`：归档 2026-07-03 订单选中摘要状态 fallback 至 trial demo browser smoke 移动入口投影收口的过程记录。

## 当前活跃事项

- 多甲方角色能力流程编排以 `docs/product/多甲方角色能力流程编排优先级.md` 和 `node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 的 `implementationOrder` 为本地优先级入口；GPT/reference 资料只作输入，当前真源仍回到代码、migration、测试和正式文档。
- 当前审计显示 P0-P4 本地证据为 ready；P5 的 133 内部验证证据已收口到 `deployments/yoyoosun/evidence/releases/2026-07-03/`，`release-evidence-status` 为 `ready`，closeoutSummary 为 `gateVerified=6/6`、`blockers=0`。
- 本次 133 发布验证已完成本地构建镜像、远端 `docker load`、运行时 `.env` 脱敏 preflight、migration before/after、pre-migration backup、隔离恢复 + migration 演练、真实库 migration、业务容器重建、目标 smoke、rollback / forward-fix evidence 和 internal-only sign-off。

## 2026-07-03 yoyoosun flow / projection closure PR

完成：在 `fix/yoyoosun-flow-projection-field-closure` 上补齐永绅客户角色能力矩阵、raw-source 到表单映射、试用数据 fixture、配置投影覆盖矩阵、流程编排覆盖矩阵、客户闭环验收清单、签收 readiness、专用 QA runner 和 yoyoosun closure / readiness 测试。流程编排覆盖矩阵明确 Workflow task、ProcessRuntime、businessFlows、stateMachines、processPolicies、UI 入口和 signoff gates 的 runtime / preview 边界，并把三条 ProcessRuntime 主流程和 preview 配置项纳入测试覆盖。

下一步：本地拉取 PR 分支后运行 `bash scripts/qa/yoyoosun-closure.sh`，再视结果运行原有 fast / strict；若需要把 planned field surfaces 升级为 runtime，应另开 runtime 代码改动，修改后端 field-policy allowlist、前端实际消费点和对应测试。

阻塞/风险：当前分支新增的是配置资产、文档、fixture 和 QA 门禁，不直接修改后端 runtime usecase、schema、migration 或真实导入；客户签收仍需目标环境 authenticated effective session readback、角色 smoke、采购 / 加工合同目标 PDF evidence 和客户确认。

## 2026-07-03 yoyoosun flow / projection closure 本地拉取验证

完成：本地 `main` 已从 `origin/fix/yoyoosun-flow-projection-field-closure` 快进到 `2660ef0a`。补两处合并后验证缺口：销售角色 guardrail 改为正向领域 usecase 边界表达，避免 yoyoosun readiness 测试把“禁止直接写库存”文案误判为 blocked wording；`scripts/qa/yoyoosun-closure.sh` 的 Go 测试切到 `server/` 目录执行；`docs/文档清单.md` 已登记本分支新增的客户闭环与签收准备 Markdown。

下一步：如需提交 / 推送，按本轮相关路径精确 stage；如继续升级 planned field surfaces，应另拆 runtime allowlist、前端消费点和测试切片。

阻塞/风险：本轮只做本地拉取、测试和 QA / 文档登记修正；未执行目标环境 authenticated readback、真实角色 smoke、PDF 目标 evidence、部署或真实导入。已通过 `bash scripts/qa/yoyoosun-closure.sh`、`node --test scripts/qa/docs-inventory.test.mjs`、`bash scripts/qa/fast.sh`。

## 2026-07-03 采购合同打印明细长编号换行修复

完成：修复采购合同打印模板明细表格长采购订单号 / 产品订单编号带值后横向溢出单元格的问题。表体默认改为允许 `overflow-wrap: anywhere` 与格内换行，保留单位列不换行；新增 `style:l1` 采购合同工作台断言，写入超长采购订单号、产品订单编号、大数量、大金额和备注后检查单元格与内容块没有横向溢出、没有越过表格 / 纸面边界。

下一步：如后续真实合同样本继续出现极端长规格或备注，可复用同一盒模型断言扩展对应列；本轮未调整采购订单业务 mapper、PDF 后端、RBAC、Workflow / Fact 或客户配置。

阻塞/风险：本轮只覆盖采购合同打印工作台和浏览器级 L1 单页回归；没有执行目标环境真实登录 PDF smoke，也没有修改加工合同表体列策略。已通过 `node --check web/scripts/styleL1.mjs web/scripts/style-l1/printAssertions.mjs web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`/usr/local/bin/pnpm --dir web lint`、`/usr/local/bin/pnpm --dir web test`、`STYLE_L1_SCENARIOS=print-workspace-material /usr/local/bin/pnpm --dir web style:l1`。

## 2026-07-03 planned field policy key 对齐与边界测试修复

完成：处理 review 中的 planned field policy key 不一致和 preview-only 禁词误伤问题。将 moduleKey 到 field policy surface 的映射收口到 `adminProfileSync.mjs`，覆盖 `sales-orders`、`accessories-purchase`、`processing-contracts`、`quality-inspections` 等页面入口别名；采购、加工、出货、质检真实列补 `effectiveFieldKey`，质量页面列表和导出都接入 field policy 标记；加工合同 planned surface 从旧 `return_date` 统一为当前业务字段 `expected_return_date`，同步后端 allowlist、客户投影矩阵和文档。yoyoosun QA 禁词断言改为拦截正向 runtime fact commit 承诺，允许“不 / 不能 / 禁止直接写...”这类 Workflow / Fact 边界文案。

下一步：planned surfaces 仍保持 `backend_runtime_allowed`，暂不发布到 runtime manifest；后续若提升采购、加工、出货、质检 field policy 为正式 runtime，需要补目标账号 effective session readback 和页面级真实隐藏 / 导出回归。

阻塞/风险：本轮不改 schema、migration、WorkflowUsecase、Fact usecase、RBAC 或真实导入；未跑目标环境 authenticated readback、真实角色 smoke、PDF 目标 evidence 或部署验证。已通过 `node --test scripts/qa/yoyoosun-release-readiness.test.mjs scripts/qa/yoyoosun-customer-closure.test.mjs`、`/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/adminProfileSync.test.mjs src/erp/utils/moduleTableColumns.test.mjs`、`cd server && go test ./internal/biz -run 'CustomerConfig|FieldPolicy'`、`bash scripts/qa/yoyoosun-closure.sh`、`bash scripts/qa/fast.sh`、`git diff --check`。

## 2026-07-03 采购合同打印单位列溢出漏检修正

完成：复核上一轮采购合同打印样式修复，确认单位列仍被 `nowrap` 特例限制且 L1 断言未覆盖 `unit` 列。已移除单位列不换行特例，并把 `米（M）` 加入采购合同明细长值盒模型回归，和采购订单号、产品订单号、数量、金额、备注一起检查内容块与单元格不横向溢出。

下一步：同类打印表格修复不得再只按“看起来短”的字段排除断言；如继续发现规格 / 厂商料号等极端值，应纳入同一 `targetColumns` 断言集合。

阻塞/风险：本轮仍只改采购合同打印样式和 L1 断言，不改业务 mapper、PDF 后端、RBAC、Workflow / Fact、客户配置或加工合同列策略。已通过 `node --check web/scripts/styleL1.mjs web/scripts/style-l1/printAssertions.mjs web/scripts/style-l1/scenarios.mjs`、`/usr/local/bin/pnpm --dir web css`、`STYLE_L1_SCENARIOS=print-workspace-material /usr/local/bin/pnpm --dir web style:l1`。

## 2026-07-03 采购合同打印单位英文口径统一

完成：将采购合同打印草稿的单位统一规范为英文单位。打印编辑器模型会把 `米（M）`、`pcs` 等单位值归一成 `M`、`PCS`；采购订单生成采购合同草稿时优先从单位选项解析英文 code，快照或选项只有中文且没有英文来源时不伪造单位值。L1 采购合同明细断言改为写入并校验 `M`，字段链路目录新增 `printUnit` 覆盖项。

下一步：如后续加工合同也要求单位英文口径，应另按加工合同字段链路和模板样本评审，不在本轮顺手扩散。

阻塞/风险：本轮只改采购合同打印草稿映射、编辑器单位规范化、字段链路目录和 L1 回归；未改全局单位选择器、主数据单位展示、PDF 后端、RBAC、Workflow / Fact、客户配置或加工合同。已通过 `node --check web/src/erp/utils/materialPurchaseContractEditor.mjs web/src/erp/utils/masterDataOrderView.mjs web/scripts/style-l1/printAssertions.mjs web/scripts/style-l1/scenarios.mjs web/scripts/styleL1.mjs`、`/usr/local/bin/pnpm --dir web exec node --test src/erp/qa/fieldLinkageCatalog.test.mjs src/erp/utils/materialPurchaseContractEditor.test.mjs src/erp/utils/masterDataOrderView.test.mjs`、`/usr/local/bin/pnpm --dir web lint`、`/usr/local/bin/pnpm --dir web css`、`/usr/local/bin/pnpm --dir web test`、`STYLE_L1_SCENARIOS=print-workspace-material /usr/local/bin/pnpm --dir web style:l1`、`git diff --check`。

## 2026-07-03 采购合同打印单位映射口径修正

完成：根据甲方样本中单位既有中文也有英文的现状，将上一轮“全英文”口径调整为“映射优先、未知保留”。采购合同单位现按映射输出打印短码：`片 / 件 / 个 / 只` -> `PCS`、`对 / 双` -> `PAIR`、`米` -> `M`、`码` -> `YD`，英文输入仍统一大写；无法确认的中文单位保留来源文本，不清空也不伪造英文。正式打印模板说明已同步。

下一步：后续若甲方确认 `对` 也要统一打成 `PCS` 而不是 `PAIR`，只调整同一映射表和测试，不在模板、样式或业务页多处补分支。

阻塞/风险：本轮不改主数据单位 schema / API、全局单位选择器、采购订单保存参数、PDF 后端、RBAC、Workflow / Fact、客户配置或加工合同。已通过 `node --check web/src/erp/utils/materialPurchaseContractEditor.mjs web/src/erp/utils/masterDataOrderView.mjs web/src/erp/qa/fieldLinkageCatalog.mjs web/scripts/style-l1/printAssertions.mjs`、`/usr/local/bin/pnpm --dir web exec node --test src/erp/qa/fieldLinkageCatalog.test.mjs src/erp/utils/materialPurchaseContractEditor.test.mjs src/erp/utils/masterDataOrderView.test.mjs`、`STYLE_L1_SCENARIOS=print-workspace-material /usr/local/bin/pnpm --dir web style:l1`。

## 2026-07-03 采购合同打印单位全量文档扫描与映射扩表

完成：按 README、docs、web/server/scripts README、`.agents/skills` Markdown 扫描单位候选；剔除普通语义单字误伤后，把采购合同打印单位映射扩到件数、配对、套、长度、重量、包装和批量单位族。映射只作用于采购合同打印草稿 `unit`，不改变全局单位选择器、主数据单位展示或其他业务页；正式打印模板说明已同步完整映射。

下一步：若甲方确认某个单位短码口径不同，例如 `对 / 双` 要改成 `PCS` 而不是 `PAIR`，只改同一映射表、测试和两份打印模板说明。

阻塞/风险：扫描覆盖仓库 Markdown / README，不解析 Excel、PDF、图片原件或外部甲方文档；未知单位仍保留来源文本，不伪造英文。已通过 `node --check web/src/erp/utils/materialPurchaseContractEditor.mjs web/src/erp/utils/masterDataOrderView.mjs web/src/erp/qa/fieldLinkageCatalog.mjs web/scripts/style-l1/printAssertions.mjs web/scripts/style-l1/scenarios.mjs web/scripts/styleL1.mjs`、`/usr/local/bin/pnpm --dir web exec node --test src/erp/qa/fieldLinkageCatalog.test.mjs src/erp/utils/materialPurchaseContractEditor.test.mjs src/erp/utils/masterDataOrderView.test.mjs`、`/usr/local/bin/pnpm --dir web lint`、`/usr/local/bin/pnpm --dir web css`、`/usr/local/bin/pnpm --dir web test`、`STYLE_L1_SCENARIOS=print-workspace-material /usr/local/bin/pnpm --dir web style:l1`、`git diff --check -- <本轮相关文件>`。

## 2026-07-03 采购合同打印单位中文口径回切

完成：按当前确认口径，将采购合同打印草稿单位从“英文短码优先”回切为“中文优先”。中文来源单位保留原文；`米（M）` 这类中文加英文括号的单位取中文前缀；`PCS / M / KG / BOX` 等已知英文 code 映射回 `件 / 米 / 千克 / 盒` 等中文单位。采购订单带值、打印编辑器、字段链路目录、L1 打印回归和两份打印模板说明已同步。

下一步：如果甲方后续确认某类英文 code 要显示成特定中文，例如 `PCS` 不用 `件` 而用 `片`，只改同一映射表和测试，不在模板或业务页多处补分支。

阻塞/风险：本轮仍只改采购合同打印草稿 `unit` 生成与显示口径，不改主数据单位 schema / API、全局单位选择器、采购订单保存参数、PDF 后端、RBAC、Workflow / Fact、客户配置或加工合同。已通过 `node --check web/src/erp/utils/materialPurchaseContractEditor.mjs web/src/erp/utils/masterDataOrderView.mjs web/src/erp/qa/fieldLinkageCatalog.mjs web/scripts/style-l1/printAssertions.mjs`、`/usr/local/bin/pnpm --dir web exec node --test src/erp/qa/fieldLinkageCatalog.test.mjs src/erp/utils/materialPurchaseContractEditor.test.mjs src/erp/utils/masterDataOrderView.test.mjs`、`/usr/local/bin/pnpm --dir web lint`、`/usr/local/bin/pnpm --dir web css`、`/usr/local/bin/pnpm --dir web test`、`STYLE_L1_SCENARIOS=print-workspace-material /usr/local/bin/pnpm --dir web style:l1`。

## 2026-07-04 合同打印补值 PR 审查修正

完成：审查 PR #3 `fix(print): complete yoyoosun contract draft display values` 后，补齐采购合同打印 hook 的供应商列表与联系人快照入参，确保历史订单供应商快照缺字段时可按既有主数据回补空白；加工合同业务打印入口接入同一打印草稿补全 helper；新增补全单测纳入 `web` 常规 `pnpm test`；客户配置预检测试同步 4 个默认方字段口径。

下一步：如后续要让采购订单明细真正带出产品订单编号 / 产品编号 / 产品名称，应单独评审采购订单明细业务模型、schema / migration / API / 导入和打印带值链路，不在打印补值层伪造。

阻塞/风险：本轮只处理打印草稿显示补齐、客户配置默认方字段和测试覆盖；不改 schema、migration、采购订单明细业务模型、RBAC、Workflow / Fact 或 PDF 后端。已通过 `git diff --check`、`PATH=/usr/local/bin:$PATH node --test web/src/erp/utils/contractPrintDraftCompleteness.test.mjs web/src/erp/data/processingContractTemplate.test.mjs web/src/erp/utils/materialPurchaseContractEditor.test.mjs web/src/erp/utils/printWorkspace.test.mjs`、`PATH=/usr/local/bin:$PATH node --test web/src/erp/config/devCustomerConfig.test.mjs web/src/erp/utils/contractPrintDraftCompleteness.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`。

## 2026-07-04 采购订单明细产品快照字段闭环

完成：审计 `docs/customers/yoyoosun/raw-source-files/` 下 5 个 Excel 工作簿后，把采购订单明细的 `product_order_no_snapshot`、`product_no_snapshot`、`product_name_snapshot` 从打印层占位提升为采购订单 source document 明细真实可选快照字段。新增 Ent schema 与 Atlas migration，补齐 biz mutation / normalization、repo create/update/save/list 转换、JSON-RPC 入参和返回、前端采购订单明细表单与保存参数；`purchase_order_items.default` field policy surface 同步登记真实 snapshot key；字段链路目录新增采购订单产品快照用例。新增 `docs/customers/yoyoosun/Excel字段产品核心映射评审.md`，明确哪些 Excel 字段进入 Product Core、哪些留在客户资料 / 配置 / 导入评审 / deferred 边界，并同步 yoyoosun README 与文档清单。

下一步：如果后续需要从销售订单 / 产品主档一键带出这些快照，应另评审采购订单明细的产品来源选择、导入 source mapping、历史数据回补和前端来源切换清值链路；本轮不新增产品外键、不引入产品来源下拉，也不把字段写入库存、质检、Workflow 或财务事实。

阻塞/风险：本轮已覆盖保存、更新清值、API 回显、真实 repo 持久化、打印草稿读取、字段链路目录、采购订单表单 L1 浏览器回归和文档清单；未做真实客户数据导入、目标环境 authenticated readback、采购订单从销售订单 / 产品主档一键带出、采购行明细导出或历史数据 backfill。已通过 `node scripts/import/customerSourceManifestCheck.mjs --manifest docs/customers/yoyoosun/source-manifest.json --raw-dir docs/customers/yoyoosun/raw-source-files`、`node scripts/import/customerSourceExtract.mjs --manifest docs/customers/yoyoosun/source-manifest.json --out output/customers/yoyoosun/source-extract-field-audit`、`make data`、`go test ./internal/biz ./internal/data ./internal/service`、`go test ./...`、`PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/masterDataOrderView.test.mjs src/erp/qa/fieldLinkageCatalog.test.mjs src/erp/utils/contractPrintDraftCompleteness.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`STYLE_L1_SCENARIOS=purchase-order-date-filter-desktop PATH=/usr/local/bin:$PATH pnpm --dir web style:l1`、`node --test scripts/qa/docs-inventory.test.mjs`、`git diff --check`。

## 2026-07-04 工程资料打印模板闭环

完成：按 `docs/customers/yoyoosun/raw-source-files/26204#抱抱猴子材料明细表2026-4-10.xlsx` 的 `材料分析明细表-1`、`色卡`、`Sheet1` 三个工作表，新增物料分析明细表、色卡、作业指导书三套正式打印模板。模板中心可直接用中性样例打开；BOM 版本页可打印物料明细 / 色卡并带产品与 BOM 明细快照；委外订单页可打印作业指导书并带订单、产品、工序和明细快照。物料明细右上与页底图片槽、作业指导书右上图片槽和行内图片槽已支持上传；作业指导书支持增删作业行。同步 PDF 模板模块门禁、客户配置控制台、打印中心 L1 场景、正式文档和 README 口径；业务入口缺失字段保持空白，不从样例兜底。

下一步：如后续要把 Excel 中更多工程字段结构化进入 BOM / 产品主数据，应另评审字段真源、导入映射、来源切换清值、历史回补、列表 / 导出 / 搜索回显和 schema / migration；本轮不把图片槽写成业务附件事实，也不新增库存、采购、生产、质检、财务或 Workflow fact。

阻塞/风险：本轮完成本地模板、业务入口和相关文档 / 测试闭环；未做目标环境真实登录 PDF smoke、真实上传文件持久化归档、客户真实数据导入或目标环境 authenticated readback。全量 `style:l1` 已通过本轮相关的 `dev-customer-config-dark-desktop,print-center-desktop`，但仍在未触达的 `business-formal-module-shells-desktop` 出货协同按钮等待处稳定失败；该失败未作为本轮改动内容修复。已通过 `PATH=/usr/local/bin:$PATH node --test web/src/erp/config/printTemplates.test.mjs web/src/erp/utils/printWorkspace.test.mjs web/src/erp/config/devCustomerConfig.test.mjs`、`PATH=/usr/local/bin:$PATH go test ./internal/server -run 'TestTemplatePDFReferencedModuleKeys|TestEnforceTemplatePDFModulesEnabled'`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH go test ./internal/server`、`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs`、`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop,print-center-desktop pnpm --dir web style:l1`、`git diff --check`。

## 2026-07-04 工程资料打印模板编辑能力补齐

完成：参考采购合同 / 加工合同打印工作台，把物料分析明细表、色卡、作业指导书的行 / 块编辑操作补到顶部工具条。物料明细支持选中行、上插 / 下插 / 追加 / 移除；色卡支持选中块、插入 / 追加 / 移除色卡块，以及块内加行 / 移除行；作业指导书支持选中作业行、插入 / 追加 / 移除作业行、段落行增删和行内图片槽新增 / 移除。三套模板保留原地编辑，新增空白模板与恢复样例入口，插入的新行不复制旧业务值，业务入口带值缺失时仍保持空白。

下一步：如果后续要继续贴近 Excel 原件细节，可补材料表格列宽 / 分页密度、真实文件上传持久化归档、PDF 预览差异回归和更多模板动作的 L1 自动断言。

阻塞/风险：本轮只补打印工作台前端编辑能力和草稿 helper，不改 schema、migration、RBAC、Workflow / Fact、库存 / 采购 / 生产 / 财务事实，也不把图片槽升级为业务附件事实。已通过 `PATH=/usr/local/bin:$PATH node --test web/src/erp/utils/engineeringPrintEditor.test.mjs web/src/erp/config/printTemplates.test.mjs web/src/erp/utils/printWorkspace.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop,print-center-desktop pnpm --dir web style:l1`；浏览器实测并截图确认三套模板的行 / 块 / 图片槽按钮可用。尚未做目标环境真实登录 PDF smoke、真实上传文件持久化归档或客户真实数据导入。

## 2026-07-04 工程物料明细 Excel 复核与合并单元格补齐

完成：用 LibreOffice 渲染并用 `.xlsx` 内部 XML 复核 `26204#抱抱猴子材料明细表2026-4-10.xlsx` 的 sheet 结构、合并区域、边框和图片锚点。确认物料明细原件包含表头合并、备注跨列合并和表格下方图片锚点；当前工程打印工作台补齐物料明细列头原地编辑、明细单元格选区、合并选区、拆分当前、插删行时合并区域同步移动 / 清理。物料明细底部改为表格外记录栏 + 图片区，并在左侧补类似加工合同的上传条，右上和表格下方图片都可从工作台上传后同步到纸面。物料明细表格补齐外边框、列宽和选区打印态隐藏，避免选区高亮进入 PDF / 打印。

下一步：如果还要更贴近 Excel 原件，可继续把色卡的图片 / 留白区与明细行做成同样的合并单元格工作台结构，并为工程打印模板新增专门的 style:l1 场景断言合并按钮、底部图片区和边框完整性。

阻塞/风险：Spreadsheets artifact-tool 依赖加载器在当前线程返回 `No handler registered`，本轮改用 LibreOffice 渲染 + `.xlsx` XML 只读抽取作为 Excel 证据；未修改原 Excel。未做目标环境真实登录 PDF smoke、真实上传持久化归档、客户真实数据导入或 schema / migration / RBAC / Workflow / Fact 改动。已通过 `PATH=/usr/local/bin:$PATH node --test web/src/erp/utils/engineeringPrintEditor.test.mjs web/src/erp/config/printTemplates.test.mjs web/src/erp/utils/printWorkspace.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop,print-center-desktop pnpm --dir web style:l1`、`git diff --check`；浏览器实测合并后 `rowSpan=2 / colSpan=2`、拆分后合并数为 0、底部图片上传后显示 2 张图、表格横向 overflow 为 0。

## 2026-07-04 作业指导书行图片收起与插行编号修正

完成：统一作业指导书作业行图片交互，空行不再先展开空图片槽，也不再显示“移除空图片槽”；行内只保留收起式“添加图片 / 继续添加图片”按钮，已上传图片才以小缩略图和数量状态显示，清空后会从该作业行图片数组移除。作业行上插、下插、追加和删除后统一重排行号，避免插入后出现重复或错位编号。

下一步：如果后续要把作业行图片做成单独的弹层管理或持久化业务附件，应另评审附件真源、上传存储、PDF 输出和业务页回显链路；当前仍只是打印草稿内图片快照。

阻塞/风险：本轮只改工程打印工作台前端草稿编辑和样式，不改 schema、migration、RBAC、Workflow / Fact、库存 / 采购 / 生产 / 财务事实，也不把图片槽升级为业务附件事实。已通过 `PATH=/usr/local/bin:$PATH node --test web/src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop,print-center-desktop pnpm --dir web style:l1`；浏览器回归确认上传前第 3 行无空图片槽、上传后缩略图高度约 68px、上下插行后行号 `1-10` 连续，截图见 `web/output/playwright/engineering-print-templates-v4/work-instruction-collapsed-upload-renumber.png`。未做目标环境真实登录 PDF smoke 或真实上传持久化归档。

## 2026-07-04 物料明细底部记录栏移除

完成：按截图反馈移除物料分析明细表纸面底部的 `日期 / 订单 / 产品编号 / 产品名称 / 数量 / 备品 / 交期 / 审核` 记录栏；底部图片区仍保留并上移到删除记录栏后的合理间距。`footerCells` 草稿字段暂保留为兼容旧草稿，不再在纸面渲染。

下一步：如果后续确认旧草稿和默认样例也不再需要 `footerCells`，可单独清理数据结构、测试断言和历史草稿兼容口径。

阻塞/风险：本轮只删纸面可见区块和对应样式，不改业务带值字段、schema、migration、RBAC、Workflow / Fact、PDF 后端或图片附件事实。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop,print-center-desktop pnpm --dir web style:l1`；浏览器回归确认 `.erp-material-detail-paper__bottom-records` 数量为 0、底部图片槽仍为 2 个，截图见 `web/output/playwright/engineering-print-templates-v5/material-detail-bottom-records-removed.png`。

## 2026-07-04 工程打印三模板字段面板全量联动

完成：将物料分析明细表、色卡、作业指导书三套工程打印模板的左侧“当前记录字段（可编辑）”扩展为同一份打印草稿的全量可编辑字段视图。物料明细覆盖公司、产品、订单、数量、备品、日期、设计师、制表、审核、毛向、顶部备注、表头和各物料行字段；色卡覆盖公司、产品、制卡、日期、审核、复核、色卡块、厂商和块内行；作业指导书覆盖公司、产品、版本、工序、发放部门、订单、制表、设计师、审核、段落标题、段落行、作业行和备注。左侧字段表和右侧纸面现在复用同一批 setter，避免两边不同步。

下一步：如果字段行数量后续影响大批量物料明细编辑效率，可再按合同工作台模式把物料行 / 色卡块 / 作业步骤拆成专门的左侧分区编辑器；本轮先保证搜索字段表与纸面原地编辑双向联动。

阻塞/风险：本轮只改工程打印工作台前端草稿编辑映射，不改 schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端或图片附件事实。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop,print-center-desktop pnpm --dir web style:l1`；浏览器回归覆盖三套模板左侧改右侧、右侧改左侧：物料明细 `物料行 1 物料名称` / `产品名称`，色卡 `色卡块 1 行 1 做法` / `色卡块 1 物料名称`，作业指导书 `裁床行 1` / `作业行 1 内容`。

## 2026-07-04 物料明细模板字号与左右留白收紧

完成：按当前反馈收紧物料分析明细表纸面版式，不影响色卡和作业指导书。物料明细 A4 纸面左右 padding 从通用 10mm 收到 5mm，字号收为 9px，表格行高和单元格 padding 同步压缩；表头和单元格默认 `nowrap`，列宽统一由 `colgroup` 控制，并把宽度优先分配给物料名称、组装部位、加工方式、总用量和备注等常规值更容易换行的列。清理物料明细表格旧的 `nth-child` 列宽覆盖，避免与 `colgroup` 双重控制。

下一步：如果后续真实物料名称或备注明显长于当前样例，需要按“长文本允许人工换行 / 缩小备注列字体 / 拆分备注展示”三选一再定版；本轮目标是常规值不换行，不把超长异常值强行塞进单行。

阻塞/风险：本轮只改工程打印工作台前端纸面版式，不改业务字段、schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端或图片附件事实。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=print-center-desktop pnpm --dir web style:l1`；浏览器 DOM 回归确认 `http://localhost:5185/erp/print-workspace/engineering-material-detail?draft=fresh` 下物料明细纸面 `paddingLeft=18.8976px / paddingRight=18.8976px / fontSize=9px`，纸面和表格横向 overflow 均为 false，表头和样例单元格 `cellOverflowCount=0 / multilineCount=0`，截图见 `web/output/playwright/engineering-print-templates-v6/material-detail-compact-table.png`。

## 2026-07-04 工程打印行操作按钮统一

完成：按采购合同 / 加工合同打印工作台的按钮语义，统一三套工程打印模板的行操作文案与选择行为。物料明细顶部只保留 `上插一行 / 下插一行 / 移除当前行 / 选择明细行 / 选择单元格 / 合并选区 / 拆分当前`；色卡拆成色卡块和块内行两组，去掉 `追加色卡块 / 块内加行 / 移除色卡行`；作业指导书拆成作业行和段落行两组，去掉 `追加作业行 / 段落加行 / 上插作业行 / 下插作业行` 等旧口径。行、块、段落现在都必须先点击对应“选择…”按钮进入选择模式，再点纸面目标行 / 块执行上下插或移除，避免普通原地编辑时误选中。

下一步：如果后续还要继续压缩工程模板工具条复杂度，可把三套模板共用的选择 / 插行 / 移除 toolbar 逻辑抽成共享 helper；当前先保持在工程打印工作台内收口，避免提前抽象影响采购合同 / 加工合同。

阻塞/风险：本轮只改工程打印工作台前端交互和 `style:l1` 回归场景，不改业务字段、schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端、图片附件事实或打印中心原型状态。已通过 `PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`git diff --check -- web/scripts/style-l1/scenarios.mjs web/src/erp/pages/EngineeringPrintWorkspacePage.jsx web/src/erp/styles/app/engineering-print.css web/src/erp/utils/engineeringPrintEditor.mjs web/src/erp/utils/engineeringPrintEditor.test.mjs web/src/erp/data/engineeringPrintTemplates.mjs`；此前同轮已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`。浏览器回归截图见 `web/output/playwright/engineering-print-templates-v7/material-detail-row-buttons.png`、`web/output/playwright/engineering-print-templates-v7/color-card-row-buttons.png`、`web/output/playwright/engineering-print-templates-v7/work-instruction-row-buttons.png`。

## 2026-07-04 工程打印编辑区圆角补齐

完成：将打印工作台左侧“当前记录字段（可编辑）”字段表改为圆角外框和裁剪边界，内部边线不再破坏四角；字段 input / textarea 的编辑焦点圆角同步从 4px 提到 8px。物料分析明细表、色卡、作业指导书三套新增工程打印模板复用同一编辑区样式，右侧纸面模板不加 UI 圆角，避免影响打印 / PDF 内容。

下一步：如果后续继续调整编辑区密度，可再统一评审字段表行高、字段分组和搜索固定位置；本轮只处理圆角视觉一致性。

阻塞/风险：本轮只改前端打印工作台样式和 `style:l1` 圆角断言，不改业务字段、schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端、图片附件事实或打印中心 To Implement 原型状态。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`git diff --check -- web/src/erp/styles/app/print-center.css web/scripts/style-l1/scenarios.mjs`；该 L1 场景已覆盖三套工程模板编辑区计算样式、按钮行为和横向 overflow。

## 2026-07-05 工程打印右侧纸面圆角修正

完成：按截图圈选位置修正前一轮误判，把物料分析明细表、色卡、作业指导书三套工程打印模板右侧 A4 白纸编辑区 `.erp-engineering-print-paper` 改为屏幕态圆角并裁剪白底外角；`@media print` 下重置为直角和可见溢出，避免实际打印纸张被 UI 圆角裁切。`style:l1` 场景同步补右侧纸面 `border-radius >= 10px` 和 `overflow: hidden` 断言。

下一步：如果后续希望采购合同 / 加工合同右侧纸面也采用同一屏幕态圆角，应单独评估两套合同模板的现有 `.erp-print-paper` 视觉和打印态重置，不在本轮顺手扩大。

阻塞/风险：本轮只改三套工程打印模板右侧纸面屏幕态样式和对应回归断言，不改左侧字段编辑逻辑、业务字段、schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端、图片附件事实或打印中心原型状态。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`git diff --check -- web/src/erp/styles/app/engineering-print.css web/scripts/style-l1/scenarios.mjs`。

## 2026-07-05 固定图片位上传交互统一

完成：按页面治理评估后的统一口径，固定图片位走左侧上传栏，右侧纸面只显示输出结果；物料分析明细表右上产品图保留占位但不再在纸面内显示上传 / 清空按钮，底部图片改名为“底部补充图”，未上传时不占用右侧纸面空间，上传后才显示对应图片，清空后再次移除。作业指导书行内图片仍保留行内“添加图片 / 继续添加图片”，因为它绑定具体工序行，不属于固定附件位。

下一步：如果后续需要继续统一固定图片位代码，可抽一个共享的打印附件上传栏 helper 给加工合同和物料明细复用；本轮先收口交互语义，不提前抽象。

阻塞/风险：本轮只改工程打印工作台前端草稿图片交互和 `style:l1` 回归，不改业务附件事实、上传持久化、schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端或打印中心原型状态。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`git diff --check -- web/src/erp/pages/EngineeringPrintWorkspacePage.jsx web/src/erp/data/engineeringPrintTemplates.mjs web/scripts/style-l1/scenarios.mjs`；该 L1 场景已覆盖物料明细底部补充图默认不渲染、左侧上传后显示、清空后移除。

## 2026-07-05 合同与物料固定附件上传区位置统一

完成：将加工合同的纸样 / 图样附件上传区、物料分析明细表的右上产品图和底部补充图上传区统一放入左侧字段面板；右侧纸面编辑区域只保留纸面 DOM 和输出结果，不再被上传按钮占位。新增 `PrintWorkspaceShell` 左侧辅助操作插槽，两个模板复用现有上传 / 清空 / 已同步状态逻辑，PDF 和打印仍以右侧纸面 DOM 为输出真源。

下一步：如果后续固定附件位继续增加，可把当前上传栏从加工合同 class 名收口为通用打印附件上传组件，避免合同与工程模板共用语义类名。

阻塞/风险：本轮只改前端打印工作台布局和 L1 断言，不改业务附件事实、上传持久化、schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端、打印中心原型状态或客户配置。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=print-workspace-processing,engineering-print-workspace-row-buttons pnpm --dir web style:l1`；L1 已断言加工合同和物料明细上传栏位于左侧字段面板且不在右侧 stage 内，并覆盖上传后同步到纸面、清空后移除。

## 2026-07-05 物料明细底部字段组间距修正

完成：修正物料分析明细表纸面底部 `审核 / 制表` 字段的排版，把原来四个 flex 子项改成两个字段组，组内 `字段名 + 值` 紧邻排列，组间保留横向分隔；避免 `审核：` 与 `审核人`、`制表：` 与 `制表人` 被统一 gap 拉得过远。

下一步：如果后续继续精修物料明细纸面，可按真实 Excel 样本继续评审底部字段是否需要增加日期、订单或签名位；本轮只修当前已渲染字段的视觉距离。

阻塞/风险：本轮只改物料明细打印纸面前端布局和 L1 box-metric 断言，不改字段真源、业务带值、schema、migration、RBAC、Workflow / Fact、PDF 后端、上传持久化、客户配置或打印中心原型状态。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`；L1 已断言物料明细底部两个字段组存在，且 label/value 视觉间距不超过 8px、不重叠。

## 2026-07-05 物料明细底部补充图输出样式对齐

完成：将物料分析明细表右侧纸面底部补充图区对齐加工合同附件输出样式：上传后按双列等宽网格展示，列间距 7mm，图片位高度 60mm，图片使用 `object-fit: contain`，并移除工程图片槽原有虚线占位边框和缩略图尺寸。未上传时仍不渲染底部图区，不占用纸面空间。

下一步：如果后续右上产品图也要改成合同附件式大图，应单独评估物料明细表头结构和 Excel 原件位置，避免把表头产品图挤压主表区域。

阻塞/风险：本轮只改物料明细打印纸面底部图片输出样式和 L1 断言，不改左侧上传入口、图片快照生成、上传持久化、字段真源、业务带值、schema、migration、RBAC、Workflow / Fact、PDF 后端、客户配置或打印中心原型状态。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`；L1 已断言上传后底部图区为双列、7mm 间距、60mm 高且无占位边框，清空后继续移除纸面底部图区。

## 2026-07-05 作业指导书右上产品图上传入口收口

完成：评估物料明细和作业指导书两类右上固定产品图后，保留右上图在纸面内的原位置和输出占位样式；作业指导书右上图不再在纸面内显示上传 / 清空按钮，改为和物料明细一致从左侧字段面板上传，上传后只在右侧纸面 header 输出图片。作业行内图片入口保持原地添加，因为它绑定具体工序行，不属于固定右上附件位。

下一步：如果后续要继续统一固定图片上传栏文案和 class 命名，可抽一个通用打印图片上传栏组件，避免继续复用加工合同上传条 class 名。

阻塞/风险：本轮只改作业指导书右上固定图片位的前端入口位置和 L1 断言，不改行内工序图片交互、图片快照生成、上传持久化、字段真源、业务带值、schema、migration、RBAC、Workflow / Fact、PDF 后端、客户配置或打印中心原型状态。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`；L1 已断言作业指导书右上产品图上传栏位于左侧字段面板、纸面 header 无上传 / 清空按钮、上传后只输出图片、清空后移除图片。

## 2026-07-05 色卡 Excel 原表线条还原

完成：按 `docs/customers/yoyoosun/raw-source-files/26204#抱抱猴子材料明细表2026-4-10.xlsx` 的 `色卡` sheet 重做色卡纸面表格结构。色卡不再用两列卡片网格，而是渲染为左侧 A/B/C 三列、D 窄隔栏、右侧 E/F/G 三列的连续表格；每个色卡块保留“材料名 / 厂商”表头，块内材料图片区用 `rowSpan` 合并多行，部位和做法列保留 Excel 原表的空白预留行。默认样例补齐 Excel 中的 12 个色卡块，并把公司名、产品编号、产品名称、制卡和日期样例值对齐原表。预留空白格仍可直接原地编辑，提交后会自动补齐对应色卡行；新增色卡块会继承当前选中块的左右栏。

下一步：如果后续要把真实 Excel 的图片/色卡布样也导入为可打印附件，需要单独评审图片来源、上传持久化和 PDF 输出链路；本轮只还原色卡表格线条、合并结构、默认样例和草稿编辑行为。

阻塞/风险：本轮只改工程打印色卡前端纸面结构、默认草稿样例、草稿编辑 helper 和对应 `style:l1` 回归，不改 schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端、图片附件事实或打印中心原型状态。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`git diff --check -- web/src/erp/data/engineeringPrintTemplates.mjs web/src/erp/pages/EngineeringPrintWorkspacePage.jsx web/src/erp/styles/app/engineering-print.css web/src/erp/utils/engineeringPrintEditor.mjs web/scripts/style-l1/scenarios.mjs`。截图验证见 `/tmp/plush-engineering-color-card.png`，浏览器指标为左右表各 36 行、12 个色卡块、12 个合并图片区。

## 2026-07-05 色卡左右栏块操作规则修正

完成：评估色卡左右栏语义后，将交互口径定为“左栏主料 / 右栏辅料或配件，每侧至少保留一个色卡块”，而不是“右侧第一个色卡块永远不能删除”。在块选择模式下，点击右侧表格任意普通行单元格也能选中对应色卡块，解决之前用户只能稳定操作左侧的问题；删除逻辑增加每侧保底校验，右侧第一个块在右侧还有其它块时可以删除，右侧只剩一个块时禁止删除并提示左右栏各至少保留一个块。

下一步：如果后续需要让用户更清楚左右栏语义，可在工具条状态里补“当前选中左栏 / 右栏第 N 块”的状态提示；本轮先修正可操作性和删除边界。

阻塞/风险：本轮只改色卡打印草稿前端选择命中、删除规则、单测和目标 L1 回归，不改 Excel 样式结构、schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端或图片附件事实。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`。

## 2026-07-05 色卡默认样例收窄

完成：按页面治理口径把色卡 fresh 草稿默认样例从 Excel 全量 12 个色卡块收窄为 2 个块：左栏保留一个毛绒主料块，右栏保留一个辅料 / 配件块。左右栏、窄隔栏、材料图片区合并行、预留空白行、原地编辑、插入 / 删除和每侧至少一个块的交互规则不变，避免默认样例过重影响板房工程部理解和编辑。

下一步：如果后续业务页带入真实 BOM / 色卡数据，仍可按真实数据生成多块；本轮只控制 fresh 默认样例密度，不改变真实带值能力。

阻塞/风险：本轮只改色卡默认草稿样例，不改 Excel 版式结构、schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端、上传持久化或图片附件事实。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=4174 STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`；默认 `4173` 端口被已有进程占用，本轮按项目端口顺延口径使用 `4174` 跑同一 L1 场景。

## 2026-07-05 色卡默认样例调整为左右各两个

完成：将色卡 fresh 草稿默认样例从“总共两个块”调整为“左栏两个主料块 + 右栏两个辅料 / 配件块”。左栏保留灰色毛绒、黄色毛绒；右栏保留黄色 T/C 布、水晶眼，继续体现色卡左右分组格式，同时避免恢复成 Excel 全量 12 块。

下一步：如果后续真实业务页带入色卡数据，仍按真实数据生成块数；默认样例只负责提示左右栏结构和编辑方式。

阻塞/风险：本轮只改色卡默认草稿样例，不改 Excel 版式结构、schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端、上传持久化或图片附件事实。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=4174 STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`。

## 2026-07-05 作业指导书行内图片入口降密度

完成：按“行内图片属于具体作业步骤、固定右上图属于整张单据附件位”的语义拆分，保留作业指导书行内图片跟随作业行输出；空行不再常驻显示“添加图片”按钮或空图片槽，只有当前作业行被选中、聚焦或 hover 时才露出轻量加图入口。顶部作业行工具条新增“给当前行加图”，未选择作业行时禁用；已有行内缩略图继续显示在对应行内，清空 / 上传按钮只在当前行交互态可见，打印态继续隐藏编辑控件。

下一步：如果后续需要进一步降低纸面编辑干扰，可把已上传缩略图的替换 / 清空动作收进悬浮小菜单，但仍应保持图片归属在对应作业行内。

阻塞/风险：本轮只改作业指导书前端行内图片入口、样式和目标 L1 断言，不改右上固定产品图、图片快照生成、上传持久化、字段真源、业务带值、schema、migration、RBAC、Workflow / Fact、PDF 后端、客户配置或打印中心原型状态。已通过 `PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`；L1 已断言默认态没有常驻行内加图按钮 / 空图片槽、选择作业行后行内入口和顶部“给当前行加图”可用、上传后缩略图留在当前行且编辑按钮只在当前行可见。

## 2026-07-05 作业指导书作业行文字 / 图片互斥与居中

完成：将作业指导书作业行收口为“文字行”或“图片行”两种状态：上传行内图片时自动清空该行文字并切到图片行；清空最后一张图片后回到空文字行；文字行不再同时承载图片槽。文字行内容单元格改为上下居中，空加图入口使用绝对定位浮层，不参与行高计算，避免输入文字后被按钮或空图片位顶到上方。

下一步：如果后续要让左侧字段面板也显式展示“该行当前为文字 / 图片”状态，可单独补轻量状态标签；本轮先保证纸面编辑和输出语义正确。

阻塞/风险：本轮只改作业指导书前端行渲染、草稿切换规则、样式和目标 L1 断言，不改图片快照生成、上传持久化、字段真源、业务带值、schema、migration、RBAC、Workflow / Fact、PDF 后端、客户配置或打印中心原型状态。已通过 `PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`；L1 已断言文字行上下居中、空加图入口不撑高、上传后切为图片行且不再显示文字编辑器、清空图片后回到空文字行。

## 2026-07-05 作业指导书 Sheet1 网格和图片行还原

完成：按 `docs/customers/yoyoosun/raw-source-files/26204#抱抱猴子材料明细表2026-4-10.xlsx` 的 `Sheet1` 复核合并格、列宽、行高和图片锚点后，将作业指导书右侧纸面从简化 header / 两列表改为 A-I 九列固定表格。表头恢复公司、标题、产品编号、版次、工序、制表、设计师、审核和右上产品图合并格，裁床、刺绣 / 印花、车缝说明、作业行和备注都在同一表格里输出；默认作业行补到 20 行。工程打印工作台左右留白收紧，作业行图片改为“选中行后由顶部加图 / 清空当前行图片”，纸面行内不再显示上传 / 清空按钮；多图一次上传后横向排列并自动换行，行高按图片内容撑开。右侧可选中文字后用顶部按钮加粗 / 标红，左侧字段表继续按纯文本批量编辑。色卡默认公司名同步回中性 `本公司`，避免客户身份进入 Product Core 默认样例。

下一步：如果后续要进一步逼近 Excel 里的蓝色标注框、箭头和真实客户照片，应作为“导入 / 附件 / 图片批注”专项评审图片来源、持久化和打印输出，不把原始客户图片硬编码进默认模板。

阻塞/风险：本轮只改工程打印工作台前端草稿、纸面渲染、样式和 L1 回归，不改 schema、migration、RBAC、Workflow / Fact、业务带值来源、PDF 后端、上传持久化、客户配置或打印中心原型状态。Excel 参考页和运行态截图见 `output/engineering-work-instruction-review/excel/work-instruction-page-8.png`、`output/engineering-work-instruction-review/excel/work-instruction-page-9.png`、`output/engineering-work-instruction-review/runtime/work-instruction-default.png`、`output/engineering-work-instruction-review/runtime/work-instruction-row18-four-images.png`；浏览器指标见 `output/engineering-work-instruction-review/runtime/work-instruction-layout-metrics.json`。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`，并用 Playwright 复核默认态、4 图换行撑高、纸面无行内按钮、顶部清图保留文字、富文本加粗 / 标红。

## 2026-07-05 作业指导书富文本保存和空图占位复核

完成：继续按 Sheet1 / 截图复核作业指导书后，修正富文本保存链路：浏览器 `foreColor` 生成的 `<font color="#ff0000">` 现在会被清洗为可持久化红字，选中文字后加红 / 加粗再失焦不会丢样式。默认作业指导书样例补入 Excel 参考中的红字 / 加粗段落；右上产品图未上传时改为白底实线空单元格，不再显示灰底虚线上传占位。此前过程记录里的“文字 / 图片互斥”口径已被本轮覆盖，当前口径是作业行文字和图片可共存，清空当前行图片后仍保留文字。目标 L1 已新增默认红字、富文本失焦持久化、空图白底实线、多图换行、图片行行高和纸面无行内按钮断言。

下一步：如果要继续向截图 1 的真实照片、蓝色批注框和箭头靠拢，需要先评审这些图片 / 批注来自 Excel raw source、用户上传、客户配置还是业务附件；本轮不把真实客户图片和批注形状硬编码进 Product Core 默认模板。

阻塞/风险：本轮仍只改工程打印工作台前端样例、富文本清洗、图片槽样式和 L1 回归，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、上传持久化、客户配置或业务来源带值。最新截图见 `output/engineering-work-instruction-review/runtime/work-instruction-default-latest.png`、`output/engineering-work-instruction-review/runtime/work-instruction-row18-four-images-latest.png`；最新指标见 `output/engineering-work-instruction-review/runtime/work-instruction-layout-metrics-latest.json`。已通过 `PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`；Playwright 指标复核为 stage padding 10px、作业行 20 行、4 图换行、图片行高度约 309px、纸面按钮 0、空图白底实线、默认红字为红色且 strong 权重 700。

## 2026-07-05 作业指导书图片批注行版式补齐

完成：继续按 Sheet1 / 截图复核作业指导书，新增作业行可选图片批注版式：有 `imageNotes.left/right` 的作业行渲染为左侧蓝框批注、中间图片区、右侧蓝框批注，批注文案可在纸面内富文本编辑，红字 / 加粗会按现有清洗规则保存。默认第 3 行补为批注行示例，但不硬编码 raw Excel 真实照片；图片仍通过顶部“给当前行加图”上传。中间图片区继续复用同一行图片数组，多张图横向装不下时自动换行，纸面仍不显示行内上传 / 清空按钮。目标 L1 从单图升级为一次上传 4 张图，断言批注行三栏、2 个批注框、无纸面按钮、红字、行高、多图换行和文字保留。

下一步：如果要达到图 1 中真实照片、蓝色形状和箭头的像素级位置，需要评审图片 / 批注来源和持久化：raw Excel 导入、用户上传、业务附件或客户配置；本轮仍不把真实客户照片硬编码进 Product Core 默认模板。

阻塞/风险：本轮只改工程打印前端草稿模型、纸面渲染、样式和 L1 回归，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、上传持久化、客户配置或业务来源带值。最新截图见 `output/engineering-work-instruction-review/runtime/work-instruction-annotation-default-latest.png`、`output/engineering-work-instruction-review/runtime/work-instruction-annotation-four-images-latest.png`；最新指标见 `output/engineering-work-instruction-review/runtime/work-instruction-annotation-metrics-latest.json`。已通过 `PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`；Playwright 指标复核为 stage padding 10px、作业行 20 行、批注行高度约 639px、批注行 3 栏、2 个批注框、4 图换行、纸面按钮 0。

## 2026-07-05 作业指导书列宽比例和编号行插入回归补强

完成：使用 bundled Python / openpyxl 只读抽取 `docs/customers/yoyoosun/raw-source-files/26204#抱抱猴子材料明细表2026-4-10.xlsx` 的 Sheet1 结构元数据：A-I 列宽约为 `5.75 / 6.375 / 6.25 / 5.75 / 9 / 10.59 / 9.625 / 17.47 / 27.45`，关键图片大行包含 147、253、359、408 等高行。作业指导书 CSS 的 A-I 列宽已按 Excel 比例归一到 200mm，右侧 I 列和中间列比例更贴近原表。空白作业行模型补齐 `imageNotes.left/right`，避免后续批注行依赖 undefined 容错。L1 回归补强：未选择作业行时上插 / 下插禁用；选择编号作业行后上插 / 下插可用；第 2 行下插后新增第 3 行为空白且被选中，原第 3 行批注大行顺延为第 4 行并保留 2 个批注框。

下一步：继续逼近 Excel Sheet1 的真实照片、红色测量线、蓝色批注框和箭头，需要先决定这些图片 / 形状是从 raw Excel 解析导入、由用户上传、来自业务附件，还是属于客户配置模板；本轮仍只做前端草稿和运行态纸面，不把 raw 客户照片硬编码进中性 Product Core 默认模板。

阻塞/风险：本轮只改作业指导书前端编辑模型、列宽样式和 L1 断言，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片 / 形状持久化、客户配置或业务来源带值。最新截图见 `output/engineering-work-instruction-review/runtime/work-instruction-default-after-col-width-latest.png`、`output/engineering-work-instruction-review/runtime/work-instruction-row-insert-after-latest.png`；最新指标见 `output/engineering-work-instruction-review/runtime/work-instruction-row-insert-metrics-latest.json`。已通过 `PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`；全量前端测试为 616 pass。

## 2026-07-05 作业指导书图片批注线承载

完成：继续按图 1 / Sheet1 的图片说明行复核，把作业行批注从“左右文字框 + 简化伪线”推进为数据驱动的 SVG 批注线层。作业行草稿新增 `imageCallouts`，默认第 3 行配置 3 条蓝色批注线，渲染时覆盖左批注框、中间图片区和右批注框，不拦截鼠标；有数据线时关闭旧 `::after` 简化伪线，避免重复线条。空白作业行默认 `imageCallouts: []`，插入作业行不会复制旧图片、旧批注或旧连线；原批注行顺延后仍保留 3 条连线。目标 L1 已断言默认批注行 2 个批注框、3 条 SVG 连线、旧伪线关闭、纸面按钮 0，以及下插空白行不复制批注线。

下一步：如果要达到图 1 / 图 2 中真实照片、红色测量线、蓝色箭头和文本框的像素级位置，需要继续评审并实现图片 / 形状来源：从 raw Excel 解析导入、用户上传后手工配置、业务附件带入，或客户配置模板。当前 Product Core 默认模板仍不硬编码 raw Excel 真实客户照片。

阻塞/风险：本轮只改作业指导书前端草稿模型、批注线渲染、样式和回归断言，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片 / 形状持久化、客户配置或业务来源带值。最新截图见 `output/engineering-work-instruction-review/runtime/work-instruction-callouts-default-latest.png`、`output/engineering-work-instruction-review/runtime/work-instruction-callouts-with-image-latest.png`；最新指标见 `output/engineering-work-instruction-review/runtime/work-instruction-callouts-metrics-latest.json`。已通过 `PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`；全量前端测试为 616 pass。

## 2026-07-05 作业指导书多图测量说明行承载

完成：继续按图 2 的第 18 行复核，扩展作业行图片说明模型：新增 `imageLabels`，和已有 `imageCallouts` 共用同一个 `InstructionImageAnnotationLayer`，可在普通图片行上传后叠加红色测量线、白底说明框和蓝底说明框。默认第 18 行不硬编码真实客户照片，只预置测量说明层；用户选择该编号行并从顶部“给当前行加图”上传多张图片后，行内自动变成大图说明行，4 张图可换行，行高约 455px，显示 3 条测量线、4 个说明框，其中 1 个为蓝底说明框，纸面仍无行内上传 / 删除按钮。空白插入行继续不复制图片、批注线和说明框。

下一步：要完全贴近图 2，仍需实现真实照片来源和 Excel shape / arrow 导入或手工标注编辑能力；本轮只补前端草稿的“多图测量说明层”承载与运行态输出。

阻塞/风险：本轮只改作业指导书前端草稿模型、图片说明层渲染、样式和回归断言，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片 / 形状持久化、客户配置或业务来源带值。最新截图见 `output/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-images-latest.png`、`output/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-row-latest.png`；最新指标见 `output/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-metrics-latest.json`。已通过 `PATH=/usr/local/bin:$PATH node --check web/scripts/style-l1/scenarios.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`；全量前端测试为 616 pass。

## 2026-07-05 作业指导书第 18 行 Excel 大图行高度回补

完成：继续用 bundled Python / openpyxl 只读抽取 `docs/customers/yoyoosun/raw-source-files/26204#抱抱猴子材料明细表2026-4-10.xlsx` 的 `Sheet1` 结构，确认第 18 工序对应原表合并区 `B58:I59`，行高为 `253pt + 359pt`，约 `216mm`；第 18 行图片承载区按约 `190mm` 回补。作业行模型新增可选 `heightMm / imageAreaHeightMm`，normalize 会对旧草稿里的“无左右文字批注但有测量标签的图片说明行”自动回补该高度；顶部给当前行加图时也会补同一布局字段，避免旧 localStorage 草稿继续停在 120mm。CSS 改为读取行级变量，并把图片测量说明行默认下限提升到 190mm。L1 回归新增 `rowMinHeightVar / rowImageAreaHeightVar` 指标和 `rowHeight >= 680px` 强断言，当前实测第 18 行为 `719.109px`，图片区域 `718.11px`，变量为 `216mm / 190mm`。

下一步：继续补齐与 Excel 完全一致还需要实现真实照片来源、Excel shape / arrow / text box 导入或可编辑标注层、以及 Sheet1 多页块（头部车缝 / 身体车缝 / 手工）重复表头的结构化承载；当前本轮没有把 raw 客户照片硬编码进 Product Core 默认模板。

阻塞/风险：本轮只改作业指导书前端草稿归一化、上传动作布局回补、样式高度和 L1 证据写入，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片 / 形状持久化、客户配置或业务来源带值。最新 L1 证据见 `web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-metrics-latest.json`、`web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-row-latest.png`、`web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-images-latest.png`。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`；后续收口继续跑 lint / 全量前端测试。

## 2026-07-05 作业指导书 A-I 主体列宽和编辑区留白修正

完成：继续用 bundled Python / openpyxl 只读核对 `Sheet1` 前 12 行，确认作业指导书主体合并范围是 `A1:F2`、`I1:I6`、`A7:I7`、`B8:I8` 等 A-I 九列；J-M 只是工作表右侧空列，不应进入纸面主体。此前临时按 A-M 13 列尝试会把公司 / 标题区压窄，本轮已改回 A-I 主体，并把 A-I 列宽按原 Excel 宽度 `5.75 / 6.375 / 6.25 / 5.75 / 9 / 10.5917 / 9.625 / 17.4667 / 27.45` 归一为百分比。工程打印工作台左侧字段面板从 320px 收到 300px，内容 gap 和 stage padding 收窄，stage 内纸面改为靠左对齐，作业指导书纸面内边距从 5mm 收到 3mm，减少编辑区域左右留白但不改变 A4 打印宽度。L1 已新增断言：作业指导书 colgroup 为 9 列、表头首行和首个作业行 colSpan 合计均为 9、右上产品图仍为 I 列合并格、纸面 padding 不超过 12px、stageWrap 为 `flex-start`。第 18 行多图测量说明仍保持 `216mm / 190mm` 行级变量，当前实测行高 `719.109px`，4 图换行、3 条线、4 个说明框、纸面按钮 0。

下一步：继续补齐与 Excel / 截图完全一致仍需要真实照片来源、Excel shape / arrow / text box 导入或可编辑标注层，以及 Sheet1 多页块重复表头 / 分页结构；这些需要单独做图片 / 形状来源和持久化评审。

阻塞/风险：本轮仍只改工程打印前端页面、样式和 L1 回归，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片 / 形状持久化、客户配置或业务来源带值。最新 L1 证据见 `web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-metrics-latest.json`、`web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-row-latest.png`、`web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-images-latest.png`。已通过 `PATH=/usr/local/bin:$PATH pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`PATH=/usr/local/bin:$PATH pnpm --dir web css`、`PATH=/usr/local/bin:$PATH pnpm --dir web build`、`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons pnpm --dir web style:l1`、`PATH=/usr/local/bin:$PATH pnpm --dir web lint`、`PATH=/usr/local/bin:$PATH pnpm --dir web test`；全量前端测试为 616 pass。

## 2026-07-05 作业指导书 Sheet1 行高比例接入

完成：继续按 `docs/customers/yoyoosun/raw-source-files/26204#抱抱猴子材料明细表2026-4-10.xlsx` 的 `Sheet1` 前页行高复核，补齐作业指导书表头 1-6 行、裁床标题 / 明细行、刺绣标题 / 明细行、车缝提示行和前 8 个作业步骤行的 `heightMm` 数据。纸面渲染改为用 `--work-instruction-row-height` / `--instruction-row-min-height` 驱动单元格高度，修正了旧 CSS 选择器 `.erp-work-instruction-paper__step-row` 不命中真实 `--text / --image` 作业行的问题。段落行从历史字符串兼容升级为 `{ text, heightMm }`，插入段落行时生成空白对象且不复制样例 Excel 行高；空白模板仍保留版式骨架但不带样例高度。L1 新增 DOM 断言：表头 6 行均为 `8.5mm`，裁床标题 `6.4mm`，裁床三行 `7.1mm / 10.8mm / 11.1mm`，刺绣行 `5mm`，车缝提示 `14.8mm`，首个作业步骤 `11.6mm`，并用 `getBoundingClientRect()` 确认这些变量实际影响浏览器行盒尺寸。第 18 行大图测量行仍保持 `216mm / 190mm`，最新实测行高 `719.109px`。

下一步：继续逼近图 1 / 图 2 仍需要实现真实 Excel 嵌入照片来源、shape / arrow / text box 导入或可编辑标注层，以及 Sheet1 多页重复表头 / 分页块结构；这些不是本轮行高接入范围。

阻塞/风险：本轮只改工程打印前端模板数据、编辑器段落行模型、纸面渲染、样式和 L1 回归，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片 / 形状持久化、客户配置或业务来源带值。最新 L1 证据见 `web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-metrics-latest.json`、`web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-row-latest.png`、`web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-images-latest.png`。已通过 `/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm --dir web css`、`/usr/local/bin/pnpm --dir web build`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`/usr/local/bin/pnpm --dir web lint`、`/usr/local/bin/pnpm --dir web test`、`git diff --check`；全量前端测试为 617 pass。

## 2026-07-05 作业指导书 Sheet1 多页块补齐

完成：继续用 bundled Python / openpyxl 只读复核 `Sheet1` A:I，确认原 Excel 不是单页，而是头部车缝、身体车缝、身体手工、头部手工四个重复页块。默认作业指导书样例新增 `continuationPages`，补齐身体车缝 20 个作业行、身体手工 3 个作业行和头部手工 3 个作业行，并按 Sheet1 行高写入 `heightMm`；其中身体车缝第 18 行继续保持 `216mm / 190mm` 大图测量行，身体手工第 2 行按原 Excel 大图行写入 `143.9mm`。纸面渲染新增只读续页表，复用 A:I colgroup、表头、notice、作业行、备注、批注线和图片槽样式，作业指导书纸面允许纵向 visible，续页表使用 `break-before: page` / `page-break-before: always`，避免后续表被父容器裁掉。L1 回归已升级为断言 4 张 A:I 表、3 张续页、每张 9 列、续页无按钮、身体车缝 20 行、身体 / 头部手工页各 3 行、续页分页样式和关键行高变量。

下一步：继续逼近“一模一样”仍需要把多页续页从只读样例推进到完整可编辑模型：续页作业行选择、插行、图片上传 / 清空、字段面板映射和保存路径都需要泛化；真实 Excel 嵌入照片、shape / arrow / text box 导入或可编辑标注层仍未实现。

阻塞/风险：本轮只改工程打印前端模板数据、只读续页渲染、样式和 L1 回归，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片 / 形状持久化、客户配置或业务来源带值。续页内容是中性样例文本，不把 raw 客户照片硬编码进 Product Core 默认模板；空白模板显式 `continuationPages: []`，不会把客户样例续页当空白业务事实。最新 L1 指标见 `web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-multipage-grid-metrics-latest.json`，续页截图辅助见 `web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-continuation-body-sewing-latest.png`。已通过 `/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm --dir web css`、`/usr/local/bin/pnpm --dir web build`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`/usr/local/bin/pnpm --dir web lint`、`/usr/local/bin/pnpm --dir web test`、`git diff --check`；全量前端测试为 618 pass。

## 2026-07-05 作业指导书续页编号行交互接入

完成：把作业指导书编号行选择从首页 `rowIndex` 泛化为 `{ pageIndex, rowIndex }` 目标，首页和续页共用顶部“上插 / 下插 / 移除 / 给当前行加图 / 清空当前行图片”工具栏。续页作业行现在可在纸面内编辑行号、正文和左右图片批注；多图上传、清空图片、批注线和说明框都复用同一套行更新路径，纸面行内仍不显示加图或删除按钮。编辑器工具函数新增续页插行 / 移除行，新增空白行不复制 Excel 样例行高、图片、批注线或说明框，并只在当前续页内重新编号。L1 回归新增续页选择、续页下插 / 移除、续页大图行 4 图上传、横向换行、`216mm` 大行高、顶部清空图片和纸面按钮 0 的断言。

下一步：继续逼近“一模一样”还剩真实 Excel 嵌入照片导入 / 持久化、Excel shape / arrow / text box 导入或可编辑标注层，以及续页行在左侧字段面板里的批量编辑映射；这些仍需要单独做图片 / 形状来源与保存边界评审。

阻塞/风险：本轮仍只改工程打印前端工作台、编辑器工具函数、L1 回归和进度记录，不改 schema、migration、RBAC、Workflow / Fact、PDF 后端、图片 / 形状持久化、客户配置或业务来源带值。续页截图辅助见 `web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-continuation-body-sewing-latest.png`，第 18 行大图截图见 `web/output/playwright/style-l1/engineering-work-instruction-review/runtime/work-instruction-row18-measurement-row-latest.png`。已通过 `/usr/local/bin/pnpm --dir web exec node --test src/erp/utils/engineeringPrintEditor.test.mjs`、`/usr/local/bin/pnpm --dir web lint`、`/usr/local/bin/pnpm --dir web css`、`/usr/local/bin/pnpm --dir web build`、`STYLE_L1_SCENARIOS=engineering-print-workspace-row-buttons /usr/local/bin/pnpm --dir web style:l1`、`/usr/local/bin/pnpm --dir web test`、`git diff --check`；全量前端测试为 619 pass。

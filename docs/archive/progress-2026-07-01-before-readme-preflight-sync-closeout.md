# plush-toy-erp progress

本文件只保留当前活跃事项、最近完成记录和归档索引；历史流水已归档到 `docs/archive/`。`progress.md` 是过程交接线索，不是正式需求、数据模型或部署真源。

## 归档索引

- `docs/archive/progress-2026-06-28-before-runtime-manifest.md`：归档 2026-06-28 之前至客户配置版本运行时最小闭环、`/__dev` 页面治理等过程记录。
- `docs/archive/progress-2026-06-29-before-release-evidence-hardening.md`：归档 2026-06-28 客户配置 runtime manifest、发布执行器、dev-only 页面治理、Workflow action 合同、导入与发布证据前置门禁等过程记录。
- `docs/archive/progress-2026-06-29-before-target-evidence-binding.md`：归档 2026-06-29 release evidence、真实导入 recovery plan、文档清单、备份恢复和回滚演练门禁早期硬化过程记录。
- `docs/archive/progress-2026-06-29-before-priority-audit-closeout.md`：归档 2026-06-29 target evidence binding 之后到 release evidence runner 脱敏报告与 URL 前置拦截的过程记录。
- `docs/archive/progress-2026-06-29-before-process-runtime-minimum.md`：归档 2026-06-29 adminProfileSync 菜单投影文档纠偏、P2 explain / entitlement / break-glass 中段过程记录。
- `docs/archive/progress-2026-06-29-before-linked-task-idempotency.md`：归档 2026-06-29 P3 ProcessRuntime expected_version 守卫之前至 linked task 幂等闭环前的过程记录。
- `docs/archive/progress-2026-06-30-before-inventory-post-inbound.md`：归档 2026-06-30 P3 / P4 前段、adminProfileSync 菜单投影文档多轮纠偏、sales_order_acceptance、material_supply definition evidence、`purchase_receipt.create` 和 `quality_inspection.decide` 领域命令 handler 过程记录。
- `docs/archive/progress-2026-06-30-before-p5-release-input-checklist.md`：归档 2026-06-30 P4-2 / P4-3、adminProfileSync 文档纠偏和进入 P5 release closeout 输入清单前的过程记录。
- `docs/archive/progress-2026-06-30-before-p5-input-checklist-followup.md`：归档 2026-06-30 P5 release closeout report-only、release evidence hardening、菜单投影纠偏和 input checklist JSON 自定义路径只读合同之前的过程记录。
- `docs/archive/progress-2026-06-30-before-outsourcing-order-api-gate.md`：归档 2026-06-30 P5 input checklist、adminProfileSync 文档纠偏、purchase / quality / shipment / stock reservation / production / outsourcing fact / sales / purchase order moduleStates 门禁等过程记录。

- `docs/archive/progress-2026-07-01-before-action-projection-l1.md`：归档 2026-06-30 outsourcing order API 门禁之后至 2026-07-01 动作投影浏览器回归扩展的过程记录。
- `docs/archive/progress-2026-07-01-before-progress-archive-and-next-no-write.md`：归档 2026-07-01 动作投影浏览器回归扩展之后至 Purchase Receipt Browser E2E 输入模板的过程记录。

## 当前活跃事项

- 多甲方角色能力流程编排以 `docs/product/多甲方角色能力流程编排优先级.md` 和 `node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 的 `implementationOrder` 为本地优先级入口；GPT/reference 资料只作输入，当前真源仍回到代码、migration、测试和正式文档。
- 当前审计显示 P0-P4 本地证据为 ready；P5 测试部署 / 导入 / 第二客户验证仍为 `target-evidence-required`，第一条 blocked release action 是 `immutable-version`。
- P5 当前只允许 report-only、input template 和 checklist 准备；没有真实目标环境、镜像 digest、migration 前后版本和 backup id 时，不写 `deployments/**/evidence/**`，不执行 `--execute`，不把本地 ready 写成目标 release evidence。
- 真实客户数据导入、正式生产发布、目标环境 smoke、目标 migration、备份恢复、回滚 / 前向修复演练、客户配置激活和签收仍未执行，不能被本地 dry-run、manifest 编译、status、gate、audit 或 runner report 替代。

## 2026-07-01 progress 归档到下一轮 no-write 闭环前

- 完成：归档前 `progress.md` 为 325 行 / 82333 bytes，超过 80KB 阈值；已将完整原文复制到 `docs/archive/progress-2026-07-01-before-progress-archive-and-next-no-write.md`。
- 完成：根 `progress.md` 收敛为归档索引、当前活跃事项、归档说明和最近 5 组记录，避免继续在超过阈值的根流水上追加。
- 下一步：继续当前 goal 时按单个可验证闭环推进；若本轮继续改代码或正式文档，完成后再在根 `progress.md` 追加新的短记录。
- 阻塞/风险：本次归档只整理过程记录，不改变 runtime、schema、RBAC、测试、发布脚本、目标环境状态、客户数据或正式业务真源。

## 2026-07-01 移动端 Workflow Smoke 输入模板
- 完成：为 `web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs` 增加 `--print-input-template` 只读模式，输出移动端 workflow 真实浏览器回归所需密码、后端 / 前端地址、runId、模拟任务计划和后续真实命令；同步 `/__dev/testing` 的 `mobile-workflow-smoke` 复制预设、`scripts/README.md`、`web/README.md`、`web/scripts/README.md`、`docs/product/自动化测试策略.md`、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs` 和 `mobile-workflow-runtime-browser-smoke.test.mjs`，避免缺少本地后端或演示密码时把占位命令误当作已验证证据。
- 验证：`PATH=/usr/local/bin:$PATH node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --print-input-template` 通过，输出 `callsBackend=false`、`startsBrowser=false`、`writesDatabase=false`；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5241 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过。
- 下一步：继续按 goal 检查客户配置 active revision 读回和真实后端 / 浏览器 smoke 前置；具备本地后端、前端 runtime 和演示账号密码后再运行 `pnpm --dir web smoke:mobile-workflow-runtime-browser` 证明老板任务阻塞 / 退回 / 完成反馈、仓库任务跨角色催办和内部提醒线索真实页面链路。
- 阻塞/风险：本组只改输入模板、dev-only 复制预设、静态守卫和文档口径；未登录、未调用后端、未创建 `simulated_only` workflow 任务、未启动浏览器、未写数据库，不修改正式菜单、后端 RBAC、customer_config runtime、schema、真实客户数据、部署或 release evidence。

## 2026-07-01 Customer Config Active Revision 读回输入模板
- 完成：为 `scripts/deploy/customer-config-release-execute.mjs` 增加 `--print-input-template` 只读模式，输出客户配置 publish / activate / rollback / `get_effective_session` 读回所需 manifest、evidence、后端 URL、管理员凭据、确认短语和后续 readiness 命令；同步 `/__dev/testing` 的 `customer-config-package-runtime` 预设、`scripts/README.md`、`docs/product/自动化测试策略.md`、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs` 和 `customer-config-release-execute.test.mjs`，让缺少目标后端或凭据时也能先拿到 active revision 读回前置清单。
- 验证：`PATH=/usr/local/bin:$PATH node --check scripts/deploy/customer-config-release-execute.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/deploy/customer-config-release-execute.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node scripts/deploy/customer-config-release-execute.mjs --print-input-template` 通过，输出 `callsBackend=false`、`writesDatabase=false`、`readsManifest=false`、`validatesReleaseEvidence=false`；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5242 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过。
- 下一步：继续按 goal 检查客户配置 active revision 目标读回、试用账号真实 RBAC 和本地业务闭环；具备目标后端、管理员凭据、release evidence 和显式确认后，才能执行 publish / activate / rollback 或 `get_effective_session` 读回。
- 阻塞/风险：本组只改输入模板、dev-only 复制预设、静态守卫和文档口径；未读取 manifest、未检查 release evidence、未调用后端、未 publish、未 activate、未 rollback、未调用 `get_effective_session`、未写数据库，不修改后端 RBAC、schema、真实客户数据、部署或 release evidence 证据形状。

## 2026-07-01 Real Login Smoke 共享输入模板
- 完成：为 `web/scripts/realLoginSmokeShared.mjs` 增加 `--print-input-template` 只读模式，输出真实登录 smoke 共享前置：后端健康检查 URL、可选前端 URL、本地管理员凭据来源、采购合同 / 加工合同 / 岗位任务端认证回跳和采购入库真实写入浏览器 e2e 命令；同步 `/__dev/testing` 的 `real-login-smoke-shared` 预设、`realLoginSmokeShared.test.mjs`、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md`、`web/scripts/README.md`、`web/README.md` 和 `docs/product/自动化测试策略.md`，把“输入模板不等于真实登录证据”和“采购入库 e2e 会按自身边界写本地 / 开发库模拟事实”写清。
- 验证：`PATH=/usr/local/bin:$PATH node --check web/scripts/realLoginSmokeShared.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test web/scripts/realLoginSmokeShared.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node web/scripts/realLoginSmokeShared.mjs --print-input-template` 通过，输出 `callsBackend=false`、`startsBrowser=false`、`writesDatabase=false`、`readsLocalConfig=false`；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5243 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过。
- 下一步：继续按 goal 检查本地后端、试用账号真实 RBAC、岗位任务端真实浏览器 smoke 和客户配置 active revision 读回；具备本地后端和真实开发账号后，才能运行 `smoke:purchase-contract-real-login`、`smoke:processing-contract-real-login`、`smoke:mobile-auth-login-route` 或采购入库真实写入浏览器 e2e。
- 阻塞/风险：本组只改共享输入模板、dev-only 复制预设、测试和文档口径；未读取本地配置、未校验账号、未调用后端 health/auth、未启动 Vite、未启动 Playwright、未登录、未写数据库，不修改后端 RBAC、schema、业务事实、客户数据、部署或 release evidence。

## 2026-07-01 Mobile Auth Route Smoke 输入模板
- 完成：为 `web/scripts/mobileAuthLoginRouteSmoke.mjs` 增加 `--print-input-template` 只读模式和 direct-run guard，输出岗位任务端认证回跳 smoke 的 9 个岗位、phone / iPad 视口、可选环境变量和真实回归命令；同步 `mobileAuthLoginRouteSmoke.test.mjs`、`fast.sh`、`strict.sh`、`/__dev/testing` 的 `real-login-smoke-shared` 预设、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md`、`web/scripts/README.md`、`web/README.md` 和 `docs/product/自动化测试策略.md`，并让 `MOBILE_AUTH_SMOKE_BASE_URL` 拒绝 URL 账号密码。
- 验证：`PATH=/usr/local/bin:$PATH node --check web/scripts/mobileAuthLoginRouteSmoke.mjs && PATH=/usr/local/bin:$PATH node --check web/scripts/mobileAuthLoginRouteSmoke.test.mjs && PATH=/usr/local/bin:$PATH bash -n scripts/qa/fast.sh && PATH=/usr/local/bin:$PATH bash -n scripts/qa/strict.sh` 通过；`PATH=/usr/local/bin:$PATH node --test web/scripts/mobileAuthLoginRouteSmoke.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node web/scripts/mobileAuthLoginRouteSmoke.mjs --print-input-template` 通过，输出 `callsBackend=false`、`startsBrowser=false`、`startsDevServer=false`、`writesDatabase=false`、`usesMockRpc=true`；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5244 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过。
- 下一步：继续按 goal 检查真实后端试用账号 RBAC、岗位任务端真实 workflow 浏览器 smoke、客户配置 active revision 读回和本地业务闭环；具备真实本地后端、演示账号密码或显式写入确认后，才能运行真实登录或真实写入类 smoke。
- 阻塞/风险：本组只改输入模板、mock smoke 边界、dev-only 复制预设、静态守卫和文档口径；未启动 Vite、未启动 Playwright、未调用真实后端、未登录真实账号、未写数据库，不修改后端 RBAC、schema、业务事实、客户数据、部署或 release evidence。

## 2026-07-01 Purchase Receipt Browser E2E 输入模板
- 完成：为 `web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs` 增加 `--print-input-template` 只读模式、direct-run guard 和延迟 runtime 创建，输出采购入库页面真实写入 e2e 的管理员凭据来源、`PR-BROWSER-*` 持久测试记录边界、localhost / 外部开发环境防呆、`--accept-persistent-test-data`、`--seed-core-demo` 和真实命令；新增 `purchaseReceiptRealWriteBrowserE2E.test.mjs`，并同步 `realLoginSmokeShared.mjs`、`fast.sh`、`strict.sh`、`/__dev/testing` 的 `real-login-smoke-shared` 预设、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md`、`web/scripts/README.md`、`web/README.md` 和 `docs/product/自动化测试策略.md`。
- 验证：`PATH=/usr/local/bin:$PATH node --check web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs && PATH=/usr/local/bin:$PATH node --check web/scripts/purchaseReceiptRealWriteBrowserE2E.test.mjs && PATH=/usr/local/bin:$PATH bash -n scripts/qa/fast.sh && PATH=/usr/local/bin:$PATH bash -n scripts/qa/strict.sh` 通过；`PATH=/usr/local/bin:$PATH node --test web/scripts/purchaseReceiptRealWriteBrowserE2E.test.mjs web/scripts/realLoginSmokeShared.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs --print-input-template` 通过，输出 `writesDatabase=false`、`callsBackend=false`、`startsBrowser=false`、`startsDevServer=false`、`downstreamWritesDatabase=true`；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5245 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过并实际执行新增“采购入库真实写入浏览器 e2e 边界测试”；`rg` 锚点扫描通过。
- 下一步：继续按 goal 检查真实后端试用账号 RBAC、岗位任务端真实 workflow 浏览器 smoke、客户配置 active revision 读回和本地业务闭环；下次更新 `progress.md` 前应先归档，因为本次追加后文件已超过 80KB。
- 阻塞/风险：本组只改输入模板、真实写入脚本防误跑边界、dev-only 复制预设、静态守卫和文档口径；未启动 Vite、未启动 Playwright、未调用后端、未登录、未创建 / 过账 / 取消采购入库单、未写数据库，不修改后端 RBAC、schema、purchase usecase、库存事实逻辑、客户数据、部署或 release evidence。

## 2026-07-01 Purchase Receipt Service E2E 输入模板
- 完成：先按阈值归档 `progress.md`，将超过 80KB 的旧流水完整复制到 `docs/archive/progress-2026-07-01-before-progress-archive-and-next-no-write.md`，同步 `docs/文档清单.md` 和 `docs/archive/README.md`；随后为 `scripts/qa/purchase-receipt-real-write-e2e.mjs` 增加 `--print-input-template` 只读模式，输出服务层采购入库真实写入 e2e 的 `--out`、`--with-postgres`、本地 PostgreSQL 测试库边界和真实命令；新增 `purchase-receipt-real-write-e2e.test.mjs`，并同步 `fast.sh`、`strict.sh`、`/__dev/testing` 的 `mvp-local-closure` 预设、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md` 和 `docs/product/自动化测试策略.md`。
- 验证：`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs scripts/qa/purchase-receipt-real-write-e2e.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --print-input-template` 通过，输出 `writesReports=false`、`runsGoTests=false`、`writesTestDatabase=false`、`callsBackend=false`；`PATH=/usr/local/bin:$PATH node --check scripts/qa/purchase-receipt-real-write-e2e.mjs && PATH=/usr/local/bin:$PATH node --check scripts/qa/purchase-receipt-real-write-e2e.test.mjs && PATH=/usr/local/bin:$PATH bash -n scripts/qa/fast.sh && PATH=/usr/local/bin:$PATH bash -n scripts/qa/strict.sh` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5246 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过并实际执行新增“采购入库服务层真实写入 e2e 边界测试”；`git diff --check` 通过。
- 下一步：继续按 goal 检查试用账号真实 RBAC、岗位任务端真实 workflow 浏览器 smoke、客户配置 active revision 读回和本地业务闭环；具备本地后端、测试库或显式写入确认后，才能运行服务层真实写入 e2e、PostgreSQL 防呆测试或浏览器真实写入 smoke。
- 阻塞/风险：本组只改 progress 归档、输入模板、静态守卫、dev-only 复制预设和文档口径；未运行 Go 服务层真实写入测试、未调用 `make purchase_receipt_pg_test`、未连接 PostgreSQL、未调用后端、未写报告、未写数据库，不修改后端 RBAC、schema、purchase / inventory usecase、客户数据、部署或 release evidence。

## 2026-07-01 Trial Simulated Data 输入模板
- 完成：为 `scripts/qa/trial-simulated-data.mjs` 增加 `--print-input-template` 只读模式，输出试用模拟数据 report-only / apply simulated data 所需 `--out`、`--backend-url`、产品 / 单位 ID、演示账号密码来源、核心 demo seed 和后续真实命令；模板显式标记 `writesReports=false`、`writesDatabase=false`、`callsBackend=false`、`importsRealCustomerData=false`，且不能和 `--apply` 混用。同步 `trial-simulated-data.test.mjs`、`/__dev/testing` 的 `trial-simulated-data` 预设、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md` 和 `docs/product/自动化测试策略.md`。
- 验证：`PATH=/usr/local/bin:$PATH node --test scripts/qa/trial-simulated-data.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node scripts/qa/trial-simulated-data.mjs --print-input-template` 通过，输出 `writesReports=false`、`writesDatabase=false`、`callsBackend=false`；`PATH=/usr/local/bin:$PATH node --check scripts/qa/trial-simulated-data.mjs && PATH=/usr/local/bin:$PATH node --check scripts/qa/trial-simulated-data.test.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5247 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过并实际执行新增 trial simulated data 输入模板测试；`git diff --check` 通过。
- 下一步：继续按 goal 检查业务事实模拟闭环、岗位任务端真实 workflow 浏览器 smoke、试用账号真实 RBAC 和客户配置 active revision 读回；若要真实写入模拟试用数据，必须先具备本地后端、演示账号密码、活跃产品 / 单位 ID，并显式提供 `TRIAL_SIM_CONFIRM=APPLY_SIMULATED_TRIAL_DATA`。
- 阻塞/风险：本组只改输入模板、静态守卫、dev-only 复制预设和文档口径；未登录、未调用后端、未生成 report-only 文件、未执行 `--apply`、未写数据库、未导入真实客户数据，不修改 schema、RBAC、MasterData / SalesOrder usecase、客户资料、部署或 release evidence。本轮开头按 goal 尝试 `git fetch --prune`，但本机到 GitHub SSH 返回 `Can't assign requested address`，因此未能刷新远端状态；后续提交 / 推送前必须重新 fetch。

## 2026-07-01 Operational Fact Simulated Closure 输入模板
- 完成：为 `scripts/qa/operational-fact-simulated-closure.mjs` 增加 `--print-input-template` 只读模式，在要求产品 / 单位 / 仓库 ID 之前输出业务事实模拟闭环 report-only / apply simulated operational facts 所需输入、`demo_pmc / demo_purchase / demo_warehouse / demo_finance` 角色账号、核心 demo seed 和后续真实命令；模板显式标记 `writesReports=false`、`writesDatabase=false`、`callsBackend=false`、`importsRealCustomerData=false`，且不能和 `--apply` 混用。同步 `operational-fact-simulated-closure.test.mjs`、`/__dev/testing` 的 `trial-simulated-data` 预设、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md` 和 `docs/product/自动化测试策略.md`。
- 验证：`PATH=/usr/local/bin:$PATH node --test scripts/qa/operational-fact-simulated-closure.test.mjs scripts/qa/trial-simulated-data.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node scripts/qa/operational-fact-simulated-closure.mjs --print-input-template` 通过，输出 `writesReports=false`、`writesDatabase=false`、`callsBackend=false`；`PATH=/usr/local/bin:$PATH node --check scripts/qa/operational-fact-simulated-closure.mjs && PATH=/usr/local/bin:$PATH node --check scripts/qa/operational-fact-simulated-closure.test.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5248 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过并实际执行新增 operational fact 输入模板测试；`git diff --check` 通过。
- 下一步：继续按 goal 检查岗位任务端真实 workflow 浏览器 smoke、试用账号真实 RBAC、客户配置 active revision 读回和第一条本地业务真实写入链路；若要真实写入业务事实模拟闭环，必须先具备本地后端、演示账号密码、活跃产品 / 单位 / 仓库 ID，并显式提供 `OPERATIONAL_FACT_SIM_CONFIRM=APPLY_SIMULATED_OPERATIONAL_FACTS`。
- 阻塞/风险：本组只改输入模板、静态守卫、dev-only 复制预设和文档口径；未登录、未调用后端、未生成 report-only 文件、未执行 `--apply`、未写数据库、未导入真实客户数据，不修改 schema、RBAC、OperationalFactUsecase、库存 / 出货 / 财务事实规则、客户资料、部署或 release evidence。

## 2026-07-01 Mobile Workflow Simulated Closure 输入模板
- 完成：本轮开头重新读取 goal objective，并成功执行 `git fetch --prune` 与 `git status -sb`，确认当前分支仍为 `main...origin/main`。为 `scripts/qa/mobile-workflow-simulated-closure.mjs` 增加 `--print-input-template` 只读模式，在创建 plan、写 report、登录或调用后端之前输出岗位任务端模拟 workflow 闭环所需 runId、演示账号密码来源、`demo_pmc / demo_boss / demo_quality / demo_warehouse` 角色账号、模拟任务组、done / blocked / evidence / exception report 动作和后续真实命令；模板显式标记 `writesReports=false`、`writesDatabase=false`、`callsBackend=false`、`factPosting=false`、`createsOperationalFacts=false`，且不能和 `--apply` 混用。同步 `mobile-workflow-simulated-closure.test.mjs`、`/__dev/testing` 的 `trial-simulated-data` 预设、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md` 和 `docs/product/自动化测试策略.md`。
- 验证：`PATH=/usr/local/bin:$PATH node --test scripts/qa/mobile-workflow-simulated-closure.test.mjs scripts/qa/operational-fact-simulated-closure.test.mjs scripts/qa/trial-simulated-data.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node scripts/qa/mobile-workflow-simulated-closure.mjs --print-input-template` 通过，输出 `scope=mobile-workflow-simulated-closure-input-template`、`writesReports=false`、`writesDatabase=false`、`callsBackend=false`、`createsOperationalFacts=false`；`PATH=/usr/local/bin:$PATH node --check scripts/qa/mobile-workflow-simulated-closure.mjs && PATH=/usr/local/bin:$PATH node --check scripts/qa/mobile-workflow-simulated-closure.test.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`rg` 锚点扫描通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5249 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过并实际执行新增 mobile workflow 输入模板 8 个测试；`git diff --check` 通过。
- 下一步：继续按 goal 检查真实岗位任务端浏览器 smoke、试用账号真实 RBAC、客户配置 active revision 读回和第一条本地业务真实写入链路；若要真实写入移动端模拟 workflow 任务，必须先具备本地后端、演示账号密码，并显式提供 `MOBILE_WORKFLOW_SIM_CONFIRM=APPLY_SIMULATED_MOBILE_WORKFLOW_TASKS`。
- 阻塞/风险：本组只改输入模板、静态守卫、dev-only 复制预设和文档口径；未登录、未调用后端、未生成 report-only 文件、未执行 `--apply`、未创建或更新 workflow 任务、未写数据库、未写任何业务事实、未导入真实客户数据，不修改 schema、RBAC、WorkflowUsecase、OperationalFactUsecase、客户资料、部署或 release evidence。

## 2026-07-01 Mobile Workflow Runtime Browser Smoke 输入模板强化
- 完成：本轮重新读取 goal objective、执行 `git fetch --prune` 和 `git status -sb`，确认当前分支仍为 `main...origin/main`。强化 `web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs` 的 `--print-input-template` 合同，新增 `startsDevServer=false`、`readsLocalConfig=false`、`createsWorkflowTasks=false`、`downstreamCreatesWorkflowTasks=true`、`downstreamStartsBrowser=true`、`downstreamCallsBackend=true` 字段；同时在 `scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 增加 CLI 级断言，证明无演示账号密码时执行 `--print-input-template` 只输出 JSON，不进入后端健康检查、不启动 Vite、不启动浏览器、不创建 `simulated_only` workflow 任务。
- 验证：`PATH=/usr/local/bin:$PATH node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过，新增 CLI 输入模板测试后共 6 个用例；`PATH=/usr/local/bin:$PATH node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs && PATH=/usr/local/bin:$PATH node --check scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --print-input-template` 通过，输出 `startsDevServer=false`、`readsLocalConfig=false`、`createsWorkflowTasks=false`、`downstreamCreatesWorkflowTasks=true`；`rg` 锚点扫描通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过并实际执行“岗位任务端真实浏览器模拟任务回归边界测试”6 个用例；`git diff --check` 通过。
- 下一步：继续按 goal 检查试用账号真实 RBAC、客户配置 active revision 读回、真实岗位任务端浏览器 smoke 和第一条本地业务真实写入链路；真正运行 `pnpm --dir web smoke:mobile-workflow-runtime-browser` 前仍必须具备本地后端、前端 runtime 和演示账号密码。
- 阻塞/风险：本组只强化输入模板和测试合同；未登录、未调用后端、未启动 Vite、未启动 Playwright、未创建 workflow 任务、未提交 mobile action、未写数据库、未写任何业务事实、未导入真实客户数据，不修改 schema、RBAC、WorkflowUsecase、客户配置、部署或 release evidence。

## 2026-07-01 Trial Demo Account Browser Smoke 输入模板边界
- 完成：本轮重新读取 goal objective，并成功执行 `git fetch --prune` 与 `git status -sb`，确认当前分支仍为 `main...origin/main`。强化 `web/scripts/trialDemoAccountBrowserSmoke.mjs` 的 `--print-input-template` 合同，新增 `startsDevServer=false`、`readsCustomerConfigScript=false`、`downstreamCallsBackend=true`、`downstreamStartsBrowser=true`、`downstreamStartsDevServer=true`、`downstreamReadsCustomerConfigScript=true` 字段；新增 `web/scripts/trialDemoAccountBrowserSmoke.test.mjs`，用 CLI 级测试证明无演示密码时只输出输入模板 JSON，不登录、不调用后端、不启动 Vite / Playwright、不读取 `customer-config.js`、不写数据库。同步 `fast.sh`、`strict.sh`、`/__dev/testing` 的 `trial-account-rbac` 预设、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`trial-role-entry-docs.test.mjs`、`scripts/README.md` 和 `docs/product/自动化测试策略.md`。
- 验证：`PATH=/usr/local/bin:$PATH node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/trial-account-rbac.test.mjs scripts/qa/trial-role-entry-docs.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 19 个用例；`PATH=/usr/local/bin:$PATH node --check web/scripts/trialDemoAccountBrowserSmoke.mjs && PATH=/usr/local/bin:$PATH node --check web/scripts/trialDemoAccountBrowserSmoke.test.mjs && PATH=/usr/local/bin:$PATH bash -n scripts/qa/fast.sh && PATH=/usr/local/bin:$PATH bash -n scripts/qa/strict.sh && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH node web/scripts/trialDemoAccountBrowserSmoke.mjs --print-input-template` 通过，输出 `writesDatabase=false`、`callsBackend=false`、`startsBrowser=false`、`startsDevServer=false`、`readsCustomerConfigScript=false` 和 10 个桌面账号、9 个移动端账号；`rg` 锚点扫描通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5250 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过并实际执行新增“试用账号浏览器 smoke 输入模板边界测试”。
- 下一步：继续按 goal 检查真实试用账号 RBAC、客户配置 active revision 读回、真实岗位任务端浏览器 smoke 和第一条本地业务真实写入链路；真正运行 `pnpm --dir web smoke:trial-demo-browser` 前仍必须具备本地后端、前端 runtime 和演示账号密码。
- 阻塞/风险：本组只强化输入模板、静态守卫、dev-only 复制预设和文档口径；未登录、未调用后端、未启动 Vite、未启动 Playwright、未读取 `customer-config.js`、未验证菜单投影、未写数据库、未写任何业务事实、未导入真实客户数据，不修改 schema、RBAC、customer_config active revision、部署或 release evidence。

## 2026-07-01 Customer Config Release Readiness 输入模板
- 完成：本轮继续按 goal objective 执行，补跑 `git fetch --prune` 后确认当前分支仍为 `main...origin/main`。为 `scripts/deploy/customer-config-release-readiness.mjs` 增加 `--print-input-template` 只读模式，输出 readiness / `--require-executed` / `--require-activated` / `--require-rollback` 所需 manifest、release evidence、执行器报告和目标 smoke `customer-config-effective-session` 读回证据要求；新增单测证明该模板不需要 manifest / evidence-dir，不读取 manifest、不检查 release evidence、不调用 `customer_config`、不写数据库，也不证明 active revision 已读回。同步 `/__dev/testing` 的 `customer-config-package-runtime` 预设、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md`、`docs/product/自动化测试策略.md`、`fast.sh` 和 `strict.sh`。
- 验证：`PATH=/usr/local/bin:$PATH node --test scripts/deploy/customer-config-release-readiness.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 34 个用例；`PATH=/usr/local/bin:$PATH node --check scripts/deploy/customer-config-release-readiness.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs && PATH=/usr/local/bin:$PATH bash -n scripts/qa/fast.sh && PATH=/usr/local/bin:$PATH bash -n scripts/qa/strict.sh` 通过；`PATH=/usr/local/bin:$PATH node scripts/deploy/customer-config-release-readiness.mjs --print-input-template` 通过，输出 `writesDatabase=false`、`callsBackend=false`、`readsManifest=false`、`readsReleaseEvidence=false`、`validatesReleaseEvidence=false` 和 `requiredReadbackEvidence`；`rg` 锚点扫描通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5251 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过并实际执行新增 readiness 输入模板用例；`git diff --check` 通过。
- 下一步：继续按 goal 检查客户配置 active revision 的真实执行报告 / 目标 smoke 证据缺口、试用账号真实 RBAC、真实岗位任务端浏览器 smoke 和第一条本地业务真实写入链路；真正声明 active revision 已读回前，必须有执行器报告 `effectiveSessionVerification` 和目标 smoke `customer-config-effective-session`。
- 阻塞/风险：本组只改 readiness 输入模板、静态守卫、dev-only 复制预设和文档口径；未读取 manifest、未检查 release evidence、未调用后端、未 publish / activate / rollback、未运行 migration / 备份恢复 / 目标 smoke、未写数据库、未导入真实客户数据、未写 Workflow / Fact runtime，不修改 schema、RBAC、customer_config usecase、部署或 release evidence。

## 2026-07-01 yoyoosun Run Smoke 输入模板
- 完成：本轮继续按 goal objective 执行，并成功执行 `git fetch --prune` 与 `git status -sb`，确认当前分支仍为 `main...origin/main`。为 `deployments/yoyoosun/scripts/run-smoke.sh` 增加 `--print-input-template` 只读模式，输出目标 smoke 所需 public endpoint、backend URL、releaseVersion、environment、report path、客户配置 revision 和 admin token env 名；模板显式标记 `writesReport=false`、`writesDatabase=false`、`callsEndpoint=false`、`callsBackend=false`、`callsCustomerConfig=false`、`readsAdminToken=false`，列出 `customer-config-effective-session` 读回证据要求但不触网、不读 token、不写 `smoke-test-report.json`、不证明 active revision 已读回。同步 `run-smoke-script.test.mjs`、`/__dev/testing` 的 `customer-config-package-runtime` 预设、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`deployments/yoyoosun/scripts/README.md`、`scripts/README.md`、`docs/product/自动化测试策略.md`、`fast.sh` 和 `strict.sh`。
- 验证：`PATH=/usr/local/bin:$PATH node --test scripts/deploy/run-smoke-script.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 18 个用例；`PATH=/usr/local/bin:$PATH bash -n deployments/yoyoosun/scripts/run-smoke.sh && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs && PATH=/usr/local/bin:$PATH bash -n scripts/qa/fast.sh && PATH=/usr/local/bin:$PATH bash -n scripts/qa/strict.sh` 通过；`PATH=/usr/local/bin:$PATH bash deployments/yoyoosun/scripts/run-smoke.sh --print-input-template` 通过，输出 `scope=yoyoosun-run-smoke-input-template`、`callsCustomerConfig=false`、`readsAdminToken=false` 和 `requiredReadbackEvidence`；`rg` 锚点扫描通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5252 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过并实际执行新增 run-smoke 输入模板用例；`git diff --check` 通过。
- 下一步：继续按 goal 检查客户配置 active revision 的真实执行报告 / 目标 smoke 证据缺口、试用账号真实 RBAC、真实岗位任务端浏览器 smoke 和第一条本地业务真实写入链路；真正声明 active revision 已读回前，仍必须运行目标 smoke 并生成 `customer-config-effective-session` 脱敏报告。
- 阻塞/风险：本组只改目标 smoke 输入模板、静态守卫、dev-only 复制预设和文档口径；未访问 endpoint、未读取 token、未调用后端、未生成 smoke report、未 publish / activate / rollback、未写数据库、未导入真实客户数据、未写 Workflow / Fact runtime，不修改 schema、RBAC、customer_config usecase、部署或 release evidence。

## 2026-07-01 Trial Account RBAC 脱敏报告入口
- 完成：本轮继续按 goal objective 执行，开头重新读取目标文件、项目 skills、AGENTS 和正式真源，并成功执行 `git fetch --prune` / `git status -sb`，确认当前分支仍为 `main...origin/main`。将 `scripts/qa/trial-account-rbac.mjs` 从纯控制台核对推进为可选 `--report output/trial-account-rbac/report.json` 的真实运行入口；报告只保存后端 endpoint alias、账号名、角色、岗位权限、debug 权限数量、super admin / disabled 布尔结果和汇总，显式标记不保存密码、access token 或 Authorization header。同步强化 `--print-input-template`，输出 `writesReport=false`、`callsAuthRPC=false`、`callsMeRPC=false`、`readsPassword=false`、`startsBrowser=false`、`startsDevServer=false` 和真实证据清单；同步 `trial-account-rbac.test.mjs`、`trial-role-entry-docs.test.mjs`、`/__dev/testing` 的 `trial-account-rbac` 预设、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md`、`docs/product/自动化测试策略.md` 和 `docs/customers/yoyoosun/试用环境执行手册.md`。
- 验证：`PATH=/usr/local/bin:$PATH node --test scripts/qa/trial-account-rbac.test.mjs scripts/qa/trial-role-entry-docs.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 18 个用例；`PATH=/usr/local/bin:$PATH node --check scripts/qa/trial-account-rbac.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH node scripts/qa/trial-account-rbac.mjs --print-input-template` 通过，锚点包含 `writesReport=false`、`callsAuthRPC=false`、`readsPassword=false`、`--report output/trial-account-rbac/report.json` 和 `requiredRealEvidence`；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5253 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过；`rg` 锚点扫描通过；`git diff --check` 通过。
- 下一步：继续按 goal 检查真实试用账号 RBAC、真实岗位任务端浏览器 smoke、客户配置 active revision 读回和第一条本地业务真实写入链路；具备本地后端和演示账号密码后，可运行 `TRIAL_ACCOUNT_PASSWORD=... node scripts/qa/trial-account-rbac.mjs --report output/trial-account-rbac/report.json` 生成脱敏报告，再继续运行 `pnpm --dir web smoke:trial-demo-browser`。
- 阻塞/风险：本组只改试用账号 RBAC 核对脚本的脱敏报告能力、输入模板、静态守卫、dev-only 复制预设和文档口径；未读取真实密码、未登录、未调用后端、未写报告、未启动 Vite / Playwright、未验证菜单投影、未写数据库、未导入真实客户数据，不修改 schema、RBAC、admin seed、customer_config active revision、部署或 release evidence。

## 2026-07-01 Mobile Auth Route Smoke 稳定性修复
- 完成：修复 `web/scripts/mobileAuthLoginRouteSmoke.mjs` 在生产单端口 `/m/<role>/tasks` 回归中的本地运行不稳定问题。脚本现在默认使用 `http://localhost:<port>` 并以 `--host localhost` 启动 Vite，避免应用本地开发 canonical host 把 `127.0.0.1` 改写成 `localhost` 后资源加载空白；脚本内 Chromium 使用 `--no-proxy-server`，只影响本次 Playwright 实例，不改系统代理或节点。补充共享 Vite 健康检查、初始登录页短重试和失败诊断，失败时输出当前 URL、body 文本片段、localStorage 登录态键和浏览器 console/pageerror 尾部日志，便于区分登录态残留、路由问题和本地网络 / host 问题。
- 验证：`PATH=/usr/local/bin:$PATH node --check web/scripts/mobileAuthLoginRouteSmoke.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test web/scripts/mobileAuthLoginRouteSmoke.test.mjs` 通过，共 3 个用例；`MOBILE_AUTH_SMOKE_APP_ID=mobile-purchasing PATH=/usr/local/bin:$PATH pnpm --dir web smoke:mobile-auth-login-route` 通过；`MOBILE_AUTH_SMOKE_APP_ID=mobile-business PATH=/usr/local/bin:$PATH pnpm --dir web smoke:mobile-auth-login-route` 通过；`PATH=/usr/local/bin:$PATH pnpm --dir web smoke:mobile-auth-login-route` 通过，验证 9 个岗位任务端、phone / iPad 视口、未登录跳转、旧登录态回登录页、密码登录、任务列表、底部导航、退出登录和后台 / 岗位端浏览器返回隔离；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过；`git diff --check` 通过。
- 下一步：继续按 goal 检查真实试用账号 RBAC、真实岗位任务端 workflow 浏览器 smoke、客户配置 active revision 读回和第一条本地业务真实写入链路；真实账号 / 真实后端 / 写入类 smoke 仍必须在具备演示账号密码、目标后端和显式写入确认后执行。
- 阻塞/风险：本组只改本地 mock RPC 浏览器回归脚本稳定性和诊断；未登录真实账号、未调用真实后端、未写数据库、未生成 release evidence、未修改 schema / RBAC / WorkflowUsecase / Fact usecase / 客户数据 / 部署配置。

## 2026-07-01 Mobile Workflow Runtime Browser Smoke 前置报告
- 完成：为 `web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs` 增加 `--preflight-report <path>` no-write 前置报告模式。该模式只探测 backend health、演示密码 env 是否存在、是否需要脚本托管 Vite、试用 customer-config 脚本是否存在，并将结果写入 repo 内 `output/...` JSON；不读取密码值、不登录、不调用 JSON-RPC、不启动 Vite / Playwright、不创建 workflow 任务、不写数据库，也不保存 token 或 Authorization header。同步 `/__dev/testing` 的 `mobile-workflow-smoke` 复制预设、`mobile-workflow-runtime-browser-smoke.test.mjs`、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md`、`web/scripts/README.md` 和 `docs/product/自动化测试策略.md`，把移动端真实 workflow smoke 的执行顺序收口为输入模板 -> preflight 报告 -> 真实 browser smoke。
- 验证：`PATH=/usr/local/bin:$PATH node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 20 个用例；`PATH=/usr/local/bin:$PATH node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs && PATH=/usr/local/bin:$PATH node --check scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report output/mobile-workflow-runtime-browser-smoke/preflight.json` 通过，报告显示 backend health 200、customer-config 脚本存在、`readyForRealSmoke=false`，唯一 blocker 为 `missing-demo-password-env`；敏感锚点扫描未发现 token / access_token / 示例密码值；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5254 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过。
- 下一步：继续按 goal 检查真实试用账号 RBAC、客户配置 active revision 读回和第一条本地业务真实写入链路；若要真正运行 `pnpm --dir web smoke:mobile-workflow-runtime-browser`，当前仍需提供演示账号密码 env，再重跑 preflight 确认 `readyForRealSmoke=true`。
- 阻塞/风险：本组只改移动端真实 browser smoke 的 no-write 前置报告、dev-only 复制预设、测试和文档口径；未登录真实账号、未调用 JSON-RPC、未创建或更新 workflow 任务、未启动 Vite / Playwright、未写数据库、未生成 release evidence，不修改 schema、RBAC、WorkflowUsecase、Fact usecase、客户数据或部署配置。

## 2026-07-01 Trial Demo Account Browser Smoke 前置报告
- 完成：为 `web/scripts/trialDemoAccountBrowserSmoke.mjs` 增加 `--preflight-report <path>` no-write 前置报告模式，并把密码读取延后到真实登录路径。该模式只探测 backend health、演示密码 env 是否存在、是否需要脚本托管 Vite、yoyoosun customer-config 脚本是否存在、桌面 / 岗位账号数量和阻塞项；不读取密码值、不登录、不调用 JSON-RPC、不启动 Vite / Playwright、不读取 customer-config 脚本内容、不写数据库，也不保存 token 或 Authorization header。同步 `/__dev/testing` 的 `trial-account-rbac` 预设、`trialDemoAccountBrowserSmoke.test.mjs`、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`trial-role-entry-docs.test.mjs`、`scripts/README.md`、`docs/product/自动化测试策略.md` 和 `docs/customers/yoyoosun/试用环境执行手册.md`；顺手修正试用手册中桌面账号通过数从 9 改为当前脚本真实的 10 个。
- 验证：`PATH=/usr/local/bin:$PATH node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/trial-account-rbac.test.mjs scripts/qa/trial-role-entry-docs.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 21 个用例；`PATH=/usr/local/bin:$PATH node --check web/scripts/trialDemoAccountBrowserSmoke.mjs && PATH=/usr/local/bin:$PATH node --check web/scripts/trialDemoAccountBrowserSmoke.test.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs && PATH=/usr/local/bin:$PATH node --check scripts/qa/trial-role-entry-docs.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node web/scripts/trialDemoAccountBrowserSmoke.mjs --preflight-report output/trial-demo-account-browser-smoke/preflight.json` 通过，报告显示 backend health 200、customer-config 脚本存在、`desktopAccountCount=10`、`mobileAccountCount=9`、`readyForRealSmoke=false`，唯一 blocker 为 `missing-demo-password-env`；敏感锚点扫描未发现 token / access_token / 示例密码值；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5255 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过。
- 下一步：继续按 goal 检查客户配置 active revision 读回和第一条本地业务真实写入链路；若要真正运行 `pnpm --dir web smoke:trial-demo-browser`，当前仍需提供演示账号密码 env，再重跑 preflight 确认 `readyForRealSmoke=true`。
- 阻塞/风险：本组只改试用账号真实 browser smoke 的 no-write 前置报告、dev-only 复制预设、测试和文档口径；未登录真实账号、未调用 JSON-RPC、未启动 Vite / Playwright、未读取 customer-config 脚本内容、未验证菜单投影、未写数据库、未生成 release evidence，不修改 schema、RBAC、customer_config usecase、WorkflowUsecase、Fact usecase、客户数据或部署配置。

## 2026-07-01 Customer Config Active Readback 前置报告
- 完成：为 `scripts/deploy/customer-config-release-readiness.mjs` 增加 `--readback-preflight-report <path>` no-write active revision 读回缺口报告。该报告只读取本地 runtime manifest、customer-config release report 和目标 smoke `smoke-test-report.json` 的脱敏结构，汇总 manifest revision、`effectiveSessionVerification`、目标 smoke `customer-config-effective-session`、backend endpoint alias 和 token env 是否存在；不调用后端、不读取 token 值、不写 release evidence、不写数据库、不导入业务数据，也不把该报告当成 active revision 已读回证明。同步 `/__dev/testing` 的 `customer-config-package-runtime` 预设，先输出当前 yoyoosun preview manifest，避免复用 stale output manifest；同步 `devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md` 和 `docs/product/自动化测试策略.md`。
- 验证：`PATH=/usr/local/bin:$PATH node --check scripts/deploy/customer-config-release-readiness.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/customer-package-lint.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs scripts/deploy/customer-config-release-execute.test.mjs scripts/deploy/customer-config-release-readiness.test.mjs scripts/deploy/run-smoke-script.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 84 个用例；`PATH=/usr/local/bin:$PATH node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，输出 `revision=yoyoosun-customer-package-v1.runtime-manifest-v1`、`modules=15`、`roles=9`、`entitlements=346`；`PATH=/usr/local/bin:$PATH node scripts/deploy/customer-config-release-readiness.mjs --manifest output/customers/yoyoosun/customer-config-runtime-manifest.json --evidence-dir deployments/yoyoosun/evidence/releases/2026-07-01-readback-preflight --release-report output/customers/yoyoosun/customer-config-release/customer-config-release-report.json --readback-preflight-report output/customers/yoyoosun/customer-config-readback-preflight.json` 通过，报告显示 manifest valid、`pageCount=19`、`fieldPolicySurfaceCount=3`、`readyForReadinessGate=false`、`readyForRealTargetSmoke=false`，当前阻塞项为 `missing-effective-session-verification` 和 `missing-smoke-report`；敏感扫描未发现 Bearer / access_token / token 值；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5256 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过；`git diff --check` 通过。
- 下一步：继续按 goal 检查第一条本地业务真实写入链路、真实试用账号 RBAC 和真实岗位任务端 browser smoke；真正声明 customer_config active revision 已读回前，仍必须具备执行器报告中的 `effectiveSessionVerification`、目标 smoke 的 `customer-config-effective-session` 脱敏证据、目标后端和 admin token env。
- 阻塞/风险：本组只改 no-write 缺口报告、dev-only 复制预设、测试和文档口径；未调用后端、未读取 token、未 publish / activate / rollback、未运行 target smoke、未写 release evidence、未写数据库、未导入真实客户数据，不修改 schema、RBAC、customer_config usecase、部署或 release evidence。

## 2026-07-01 Purchase Receipt Service E2E 前置报告与默认链路
- 完成：为 `scripts/qa/purchase-receipt-real-write-e2e.mjs` 增加 `--preflight-report <path>` 本地脱敏前置报告。该报告确认 Go 命令、服务层 JSON-RPC 测试锚点、`scripts/purchase-receipt-pg.sh` PostgreSQL guard 和 `purchase_receipt_pg_test` Make target 是否存在；不运行 Go test、不调用 `make`、不连接 PostgreSQL、不保存 `PURCHASE_RECEIPT_PG_DB_URL` 值、不写业务报告、不写数据库。同步 `/__dev/testing` 的 `mvp-local-closure` 预设、`devTesting.test.mjs`、`dev-entry-boundary.test.mjs`、`scripts/README.md` 和 `docs/product/自动化测试策略.md`，把采购入库服务层真实写入链路顺序收口为输入模板 -> preflight 报告 -> 默认服务层 e2e -> 可选 PostgreSQL 防呆。
- 验证：`PATH=/usr/local/bin:$PATH node --check scripts/qa/purchase-receipt-real-write-e2e.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/purchase-receipt-real-write-e2e.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 17 个用例；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --preflight-report output/qa/purchase-receipt-real-write-e2e/preflight.json` 通过，报告 `readyForRequestedCommand=true`、`serviceLayer.readyForLocalServiceE2E=true`、`postgresGuard.requested=false`、`blockers=[]`；敏感扫描未发现 DSN、Bearer、access_token 或 Authorization；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --out output/qa/purchase-receipt-real-write-e2e` 通过，报告 `status=PASS`、默认服务层 Go 测试通过、PostgreSQL 防呆测试明确 `SKIPPED`，覆盖采购入库草稿、加行、非法明细失败、过账、`get/list` 回显、看板 projection、库存事实、取消冲正、权限拒绝和重复过账 / 取消幂等；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5257 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过；`git diff --check` 通过。
- 下一步：继续按 goal 推进真实试用账号 RBAC、真实岗位任务端 browser smoke 或带 `--with-postgres` 的本地 PostgreSQL 防呆链路；运行 PostgreSQL 防呆前仍需确认本地测试库 / 容器可用，且只允许命中 `purchase_receipt` 或 `test` 命名的本地库。
- 阻塞/风险：本组已实际运行默认采购入库服务层 e2e，但未运行 `--with-postgres`、未连接本地 PostgreSQL、未启动后端服务、未跑浏览器真实写入 smoke、未登录真实账号、未写生产 / 目标环境、未导入真实客户数据，不修改 schema、RBAC、purchase / inventory usecase、Workflow / Fact 规则、客户资料、部署或 release evidence。

## 2026-07-01 Purchase Receipt PostgreSQL 防呆链路
- 完成：修复 `scripts/qa/purchase-receipt-real-write-e2e.mjs --with-postgres --preflight-report` 的误导性 ready 判断，请求 `--with-postgres` 时会把 `PURCHASE_RECEIPT_PG_DB_URL` 或默认本地 DSN 解析成脱敏 host / port / db，校验 host 只能是本机、库名必须包含 `purchase_receipt` 或 `test`，并用 `pg_isready` 做无凭据可达性检查；若 PostgreSQL 不可达、`pg_isready` 缺失、目标不是本机或库名不像测试库，报告会进入 blocker。同步为 e2e 报告 stdout / stderr 增加 PostgreSQL URL、Bearer、access_token 和 Authorization 脱敏。启动本地 `plush-toy-erp-purchase-receipt-postgres` 测试容器，仅绑定 `127.0.0.1:55432`，通过项目 Make target 创建 `plush_erp_purchase_receipt_test` 并应用 migration，再运行完整 `--with-postgres` e2e。
- 验证：`/opt/homebrew/bin/psql --version` 返回 PostgreSQL 14.22，`/usr/local/bin/atlas version` 返回 `v0.38.1-e142280-canary`；初始 `pg_isready -h 127.0.0.1 -p 55432` 为 `no response`，启动本地 Docker 测试容器后变为 `accepting connections`；`PATH=/usr/local/bin:$PATH make purchase_receipt_pg_createdb` 通过；`PATH=/usr/local/bin:$PATH make purchase_receipt_migrate_apply` 通过并应用 37 个 migration 到 `20260630020907`；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --with-postgres --preflight-report output/qa/purchase-receipt-real-write-e2e/preflight-with-postgres.json` 通过，报告 `ready=true`、`postgresReady=true`、`reachable=true`、`safeTarget=127.0.0.1:55432/plush_erp_purchase_receipt_test` 且不保存 DSN；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --with-postgres --out output/qa/purchase-receipt-real-write-e2e` 通过，报告 `status=PASS`、`passed=2`、`failed=0`、服务层 JSON-RPC 测试和 `make purchase_receipt_pg_test` 均通过；敏感扫描未发现 postgres URL、测试密码、Bearer、access_token 或 Authorization；`PATH=/usr/local/bin:$PATH node --check scripts/qa/purchase-receipt-real-write-e2e.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/purchase-receipt-real-write-e2e.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 19 个用例；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop STYLE_L1_PORT=5258 pnpm --dir web style:l1` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过。
- 下一步：继续按 goal 推进真实试用账号 RBAC、真实岗位任务端 browser smoke、客户配置 active revision 读回和采购入库浏览器真实写入；若继续复跑 PostgreSQL 防呆，可复用当前本地测试容器，否则需要重新启动本地测试库。
- 阻塞/风险：本组只写本地 Docker 测试库和 repo 内 `output/qa/purchase-receipt-real-write-e2e` 脱敏报告；未连接 106 / 133 / 生产 / 目标环境，未导入真实客户数据，未启动后端服务，未跑浏览器真实写入 smoke，未改 schema、RBAC、purchase / inventory usecase、Workflow / Fact 规则、客户资料、部署或 release evidence。`plush-toy-erp-purchase-receipt-postgres` 容器仍在本机运行，绑定 `127.0.0.1:55432` 供后续复跑；启动时 Docker 提示镜像为 `linux/amd64`、宿主为 `linux/arm64/v8`，当前测试已通过但后续性能不代表目标环境。

## 2026-07-01 Purchase Receipt Browser E2E 安全目标校验修复
- 完成：修复 `web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs` 的真实写入启动前阻断 bug。脚本此前在创建 `createRealLoginSmokeRuntime` 之前调用安全目标校验，并在 helper 中隐式读取局部 `runtime`；一旦传入 `--accept-persistent-test-data`，会在真正检查后端健康或进入浏览器前因 `runtime is not defined` 崩溃。现在先创建 runtime，再把 `runtime.baseURL` 显式传给本地目标校验、登录页导航和入库页导航 helper，安全边界仍保持：未确认持久测试数据时拒绝，非本地页面目标仍需额外 `--allow-external-base-url`。
- 验证：`PATH=/usr/local/bin:$PATH node --check web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test web/scripts/purchaseReceiptRealWriteBrowserE2E.test.mjs web/scripts/realLoginSmokeShared.test.mjs` 通过，共 8 个用例；新增 CLI 级测试证明本地目标已确认写入后会继续进入后端健康检查失败路径，且 stderr 不再出现 `runtime is not defined`。
- 下一步：继续按 goal 推进真实试用账号 RBAC、真实岗位任务端 browser smoke、客户配置 active revision 读回和采购入库浏览器真实写入；真正运行 `pnpm --dir web smoke:purchase-receipt-real-write` 仍需要本地后端、开发账号密码、可接受保留 `PR-BROWSER-*` 测试事实记录，并显式确认持久测试数据。
- 阻塞/风险：本组只修脚本执行路径和 no-write 单测；未启动后端、未启动 Vite / Playwright、未登录真实账号、未创建 / 过账 / 取消采购入库单、未写数据库、未连接生产 / 目标环境、未导入真实客户数据，不修改 schema、RBAC、purchase / inventory usecase、Workflow / Fact 规则、客户资料、部署或 release evidence。

## 2026-07-01 Purchase Receipt Browser E2E 前置报告
- 完成：为 `web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs` 增加 `--preflight-report <path>` no-write 前置报告。报告只探测后端 health、显式管理员账号密码 env 是否齐全、是否已确认持久测试数据、页面目标是否为本机或已显式允许外部测试目标；不读取本地配置、不读取密码值、不登录、不调用 JSON-RPC、不启动 Vite / Playwright、不创建或过账采购入库单、不写数据库，也不保存 token 或 Authorization header。同步 `web/README.md`、`web/scripts/README.md`、`scripts/README.md`、`docs/product/自动化测试策略.md`、`/__dev/testing` 的 `real-login-smoke-shared` 预设和 `devTesting.test.mjs`。
- 验证：`PATH=/usr/local/bin:$PATH node --check web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test web/scripts/purchaseReceiptRealWriteBrowserE2E.test.mjs web/scripts/realLoginSmokeShared.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 22 个用例；`PATH=/usr/local/bin:$PATH node web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs --preflight-report output/purchase-receipt-real-write-browser-e2e/preflight.json` 通过，报告 `readyForRealSmoke=false`，本机后端 health 为 200，阻塞项为 `missing-persistent-test-data-acceptance` 和 `missing-admin-credential-env`；敏感扫描未发现 Bearer、access_token、Authorization header 或示例密码值。
- 下一步：继续按 goal 推进真实试用账号 RBAC、真实岗位任务端 browser smoke、客户配置 active revision 读回和采购入库浏览器真实写入；真正运行 `pnpm --dir web smoke:purchase-receipt-real-write` 前，应先提供显式管理员账号密码 env、确认可保留 `PR-BROWSER-*` 持久测试事实记录，并复跑 preflight 到 `readyForRealSmoke=true`。
- 阻塞/风险：本组只写 repo 内 `output/purchase-receipt-real-write-browser-e2e/preflight.json` 脱敏报告；未启动 Vite / Playwright、未登录真实账号、未创建 / 过账 / 取消采购入库单、未写数据库、未连接生产 / 目标环境、未导入真实客户数据，不修改 schema、RBAC、purchase / inventory usecase、Workflow / Fact 规则、客户资料、部署或 release evidence。

## 2026-07-01 Trial Account RBAC 前置报告
- 完成：为 `scripts/qa/trial-account-rbac.mjs` 增加 `--preflight-report <path>` no-write 前置报告。报告只探测后端 health、演示账号密码 env 是否存在、预期账号数量和阻塞项；不读取密码值、不登录、不调用 `admin_login / me`、不写数据库，也不保存 access token 或 Authorization header。同步 `scripts/README.md`、`docs/product/自动化测试策略.md`、`docs/customers/yoyoosun/试用环境执行手册.md`、`/__dev/testing` 的 `trial-account-rbac` 预设、`devTesting.test.mjs` 和 `dev-entry-boundary.test.mjs`。
- 验证：`PATH=/usr/local/bin:$PATH node --check scripts/qa/trial-account-rbac.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/trial-account-rbac.test.mjs web/scripts/trialDemoAccountBrowserSmoke.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 22 个用例；`PATH=/usr/local/bin:$PATH node scripts/qa/trial-account-rbac.mjs --preflight-report output/trial-account-rbac/preflight.json` 通过，报告 `readyForRealRBACCheck=false`，本机后端 health 为 200，唯一阻塞项为 `missing-trial-account-password-env`；敏感扫描未发现 Bearer、access_token、Authorization header 或示例密码值，输出目录未遗留 `.tmp` 文件。
- 下一步：继续按 goal 推进真实试用账号 RBAC、真实岗位任务端 browser smoke、客户配置 active revision 读回和采购入库浏览器真实写入；真正运行 `node scripts/qa/trial-account-rbac.mjs --report output/trial-account-rbac/report.json` 前，应先提供演示账号密码 env，并复跑 preflight 到 `readyForRealRBACCheck=true`。
- 阻塞/风险：本组只写 repo 内 `output/trial-account-rbac/preflight.json` 脱敏报告；未读取密码、未登录真实账号、未调用 `/rpc/auth`、未验证真实角色 / mobile 权限、未启动浏览器、未写数据库、未连接生产 / 目标环境、未导入真实客户数据，不修改 schema、RBAC seed、customer_config active revision、Workflow / Fact 规则、部署或 release evidence。

## 2026-07-01 Trial Account RBAC 静态投影前置守卫
- 完成：强化 `scripts/qa/trial-account-rbac.mjs --preflight-report`，在原有后端 health 与演示密码 env 检查之外，新增 `staticProjection` 静态角色投影核对。该核对读取 Go seed、后端 RBAC mobile 权限、前端移动角色入口、浏览器 smoke 账号、`scripts/README.md`、`web/README.md` 和 `server/docs/config.md`，确认 10 个 `demo_*` 账号、9 个岗位移动入口、`demo_admin` 无岗位权限拒绝态和文档入口没有漂移；若静态来源缺失或任一角色投影不一致，preflight 会加入 `static-role-projection-drift` blocker，不允许把真实 RBAC 检查视为 ready。同步 `trial-account-rbac.test.mjs`、`trial-role-entry-docs.test.mjs`、`scripts/README.md`、`docs/product/自动化测试策略.md` 和 `docs/customers/yoyoosun/试用环境执行手册.md`。
- 验证：`PATH=/usr/local/bin:$PATH node --check scripts/qa/trial-account-rbac.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/trial-account-rbac.test.mjs scripts/qa/trial-role-entry-docs.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 22 个用例；`PATH=/usr/local/bin:$PATH node scripts/qa/trial-account-rbac.mjs --preflight-report output/trial-account-rbac/preflight.json` 通过，报告 `staticProjection.ok=true`、`passedRows=10`、`failedRows=0`，当前 `readyForRealRBACCheck=false` 的唯一 blocker 仍是 `missing-trial-account-password-env`；敏感扫描未发现 Bearer、access_token、Authorization、示例密码或测试密码值，输出目录未遗留 `.tmp` 文件；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过。
- 下一步：继续按 goal 推进真实试用账号 RBAC、真实岗位任务端 browser smoke、客户配置 active revision 读回和采购入库浏览器真实写入；真正运行真实 RBAC 前仍需提供演示账号密码 env，并复跑 preflight 到静态投影与运行前置都 ready。
- 阻塞/风险：本组只增加静态投影守卫、no-write preflight 报告字段、测试和文档口径；未读取密码、未登录真实账号、未调用 `admin_login / me`、未启动 Vite / Playwright、未验证真实菜单投影、未写数据库、未连接生产 / 目标环境、未导入真实客户数据，不修改 schema、RBAC seed、customer_config active revision、Workflow / Fact 规则、部署或 release evidence。

## 2026-07-01 Mobile Workflow Browser Smoke 动作计划前置守卫
- 完成：强化 `web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs` 的 `--print-input-template` 与 `--preflight-report`，新增共享 `buildSimulatedTaskPlan` 和 `buildSimulatedTaskPlanCoverage`。输入模板和 preflight 现在共用同一份模拟任务动作计划，明确真实 smoke 预期覆盖 4 个 `simulated_only` workflow 任务：老板阻塞、老板完成、老板退回、仓库任务跨角色催办；coverage 同步检查 reason 必填、完成反馈、已办列表反馈、异常上报、evidence refs 和 `approval_required / shipment_release_pending` 内部提醒线索。若计划缺少任一关键动作或提醒线索，preflight 会加入 `simulated-task-plan-incomplete` blocker。同步 `mobile-workflow-runtime-browser-smoke.test.mjs`、`scripts/README.md`、`web/scripts/README.md`、`docs/product/自动化测试策略.md` 和 `/__dev/testing` 的 `mobile-workflow-smoke` 描述。
- 验证：`PATH=/usr/local/bin:$PATH node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 21 个用例；`PATH=/usr/local/bin:$PATH node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report output/mobile-workflow-runtime-browser-smoke/preflight.json` 通过，报告 `simulatedTaskPlanCoverage.ok=true`、`taskCount=4`、actions 为 `block / complete / reject / urge-only`，当前 `readyForRealSmoke=false` 的唯一 blocker 仍是 `missing-demo-password-env`；敏感扫描未发现 Bearer、access_token、Authorization、示例密码或测试密码值，输出目录未遗留 `.tmp` 文件；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过。
- 下一步：继续按 goal 推进真实试用账号 RBAC、真实岗位任务端 browser smoke、客户配置 active revision 读回和采购入库浏览器真实写入；真正运行 `pnpm --dir web smoke:mobile-workflow-runtime-browser` 前仍需本地后端、前端 runtime、演示账号密码，并复跑 preflight 到动作计划与运行前置都 ready。
- 阻塞/风险：本组只增加 no-write 动作计划 coverage、preflight 报告字段、测试和文档口径；未读取密码、未登录真实账号、未调用 JSON-RPC、未创建 workflow 任务、未启动 Vite / Playwright、未写数据库、未连接生产 / 目标环境、未导入真实客户数据，不修改 schema、RBAC、WorkflowUsecase、Fact usecase、customer_config active revision、部署或 release evidence。

## 2026-07-01 Trial Demo Browser Smoke 菜单投影计划前置守卫
- 完成：强化 `web/scripts/trialDemoAccountBrowserSmoke.mjs` 的 `--print-input-template` 与 `--preflight-report`，新增共享 `menuProjectionPlan` 和 `menuProjectionCoverage`。输入模板和 preflight 现在共用同一份静态菜单投影计划，明确真实 browser smoke 预期覆盖 10 个桌面账号、9 个岗位任务端入口、`demo_admin` 桌面只看权限中心、`demo_admin` 移动端拒绝态、yoyoosun 客户隐藏菜单和旧入口清理；若计划缺口出现，preflight 会加入 `menu-projection-plan-incomplete` blocker。同步 `trialDemoAccountBrowserSmoke.test.mjs`、`scripts/README.md`、`docs/product/自动化测试策略.md` 和 `docs/customers/yoyoosun/试用环境执行手册.md`。
- 验证：`PATH=/usr/local/bin:$PATH node --check web/scripts/trialDemoAccountBrowserSmoke.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/trial-role-entry-docs.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 16 个用例；`PATH=/usr/local/bin:$PATH node web/scripts/trialDemoAccountBrowserSmoke.mjs --preflight-report output/trial-demo-account-browser-smoke/preflight.json` 通过，报告 `menuProjectionCoverage.ok=true`、`desktopAccountCount=10`、`mobileAccountCount=9`、`mobileDeniedAccountCount=1`，当前 `readyForRealSmoke=false` 的唯一 blocker 仍是 `missing-demo-password-env`；敏感扫描未发现 Bearer、access_token、Authorization 或示例密码值，输出目录未遗留 `.tmp` 文件。
- 下一步：继续按 goal 推进真实试用账号 RBAC、真实岗位任务端 browser smoke、customer_config active revision 读回和采购入库浏览器真实写入；真正运行 `pnpm --dir web smoke:trial-demo-browser` 前仍需本地后端、前端 runtime、演示账号密码，并复跑 preflight 到菜单投影计划与运行前置都 ready。
- 阻塞/风险：本组只增加 no-write 菜单投影 coverage、preflight 报告字段、测试和文档口径；未读取密码、未登录真实账号、未调用 JSON-RPC、未启动 Vite / Playwright、未读取 customer-config 脚本内容、未验证真实菜单投影、未写数据库、未连接生产 / 目标环境、未导入真实客户数据，不修改 schema、RBAC、customer_config usecase、WorkflowUsecase、Fact usecase、部署或 release evidence。

## 2026-07-01 Dev Customer Config 错误提示收口
- 完成：收口 `/__dev/customer-config` 客户配置控制台的用户可见错误提示。`DevCustomerConfigPage.jsx` 现在复用共享 `getActionErrorMessage`，未知后端 / transport 异常不再直接把 `error.message` 显示给用户；未登录、无权限和本地后端不可达仍保留场景化中文提示。同步强化 `scripts/qa/frontend-error-message-boundary.test.mjs`，把 `DevCustomerConfigPage.jsx` 作为 dev-only 但用户可见的控制台纳入 raw error message 静态守卫。
- 验证：`PATH=/usr/local/bin:$PATH node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/config/devCustomerConfig.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 19 个用例；`PATH=/usr/local/bin:$PATH pnpm --dir web exec eslint --ext .js --ext .jsx src/erp/pages/DevCustomerConfigPage.jsx` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过。曾尝试 `node --check web/src/erp/pages/DevCustomerConfigPage.jsx`，Node 对 `.jsx` 扩展不适用，改用项目 eslint 和测试入口验证。
- 下一步：继续按 goal 推进客户配置控制台、试用账号真实 RBAC、真实岗位任务端 browser smoke、customer_config active revision 读回和采购入库浏览器真实写入；若后续触达控制台交互/布局，再补 `STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop` 浏览器级回归。
- 阻塞/风险：本组只改 dev-only 控制台错误提示和静态守卫；未调用后端、未发布或激活客户配置、未读取 token、未导入真实客户业务数据、未写数据库，不修改 schema、RBAC、customer_config usecase、Workflow / Fact 规则、部署或 release evidence。页面视觉和真实后端错误链路未做浏览器运行时回归，本轮由 T5 静态/单测/fast 覆盖错误提示边界。

## 2026-07-01 Mobile Auth Route Smoke 前置报告
- 完成：为 `web/scripts/mobileAuthLoginRouteSmoke.mjs` 增加 `--preflight-report <path>` no-write 前置报告。报告复用当前移动端应用登记和 phone / iPad 视口真源，记录脚本存在性、9 个岗位 `/m/<role>/tasks` 路由计划、生产单端口覆盖、未登录 / 旧登录态拦截、密码 / 短信登录回跳、Workflow owner_role 查询、通知 / 预警、退出登录 / 返回栈和横向溢出检查口径；不启动 Vite / Playwright、不调用后端 / JSON-RPC、不读取密码、不登录、不保存 token / Authorization header、不写数据库。同步 `web/scripts/README.md`、`scripts/README.md`、`docs/product/自动化测试策略.md`、`/__dev/testing` 的 `real-login-smoke-shared` 预设、`devTesting.test.mjs` 和 `dev-entry-boundary.test.mjs`。
- 验证：`PATH=/usr/local/bin:$PATH node --check web/scripts/mobileAuthLoginRouteSmoke.mjs && PATH=/usr/local/bin:$PATH node --check web/src/erp/config/devTesting.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test web/scripts/mobileAuthLoginRouteSmoke.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，共 17 个用例；`PATH=/usr/local/bin:$PATH node web/scripts/mobileAuthLoginRouteSmoke.mjs --preflight-report output/mobile-auth-login-route-smoke/preflight.json` 通过，报告 `readyForMockSmoke=true`、`blockers=[]`、`roles=9`、`viewports=phone/ipad`、`startsBrowser=false`、`startsDevServer=false`、`callsJSONRPC=false`、`writesDatabase=false`；敏感扫描未发现 Bearer、access_token、Authorization header、示例密码或 token；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 通过；`PATH=/usr/local/bin:$PATH pnpm --dir web smoke:mobile-auth-login-route` 通过，已验证 9 个岗位任务端角色的 mock 认证回跳、任务池、通知 / 预警、退出和 phone / iPad 布局。
- 下一步：继续按 goal 推进真实试用账号 RBAC、真实岗位任务端 browser smoke、customer_config active revision 读回和采购入库浏览器真实写入；真正运行 `pnpm --dir web smoke:mobile-auth-login-route` 时仍会启动 Vite / Playwright，但使用 mock auth / workflow RPC，不替代后端 RBAC、真实账号、customer config active revision 或 usecase 测试。
- 阻塞/风险：本组只增加 no-write 路由计划 preflight、测试入口和文档口径；未启动真实浏览器、未登录真实账号、未调用后端 / JSON-RPC、未验证真实菜单投影、未写数据库、未连接生产 / 目标环境、未导入真实客户数据，不修改 schema、RBAC、WorkflowUsecase、Fact usecase、customer_config active revision、部署或 release evidence。

## 2026-07-01 Web README Mobile Auth Preflight 口径同步
- 完成：同步 `web/README.md` 的当前回归命令和移动端认证回跳说明，补上 `node scripts/mobileAuthLoginRouteSmoke.mjs --preflight-report output/mobile-auth-login-route-smoke/preflight.json`，并明确该报告只写本地 JSON、记录脚本 / 路由 / phone-iPad 视口 / mock RPC 覆盖口径，不调用后端 / JSON-RPC、不读取密码、不保存 token、不写数据库；真实 `smoke:mobile-auth-login-route` 仍只证明 mock auth / workflow RPC 下的生产单端口岗位路由和登录回跳。
- 验证：`rg -n "mobileAuthLoginRouteSmoke\\.mjs --preflight-report|mobile-auth-login-route-smoke/preflight\\.json|真实后端 RBAC、真实账号" web/README.md docs/product/自动化测试策略.md scripts/README.md web/scripts/README.md web/src/erp/config/devTesting.mjs` 通过，确认 web README、测试策略、脚本 README 和 `__dev/testing` 复制入口口径一致；`git diff --check -- web/README.md` 通过。
- 下一步：继续按 goal 推进真实试用账号 RBAC、真实岗位任务端 browser smoke、customer_config active revision 读回和采购入库浏览器真实写入；若继续触达 README 中其它回归命令，再按影响面补对应测试或引用扫描。
- 阻塞/风险：本组只同步正式 README 文档口径；未改脚本运行逻辑、未启动浏览器、未登录真实账号、未调用后端 / JSON-RPC、未写数据库、未连接生产 / 目标环境、未导入真实客户数据，不修改 schema、RBAC、Workflow / Fact usecase、customer_config active revision、部署或 release evidence。`web/README.md` 中存在本轮前已有的其它未提交改动，本组未回退或重排。

## 2026-07-01 Web README Mobile Workflow Preflight 口径同步
- 完成：同步 `web/README.md` 的移动端 workflow 真实浏览器回归说明，把 `node scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report output/mobile-workflow-runtime-browser-smoke/preflight.json` 写入本地前置路径，并明确该报告只探测 backend health、演示密码 env、Vite 托管需求、试用 customer-config 脚本存在性和模拟任务动作计划 coverage，不读取密码、不调用 JSON-RPC、不启动 Vite / Playwright、不创建任务、不保存 token。
- 验证：`rg -n "mobileWorkflowRuntimeBrowserSmoke\\.mjs --preflight-report|mobile-workflow-runtime-browser-smoke/preflight\\.json|不读取密码值、不调用 JSON-RPC" web/README.md docs/product/自动化测试策略.md scripts/README.md web/scripts/README.md web/src/erp/config/devTesting.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过，确认 web README、测试策略、脚本 README 和 `__dev/testing` 复制入口口径一致；`git diff --check -- web/README.md` 通过。
- 下一步：继续按 goal 推进真实试用账号 RBAC、真实岗位任务端 browser smoke、customer_config active revision 读回和采购入库浏览器真实写入；真正运行 `pnpm --dir web smoke:mobile-workflow-runtime-browser` 前仍需要本地后端、前端 runtime 和演示账号密码，并复跑 preflight。
- 阻塞/风险：本组只同步正式 README 文档口径；未运行真实 mobile workflow browser smoke、未读取密码、未登录真实账号、未调用 JSON-RPC、未创建 workflow 任务、未写数据库、未连接生产 / 目标环境、未导入真实客户数据，不修改 schema、RBAC、WorkflowUsecase、Fact usecase、customer_config active revision、部署或 release evidence。

# plush-toy-erp progress

本文件只保留当前活跃事项、最近完成记录和归档索引；历史流水已归档到 `docs/archive/`。`progress.md` 是过程交接线索，不是正式需求、数据模型或部署真源。

## 归档索引

- `docs/archive/progress-2026-06-28-before-runtime-manifest.md`：归档 2026-06-28 之前至客户配置版本运行时最小闭环、`/__dev` 页面治理等过程记录。
- `docs/archive/progress-2026-06-29-before-release-evidence-hardening.md`：归档 2026-06-28 客户配置 runtime manifest、发布执行器、dev-only 页面治理、Workflow action 合同、导入与发布证据前置门禁等过程记录。
- `docs/archive/progress-2026-06-29-before-target-evidence-binding.md`：归档 2026-06-29 release evidence、真实导入 recovery plan、文档清单、备份恢复和回滚演练门禁早期硬化过程记录。
- `docs/archive/progress-2026-06-29-before-priority-audit-closeout.md`：归档 2026-06-29 target evidence binding 之后到 release evidence runner 脱敏报告与 URL 前置拦截的过程记录。
- `docs/archive/progress-2026-06-29-before-process-runtime-minimum.md`：归档 2026-06-29 adminProfileSync 菜单投影文档纠偏、P2 explain / entitlement / break-glass 中段过程记录。
- `docs/archive/progress-2026-06-29-before-linked-task-idempotency.md`：归档 2026-06-29 P3 ProcessRuntime expected_version 守卫之前至 linked task 幂等闭环前的过程记录。
- `docs/archive/progress-2026-06-30-before-inventory-post-inbound.md`：归档 2026-06-30 P3 / P4 前段、adminProfileSync 菜单投影文档多轮纠偏、sales_order_acceptance、material_supply definition evidence、`purchase_receipt.create` 和 `quality_inspection.decide` 领域命令 handler 过程记录。
- `docs/archive/progress-2026-06-30-before-p5-release-input-checklist.md`：归档 2026-06-30 P4-2 / P4-3、adminProfileSync 文档纠偏和进入 P5 release closeout 输入清单前的过程记录。
- `docs/archive/progress-2026-06-30-before-p5-input-checklist-followup.md`：归档 2026-06-30 P5 release closeout report-only、release evidence hardening、菜单投影纠偏和 input checklist JSON 自定义路径只读合同之前的过程记录。
- `docs/archive/progress-2026-06-30-before-outsourcing-order-api-gate.md`：归档 2026-06-30 P5 input checklist、adminProfileSync 文档纠偏、purchase / quality / shipment / stock reservation / production / outsourcing fact / sales / purchase order moduleStates 门禁等过程记录。
- `docs/archive/progress-2026-07-01-before-action-projection-l1.md`：归档 2026-06-30 outsourcing order API 门禁之后至 2026-07-01 动作投影浏览器回归扩展的过程记录。
- `docs/archive/progress-2026-07-01-before-progress-archive-and-next-no-write.md`：归档 2026-07-01 动作投影浏览器回归扩展之后至 Purchase Receipt Browser E2E 输入模板的过程记录。
- `docs/archive/progress-2026-07-01-before-readme-preflight-sync-closeout.md`：归档 2026-07-01 Purchase Receipt Browser E2E 输入模板之后至 Web README mobile preflight 口径同步的过程记录。
- `docs/archive/progress-2026-07-01-before-bom-inline-editor.md`：归档 2026-07-01 Web README preflight 口径同步之后至 BOM 明细弹窗内联编辑前的过程记录。
- `docs/archive/progress-2026-07-02-before-trial-real-smoke.md`：归档 2026-07-01 BOM 明细内联编辑前至 2026-07-01 qa:full 生命周期收口的过程记录。
- `docs/archive/progress-2026-07-02-before-effective-session-diagnostic-smoke.md`：归档 2026-07-02 试用真实 smoke 前归档说明至旧兼容接口 / 旧 mobile 运行时清理的过程记录。
- `docs/archive/progress-2026-07-02-before-next-no-write-closure.md`：归档 2026-07-02 effective session 诊断 smoke 前归档说明至移动端 workflow smoke 文档边界守卫的过程记录。
- `docs/archive/progress-2026-07-02-before-dashboard-source-label.md`：归档 2026-07-02 __dev/testing 错误提示与技术字段预设补齐至 mobile-workflow-smoke 测试入口补齐的过程记录。
- `docs/archive/progress-2026-07-02-before-sku-source-field-linkage.md`：归档 2026-07-02 Dashboard 来源标签收口之后至 yoyoosun asset 验证命令防误判收口的过程记录。
- `docs/archive/progress-2026-07-02-before-next-local-closure.md`：归档 2026-07-02 SKU 来源字段链路收口之后至移动端任务端 no-write preflight 复核的过程记录。
- `docs/archive/progress-2026-07-02-before-post-fast-local-closure.md`：归档 2026-07-02 客户配置控制台 no-write 读回预检之后至 L1 与移动端登录回跳本地回归复核的过程记录。
- `docs/archive/progress-2026-07-02-before-simulated-report-only-closure.md`：归档 2026-07-02 下一本地闭环前归档说明至 active module state / Workflow 写门禁复核的过程记录。
- `docs/archive/progress-2026-07-02-before-review-driven-config-contract.md`：归档 2026-07-02 模拟 report-only 后至测试业务数据隔离守卫收口的过程记录。
- `docs/archive/progress-2026-07-02-before-p5-release-candidate.md`：归档 2026-07-02 P5 本地前置验证、server 镜像构建守卫、133 baseline 与 fast 门禁复核后的过程记录。
- `docs/archive/progress-2026-07-03-before-dev-customer-visible-labels.md`：归档 2026-07-02 P5 release candidate 前归档说明至 2026-07-03 客户配置 compiled catalog 可见标签收口的过程记录。
- `docs/archive/progress-2026-07-03-before-collaboration-submit-guard.md`：归档 2026-07-03 客户配置可见标签前归档说明至移动任务端岗位标签投影收口的过程记录。
- `docs/archive/progress-2026-07-03-before-order-selection-status-fallback.md`：归档 2026-07-03 本页协同提交 guard 前归档说明至客户配置控制台打印字段要求预检收口的过程记录。

## 当前活跃事项

- 多甲方角色能力流程编排以 `docs/product/多甲方角色能力流程编排优先级.md` 和 `node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 的 `implementationOrder` 为本地优先级入口；GPT/reference 资料只作输入，当前真源仍回到代码、migration、测试和正式文档。
- 当前审计显示 P0-P4 本地证据为 ready；P5 测试部署 / 导入 / 第二客户验证仍为 `target-evidence-required`，第一条 blocked release action 是 `immutable-version`。
- P5 当前只允许 report-only、input template、preflight 和 checklist 准备；没有真实目标环境、镜像 digest、migration 前后版本、backup id、目标管理员凭据和用户确认时，不写 `deployments/**/evidence/**`，不执行 `--execute`，不把本地 ready 写成目标 release evidence。
- 真实客户数据导入、正式生产发布、目标环境 smoke、目标 migration、备份恢复、回滚 / 前向修复演练、客户配置激活和签收仍未执行，不能被本地 dry-run、manifest 编译、status、gate、audit、runner report、input template 或 preflight 替代。
- 当前可继续推进的本地闭环优先落在：试用账号 / 菜单 / 岗位任务端前置检查、客户配置控制台 no-write 诊断、字段链路守卫、错误提示和 README / `__dev/testing` 入口口径一致性。

## 2026-07-03 订单选中摘要状态 fallback 收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理采购订单和委外订单选中摘要的用户可见 raw key 缺口。采购订单批量选中标题和委外订单选中条标题现在统一通过 `statusText` 投影未知 `lifecycle_status`，未知非空状态只显示“采购订单状态 / 委外订单状态”，不再把底层状态 key 直接展示给业务用户。未改后端状态机、JSON-RPC、RBAC、订单保存、WorkflowUsecase、打印模板或真实数据。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/masterDataOrderView.test.mjs scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/data/processingContractTemplate.test.mjs` 通过 124 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/components/purchase-orders/purchaseOrderPageConfig.mjs src/erp/pages/V1OutsourcingOrdersPage.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；目标 `rg` 扫描旧 raw fallback 无命中；首次 L1 使用已失效 `business-v1-outsourcing-mobile` 场景被 runner 拒绝，已改用有效场景重跑，`STYLE_L1_SCENARIOS=purchase-order-date-filter-desktop,business-collaboration-purchase-selected-desktop,business-collaboration-mobile /usr/local/bin/pnpm --dir web style:l1` 通过 3 个场景。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、effective session 与菜单实际入口一致性、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明前端采购 / 委外订单选中摘要状态 fallback、用户可见技术字段守卫、Workflow action 边界和本地 L1 场景；不证明真实账号登录、真实浏览器点击写动作、后端 RBAC 拒绝路径、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Workflow 来源号共享投影收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 Workflow 任务来源投影分叉和内部来源号泄露风险。`dashboardTaskDisplay.formatWorkflowTaskSource` 新增内部来源号识别：`TASK-*`、纯 `source_id` 或 `ID-source_id` 这类 fallback 不再作为业务来源号展示，而是回到“来源模块 / 已关联业务来源”；`WorkflowBusinessModulePage` 删除页面私有 `formatTaskSource`，来源列展示和导出统一复用共享 formatter。未改后端 WorkflowUsecase、JSON-RPC、RBAC、任务写入、source_no 存储或真实业务数据。
- 验证：`node --test web/src/erp/utils/dashboardTaskDisplay.test.mjs web/src/erp/utils/workflowDashboardStats.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/qa/workflow-ui-action-boundary.test.mjs` 通过 75 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/utils/dashboardTaskDisplay.mjs src/erp/utils/dashboardTaskDisplay.test.mjs src/erp/pages/WorkflowBusinessModulePage.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；目标 `rg` 扫描 `formatTaskSource`、`task.source_no` 直返和 `TASK-${...id}` 旧写法无命中；`STYLE_L1_SCENARIOS=erp-task-board-desktop,business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 2 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、effective session 与菜单实际入口一致性、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明前端 Workflow / Dashboard 来源展示共享 formatter、静态守卫和本地 L1 场景；不证明历史数据库里的全部内部来源号已清洗、不证明真实账号登录、真实浏览器点击写动作、后端 RBAC 拒绝路径、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 来料质检原批次状态可见文案收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理来料质检列和导出把 `original_lot_status` 批次状态 key 直接展示给业务用户的问题。`qualityInspectionColumns` 新增 `lotStatusText`，原批次状态列、物料批次摘要和导出值统一显示“可用 / 冻结 / 不合格 / 停用”，未知非空值只显示“批次状态”，不再露 `ACTIVE / HOLD / REJECTED / DISABLED` 这类底层 key。未改 QualityUsecase、库存批次状态机、JSON-RPC、RBAC、迁移、质检判定写入或真实库存事实。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 44 个测试；`node --test web/src/erp/config/printTemplates.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs web/src/common/utils/errorMessage.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/dashboardTaskDisplay.test.mjs` 通过 70 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/components/quality-inspections/qualityInspectionColumns.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、effective session 与菜单实际入口一致性、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明来料质检前端表格 / 摘要 / 导出不再透出批次状态 raw key、用户可见技术字段守卫和本地 L1 场景；不证明后端质检状态机新增能力、不证明真实账号登录、真实浏览器写动作、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 加工合同金额合并与合计可见口径收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理加工合同打印工作台合并金额列后的隐藏金额残值问题。`processingContractEditor` 现在把金额列也纳入合并覆盖清理，避免被覆盖行继续保留旧手工金额；`calculateProcessingContractTotals` 支持按当前 `merges` 跳过纸面被合并覆盖的数量 / 金额单元格，`ProcessingContractPaper` 的合计改为按纸面可见单元格计算。未新增销售订单打印模板，未发布 `sales_order_items.default`，未改后端 PDF、WorkflowUsecase、RBAC、JSON-RPC、真实业务事实或客户配置发布门禁。
- 验证：`node --test web/src/erp/data/processingContractTemplate.test.mjs web/src/erp/utils/processingContractEditor.test.mjs` 通过 24 个测试；`node --test web/src/erp/config/printTemplates.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/processingContractEditor.test.mjs web/src/erp/data/processingContractTemplate.test.mjs` 通过 72 个测试；`node --test scripts/qa/sales-order-field-chain-boundary.test.mjs` 通过 6 个测试，继续证明销售订单打印边界未升级；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/data/processingContractTemplate.mjs src/erp/components/print/ProcessingContractPaper.jsx src/erp/utils/processingContractEditor.mjs src/erp/data/processingContractTemplate.test.mjs src/erp/utils/processingContractEditor.test.mjs` 通过；`STYLE_L1_SCENARIOS=print-workspace-processing /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进打印静态预览与工作台纸面一致性、采购合同金额合并同类风险、真实造数任务端动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、effective session 与菜单实际入口一致性。
- 阻塞/风险：本轮只证明加工合同工作台金额列合并、纸面合计、相关静态守卫和目标 L1 场景；不证明采购合同同类合并金额风险已收口、不证明销售订单打印链路已完成、不证明真实账号登录、真实浏览器写动作、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 采购合同金额合并与合计可见口径收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理采购合同打印工作台合并金额列后的隐藏金额仍参与合计问题。`computeMaterialPurchaseTotals` 现在支持按当前 `merges` 跳过纸面被合并覆盖的采购数量 / 采购金额单元格，`MaterialPurchaseContractWorkbench` 的合计改为传入 `draft.merges`，与加工合同合并 / 合计口径一致。未新增销售订单打印模板，未发布 `sales_order_items.default`，未改后端 PDF、WorkflowUsecase、RBAC、JSON-RPC、采购事实、真实业务数据或客户配置发布门禁。
- 验证：`node --test web/src/erp/utils/materialPurchaseContractEditor.test.mjs web/src/erp/data/processingContractTemplate.test.mjs web/src/erp/utils/processingContractEditor.test.mjs` 通过 37 个测试；`node --test web/src/erp/config/printTemplates.test.mjs scripts/qa/sales-order-field-chain-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/materialPurchaseContractEditor.test.mjs web/src/erp/data/processingContractTemplate.test.mjs web/src/erp/utils/processingContractEditor.test.mjs` 通过 91 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/utils/materialPurchaseContractEditor.mjs src/erp/utils/materialPurchaseContractEditor.test.mjs src/erp/components/print/MaterialPurchaseContractWorkbench.jsx` 通过；`STYLE_L1_SCENARIOS=print-workspace-material /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进打印静态预览与工作台纸面一致性、真实造数任务端动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、effective session 与菜单实际入口一致性。
- 阻塞/风险：本轮只证明采购合同工作台金额列合并、纸面合计、相关静态守卫和目标 L1 场景；不证明销售订单打印链路已完成、不证明真实账号登录、真实浏览器写动作、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Workflow 催办 payload source 真源收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理移动任务端、桌面任务看板、正式业务页、采购订单和委外订单催办动作把前端任务对象里的 `source_type/source_id/source_no` 回传到 `urgeWorkflowTask` payload 的问题。催办提交现在只传 `task_id/action/reason` 和入口 / 移动证据等动作上下文，source 真源统一由后端按 `task_id` 读取当前 WorkflowTask；静态守卫新增对所有正式 urge 入口的 `task.source_*` / `selectedTask.source_*` 回传拦截。未改 WorkflowUsecase 写事实边界、任务完成 / 阻塞 / 退回语义、RBAC 权限码、JSON-RPC 方法名、真实业务事实、菜单投影或客户配置发布门禁。
- 验证：`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 85 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .js --ext .mjs src/erp/mobile/hooks/useMobileRoleTaskActions.js src/erp/pages/DashboardPage.jsx src/erp/pages/FormalBusinessModulePage.jsx src/erp/pages/WorkflowBusinessModulePage.jsx src/erp/components/purchase-orders/usePurchaseOrderWorkflowActions.mjs src/erp/components/outsourcing-orders/useOutsourcingOrderWorkflowActions.mjs` 通过；`go test ./internal/biz ./internal/service` 在 `server/` 下通过；`STYLE_L1_SCENARIOS=erp-task-board-mobile,business-collaboration-mobile /usr/local/bin/pnpm --dir web style:l1` 通过 2 个场景；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok: true`、`releaseReady: false`，仍把目标环境 release evidence 保持为缺口。测试中仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke、客户配置规则 / 策略 / 扩展点运行时边界、effective session 与菜单实际入口一致性、打印静态预览与工作台纸面一致性。
- 阻塞/风险：本轮只证明正式前端催办入口不再回传 source 技术字段、Workflow action 静态边界、后端 Workflow 定向测试和目标 L1 场景；不证明真实账号登录、真实浏览器点击写动作、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 移动端 Workflow 真实 smoke 报告脱敏口径收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 `mobileWorkflowRuntimeBrowserSmoke` 真实浏览器 smoke 报告字段口径不严谨的问题。原报告一边声明 `storesActionList: false`，一边在 `tasks[].action` 中保存脱敏动作摘要；现在改为明确声明 `storesRawActionRequest: false`、`storesRawWorkflowPayload: false`、`storesRedactedActionSummary: true`，保留对验收有用的动作摘要，同时锁住不保存 raw action request、raw workflow payload、密码、token、Authorization header、raw customer package。未改真实 smoke 登录流程、任务创建逻辑、WorkflowUsecase、JSON-RPC、RBAC、移动端页面动作、真实业务事实或 release evidence。
- 验证：`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs` 通过 41 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`node --check scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs && node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs` 通过；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs scripts/mobileWorkflowRuntimeBrowserSmoke.mjs src/erp/mobile/utils/mobileRoleTaskModel.test.mjs src/erp/utils/workflowTaskBoard.test.mjs` 通过；首次把仓库根 `scripts/qa/...` 交给 web eslint 时收到 outside base path warning，已改用 `node --check` 覆盖该文件语法；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要返回 `ok: true`、`releaseReady: false`、`readOnly: true`。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke 的运行前置、客户配置规则 / 策略 / 扩展点运行时边界、effective session 与菜单实际入口一致性、打印静态预览与工作台纸面一致性。
- 阻塞/风险：本轮只证明移动端 Workflow 真实 smoke 的本地报告脱敏字段和测试口径更准确；没有真实密码，未运行真实浏览器 smoke，未登录、未创建模拟 workflow task、未提交 done / blocked / rejected / urge 动作、未写数据库、未证明后端 RBAC / effective session、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 业务事实对象类型可见文案收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 Operational Fact 业务事实对象列仍可能把 `subject_type` 枚举 key 当用户可见标签的问题。`operationalFactPageConfig` 新增 `SUBJECT_TYPE_LABELS`，对象列现在把 `MATERIAL / PRODUCT / PRODUCT_SKU / PROCESS / OTHER` 投影为“物料 / 产品 / 产品规格 / 工序 / 业务对象”，未知非空对象类型只显示“业务对象已关联”，不再出现 `MATERIAL已关联` 这类 raw key fallback；`userVisibleTechnicalFields` 同步加守卫，禁止回退到 `record.subject_type`。未改后端事实 usecase、JSON-RPC、RBAC、schema、真实业务数据、库存 / 出货 / 财务事实或客户配置发布门禁。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/components/operational-facts/OperationalFactForms.test.mjs web/src/erp/components/operational-facts/OperationalFactForms.test.mjs` 通过 45 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/components/operational-facts/operationalFactPageConfig.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs src/erp/components/operational-facts/OperationalFactForms.test.mjs` 通过；`STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要返回 `ok: true`、`releaseReady: false`、`readOnly: true`。
- 下一步：继续按 review findings 推进其他业务事实 / Workflow / 客户配置可见层 raw key、真实造数任务端动作 smoke 的运行前置、effective session 与菜单实际入口一致性、打印静态预览与工作台纸面一致性。
- 阻塞/风险：本轮只证明业务事实前端对象列和导出 / 排序 helper 不再透出对象类型 raw key、用户可见技术字段守卫和目标 L1 场景；不证明真实账号登录、真实浏览器写动作、后端事实状态机新增能力、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Workflow 相关单据可见编号防回归守卫

- 完成：继续当前 active goal 的 review findings 驱动口径，确认 `finishedGoodsFlow`、`shipmentFinanceFlow`、`payableReconciliationFlow`、`purchaseInboundFlow`、`outsourceReturnFlow` 的相关单据可见编号已经不再用内部 `record.id` fallback，并在 `userVisibleTechnicalFields` 新增跨 helper 静态守卫，锁住 `resolve*SourceNo` 只能取 `document_no / source_no / title` 等业务可读字段，不允许回退到内部 ID。未改 WorkflowUsecase、JSON-RPC、RBAC、任务写入、source_id 真实关联、业务事实或客户配置发布门禁。
- 验证：`node --test web/src/erp/utils/finishedGoodsFlow.test.mjs web/src/erp/utils/shipmentFinanceFlow.test.mjs web/src/erp/utils/payableReconciliationFlow.test.mjs web/src/erp/utils/purchaseInboundFlow.test.mjs web/src/erp/utils/outsourceReturnFlow.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 98 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/utils/userVisibleTechnicalFields.test.mjs src/erp/utils/finishedGoodsFlow.mjs src/erp/utils/shipmentFinanceFlow.mjs src/erp/utils/payableReconciliationFlow.mjs src/erp/utils/purchaseInboundFlow.mjs src/erp/utils/outsourceReturnFlow.mjs` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok: true`、`releaseReady: false`、`readOnly: true`，目标环境 release evidence 仍保持缺口。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke 的运行前置、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印静态预览与工作台纸面一致性。
- 阻塞/风险：本轮只证明 Workflow 相关单据可见编号的前端 helper 防回归，不证明历史数据库可见来源号已清洗、不证明真实账号登录、真实浏览器写动作、后端 RBAC 拒绝路径、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 移动端任务动作 payload 收窄

- 完成：继续当前 active goal 的 review findings 驱动口径，处理移动端 done / blocked / rejected 动作把前端 `task.payload` 整包回传给 Workflow action API 的问题。`useMobileRoleTaskActions` 现在只提交 `task_id`、受控 `business_status_key`、reason、移动端动作证据和明确结果字段，不再 echo raw workflow payload 或 `source_type/source_id/source_no`；后端仍按 `task_id` 读取当前 WorkflowTask 并由 WorkflowUsecase merge 当前 payload。`workflow-ui-action-boundary` 同步新增守卫，禁止移动端动作 hook 回退到 `...(task.payload || {})`、`...task.payload` 或提交 source 字段。未改后端 action 方法、RBAC 权限码、WorkflowUsecase 任务派生规则、真实 Fact 写入、桌面采购 / 委外 action payload 或客户配置发布门禁。
- 验证：`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs` 通过 65 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .js --ext .mjs src/erp/mobile/hooks/useMobileRoleTaskActions.js src/erp/mobile/utils/mobileRoleTaskModel.test.mjs src/erp/utils/mobileTaskView.test.mjs src/erp/utils/workflowTaskActionSubmitGuard.test.mjs` 通过；`go test ./internal/biz ./internal/service` 在 `server/` 下通过；`STYLE_L1_SCENARIOS=erp-task-board-mobile,business-collaboration-mobile /usr/local/bin/pnpm --dir web style:l1` 通过 2 个场景；`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs scripts/qa/mobile-workflow-simulated-closure.test.mjs` 通过 21 个测试；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要返回 `ok: true`、`readOnly: true`、`releaseReady: false`，release evidence 仍保持目标环境缺口。测试中仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进桌面采购 / 委外 Workflow action payload 收窄、真实造数任务端动作 smoke 的运行前置、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印静态预览与工作台纸面一致性。
- 阻塞/风险：本轮只证明移动端任务动作提交不再回传 raw workflow payload / source 字段、共享 action explain 守卫、后端 Workflow 定向合同和移动端 L1 场景；没有真实密码，未运行真实登录 / 浏览器写动作，未证明真实账号 RBAC、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 桌面采购委外任务动作 payload 收窄

- 完成：继续当前 active goal 的 review findings 驱动口径，处理采购订单和委外订单桌面协同动作 complete / block / reject 仍通过 `workflowPayloadOf(task)` 回传页面解析出的 Workflow payload 快照的问题。`usePurchaseOrderWorkflowActions` 和 `useOutsourcingOrderWorkflowActions` 现在只提交 `task_id`、action、reason、受控 `business_status_key` 和页面动作标记 / 原因字段，不再 echo 旧 payload 快照；后端仍按 `task_id` 读取当前 WorkflowTask 并由 WorkflowUsecase merge 当前 payload。`workflow-ui-action-boundary` 同步新增守卫，禁止采购 / 委外 action hook 回退到 `workflowPayloadOf(task)` 或 `...workflowPayloadOf`。未改后端 action 方法、RBAC 权限码、WorkflowUsecase 任务派生规则、真实 Fact 写入、移动端 action payload、催办 payload 或客户配置发布门禁。
- 验证：`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs` 通过 26 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/components/purchase-orders/usePurchaseOrderWorkflowActions.mjs src/erp/components/outsourcing-orders/useOutsourcingOrderWorkflowActions.mjs src/erp/utils/workflowTaskActionSubmitGuard.test.mjs` 通过；`go test ./internal/biz ./internal/service` 在 `server/` 下通过；`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/masterDataOrderView.test.mjs web/src/erp/utils/businessCollaborationTasks.test.mjs` 通过 102 个测试；`STYLE_L1_SCENARIOS=business-collaboration-purchase-selected-desktop,business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 2 个场景；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要返回 `ok: true`、`readOnly: true`、`releaseReady: false`，release evidence 仍保持目标环境缺口。测试中仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进 Dashboard / WorkflowBusiness / FormalBusiness 其他桌面 action payload 是否还存在快照回传、真实造数任务端动作 smoke 的运行前置、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印静态预览与工作台纸面一致性。
- 阻塞/风险：本轮只证明采购 / 委外桌面协同动作不再回传页面解析出的 workflow payload 快照、共享 action explain 守卫、后端 Workflow 定向合同和目标 L1 场景；未运行真实登录 / 浏览器写动作，未证明真实账号 RBAC、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 通用桌面任务动作 payload 收窄

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 Dashboard 任务看板、WorkflowBusiness 通用协同页和 FormalBusiness 出货放行壳的 complete / block / reject 仍回传页面 payload 快照的问题。`DashboardPage` 不再把 `payloadOf(selectedTask)` 写入 action payload；`WorkflowBusinessModulePage` 和 `FormalBusinessModulePage` 不再把 `workflowPayloadOf(task)` 写入 action payload；三处只提交 `task_id`、action、reason、受控业务状态、页面动作标记和原因字段，后端继续按 `task_id` 读取当前 WorkflowTask 并由 WorkflowUsecase merge 当前 payload。`workflow-ui-action-boundary` 同步把 Dashboard / WorkflowBusiness / FormalBusiness 纳入“不得 echo page-derived workflow payload snapshot”守卫。未改后端 action 方法、RBAC 权限码、WorkflowUsecase 任务派生规则、真实 Fact 写入、移动端 action payload、采购 / 委外 hook、催办 payload 或客户配置发布门禁。
- 验证：目标 `rg` 扫描 `DashboardPage.jsx`、`WorkflowBusinessModulePage.jsx`、`FormalBusinessModulePage.jsx` 中 `workflowPayloadOf(task)`、`payloadOf(selectedTask)`、`...workflowPayloadOf`、`...payloadOf` 无命中；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs` 通过 26 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/DashboardPage.jsx src/erp/pages/WorkflowBusinessModulePage.jsx src/erp/pages/FormalBusinessModulePage.jsx src/erp/utils/workflowTaskActionSubmitGuard.test.mjs` 通过；`go test ./internal/biz ./internal/service` 在 `server/` 下通过；`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs web/src/erp/utils/businessCollaborationTasks.test.mjs` 通过 65 个测试；`STYLE_L1_SCENARIOS=erp-task-board-desktop,business-formal-module-shells-desktop,business-collaboration-purchase-selected-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 3 个场景；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要返回 `ok: true`、`readOnly: true`、`releaseReady: false`，release evidence 仍保持目标环境缺口。测试中仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke 的运行前置、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印静态预览与工作台纸面一致性，以及检查其他非 action 展示是否仍把 workflow payload 当业务事实真源。
- 阻塞/风险：本轮只证明 Dashboard / WorkflowBusiness / FormalBusiness 桌面协同动作不再回传页面 payload 快照、共享 action explain 守卫、后端 Workflow 定向合同和目标 L1 场景；未运行真实登录 / 浏览器写动作，未证明真实账号 RBAC、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Workflow action payload 后端系统字段门禁

- 完成：继续当前 active goal 的 review findings 驱动口径，处理前端 action payload 已收窄但 JSON-RPC 后端仍可能接受系统字段的问题。`complete_task_action`、`block_task_action`、`reject_task_action` 和 `urge_task` 现在都会拒绝 action payload 里的 `source_*`、`owner_role_key`、`task_status_key`、`business_status_key`、`command_key / domain_command_key / domain_command` 等系统字段；`urge_task` 也补齐顶层 `task_status_key` 和 `actor_role_key` 拒绝，保持处理角色由服务端根据当前任务、RBAC 和 active scope 推导。保留受控 `outcome / decision / transition_status` 用于 ProcessRuntime linked human task 的流程分支语义；未让 Workflow task done 自动执行 domain command、未写库存 / 出货 / 财务 Fact、未改 WorkflowUsecase 状态派生、未改客户配置发布门禁。
- 验证：`cd server && go test ./internal/service -run 'TestJsonrpcDispatcher_Workflow(UrgeTaskRecordsEventIntent|UrgeTaskRejectsClientControlledSystemFields|ControlledTaskActionsRejectRawStatusActorRoleAndInvalidAction|CompleteTaskActionCompletesLinkedProcessNode)$'` 通过；`cd server && go test ./internal/biz -run 'TestProcessRuntimeUsecaseCompleteLinkedWorkflowTask'` 通过；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs` 通过 19 个测试；`cd server && go test ./internal/biz ./internal/service` 通过；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs web/src/erp/config/menuPermissions.test.mjs web/src/erp/utils/adminProfileSync.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs` 通过 98 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok: true`、`readOnly: true`、`releaseReady: false`，target release evidence 仍保持缺口。
- 下一步：继续按 review findings 推进后端 Workflow action / ProcessRuntime 其余边界回归、真实造数任务端动作 smoke 的运行前置、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印静态预览与工作台纸面一致性。
- 阻塞/风险：本轮只证明本地 JSON-RPC action payload 系统字段门禁、催办顶层 actor/status 门禁、ProcessRuntime linked task outcome 既有语义和前端静态 action 边界；未运行真实登录 / 浏览器写动作，未证明真实账号 RBAC、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Workflow action 顶层业务状态后端推导收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理正式任务动作顶层参数仍允许 / 提交 `business_status_key` 的残留问题。`complete_task_action`、`block_task_action`、`reject_task_action` 和 `urge_task` 现在都会拒绝顶层 `business_status_key`，前端移动任务端、Dashboard、WorkflowBusiness、FormalBusiness、采购订单和委外订单 action 入口不再提交该字段；业务状态只由后端 action contract / WorkflowUsecase 根据动作语义推导。静态守卫同步锁住六个正式 action 文件不得回退到 `business_status_key:` 或旧 `completeBusinessStatusKey`。未让 Workflow task done 自动写 Fact，未改库存 / 出货 / 财务 / 开票事实，未改真实客户配置发布门禁。
- 验证：`cd server && go test ./internal/service -run 'TestJsonrpcDispatcher_Workflow(UrgeTaskRecordsEventIntent|UrgeTaskRejectsClientControlledSystemFields|ControlledTaskActionsRejectRawStatusActorRoleAndInvalidAction|CompleteTaskActionCompletesLinkedProcessNode)$'` 通过；`cd server && go test ./internal/biz ./internal/service` 通过；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs` 通过 26 个测试；`node --test web/src/erp/config/menuPermissions.test.mjs web/src/erp/utils/adminProfileSync.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs` 通过 72 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .js --ext .mjs src/erp/mobile/hooks/useMobileRoleTaskActions.js src/erp/pages/DashboardPage.jsx src/erp/pages/WorkflowBusinessModulePage.jsx src/erp/pages/FormalBusinessModulePage.jsx src/erp/components/purchase-orders/usePurchaseOrderWorkflowActions.mjs src/erp/components/outsourcing-orders/useOutsourcingOrderWorkflowActions.mjs` 通过；`STYLE_L1_SCENARIOS=erp-task-board-desktop,erp-task-board-mobile,business-formal-module-shells-desktop,business-collaboration-purchase-selected-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 4 个场景；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok: true`、`readOnly: true`、`releaseReady: false`。Node 测试仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke 的运行前置、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印静态预览与工作台纸面一致性，以及 Workflow / Fact 边界里其他“按钮参数驱动业务状态”的残留排查。
- 阻塞/风险：本轮只证明本地 JSON-RPC action 顶层业务状态门禁、六个正式前端 action 文件不再提交业务状态、前端 action/menu/adminProfile/mobile 任务测试和目标 L1 场景；未运行真实登录 / 浏览器写动作，未证明真实账号 RBAC、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 模拟任务端 apply action payload 收窄

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 `mobile-workflow-simulated-closure` apply 路径仍把创建后的 WorkflowTask payload 整包回传给 complete / block / reject / urge action 的问题。脚本新增 `buildTaskStatusActionParams` 和 `buildUrgeTaskParams`，apply 写动作现在只提交 `task_id`、action、reason、模拟移动端证据和 `mobile_role_key`，不再回传 `task.payload` 快照里的 `source_*`、`business_status_key`、`task_status_key`、`owner_role_key`、`domain_command_key` 或客户字段。保留 `create_task` 造本地模拟任务所需的 source / business status 字段；该字段只用于创建模拟任务，不作为后续 action 更新来源。未运行真实登录 / 写库 apply，未改正式前端页面、后端 WorkflowUsecase、RBAC、Fact 写入、真实客户数据或 release evidence。
- 验证：`node --test scripts/qa/mobile-workflow-simulated-closure.test.mjs` 通过 9 个测试；`node --check scripts/qa/mobile-workflow-simulated-closure.mjs && node --check scripts/qa/mobile-workflow-simulated-closure.test.mjs` 通过；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs scripts/qa/mobile-workflow-simulated-closure.test.mjs` 通过 48 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`cd server && go test ./internal/service -run 'TestJsonrpcDispatcher_Workflow(UrgeTaskRecordsEventIntent|UrgeTaskRejectsClientControlledSystemFields|ControlledTaskActionsRejectRawStatusActorRoleAndInvalidAction|CompleteTaskActionCompletesLinkedProcessNode)$'` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok: true`、`readOnly: true`、`releaseReady: false`。`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs ../scripts/qa/mobile-workflow-simulated-closure.mjs ../scripts/qa/mobile-workflow-simulated-closure.test.mjs` 只返回 web eslint 的 outside base path warning，未作为有效 lint 证据。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke 的运行前置、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印静态预览与工作台纸面一致性，以及其他 QA / smoke 脚本中是否仍把 workflow payload 当 action 真源的残留排查。
- 阻塞/风险：本轮只证明模拟 apply 的 action 参数构造不再回传 workflow payload 快照和系统字段；没有密码，未执行 `--apply`，未登录真实试用账号，未在浏览器点击 done / blocked / rejected / urge，未证明真实账号 RBAC、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 移动端真实浏览器 smoke 业务状态口径收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 `mobileWorkflowRuntimeBrowserSmoke` 真实浏览器 smoke 把完成 / 退回后的 `business_status_key` 当成动作验收条件的问题。报告任务条目不再输出 boss complete / reject 的 `businessStatus`，脚本也不再断言 `updatedBossDoneTask.business_status_key === project_approved` 或 `updatedBossRejectTask.business_status_key === project_pending`；真实 smoke 只证明浏览器能完成 done / blocked / rejected / urge 任务动作、原因和移动端证据写入，业务状态仍由后端 Workflow action / usecase 推导，不由 smoke 或前端按钮参数驱动。保留模拟任务创建时的初始 `business_status_key`，用于造本地 simulated workflow task，不代表动作验收或 Fact 落账。
- 验证：`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 13 个测试；`node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs && node --check scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionSubmitGuard.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs` 通过 65 个测试，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`cd server && go test ./internal/service -run 'TestJsonrpcDispatcher_Workflow(UrgeTaskRecordsEventIntent|UrgeTaskRejectsClientControlledSystemFields|ControlledTaskActionsRejectRawStatusActorRoleAndInvalidAction|CompleteTaskActionCompletesLinkedProcessNode)$'` 通过；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs scripts/mobileWorkflowRuntimeBrowserSmoke.mjs` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；目标 `rg` 扫描 `updatedBoss(Done|Reject)Task.business_status_key`、`businessStatus: updatedBoss`、`business_status_key, 'project_approved'` 无命中；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok: true`、`readOnly: true`、`releaseReady: false`。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke 的运行前置、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印静态预览与工作台纸面一致性，以及其他 smoke / report 是否把业务状态或 payload 当动作完成证据的残留排查。
- 阻塞/风险：本轮只证明移动端真实浏览器 smoke 的本地报告和脚本断言不再把业务状态当动作验收条件；没有密码，未运行真实浏览器 smoke，未登录、未写数据库、未证明真实账号 RBAC、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 客户配置开发页回滚命令入口收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 `/__dev/customer-config` 备用命令把带 `CUSTOMER_CONFIG_ADMIN_TOKEN` 占位符和 `--execute --rollback` 的客户配置回滚执行命令直接展示给开发态页面的问题。开发页现在只展示 `customer-config-release-execute.mjs --print-input-template`，保留受控发布 / 激活 / 回滚的输入模板入口，不在 `__dev` 页面渲染可直接执行的 release rollback 写命令、token 占位符或确认短语。未改发布执行器自身的受控执行能力、后端 `rollback_customer_config`、release evidence gate、真实目标环境、生产部署或备份恢复演练。
- 验证：`node --test web/src/erp/config/devCustomerConfig.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 22 个测试；`node --check web/src/erp/config/devCustomerConfig.mjs && node --check web/src/erp/config/devCustomerConfig.test.mjs && node --check scripts/qa/dev-entry-boundary.test.mjs` 通过；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/config/devCustomerConfig.mjs src/erp/config/devCustomerConfig.test.mjs` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。曾尝试用 web ESLint 同时 lint `scripts/qa/dev-entry-boundary.test.mjs`，该文件因位于 web base path 外被忽略并只给 warning，已用 Node check/test 作为有效证据。
- 下一步：继续按 review findings 推进 effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印静态预览与工作台纸面一致性，以及其他 `__dev`/测试入口是否仍暴露 release/真实写入执行命令。
- 阻塞/风险：本轮只证明开发态客户配置页和 dev entry boundary 不再展示 token/`--execute` 回滚命令；不证明发布执行器真实执行、目标环境 active revision、真实生产部署、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 库存台账来源单据 raw id 投影收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理库存台账流水来源单据列仍用 `record.source_id` 作为可见兜底判断的问题。`formatSourceDocumentRef` 现在只展示 `source_no / document_no / order_no / receipt_no / shipment_no` 这类业务来源号；后端当前未返回业务来源号时显示“未提供业务单号”，不再用内部 `source_id` 生成“来源单据已关联”。库存流水“来源单据”关联动作也收窄为必须同时有可路由来源类型和有效 `source_id` 才展示，避免半截上下文跳转。未改库存 JSON-RPC、repo、schema、Fact 写入、来源 read model、打印模板或真实导出。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 45 个测试；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/V1InventoryLedgerPage.jsx src/erp/utils/userVisibleTechnicalFields.test.mjs scripts/style-l1/businessFormalScenarios.mjs` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=business-formal-module-shells-desktop STYLE_L1_PORT=5236 /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景，覆盖库存台账流水 tab 中“未提供业务单号”可见且 `MANUAL_SEED / INV-TXN-001 / 9001 / 9002 / 888500999` 不可见；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。L1 仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进库存 / 业务事实来源号 read model 是否需要后端补业务号、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印静态预览与工作台纸面一致性。
- 阻塞/风险：本轮只证明库存台账前端可见投影和 L1 渲染不再展示或用内部来源 ID 兜底；后端库存流水仍未提供业务来源号，打印 / 导出 / 历史流水来源号完整闭环未补；未运行真实账号登录、真实客户数据导入、目标环境 active revision、生产发布、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 打印中心静态预览合并金额口径收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理打印中心静态预览 `PrintTemplateRenderer` 仍用页面私有 `resolveLineTotals(lines)` 计算合计、没有读取采购 / 加工工作台 `merges` 的问题。新增 `printTemplateRendererModel.mjs`，静态预览现在复用采购合同 `computeMaterialPurchaseTotals` 和加工合同 `calculateProcessingContractTotals`，并通过共享 merge helper 跳过被合并覆盖的明细单元格，避免采购金额 / 委托加工金额在打印中心预览里和可编辑工作台纸面、PDF / 打印输出口径不一致。未新增销售订单打印模板，未发布 `sales_order_items.default`，未改后端 PDF、schema、RBAC、JSON-RPC、真实业务事实或客户配置发布门禁。
- 验证：`node --test web/src/erp/components/print/printTemplateRendererModel.test.mjs web/src/erp/config/printTemplates.test.mjs web/src/erp/utils/materialPurchaseContractEditor.test.mjs web/src/erp/utils/processingContractEditor.test.mjs web/src/erp/data/processingContractTemplate.test.mjs` 通过 43 个测试；`node --check web/src/erp/components/print/printTemplateRendererModel.mjs && node --check web/src/erp/components/print/printTemplateRendererModel.test.mjs && node --check web/src/erp/config/printTemplates.test.mjs` 通过；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/components/print/PrintTemplateRenderer.jsx src/erp/components/print/printTemplateRendererModel.mjs src/erp/components/print/printTemplateRendererModel.test.mjs src/erp/config/printTemplates.test.mjs` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=print-center-desktop,print-preview-material,print-preview-processing STYLE_L1_PORT=5237 /usr/local/bin/pnpm --dir web style:l1` 通过 3 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进打印业务带值 mapper / 采集 / 导出 / 历史缺值链路、库存 / 业务事实来源号 read model、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界。
- 阻塞/风险：本轮只证明打印中心静态预览的采购 / 加工合计和 merge 覆盖单元格不再分叉；不证明销售订单打印链路已完成、不证明真实业务页带值打印的所有字段都有历史缺值回补、不证明真实账号登录、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 客户配置开发页来源投影与 L1 口径收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 `/__dev/customer-config` 已登记客户包页面仍把 `sourcePath` 直接渲染到品牌卡、评审清单、来源引用和 header，并用 `customerKey.slice(0, 1)` 作为品牌兜底的问题。`devCustomerConfig` 现在保留 `sourcePath` 作为脚本 / 测试真源，同时新增 `sourceLabel` 和品牌可读兜底；页面展示改用“客户菜单配置 / 客户配置包说明 / 打印模板字段配置”等标签，已登记客户下拉和已登记包列表不再拼 raw customer key。同步修正 `dev-customer-config-dark-desktop` L1 场景里的旧回滚执行器 / 旧确认短语 / 旧下拉标签断言，保持 `__dev` 只展示受控输入模板入口，不展示裸回滚执行器。未改后端 customer_config usecase、RBAC、active revision、发布执行器真实写入、真实客户数据导入或 release evidence 门禁。
- 验证：`node --test web/src/erp/config/devCustomerConfig.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 22 个测试；`node --check web/src/erp/config/devCustomerConfig.mjs && node --check web/src/erp/config/devCustomerConfig.test.mjs` 通过；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/DevCustomerConfigPage.jsx src/erp/config/devCustomerConfig.mjs src/erp/config/devCustomerConfig.test.mjs scripts/style-l1/scenarios.mjs` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop STYLE_L1_PORT=5238 /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`node --test scripts/qa/docs-inventory.test.mjs` 通过。
- 下一步：继续按 review findings 推进 effective session 与正式菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路、库存 / 业务事实来源号 read model。
- 阻塞/风险：本轮只证明开发态客户配置页的已登记客户包可见来源不再直出 raw source path / customer key 兜底，以及对应 L1 场景与当前受控入口一致；missing customer 状态仍显示用户输入的 query key 用于排障，不代表正式客户菜单暴露；未证明真实登录、真实账号 RBAC、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Workflow 任务相关单据 raw id 投影收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理采购 IQC、委外回货、成品、出货财务、应付对账派生任务的 `payload.related_documents` 仍可能把内部 `source_no` / `id` 兜底拼成“到货记录：66”“加工合同：88”“出货记录：56”“采购单：71”等用户可见文案的问题。新增 `workflowDocumentRefs.mjs` 作为共享投影 helper，统一识别 `66`、`#66`、`ID-66`、`TASK-*` 这类内部来源号 fallback；业务可读编号继续展示为“单据：业务号”，内部 fallback 改为“单据已关联”。五条 flow 的 `resolve*SourceNo` 和 related documents 拼接均改走共享 helper，静态守卫要求后续任务派生继续使用该 helper。未改后端 WorkflowUsecase、JSON-RPC、RBAC、Fact 写入、真实任务执行、打印模板或 release evidence。
- 验证：`node --test web/src/erp/utils/workflowDocumentRefs.test.mjs web/src/erp/utils/purchaseInboundFlow.test.mjs web/src/erp/utils/outsourceReturnFlow.test.mjs web/src/erp/utils/finishedGoodsFlow.test.mjs web/src/erp/utils/shipmentFinanceFlow.test.mjs web/src/erp/utils/payableReconciliationFlow.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 101 个测试；`node --check web/src/erp/utils/workflowDocumentRefs.mjs && node --check web/src/erp/utils/workflowDocumentRefs.test.mjs && node --check web/src/erp/utils/purchaseInboundFlow.mjs && node --check web/src/erp/utils/outsourceReturnFlow.mjs && node --check web/src/erp/utils/finishedGoodsFlow.mjs && node --check web/src/erp/utils/shipmentFinanceFlow.mjs && node --check web/src/erp/utils/payableReconciliationFlow.mjs` 通过；`/usr/local/bin/pnpm --dir web exec eslint src/erp/utils/workflowDocumentRefs.mjs src/erp/utils/workflowDocumentRefs.test.mjs src/erp/utils/purchaseInboundFlow.mjs src/erp/utils/purchaseInboundFlow.test.mjs src/erp/utils/outsourceReturnFlow.mjs src/erp/utils/outsourceReturnFlow.test.mjs src/erp/utils/finishedGoodsFlow.mjs src/erp/utils/finishedGoodsFlow.test.mjs src/erp/utils/shipmentFinanceFlow.mjs src/erp/utils/shipmentFinanceFlow.test.mjs src/erp/utils/payableReconciliationFlow.mjs src/erp/utils/payableReconciliationFlow.test.mjs src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=erp-task-board-mobile,business-collaboration-mobile STYLE_L1_PORT=5239 /usr/local/bin/pnpm --dir web style:l1` 通过 2 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：继续按 review findings 推进任务端真实账号 / 造数 smoke、effective session 与菜单实际入口一致性、客户配置规则 / 策略 / 扩展点运行时边界、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明本地任务派生 helper、静态守卫和移动 / 协同任务页面 L1 不再展示内部来源号 fallback；没有运行真实登录，没有密码时仍不证明真实账号 RBAC、真实浏览器写动作、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 开发测试入口命令来源标签收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 `/__dev/testing` 命令块 header 仍把 `block.sourcePath` 直接作为主要可见文案的问题。`devTesting` 模型现在为文档和 fenced command block 生成 `sourceLabel`，按“测试策略 / QA 脚本 / 前端脚本 / 部署验证”等可读来源标签投影；`DevTestingPage` 命令块主文案改用 `sourceLabel`，保留 path 作为模型 key、搜索和文档列表定位信息。同步补 `dev-entry-boundary` 守卫，禁止命令块回退到 `{block.sourcePath}` 直出。未改正式菜单、正式客户页面、真实命令执行、后端 RBAC、customer config active revision 或 release evidence。
- 验证：`node --test web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 14 个测试；`node --check web/src/erp/config/devTesting.mjs && node --check web/src/erp/config/devTesting.test.mjs && node --check scripts/qa/dev-entry-boundary.test.mjs` 通过；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/pages/DevTestingPage.jsx src/erp/config/devTesting.mjs src/erp/config/devTesting.test.mjs` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-testing-dark-desktop,dev-testing-light-desktop STYLE_L1_PORT=5240 /usr/local/bin/pnpm --dir web style:l1` 通过 2 个场景，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进真实账号 / 造数 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路、以及 `__dev`/测试入口是否仍存在过期命令或误导性 release evidence 口径。
- 阻塞/风险：本轮只证明 `__dev/testing` 的本地页面模型和浏览器 L1 不再把命令来源 raw path 作为主要文案；文档列表仍保留路径用于开发态定位真源，不代表正式客户菜单暴露；没有运行真实登录、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Effective session 客户 key 与本地诊断直达收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理后台 layout 请求 `get_effective_session` 时把缺失 runtime customer key 静默 fallback 到 `yoyoosun` 的问题。新增 `resolveEffectiveSessionCustomerKey`，没有明确 runtime customer key 时只挂载 unavailable effective session，不再伪装成种子客户配置；formal boundary 守卫禁止恢复 `activeBrand?.customerKey || 'yoyoosun'`。同时修正本地开发诊断直达隐藏页的 outlet 拦截：侧栏菜单仍按 effective session active pages 收窄，但 RBAC 允许且 `shouldRedirectFromCurrentNavigation` 放行的本地诊断页不再因为 `visibleSections.length === 0` 被“当前账号暂无可见后台入口”挡住。同步把 L1 场景从“要求隐藏菜单可见”改为验证 `data-effective-session-mode` 与页面内容，保留无 RBAC / 无菜单账号仍被 block outlet 的场景。未改后端 customer_config usecase、RBAC 权限码、正式客户菜单、真实账号登录、active revision 发布或 release evidence。
- 验证：预检 `node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok: true` 且 `releaseReady: false`，只作为本地 ready / target-evidence-required 边界；`node --test web/src/erp/utils/adminProfileSync.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs` 通过 24 个测试；`node --check web/src/erp/utils/adminProfileSync.mjs && node --check web/src/erp/utils/adminProfileSync.test.mjs && node --check scripts/qa/formal-frontend-customer-config-boundary.test.mjs && node --check web/scripts/styleL1.mjs && node --check web/scripts/style-l1/scenarios.mjs` 通过；`/usr/local/bin/pnpm --dir web exec eslint --ext .jsx --ext .mjs src/erp/components/ERPLayout.jsx src/erp/utils/adminProfileSync.mjs src/erp/utils/adminProfileSync.test.mjs scripts/styleL1.mjs scripts/style-l1/scenarios.mjs` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=erp-effective-session-direct-url-local-dev-diagnostic STYLE_L1_PORT=5241 /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=erp-effective-session-super-admin-product-core,erp-effective-session-direct-url-local-dev-diagnostic,erp-effective-session-sync-failure-local-dev-diagnostic,erp-effective-session-empty-pages-local-dev-diagnostic,erp-no-visible-menu-blocks-outlet,erp-effective-session-action-projection-business-pages STYLE_L1_PORT=5241 /usr/local/bin/pnpm --dir web style:l1` 通过 6 个场景。L1 仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning。
- 下一步：继续按 review findings 推进真实账号 / 造数 smoke、客户配置规则 / 策略 / 扩展点运行时边界、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路、Workflow action / RBAC / usecase parity。
- 阻塞/风险：本轮只证明本地开发和 L1 模拟环境下 effective session customer key、菜单投影、直达诊断页和 action projection 的前端边界；没有真实密码时仍不证明真实登录、真实账号 RBAC、真实浏览器写动作、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 模拟任务端 action 报告业务状态口径收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 `mobile-workflow-simulated-closure` 虽已收窄 action payload，但 apply 报告仍把动作后的 `updated.business_status_key` 输出为 `business_status`，Markdown 也拼 `business=...` 的问题。模拟任务端 action 报告现在只输出 workflow task action 状态、移动端证据数、异常反馈、催办和内部通知类型，不再把业务状态作为 action evidence；保留 `create_task` 造模拟任务所需的初始 `business_status_key`，它只用于本地模拟任务创建，不代表 action 验收或 Fact 落账。未改正式前端页面、后端 WorkflowUsecase、JSON-RPC、RBAC、Fact 写入、真实浏览器 smoke、真实账号或 release evidence。
- 验证：预检 `node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok: true`、`readOnly: true`、`releaseReady: false`；`node --test scripts/qa/mobile-workflow-simulated-closure.test.mjs scripts/qa/workflow-ui-action-boundary.test.mjs` 通过 29 个测试；`node --check scripts/qa/mobile-workflow-simulated-closure.mjs && node --check scripts/qa/mobile-workflow-simulated-closure.test.mjs` 通过；目标 `rg` 扫描 `business_status: updated.business_status_key`、`business=${step.business_status}` 和 `updated.business_status_key`，运行时代码无命中，仅测试守卫保留禁止恢复的字符串。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke 的运行前置、客户配置规则 / 策略 / 扩展点运行时边界、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路、以及其他 QA / smoke 报告是否仍把 workflow payload 或业务状态当动作完成证据。
- 阻塞/风险：本轮只证明模拟任务端 apply 报告不再把业务状态写成 action evidence；未执行 `--apply`，未登录真实试用账号，未在浏览器点击 done / blocked / rejected / urge，未证明真实账号 RBAC、真实后端写入、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 移动任务端浏览器 smoke 报告 raw key 投影收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 `mobileWorkflowRuntimeBrowserSmoke` 报告虽声明 `storesRedactedActionSummary: true`，但 `tasks` 和 simulated task coverage 仍输出 `ownerRoleKey`、`taskGroup`、raw action / status / notification type 的问题。真实浏览器 smoke 报告现在复用岗位和 Workflow task group 标签，任务条目只输出 `ownerRole`、`taskGroupLabel`、`statusLabel`、`actionLabel`、`assigneeRole` 等业务投影；coverage 输出 `ownerRoles`、`actionLabels`、`notificationHints`，内部 key 只留在造数计划和运行时校验里，不进入本地验收报告。未改正式页面、后端 WorkflowUsecase、JSON-RPC、RBAC、Fact 写入、真实账号、发布执行器或 release evidence。
- 验证：`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 13 个测试；`node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs && node --check scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 20 个测试并完成 docs inventory；测试新增序列化守卫，禁止报告恢复 `"ownerRoleKey"`、`"ownerRoleKeys"`、`"taskGroup"`、raw task status/action 字段、`owner_role_key`、`task_status_key` 和常见 raw task group key。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke 的密码前置与 effective session/RBAC 真实运行、客户配置规则 / 策略 / 扩展点运行时边界、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路，以及其他 QA / smoke 报告是否仍暴露 raw payload、raw source 或内部编号 fallback。
- 阻塞/风险：本轮只证明移动端浏览器 smoke 报告模型和测试守卫不再把 raw key 作为验收输出；没有密码，未实际运行真实浏览器 smoke，未登录真实试用账号、未写数据库、未证明真实账号 RBAC、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 移动任务端浏览器 smoke 前置模板 raw plan 收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 `mobileWorkflowRuntimeBrowserSmoke` 的 `--print-input-template` 和 `--preflight-report` 仍直接输出 `simulatedTaskPlan`，从而把 `ownerRoleKey`、`taskGroup`、`browserAction`、`notificationType` 这类内部造数字段带进无密码前置证据的问题。新增 `buildSimulatedTaskPlanSummary`，对外模板和 preflight 报告只输出 `simulatedTaskPlanSummary`，包含岗位、任务组、动作、预期状态、通知提示等业务标签；内部 `buildSimulatedTaskPlan()` 仍保留给真实 smoke 造数和 coverage 计算使用。未改真实浏览器执行路径、后端 WorkflowUsecase、JSON-RPC、RBAC、Fact 写入、真实账号、生产发布或 release evidence。
- 验证：`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 13 个测试，覆盖函数返回、CLI `--print-input-template` 和 CLI `--preflight-report` 的 raw plan 序列化守卫；`node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs && node --check scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 20 个测试并完成 docs inventory；`/usr/local/bin/pnpm --dir web exec eslint --ext .mjs scripts/mobileWorkflowRuntimeBrowserSmoke.mjs ../scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 对 web 脚本通过，外层 QA 测试文件因 web base path 被忽略，仅有 warning，已由 Node check/test 覆盖。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke 的密码前置与 effective session/RBAC 真实运行、客户配置规则 / 策略 / 扩展点运行时边界、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路，以及其他 QA / smoke / dev 页面是否仍输出 raw payload、raw source 或内部编号 fallback。
- 阻塞/风险：本轮只证明无密码 input template / preflight 报告不再输出 raw simulated task plan；没有密码，未实际运行真实浏览器 smoke，未登录真实试用账号、未写数据库、未证明真实账号 RBAC、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Trial account RBAC 报告岗位投影收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 `trial-account-rbac` 的 input template、preflight static projection 和真实 `--report` 仍把 `roleKey`、`mobilePermission`、`mobilePath`、`mobile.<role>.access` 等内部权限 / 路由锚点作为验收输出的问题。脚本现在把对外 `expectedAccountSummaries`、`staticProjection.roleRows` 和 `checkedAccounts` 投影为岗位名称、岗位任务端入口和移动端访问是否预期 / 已验证；内部 `expectedTrialRoleProjections`、静态扫描 token 和真实 `collectAccountFailures` 仍保留 raw key 用于检查，不进入报告 JSON。未改真实登录流程、后端 RBAC、账号 seed、菜单权限、浏览器 smoke、生产发布或 release evidence。
- 验证：`node --test scripts/qa/trial-account-rbac.test.mjs` 通过 10 个测试，覆盖 input template、preflight report、CLI preflight 和 verification report 的 raw role / mobile permission / mobile path 序列化守卫；`node --check scripts/qa/trial-account-rbac.mjs && node --check scripts/qa/trial-account-rbac.test.mjs` 通过；`node --test scripts/qa/trial-role-entry-docs.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 2 个测试并完成 docs inventory；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍返回 `ok: true`、`readOnly: true`、`releaseReady: false`，剩余阻断归类为 `external-release-evidence-required`。
- 下一步：继续按 review findings 推进 trial demo browser smoke 的菜单 / 移动入口报告投影、真实造数任务端动作 smoke 的密码前置与 effective session/RBAC 真实运行、客户配置规则 / 策略 / 扩展点运行时边界、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路。
- 阻塞/风险：本轮只证明 trial account RBAC 脚本的无密码模板、preflight 和真实报告模型不再输出 raw role / mobile permission / mobile path；没有密码，未执行真实 `admin_login / me`，未运行 trial demo browser smoke，未证明真实账号 RBAC、菜单投影、移动端入口、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

## 2026-07-03 Trial demo browser smoke 移动入口投影收口

- 完成：继续当前 active goal 的 review findings 驱动口径，处理 `trialDemoAccountBrowserSmoke` 的 `--print-input-template`、`--preflight-report` 和真实 `--report` 仍把移动端 `roleKey`、`/m/<role>/tasks` 这类内部路由锚点作为验收输出的问题。脚本现在复用角色显示名，把移动账号、拒绝访问账号和 menu projection plan 对外投影为岗位名称与“岗位任务端”入口摘要；内部 `mobileAccounts` 和 `verifyMobileAccount` 仍保留 raw role/path 只用于真实浏览器导航与断言。未改真实登录流程、后端 RBAC、账号 seed、菜单权限、Workflow usecase、Fact 写入、生产发布或 release evidence。
- 验证：预检 `node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍返回 `ok: true`、`readOnly: true`、`releaseReady: false`，本轮只按本地 ready / target-evidence-required 边界处理；`node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过 7 个测试，覆盖 input template、CLI preflight 和 report 前置的 raw `roleKey` / 移动路径序列化守卫；`node --check web/scripts/trialDemoAccountBrowserSmoke.mjs && node --check web/scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过；`node --test scripts/qa/trial-account-rbac.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 11 个相邻测试；`node --test scripts/qa/docs-inventory.test.mjs` 通过并完成 docs inventory；`git diff --check` 通过。
- 下一步：继续按 review findings 推进真实造数任务端动作 smoke 的密码前置与 effective session/RBAC 真实运行、客户配置规则 / 策略 / 扩展点运行时边界、打印业务带值 mapper / 采集 / 导出 / 历史缺值链路，以及其他 QA / smoke / dev 页面是否仍输出 raw payload、raw source、内部编号 fallback 或底层英文错误。
- 阻塞/风险：本轮只证明 trial demo browser smoke 的无密码模板、preflight 和报告模型不再输出移动端 raw role/path；没有密码，未实际运行真实浏览器 smoke，未登录真实试用账号，未证明真实账号 RBAC、菜单投影、移动端入口、目标环境 active revision、生产发布、真实客户导入、客户签收、备份恢复或回滚演练。当前工作区仍有大量未提交改动，未提交、未推送、未 stash、未清理。

# progress 2026-07-02 qa:fast 后续本地闭环前归档

本文件归档 `progress.md` 中 2026-07-02 从 `客户配置控制台 no-write 读回预检收口` 到 `L1 与移动端登录回跳本地回归复核` 的过程流水。

这些记录只用于追溯本地 no-write / mock / L1 证据，不作为当前需求、runtime、schema、RBAC、Workflow / Fact、部署或 release evidence 真源。当前状态仍以 `docs/当前真源与交接顺序.md`、正式产品文档、代码和测试为准。

## 2026-07-02 客户配置控制台 no-write 读回预检收口

- 完成：继续当前本地试用闭环，不扩张 release evidence。`__dev/customer-config` 的备用命令列表新增 active revision 读回 no-write preflight 命令，并在配置摘要中明确该报告只写 `output/customers/yoyoosun/customer-config-readback-preflight.json`、不调用后端、不读取 token、不写 release evidence、不发布或激活客户配置；命令使用当前仓库已有 `deployments/yoyoosun/evidence/releases/2026-06-29` 只读 evidence 目录，避免页面复制 `<YYYY-MM-DD>` 占位符后被 shell 当作重定向。同步 `web/README.md` 说明命令兜底区包含 no-write 读回缺口报告。
- 验证：追加前 `progress.md` 为 224 行 / 75,256 bytes，未达到 600 行或 80KB 归档阈值；`node --check web/src/erp/config/devCustomerConfig.mjs && node --check web/src/erp/config/devCustomerConfig.test.mjs && node --check scripts/qa/dev-entry-boundary.test.mjs && node --check web/scripts/style-l1/scenarios.mjs` 通过；`node --test web/src/erp/config/devCustomerConfig.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 18 项；触达 web 文件 ESLint 通过；`STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过；`node scripts/deploy/customer-config-release-readiness.mjs --manifest output/customers/yoyoosun/customer-config-runtime-manifest.json --evidence-dir deployments/yoyoosun/evidence/releases/2026-06-29 --release-report output/customers/yoyoosun/customer-config-release/customer-config-release-report.json --readback-preflight-report output/customers/yoyoosun/customer-config-readback-preflight.json --json` 只生成 no-write 缺口报告，`readyForReadinessGate=false`，blockers 为 `missing-effective-session-verification`、`smoke-report-missing-backend-endpoint-alias`、`missing-smoke-effective-session-check`；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / total=69 / blockingCategory=external-release-evidence-required`。
- 下一步：无演示密码时，继续普通账号菜单投影 preflight 或移动端任务端本地证据；有演示密码和可用本地前后端后，按 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report`、`mobileWorkflowRuntimeBrowserSmoke --report` 补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮只改 dev-only 控制台配置摘要、页面可见备用命令和 README / progress 口径；不发布 / 激活 / 回滚客户配置，不调用 JSON-RPC，不读取密码 / token，不写数据库，不导入真实客户数据，不改后端 RBAC / usecase / schema / migration，不做生产部署、目标环境 release evidence、备份恢复、回滚演练、客户签收、提交或推送。生成的 readback preflight 报告仍不证明 active revision 已从目标后端读回、`get_effective_session` 目标 smoke 已通过或 release evidence 已完成。

## 2026-07-02 试用账号浏览器 preflight 未证明项收口

- 完成：继续普通账号菜单投影 no-write 路径，不扩张 release evidence。`trialDemoAccountBrowserSmoke.mjs` 的输入模板和 preflight 报告补 `preflightOnly=true`、`realSmokeRequires` 与 `notProvenByThisPreflight`，明确该报告只证明后端 health、密码 env、yoyoosun 端口审计、静态菜单计划和 customer config 脚本前置，不证明真实登录、后端 RBAC、普通账号桌面菜单投影、岗位任务端、`demo_admin` 移动拒绝态、DEV-only effective session 读回、active revision source 或目标 release evidence。同步 `scripts/README.md`、自动化测试策略和 yoyoosun 试用手册。
- 验证：追加前 `progress.md` 为 231 行 / 78,654 bytes，未达归档阈值；`node --check web/scripts/trialDemoAccountBrowserSmoke.mjs && node --check web/scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过；`node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs web/scripts/yoyoosunEntryPlan.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 13 项；触达 web 脚本 ESLint 通过；无密码执行 `trialDemoAccountBrowserSmoke.mjs --preflight-report output/trial-demo-account-browser-smoke/current-preflight.json` 输出 `readyForRealSmoke=false / blockers=missing-demo-password-env / menuProjectionOk=true / yoyoosunPorts=5177`，报告未命中 token / password 文本；`start:yoyoosun -- --print-plan` 和 `preview:yoyoosun -- --print-plan --skip-build --skip-health-check` 均提示 5176 占用并顺延 5178；`audit:yoyoosun-entry` 仍确认 5177=yoyoosun、5175=Product Core placeholder、5175/5176=HTML fallback；customer config boundaries、runtime manifest、docs inventory、dev-entry-boundary、priority audit 和 `git diff --check` 均通过。
- 下一步：有演示密码后跑 `trial-account-rbac --report` 与 `trialDemoAccountBrowserSmoke --report` 补真实登录 / RBAC / 菜单投影 / effective session 脱敏读回；无密码时继续移动端任务端本地证据或客户配置控制台 no-write 缺口。
- 阻塞/风险：本轮不登录、不调用 JSON-RPC、不启动浏览器、不写数据库、不改后端 RBAC / usecase / schema / migration、不发布客户配置、不做真实客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送；静态菜单计划通过仍不能替代真实浏览器菜单投影验收。

## 2026-07-02 移动端任务端 no-write preflight 复核

- 完成：继续移动端任务端本地证据复核，不扩张 release evidence。本轮未改代码；实际运行 `mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report` 生成当前 no-write 报告，确认无演示密码时报告保持 `preflightOnly=true / readyForRealSmoke=false`，只证明 backend health、yoyoosun 端口归属和模拟任务计划 coverage，不证明真实登录、后端 RBAC / effective session、浏览器布局、workflow 任务创建、done / blocked / rejected / urge 提交、完成反馈 / 内部通知展示或目标环境 release evidence。
- 验证：追加前 `progress.md` 为 238 行 / 81,098 bytes，未达 80KB=81,920 bytes 归档阈值；`MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD='' TRIAL_ACCOUNT_PASSWORD='' ERP_ROLE_DEMO_PASSWORD='' PATH=/usr/local/bin:$PATH node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report output/mobile-workflow-runtime-browser-smoke/current-preflight.json --run-id current` 输出 `ready=false`，报告为 `preflightOnly=true / blockers=missing-demo-password-env / backendHealth.ok=true / yoyoosunPorts=5177 / productCorePlaceholderPorts=5175 / htmlFallbackPorts=5175,5176 / actions=block,complete,reject,urge-only`；`node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs && node --check scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过；`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs` 通过 34 项；触达 web 脚本 ESLint 通过；`node --test scripts/qa/docs-inventory.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 5 项；customer config boundaries、runtime manifest preview、priority audit 和 `git diff --check` 均通过，其中 priority audit 仍为 `ok=true / readOnly=true / releaseReady=false / total=69 / ready=67 / guarded=1 / evidence-required=1`。
- 下一步：`progress.md` 本次追加后预计超过 80KB，下一次写入前应先归档旧流水；有演示密码后再跑 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`，补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮不读取密码 / token，不登录，不调用 JSON-RPC，不启动 Vite / Playwright，不创建 workflow 任务，不写数据库，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；no-write preflight 不能替代真实移动端 RBAC、任务动作和页面回归。

## 2026-07-02 字段链路与客户配置读回边界复核

- 完成：继续当前本地试用闭环，不重启旧计划、不扩张 release evidence。本轮先跑字段链路守卫，确认当前生成报告已从旧的 `coveredFields=8 / partialFields=1 / missingFields=21` 收口为 `coveredFields=30 / partialFields=0 / missingFields=0`，49 个 case、53 个 scenario 全部通过；因此字段链路不再作为本轮新增实现缺口。随后重新生成 yoyoosun preview runtime manifest 和 active revision no-write 读回预检报告，确认 manifest 本身有效，客户配置读回仍只卡真实执行 / 目标 smoke 证据。
- 验证：追加前 `progress.md` 为 62 行 / 15,537 bytes，未达归档阈值；`node scripts/qa/erp-field-linkage.mjs` 通过 108 项并更新 `web/public/qa/erp-field-linkage-coverage.latest.json`，摘要为 `totalFields=30 / coveredFields=30 / partialFields=0 / missingFields=0 / totalCases=49 / passedCases=49 / totalScenarios=53 / passedScenarios=53`；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，输出 `modules=15 / roles=9 / entitlements=346`；`node scripts/deploy/customer-config-release-readiness.mjs --manifest output/customers/yoyoosun/customer-config-runtime-manifest.json --evidence-dir deployments/yoyoosun/evidence/releases/2026-06-29 --release-report output/customers/yoyoosun/customer-config-release/customer-config-release-report.json --readback-preflight-report output/customers/yoyoosun/customer-config-readback-preflight.json --json` 只生成 no-write 报告，`readyForReadinessGate=false`，blockers 为 `missing-effective-session-verification`、`smoke-report-missing-backend-endpoint-alias`、`missing-smoke-effective-session-check`；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / total=69 / ready=67 / guarded=1 / evidence-required=1`。
- 下一步：无演示密码时，继续客户配置 readback 的本地报告口径、普通账号菜单投影 preflight 或移动端任务端本地证据；有演示密码和可用本地前后端后，再跑真实 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report`、`mobileWorkflowRuntimeBrowserSmoke --report`，补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮不读取密码 / token，不登录，不调用 JSON-RPC，不写数据库，不发布 / 激活 / 回滚客户配置，不改后端 RBAC / usecase / schema / migration，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；readback preflight 仍不证明目标后端 active revision 已读回、`get_effective_session` 目标 smoke 已通过或 release evidence 已完成。

## 2026-07-02 yoyoosun 本地入口与 effective session 诊断

- 完成：继续当前最高优先级的 yoyoosun 本地入口诊断，不改代码。`start:yoyoosun -- --print-plan` 显示请求端口 5176 已占用，dev 入口会顺延到 5178；`preview:yoyoosun -- --print-plan` 会顺延到 5179。当前已运行端口中，5175 是本仓 `plush-toy-erp/web` 的 Product Core placeholder，5176 是 `/Users/simon/projects/openai-oauth-api-service/web` 的 Vite 服务，5177 是本仓 yoyoosun dev/static 前端；5177 的 `/customer-config.js` 命中 `customerKey: "yoyoosun"`，`/customer-assets/yoyoosun/favicon-yoyoosun.svg` 返回 `content-type: image/svg+xml`。后端 `8300/healthz` 可达；无 token 调用 `customer_config.get_effective_session` 返回 `40302 未登录`，服务端 `get_effective_session` 入口先取 `CurrentAdmin`，所以没有演示密码 / 登录态时不能证明真实账号的 active revision 来源。
- 验证：追加前 `progress.md` 为 69 行 / 18,488 bytes，未达归档阈值；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web start:yoyoosun -- --print-plan` 通过并输出 5178 计划端口；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web preview:yoyoosun -- --print-plan` 通过并输出 5179 计划端口；`lsof -nP -iTCP:5175 -iTCP:5176 -iTCP:5177 -sTCP:LISTEN` 和 `ps/lsof cwd` 确认 5175、5177 属于本仓 web，5176 属于 openai-oauth；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web audit:yoyoosun-entry` 输出 `yoyoosun ports=5177`、`5175 config=product_core_placeholder`、`5176 config=html_fallback`、`5177 config=yoyoosun_config asset=yoyoosun_asset`；对 5175/5176/5177 执行 `curl /customer-config.js` 与 yoyoosun asset HEAD，结果与 audit 一致；`curl -i -X POST http://127.0.0.1:8300/rpc/customer_config ... get_effective_session ...` 返回 `code=40302 / message=未登录`；`TRIAL_ACCOUNT_PASSWORD='' ERP_ROLE_DEMO_PASSWORD='' PATH=/usr/local/bin:$PATH node scripts/qa/trial-account-rbac.mjs --preflight-report output/trial-account-rbac/current-preflight.json` 生成 no-write 报告，`backendHealth.ok=true`、blocker 为 `missing-trial-account-password-env`，并明确不证明真实登录、RBAC、active revision、桌面菜单投影、移动任务入口或 effective session 诊断读回。
- 下一步：本地入口本身不再是阻断；无演示密码时继续普通账号菜单投影 / 移动端任务端 preflight 和文档口径收口。有演示密码后，优先运行 `trial-account-rbac --report` 与 `trialDemoAccountBrowserSmoke --report`，在 5177 或脚本新打印的实际 yoyoosun 端口上补真实登录、普通客户角色菜单投影和脱敏 effective session 来源证据。
- 阻塞/风险：本轮不读取密码 / token，不保存 response body，不启动新长期 dev server，不登录浏览器，不执行客户配置 publish / activate / rollback，不写数据库，不改后端 RBAC / usecase / schema / migration，不做真实客户数据导入、生产部署、目标环境 release evidence、备份恢复、回滚演练、客户签收、提交或推送；5177 证明的是当前本地 yoyoosun 前端资源注入，不证明后端 active customer config revision 已生效，也不证明普通账号真实菜单投影。

## 2026-07-02 business-formal L1 阻断复核

- 完成：按 goal 中 yoyoosun 入口诊断后的后续优先级，复核此前记录的 `business-formal-module-shells-desktop` 浏览器 L1 阻断。本轮未改页面、样式或测试代码；当前工作区 targeted L1 已通过，说明该场景在当前状态下不再阻塞本地闭环。
- 验证：追加前 `progress.md` 为 76 行 / 21,780 bytes，未达归档阈值；`STYLE_L1_SCENARIOS=business-formal-module-shells-desktop PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web style:l1` 通过，输出 `[style:l1] 通过，共验证 1 个场景`；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要为 `ok=true / readOnly=true / releaseReady=false / total=69 / ready=67 / guarded=1 / evidence-required=1 / blockingCategory=external-release-evidence-required`。
- 下一步：本地入口和该 L1 阻断均已从当前最高优先级里移除；无演示密码时继续 ordinary account / mobile task 的 no-write preflight、错误提示和 `__dev/testing` 入口口径收口；有演示密码后优先补 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report` 的真实登录 / 真实浏览器证据。
- 阻塞/风险：本轮 targeted L1 只证明 `business-formal-module-shells-desktop` 当前通过，不代表全量 `style:l1`、`qa:full`、真实后端读写、普通账号真实菜单投影、移动端真实任务动作、目标环境 release evidence、生产部署、真实客户导入、备份恢复、回滚演练或客户签收已完成；未提交、未推送。

## 2026-07-02 __dev/testing 客户配置 runtime 预设收口

- 完成：继续 ordinary account / mobile task no-write 与 `__dev/testing` 入口口径收口，不扩张 release evidence。三条无密码 preflight 仍按预期停在缺演示密码：`trial-account-rbac` 只证明 backend health 和静态角色投影，`trialDemoAccountBrowserSmoke` 只证明 yoyoosun 端口 / 菜单计划前置，`mobileWorkflowRuntimeBrowserSmoke` 只证明移动端模拟动作计划前置。本轮修正 `/__dev/testing` 的 `customer-config-package-runtime` 可复制预设，把 active revision 读回 no-write 命令里的 `<YYYY-MM-DD>` evidence 目录占位替换为当前已有只读目录 `deployments/yoyoosun/evidence/releases/2026-06-29`，避免复制后被 shell 当成重定向；同步 `docs/product/自动化测试策略.md` 并补测试拒绝该预设继续暴露 shell-redirection 占位。
- 验证：追加前 `progress.md` 为 83 行 / 23,422 bytes，未达归档阈值；`TRIAL_ACCOUNT_PASSWORD='' ERP_ROLE_DEMO_PASSWORD='' PATH=/usr/local/bin:$PATH node scripts/qa/trial-account-rbac.mjs --preflight-report output/trial-account-rbac/current-preflight.json` 生成 no-write 报告，blocker 为 `missing-trial-account-password-env`；`TRIAL_ACCOUNT_PASSWORD='' ERP_ROLE_DEMO_PASSWORD='' PATH=/usr/local/bin:$PATH node web/scripts/trialDemoAccountBrowserSmoke.mjs --preflight-report output/trial-demo-account-browser-smoke/current-preflight.json` 生成 no-write 报告，blocker 为 `missing-demo-password-env`；`MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD='' TRIAL_ACCOUNT_PASSWORD='' ERP_ROLE_DEMO_PASSWORD='' PATH=/usr/local/bin:$PATH node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report output/mobile-workflow-runtime-browser-smoke/current-preflight.json --run-id current` 生成 no-write 报告，blocker 为 `missing-demo-password-env`；`node --check web/src/erp/config/devTesting.mjs && node --check web/src/erp/config/devTesting.test.mjs && node --check scripts/qa/dev-entry-boundary.test.mjs` 通过；`node --test web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 15 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint src/erp/config/devTesting.mjs src/erp/config/devTesting.test.mjs` 通过；`node scripts/deploy/customer-config-release-readiness.mjs --manifest output/customers/yoyoosun/customer-config-runtime-manifest.json --evidence-dir deployments/yoyoosun/evidence/releases/2026-06-29 --release-report output/customers/yoyoosun/customer-config-release/customer-config-release-report.json --readback-preflight-report output/customers/yoyoosun/customer-config-readback-preflight.json --json` 只写 no-write 缺口报告，`readyForReadinessGate=false`；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`。
- 下一步：无演示密码时继续前端错误提示 / raw id / 技术字段可见性和 `__dev/testing` 预设一致性；有演示密码后再跑 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report` 补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮不读取密码 / token，不登录，不调用真实登录 JSON-RPC，不启动 Vite / Playwright，不创建 workflow 任务，不写数据库，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；`2026-06-29` evidence 目录只作为当前已有 release evidence 的只读输入，不表示目标 release evidence 已完成。

## 2026-07-02 前端错误提示与技术字段可见性复核

- 完成：继续本地试用闭环的用户可见层守卫，不扩张 release evidence。本轮未改业务代码；已复核正式页面错误提示、用户可见技术字段、raw id fallback、Dashboard / Workflow 任务来源、移动端任务模型和任务看板动作边界。`WorkflowBusinessModulePage`、`DashboardPage` 等命中 `task_status_key` / `owner_role_key` 的位置仅作为数据索引或请求参数，渲染和导出仍走中文 helper；打印工作台命中的 `result.message` 来自前端本地中文行编辑 / 合并校验，不是后端或浏览器原始异常。
- 验证：追加前 `progress.md` 为 90 行 / 27,261 bytes，未达归档阈值；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/common/utils/errorMessage.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/dashboardTaskDisplay.test.mjs` 通过 38 项；`node --test web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs` 通过 21 项；源码扫描未发现正式页面直接 `message.error(error.message)` 或 raw id / owner_role_key / task_status_key 用户可见泄露点；`node --test web/src/erp/config/menuPermissions.test.mjs web/src/erp/utils/adminProfileSync.test.mjs scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs` 通过 65 项，覆盖菜单权限、effective session 同步失败、动作投影、移动端任务和任务看板；`go test ./internal/biz ./internal/service -run 'Test.*(AdminMenu|RBAC|Workflow|ProcessRuntime|CustomerConfig|Shipment|Inventory|Quality)'` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false`，P0-P4 本地 ready，P5 `target-evidence-required`；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：无演示密码时继续 ordinary account / mobile task 的 no-write 报告口径、客户配置控制台 readback 缺口或 `__dev/testing` 入口一致性；有演示密码后再跑真实登录 / RBAC / 菜单投影 / effective session / 移动端动作报告。
- 阻塞/风险：本轮只证明当前静态守卫和本地单元测试覆盖的用户可见层边界，不证明真实浏览器完整页面、真实登录、后端 RBAC、active revision 目标读回、岗位任务真实提交、全量 `style:l1`、`qa:full`、生产部署、真实客户导入、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 yoyoosun 入口与 no-write 投影边界复核

- 完成：按 goal 重新读取长期目标和项目真源后，复核当前 yoyoosun 本地入口，不改业务代码、不扩张 release evidence。当前 5175 是本仓 Product Core dev placeholder，5176 是 `/Users/simon/projects/openai-oauth-api-service/web` 的 Vite 服务，5177 是本仓 yoyoosun 注入入口，5178 只是 `start:yoyoosun` / `preview:yoyoosun` 下一次会顺延使用的计划端口、当前未监听。5177 的 `/customer-config.js` 命中 `customerKey: "yoyoosun"`，`/customer-assets/yoyoosun/favicon-yoyoosun.svg` 返回 `image/svg+xml`；5175/5176 的 yoyoosun asset 均不是有效客户资产。无 token 调用 `customer_config.get_effective_session` 仍返回 `40302 未登录`，所以当前仍只能证明静态注入和 no-write 前置，不能证明普通账号 active revision / RBAC / 菜单投影。
- 验证：追加前 `progress.md` 为 97 行 / 29,936 bytes，未达归档阈值；`git fetch --prune && git status -sb --untracked-files=all` 通过，当前 `main...origin/main` 但工作区仍有大量未提交改动；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 P0-P4 本地 ready、P5 `target-evidence-required`；`pnpm --dir web start:yoyoosun -- --print-plan` 和 `pnpm --dir web preview:yoyoosun -- --print-plan --skip-build --skip-health-check` 均输出请求 5176 被占用并计划使用 5178；`pnpm --dir web audit:yoyoosun-entry -- --json` 输出 `yoyoosunPorts=[5177] / productCorePlaceholderPorts=[5175] / htmlFallbackPorts=[5175,5176] / backendHealth.ok=true`；对 5175-5178 的 `/customer-config.js` 和 yoyoosun favicon 执行 curl，结果与 audit 一致；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`trial-account-rbac`、`trialDemoAccountBrowserSmoke`、`mobileWorkflowRuntimeBrowserSmoke` 三个 no-write preflight 均写入当前报告并停在缺演示密码；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：无演示密码时继续收口客户配置 readback 缺口、`__dev/testing` 入口一致性或 no-write 证据口径；有演示密码后优先运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`，补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮不读取密码 / token，不登录，不启动 Vite / Playwright，不调用真实登录 JSON-RPC，不创建 workflow 任务，不写数据库，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；当前证据仍不能替代真实账号、真实浏览器、后端 RBAC、active revision 目标读回、完整 `style:l1`、`qa:full` 或外部 release evidence。

## 2026-07-02 yoyoosun 入口与测试入口一致性复核

- 完成：继续当前本地试用闭环，只复核 yoyoosun 本地入口、`__dev/testing` 预设、README 命令和 no-write preflight 口径，不改业务代码、不扩张 release evidence。`start:yoyoosun --print-plan`、`start:yoyoosun -- --print-plan`、`preview:yoyoosun --print-plan --skip-build --skip-health-check` 和 `preview:yoyoosun -- --print-plan --skip-build --skip-health-check` 均可执行；端口从 5176 起探测，遇本机 5176 被其他项目占用会输出实际可用端口。当前端口审计仍确认 5177 是本仓 yoyoosun 注入入口，5175 是 Product Core placeholder，5176 是 openai-oauth 前端，5178 当前未监听；后端 `8300/healthz` 可达。无登录态调用 `customer_config.get_effective_session` 使用字符串 JSON-RPC id 后返回 `40302 未登录`，所以本轮仍只证明本地入口和 no-write 前置，不证明真实 active revision、普通账号菜单投影或后端 RBAC。
- 验证：追加前 `progress.md` 为 104 行 / 33,093 bytes，未达归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false`，P0-P4 本地 ready，P5 为 target evidence 缺口；`node --test scripts/qa/dev-entry-boundary.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 15 项；`node --test web/scripts/yoyoosunEntryPlan.test.mjs web/devCustomerConfigPlugin.test.mjs` 通过 9 项；`node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs scripts/qa/trial-account-rbac.test.mjs` 通过 29 项；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`pnpm --dir web --silent audit:yoyoosun-entry -- --json` 输出 `yoyoosunPorts=[5177] / productCorePlaceholderPorts=[5175] / htmlFallbackPorts=[5175,5176] / backendHealth.ok=true`；三条无密码 preflight 均写入当前报告并停在缺演示密码，blocker 分别为 `missing-trial-account-password-env` 和 `missing-demo-password-env`。
- 下一步：本地入口和测试入口口径暂不作为阻断；无演示密码时继续客户配置 readback 缺口、错误提示 / raw id 守卫或移动端任务端 no-write 证据；有演示密码后再跑 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report`、`mobileWorkflowRuntimeBrowserSmoke --report`，补真实登录、普通客户角色菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮不读取密码 / token，不登录，不启动新的长期 Vite / Playwright，不创建 workflow 任务，不写数据库，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；当前 no-write 报告仍不能替代真实账号、真实浏览器、后端 RBAC、active revision 目标读回、完整 `style:l1`、`qa:full` 或外部 release evidence。

## 2026-07-02 移动端登录回跳 smoke 稳定性收口

- 完成：继续移动端任务端本地可验证闭环，不扩张 release evidence。本轮修复 `mobileAuthLoginRouteSmoke.mjs` 在切到“我的 / 登录与安全”后立即点击登出按钮时的浏览器回归不稳定点：按钮已渲染但 React 路由 settle / DOM 重渲染会让裸 `locator.click()` 撞上 detached DOM，导致全角色 smoke 偶发超时。修复只在 smoke 脚本内增加受控点击 helper，对导航上下文销毁、DOM detach 和元素稳定等待超时做有限重试；运行时页面、后端 RBAC、Workflow / Fact、客户配置和移动端业务逻辑不变。同步脚本测试锁住登出按钮继续走该 helper，避免后续退回裸点击。
- 验证：追加前 `progress.md` 为 111 行 / 36,364 bytes，未达归档阈值；首次 `PATH=/usr/local/bin:$PATH pnpm --dir web smoke:mobile-auth-login-route` 复现 purchase 岗位登出点击超时，错误为 `mobile-role-logout-button` 点击期间 `element was detached from the DOM`；修复后 `node --check web/scripts/mobileAuthLoginRouteSmoke.mjs && node --check web/scripts/mobileAuthLoginRouteSmoke.test.mjs` 通过；`node --test web/scripts/mobileAuthLoginRouteSmoke.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs` 通过 40 项；触达 web 脚本 ESLint 通过；`pnpm --dir web smoke:mobile-auth-login-route` 通过，覆盖 9 个岗位任务端角色和 phone / iPad 视口；`mobileAuthLoginRouteSmoke --preflight-report` 生成 no-write 报告，`readyForMockSmoke=true / blockers=[] / coversAllRolesByDefault=true / coversPhoneAndIpad=true / validatesLogoutAndBackNavigation=true`；`mobileWorkflowRuntimeBrowserSmoke --preflight-report` 仍因缺演示密码停在 `readyForRealSmoke=false / blockers=missing-demo-password-env`，但模拟任务计划 coverage 仍覆盖 `block / complete / reject / urge-only`、reason 必填、完成反馈、异常上报、evidence refs 和内部 notification hints；4193 / 4195 无遗留监听进程。
- 下一步：无演示密码时继续移动端任务动作的 no-write / mock 证据、客户配置 readback 缺口或错误提示 / raw id 守卫；有演示密码后再运行 `mobileWorkflowRuntimeBrowserSmoke --report`、`trial-account-rbac --report` 和 `trialDemoAccountBrowserSmoke --report`，补真实登录、后端 RBAC / effective session、岗位任务动作提交和完成反馈 / 内部通知展示证据。
- 阻塞/风险：本轮不读取密码 / token，不调用真实登录 JSON-RPC，不创建 workflow 任务，不写数据库，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；mock browser smoke 证明的是本地前端路由、登录回跳、任务 UI、通知线索、登出和视口布局，不证明真实账号、真实后端 RBAC、active revision 目标读回、真实移动端任务动作或外部 release evidence。

## 2026-07-02 yoyoosun 端口审计顺延范围收口

- 完成：继续 yoyoosun 本地入口 / customer-config 投影诊断主线，不扩张 release evidence。当前端口复核显示 5175 是本仓 Product Core placeholder，5176 是 `/Users/simon/projects/openai-oauth-api-service/web`，5177 是本仓 yoyoosun 注入入口，5178 / 5179 当前未监听；`start:yoyoosun -- --print-plan` 和 `preview:yoyoosun -- --print-plan --skip-build --skip-health-check` 当前都会因 5176 占用而计划使用 5178。为避免后续 dev / preview 已占用 5178 时继续顺延到 5179 却被默认审计漏掉，本轮把 `audit:yoyoosun-entry` 默认检查端口从 `5175,5176,5177,5178` 扩到 `5175,5176,5177,5178,5179`，并同步 web README、web scripts README 和 yoyoosun 试用手册的只读诊断口径。该改动只增强本地端口归属 / 静态配置 / asset 诊断，不调用 JSON-RPC，不证明后端 active revision。
- 验证：追加前 `progress.md` 为 118 行 / 39,485 bytes，未达归档阈值；`pnpm --dir web start:yoyoosun -- --print-plan` 通过并输出计划端口 5178；`pnpm --dir web preview:yoyoosun -- --print-plan --skip-build --skip-health-check` 通过并输出计划端口 5178；`pnpm --dir web --silent audit:yoyoosun-entry -- --json` 通过，`checkedPorts=[5175,5176,5177,5178,5179] / yoyoosunPorts=[5177] / productCorePlaceholderPorts=[5175] / htmlFallbackPorts=[5175,5176] / backendHealth.ok=true`；`node --check web/scripts/yoyoosunLocalEntryAudit.mjs && node --check web/scripts/yoyoosunEntryPlan.test.mjs` 通过；`node --test web/scripts/yoyoosunEntryPlan.test.mjs scripts/qa/trial-role-entry-docs.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 12 项；触达 web 脚本 ESLint 通过；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node --test scripts/qa/docs-inventory.test.mjs` 和 `git diff --check` 通过。
- 下一步：yoyoosun 静态入口诊断范围已补齐；无演示密码时继续客户配置 readback no-write 缺口、普通账号菜单投影 preflight 或移动端任务端 no-write 证据。有演示密码后优先运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report`、`mobileWorkflowRuntimeBrowserSmoke --report`，补真实登录、普通客户角色菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮不读取密码 / token，不登录，不启动新的长期 Vite / Playwright，不调用真实登录 JSON-RPC，不创建 workflow 任务，不写数据库，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；端口审计通过仍不能替代真实账号、真实浏览器、后端 RBAC、active revision 目标读回、完整 `style:l1`、`qa:full` 或外部 release evidence。

## 2026-07-02 yoyoosun 端口审计 CLI 帮助口径补齐

- 完成：继续 yoyoosun 本地入口 / no-write preflight 闭环，不扩张 release evidence。复核发现 `audit:yoyoosun-entry` 默认扫描和 README 已覆盖 `5175,5176,5177,5178,5179`，但 `yoyoosunLocalEntryAudit.mjs --help` 仍展示旧示例 `5175,5176,5177,5178`；本轮只修正 CLI 帮助文本，并新增测试锁住帮助输出包含 5179，避免后续复制命令漏查第二顺延端口。
- 验证：追加前 `progress.md` 为 125 行 / 42,585 bytes，未达归档阈值；`node --check web/scripts/yoyoosunLocalEntryAudit.mjs && node --check web/scripts/yoyoosunEntryPlan.test.mjs` 通过；`node --test web/scripts/yoyoosunEntryPlan.test.mjs scripts/qa/trial-role-entry-docs.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 13 项；触达 web 脚本 ESLint 通过；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`pnpm --dir web --silent audit:yoyoosun-entry -- --json` 输出 `checkedPorts=[5175,5176,5177,5178,5179] / yoyoosunPorts=[5177] / productCorePlaceholderPorts=[5175] / htmlFallbackPorts=[5175,5176] / backendHealth.ok=true`；三条无密码 preflight 均生成 no-write 报告并按预期停在缺演示密码；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`node --test scripts/qa/docs-inventory.test.mjs` 和 `git diff --check` 通过。
- 下一步：无演示密码时继续客户配置 readback no-write 缺口、普通账号菜单投影 preflight、移动端任务端 no-write 证据或错误提示 / raw id 守卫；有演示密码后再运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report`、`mobileWorkflowRuntimeBrowserSmoke --report` 补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮只改本地诊断 CLI 帮助和测试，不读取密码 / token，不登录，不启动新的长期 Vite / Playwright，不调用 JSON-RPC，不创建 workflow 任务，不写数据库，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；CLI 口径一致不证明真实账号、真实浏览器、后端 RBAC、active revision 目标读回、完整 `style:l1`、`qa:full` 或外部 release evidence。

## 2026-07-02 no-write preflight 内嵌端口审计口径收口

- 完成：继续 yoyoosun 本地入口和普通账号 / 移动端 no-write 前置证据闭环，不扩张 release evidence。复核发现独立 `audit:yoyoosun-entry` 已默认检查 `5175,5176,5177,5178,5179`，但 `trialDemoAccountBrowserSmoke` 和 `mobileWorkflowRuntimeBrowserSmoke` 的 preflight 报告内嵌审计仍各自手写 `5175..5178`，会漏掉第二顺延端口。已将 `yoyoosunLocalEntryAudit.mjs` 的默认端口常量导出，并让两个 preflight 脚本复用同一真源；测试同步锁住两个报告都包含 5179。该改动只影响 no-write 诊断报告，不登录、不调用 JSON-RPC、不写库。
- 验证：追加前 `progress.md` 为 132 行 / 45,277 bytes，未达归档阈值；`node --check web/scripts/yoyoosunLocalEntryAudit.mjs && node --check web/scripts/trialDemoAccountBrowserSmoke.mjs && node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs && node --check web/scripts/trialDemoAccountBrowserSmoke.test.mjs && node --check scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过；`node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/scripts/yoyoosunEntryPlan.test.mjs` 通过 27 项；触达 web 脚本 ESLint 通过；两个 no-write preflight 重新落盘后均输出 `checkedPorts=[5175,5176,5177,5178,5179] / yoyoosunPorts=[5177] / productCorePlaceholderPorts=[5175] / htmlFallbackPorts=[5175,5176] / readyForStaticYoyoosunPreview=true / blockers=missing-demo-password-env`；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`node --test scripts/qa/docs-inventory.test.mjs` 和 `git diff --check` 通过。
- 下一步：无演示密码时继续客户配置 readback no-write 缺口、普通账号菜单投影 preflight、移动端任务端 no-write 证据或错误提示 / raw id 守卫；有演示密码后再运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report`、`mobileWorkflowRuntimeBrowserSmoke --report`，补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮不读取密码 / token，不登录，不启动新的长期 Vite / Playwright，不调用 JSON-RPC，不创建 workflow 任务，不写数据库，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；preflight 端口口径一致仍不能证明真实账号、真实浏览器、后端 RBAC、active revision 目标读回、完整 `style:l1`、`qa:full` 或外部 release evidence。

## 2026-07-02 业务可见技术字段属性守卫补强

- 完成：继续错误提示 / raw id / 技术字段用户可见层收口，不扩张 release evidence。本轮只补测试守卫，在 `userVisibleTechnicalFields.test.mjs` 中新增业务可见属性扫描，覆盖 `label / placeholder / title / exportTitle / createLabel / description / aria-label / message` 等页面可见字符串，禁止正式业务页面把 `idempotency_key / owner_role_key / task_status_key / source_type / source_id / *_id / *_key / payload` 这类 snake_case 技术字段直接写进用户文案。首次扫描命中 dev-only `DevCustomerConfigPage` 中用于说明禁止新增 `tenant_id` 的治理文案；已按边界把该新增守卫收窄为正式业务可见层，不误伤本地 `__dev` 规则说明。
- 验证：追加前 `progress.md` 为 139 行 / 48,361 bytes，未达 600 行或 80KB 归档阈值；`node --check web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/common/utils/errorMessage.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/utils/dashboardTaskDisplay.test.mjs` 通过 39 项；触达测试文件 ESLint 通过；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`。
- 下一步：无演示密码时继续客户配置 readback no-write 缺口、普通账号菜单投影 preflight、移动端任务端 no-write 证据或 `__dev/testing` 入口一致性；有演示密码后再运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`，补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮只补正式业务可见层静态守卫，不改运行时页面、不改后端 RBAC / usecase / schema / migration、不读取密码 / token、不登录、不调用 JSON-RPC、不启动 Vite / Playwright、不创建 workflow 任务、不写数据库、不发布 / 激活 / 回滚客户配置、不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；该守卫不证明真实浏览器页面、真实账号菜单投影、active revision 目标读回、完整 `style:l1`、`qa:full` 或外部 release evidence 已完成。

## 2026-07-02 试用浏览器 preflight 建议入口收口

- 完成：继续 yoyoosun 本地入口与普通账号 / 移动端 no-write preflight 闭环，不扩张 release evidence。当前端口审计仍确认 5177 是 yoyoosun 注入入口，5175 是 Product Core placeholder，5176 是其他项目端口；本轮在 `trialDemoAccountBrowserSmoke` 和 `mobileWorkflowRuntimeBrowserSmoke` 的 preflight 报告中补充 `auditedYoyoosunURLs` 与 `suggestedExternalBaseURL`，让后续具备演示密码时可以直接从 no-write 报告拿到已审计的真实 smoke 前端 URL，而不是人工从 `yoyoosunPorts` 推断。报告仍保持 no-write，不读取密码、不登录、不调用 JSON-RPC、不启动浏览器、不写数据库。
- 验证：追加前 `progress.md` 为 146 行 / 51,183 bytes，未达 600 行或 80KB 归档阈值；`node --check web/scripts/trialDemoAccountBrowserSmoke.mjs && node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs && node --check web/scripts/trialDemoAccountBrowserSmoke.test.mjs && node --check scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过；`node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/scripts/yoyoosunEntryPlan.test.mjs` 通过 27 项；触达 web 脚本 ESLint 通过；两条 no-write preflight 重新生成当前报告，均输出 `ready=false / blockers=missing-demo-password-env / yoyoosunPorts=[5177] / suggestedExternalBaseURL=http://localhost:5177/erp`；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / blockingCategory=external-release-evidence-required`；`git diff --check` 通过。
- 下一步：无演示密码时继续客户配置 readback no-write 缺口、普通账号菜单投影 preflight、移动端任务端 no-write 证据或 `__dev/testing` 入口一致性；有演示密码后使用报告中的 `suggestedExternalBaseURL` 运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`，补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮只增强 no-write preflight 报告字段和对应测试，不改业务页面、不改后端 RBAC / usecase / schema / migration、不读取密码 / token、不登录、不调用 JSON-RPC、不创建 workflow 任务、不写数据库、不发布 / 激活 / 回滚客户配置、不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；`suggestedExternalBaseURL` 只证明本机静态 yoyoosun 配置 / asset 命中，不证明真实账号、真实浏览器、后端 active revision、完整 `style:l1`、`qa:full` 或外部 release evidence。

## 2026-07-02 试用浏览器 preflight 真实 smoke 命令模板收口

- 完成：继续 yoyoosun 本地入口与普通账号 / 移动端 no-write preflight 闭环，不扩张 release evidence。本轮在 `trialDemoAccountBrowserSmoke` 和 `mobileWorkflowRuntimeBrowserSmoke` 的 preflight 报告中补充脱敏 `suggestedRealSmokeCommand`，命令自动使用已审计的 `http://localhost:5177/erp` 作为真实 smoke 前端入口，并用 `<local-demo-password>` 占位提醒必须由本地显式提供密码。报告仍在缺密码时保持 `ready=false`，`nextCommand` 仍要求先解决 blocker，避免把 no-write 报告误读为真实登录 / RBAC / 岗位动作验收。
- 验证：追加前 `progress.md` 为 153 行 / 54,309 bytes，未达 600 行或 80KB 归档阈值；两个 no-write preflight 重新生成当前报告，均输出 `ready=false / blockers=missing-demo-password-env / suggestedExternalBaseURL=http://localhost:5177/erp`，并分别给出 `TRIAL_ACCOUNT_PASSWORD='<local-demo-password>' TRIAL_BROWSER_SMOKE_BASE_URL='http://localhost:5177/erp' ... trialDemoAccountBrowserSmoke.mjs --report ...` 与 `MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD='<local-demo-password>' MOBILE_WORKFLOW_BROWSER_SMOKE_BASE_URL='http://localhost:5177/erp' ... mobileWorkflowRuntimeBrowserSmoke.mjs --report ...`；`node --check web/scripts/trialDemoAccountBrowserSmoke.mjs && node --check web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs && node --check web/scripts/trialDemoAccountBrowserSmoke.test.mjs && node --check scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过；`node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/scripts/yoyoosunEntryPlan.test.mjs` 通过 27 项；触达 web 脚本 ESLint 通过；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true`，外部目标环境 evidence 不升级为本轮主任务；`git diff --check` 通过。
- 下一步：无演示密码时继续客户配置 readback no-write 缺口、普通账号菜单投影 preflight、移动端任务端 no-write 证据、`__dev/testing` 入口一致性或错误提示 / raw id 守卫；有演示密码后按 preflight 报告中的 `suggestedRealSmokeCommand` 运行 `trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`，并继续运行 `trial-account-rbac --report` 补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮只增强 no-write preflight 报告字段和对应测试，不改业务页面、不改样式、不改后端 RBAC / usecase / schema / migration、不读取密码 / token、不登录、不调用 JSON-RPC、不启动浏览器、不创建 workflow 任务、不写数据库、不发布 / 激活 / 回滚客户配置、不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；`suggestedRealSmokeCommand` 只是受控入口模板，不证明真实账号、真实浏览器、后端 RBAC、active revision 目标读回、移动端真实动作、完整 `style:l1`、`qa:full` 或外部 release evidence。

## 2026-07-02 试用账号 RBAC preflight 真实检查命令模板收口

- 完成：继续普通账号 RBAC / 菜单投影 no-write 前置证据闭环，不扩张 release evidence。前两轮浏览器和移动端 preflight 已给出脱敏真实 smoke 命令，本轮把 `trial-account-rbac` 的 preflight 报告同步补齐 `suggestedRealRBACCommand`，命令显式带上当前脱敏后端地址 `http://127.0.0.1:8300`、`<local-demo-password>` 密码占位和 `--report output/trial-account-rbac/report.json`。缺密码时报告仍保持 `readyForRealRBACCheck=false`，`nextCommand` 仍要求先解决 blocker，避免把 no-write 报告误读为真实 `admin_login + me`、RBAC、active revision 或菜单投影验收。
- 验证：追加前 `progress.md` 为 160 行 / 57,872 bytes，未达 600 行或 80KB 归档阈值；`node --check scripts/qa/trial-account-rbac.mjs && node --check scripts/qa/trial-account-rbac.test.mjs` 通过；`node --test scripts/qa/trial-account-rbac.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 11 项；重新生成 `output/trial-account-rbac/current-preflight.json`，输出 `ready=false / blockers=missing-trial-account-password-env / backendEndpointAlias=http://127.0.0.1:8300 / passwordEnvPresent=false`，并给出 `TRIAL_ACCOUNT_PASSWORD='<local-demo-password>' TRIAL_ACCOUNT_BACKEND_URL='http://127.0.0.1:8300' ... trial-account-rbac.mjs --report output/trial-account-rbac/report.json`；`node --test scripts/qa/docs-inventory.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`git diff --check` 通过。根目录没有 `package.json`，`pnpm --dir web exec eslint ../scripts/qa/...` 会把根 `scripts/qa` 文件视为 web base 外部文件并仅报 ignored warning，本轮不把该 warning 当作有效 lint 证据。
- 下一步：无演示密码时继续客户配置 readback no-write 缺口、移动端任务端 no-write 证据、`__dev/testing` 入口一致性或错误提示 / raw id 守卫；有演示密码后按三份 preflight 报告中的命令依次运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`，补真实登录、普通账号菜单投影、effective session 脱敏读回、岗位任务动作和完成反馈 / 内部通知证据。
- 阻塞/风险：本轮只增强 no-write preflight 报告字段和对应测试，不改业务页面、不改样式、不改后端 RBAC / usecase / schema / migration、不读取密码 / token、不登录、不调用 JSON-RPC、不启动浏览器、不创建 workflow 任务、不写数据库、不发布 / 激活 / 回滚客户配置、不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；`suggestedRealRBACCommand` 只是受控入口模板，不证明真实账号、真实后端 RBAC、customer config active revision、桌面菜单投影、岗位任务端、完整 `style:l1`、`qa:full` 或外部 release evidence。

## 2026-07-02 移动端登录回跳 preflight mock smoke 命令模板收口

- 完成：继续移动端任务端本地可验证闭环，不扩张 release evidence。`mobileAuthLoginRouteSmoke` 的输入模板和 no-write preflight 报告补齐 `suggestedMockSmokeCommand` 与 `notProvenByThisPreflight / notProvenByThisTemplate`，明确 mock smoke 的后续命令是 `pnpm --dir web smoke:mobile-auth-login-route`，并机器可读地列出不证明真实后端 RBAC、真实账号登录、customer config active revision 读回、桌面菜单投影、workflow usecase 写入或目标环境 release evidence。报告仍保持 `readyForMockSmoke=true` 时才把 `nextCommand` 指向 mock smoke；阻断时仍先要求解决 blockers。
- 验证：追加前 `progress.md` 为 167 行 / 61,319 bytes，未达 600 行或 80KB 归档阈值；`node --check web/scripts/mobileAuthLoginRouteSmoke.mjs && node --check web/scripts/mobileAuthLoginRouteSmoke.test.mjs` 通过；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint scripts/mobileAuthLoginRouteSmoke.mjs scripts/mobileAuthLoginRouteSmoke.test.mjs` 通过；`node --test web/scripts/mobileAuthLoginRouteSmoke.test.mjs` 通过 6 项，仍有既有 `MODULE_TYPELESS_PACKAGE_JSON` warning；重新生成 `output/mobile-auth-login-route-smoke/current-preflight.json`，输出 `readyForMockSmoke=true / blockers=[] / suggestedMockSmokeCommand=PATH=/usr/local/bin:$PATH pnpm --dir web smoke:mobile-auth-login-route / routeCoverage.coversAllRolesByDefault=true / routeCoverage.coversPhoneAndIpad=true`；`node --test scripts/qa/dev-entry-boundary.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 5 项；`node --test scripts/qa/formal-frontend-customer-config-boundary.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs` 通过 17 项；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`git diff --check` 通过。
- 下一步：无演示密码时继续客户配置 readback no-write 缺口、`__dev/testing` 入口一致性或用户可见错误 / raw id 守卫；有演示密码后按三份 preflight 报告中的真实命令补 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`，并可单独执行 `pnpm --dir web smoke:mobile-auth-login-route` 复核 mock 移动端登录回跳。
- 阻塞/风险：本轮只增强移动端登录回跳 smoke 的 no-write 报告字段和测试，不改业务页面、不改样式、不改后端 RBAC / usecase / schema / migration、不读取密码 / token、不登录真实账号、不调用真实 JSON-RPC、不启动浏览器、不创建 workflow 任务、不写数据库、不发布 / 激活 / 回滚客户配置、不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；mock smoke 命令模板不证明真实账号、真实后端 RBAC、active revision 目标读回、真实移动端任务动作、完整 `style:l1`、`qa:full` 或外部 release evidence。

## 2026-07-02 yoyoosun 本地入口审计报告落盘边界收口

- 完成：继续 yoyoosun 本地入口 / customer-config 投影诊断主线，不扩张 release evidence。`audit:yoyoosun-entry` 新增 `--report`，允许把本机 no-write 端口归属、客户配置注入、yoyoosun asset、后端 health 和下一步建议落到 `output/yoyoosun-local-entry-audit/*.json`；同时禁止报告写入 `deployments/**`，避免本地诊断文件被误当成目标环境 release evidence。当前重新生成的本地报告仍确认 5177 是 yoyoosun 注入入口，5175 是 Product Core placeholder，5176 是其他项目 HTML fallback，5178 / 5179 当前未监听，8300 health ok；无 token 时 `customer_config.get_effective_session` 仍为 `40302 未登录`，所以本轮不证明 active revision、普通账号菜单投影或真实 RBAC。
- 验证：追加前 `progress.md` 为 174 行 / 64,538 bytes，未达 600 行或 80KB 归档阈值；`node --check web/scripts/yoyoosunLocalEntryAudit.mjs && node --check web/scripts/yoyoosunEntryPlan.test.mjs` 通过；`node --test web/scripts/yoyoosunEntryPlan.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 11 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint scripts/yoyoosunLocalEntryAudit.mjs scripts/yoyoosunEntryPlan.test.mjs` 通过；`pnpm --dir web audit:yoyoosun-entry -- --json --report output/yoyoosun-local-entry-audit/current.json` 通过并输出 `writesReport=true / checkedPorts=[5175,5176,5177,5178,5179] / yoyoosunPorts=[5177] / productCorePlaceholderPorts=[5175] / htmlFallbackPorts=[5175,5176] / readyForStaticYoyoosunPreview=true`；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`node --test scripts/qa/docs-inventory.test.mjs` 和 `git diff --check` 通过。
- 下一步：无演示密码时继续客户配置 readback no-write 缺口、`__dev/testing` 入口一致性、普通账号菜单投影 preflight 或用户可见错误 / raw id 守卫；有演示密码后按现有 preflight 报告命令运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`，补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮只增强本机 no-write 端口审计报告落盘和文档口径，不读取密码 / token，不登录，不启动新的长期 Vite / Playwright，不调用 JSON-RPC，不创建 workflow 任务，不写数据库，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；`output/**` 报告不是 release evidence，不能替代真实账号、真实浏览器、后端 RBAC、active revision 目标读回、完整 `style:l1`、`qa:full` 或外部目标环境证据。

## 2026-07-02 effective session 本地无凭据读回探针收口

- 完成：继续 yoyoosun 本地入口 / customer-config 投影诊断主线，不扩张 release evidence。新增 `scripts/qa/customer-config-effective-session-probe.mjs` 作为本机 no-write 探针：先检查 `8300/healthz`，再在不携带 Authorization、不读取 token / 密码、不登录、不写数据库的前提下调用 `customer_config.get_effective_session`，并可把结果写入 `output/customers/yoyoosun/customer-config-effective-session-probe/current.json`；报告禁止写入 `deployments/**`，避免被误当成目标环境 release evidence。当前本机结果为 health ok、JSON-RPC 返回 `40302 未登录`，所以只证明后端可达和鉴权边界存在，不证明 active revision、普通账号菜单投影或后端 RBAC。同步 `scripts/README.md`、`scripts/qa/README.md`、`web/README.md`、yoyoosun 试用手册、trial role docs 守卫，以及 `fast.sh` / `strict.sh` 的无后端单测入口。
- 验证：追加前 `progress.md` 为 181 行 / 67,797 bytes，未达 600 行或 80KB 归档阈值；`node --check scripts/qa/customer-config-effective-session-probe.mjs && node --check scripts/qa/customer-config-effective-session-probe.test.mjs` 通过；`node --test scripts/qa/customer-config-effective-session-probe.test.mjs` 通过 3 项；`node --test scripts/qa/customer-config-effective-session-probe.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 4 项；`node scripts/qa/customer-config-effective-session-probe.mjs --json --report output/customers/yoyoosun/customer-config-effective-session-probe/current.json` 通过并输出 `auth_required / resultCode=40302 / activeRevisionVerified=false / blockers=missing-authenticated-admin-session`；`bash -n scripts/qa/fast.sh scripts/qa/strict.sh` 通过；`node --test scripts/qa/customer-config-effective-session-probe.test.mjs scripts/qa/trial-role-entry-docs.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 5 项，`docs-inventory` 扫描 `markdownFiles=262`；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`。根目录没有 `package.json`，`pnpm --dir web exec eslint ../scripts/qa/...` 仅把根 `scripts/qa` 文件报为 web base 外部 ignored warning，本轮不把该 warning 当作有效 lint 证据。
- 下一步：无演示密码时继续 `__dev/testing` 入口一致性、普通账号菜单投影 preflight、移动端任务端 no-write 证据或用户可见错误 / raw id 守卫；有演示密码或受控登录态后再运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report`、`mobileWorkflowRuntimeBrowserSmoke --report`，并补 authenticated effective session 脱敏读回，验证普通账号菜单投影、active revision 来源和岗位任务动作。
- 阻塞/风险：本轮不读取密码 / token，不登录，不启动新的长期 Vite / Playwright，不创建 workflow 任务，不写数据库，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；无凭据探针不能替代真实账号、真实浏览器、后端 RBAC、active revision 目标读回、完整 `style:l1`、`qa:full` 或外部 release evidence。

## 2026-07-02 __dev/testing effective session 探针入口同步

- 完成：继续 customer-config / effective session 本地 no-write 诊断闭环，不扩张 release evidence。上一轮新增的 `customer-config-effective-session-probe.mjs` 已进入 README、QA 子目录说明和 yoyoosun 试用手册；本轮补齐 `/__dev/testing` 的 `customer-config-package-runtime` 可复制预设，把无 Authorization 的 `get_effective_session` 探针命令放到 runtime manifest preview 与 release readiness preflight 之间，方便本地开发者在确认 yoyoosun 静态入口后直接记录 `40302 未登录` / 缺真实登录证据边界。同步 `web/src/erp/config/devTesting.test.mjs`、`scripts/qa/dev-entry-boundary.test.mjs` 和 `docs/product/自动化测试策略.md`，明确该探针只做本地健康检查和未授权 JSON-RPC，不登录、不读 token、不证明 active revision。
- 验证：追加前 `progress.md` 为 188 行 / 71,467 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`node --check web/src/erp/config/devTesting.mjs && node --check web/src/erp/config/devTesting.test.mjs && node --check scripts/qa/dev-entry-boundary.test.mjs` 通过；`node --test web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs scripts/qa/customer-config-effective-session-probe.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 18 项；`node --test scripts/qa/customer-package-lint.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs scripts/deploy/customer-config-release-execute.test.mjs scripts/deploy/customer-config-release-readiness.test.mjs scripts/deploy/run-smoke-script.test.mjs` 通过 75 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint src/erp/config/devTesting.mjs src/erp/config/devTesting.test.mjs` 通过；`node scripts/qa/customer-config-effective-session-probe.mjs --json --report output/customers/yoyoosun/customer-config-effective-session-probe/current.json` 通过并输出 `auth_required / resultCode=40302 / activeRevisionVerified=false / blockers=missing-authenticated-admin-session`；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过，`modules=15 / roles=9 / entitlements=346`；`node scripts/qa/customer-config-boundaries.mjs` 通过。
- 下一步：无演示密码时继续普通账号菜单投影 preflight、移动端任务端 no-write 证据或用户可见错误 / raw id 守卫；有演示密码或受控登录态后再运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report`、`mobileWorkflowRuntimeBrowserSmoke --report`，并补 authenticated effective session 脱敏读回，验证普通账号菜单投影、active revision 来源和岗位任务动作。
- 阻塞/风险：本轮只同步 dev-only 测试入口和测试策略，不改正式菜单、不改后端 RBAC / usecase / schema / migration、不读取密码 / token、不登录、不启动浏览器、不写数据库、不发布 / 激活 / 回滚客户配置、不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；`/__dev/testing` 可复制命令通过不等于真实账号、真实浏览器、后端 RBAC、active revision 目标读回、完整 `style:l1`、`qa:full` 或外部 release evidence 完成。

## 2026-07-02 yoyoosun 本地入口与投影守卫复核

- 完成：继续 yoyoosun 本地入口和 customer-config 投影诊断，不重启旧计划、不扩张 release evidence。本轮复跑 `start:yoyoosun -- --print-plan`、`preview:yoyoosun -- --print-plan` 和 `audit:yoyoosun-entry -- --json --report output/yoyoosun-local-entry-audit/current.json`，确认 5175 是本仓 Product Core placeholder，5176 是 `/Users/simon/projects/openai-oauth-api-service/web`，5177 是本仓 yoyoosun 注入入口，5178 / 5179 当前未监听；5177 的 `/customer-config.js` 命中 `customerKey=yoyoosun`，yoyoosun favicon 返回 `image/svg+xml`，后端 `8300/healthz` 可达。继续复跑无凭据 `get_effective_session` 探针，结果仍为 `40302 未登录 / missing-authenticated-admin-session`，所以本轮只证明本地静态注入和鉴权边界，不证明 active revision、普通账号菜单投影或真实 RBAC。
- 验证：追加前 `progress.md` 为 195 行 / 75,052 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 仍为 `ok=true / readOnly=true / releaseReady=false`，目标 release evidence 继续后置；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web start:yoyoosun -- --print-plan` 和 `PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web preview:yoyoosun -- --print-plan` 均通过并输出 5178 计划端口；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web --silent audit:yoyoosun-entry -- --json --report output/yoyoosun-local-entry-audit/current.json` 通过，摘要为 `yoyoosunPorts=[5177] / productCorePlaceholderPorts=[5175] / htmlFallbackPorts=[5175,5176] / readyForStaticYoyoosunPreview=true`；`node scripts/qa/customer-config-effective-session-probe.mjs --json --report output/customers/yoyoosun/customer-config-effective-session-probe/current.json` 通过，输出 `auth_required / resultCode=40302 / activeRevisionVerified=false`；角色菜单 / entry / seed / workflowStatus / adminProfileSync 测试通过 47 项；错误提示 / raw id / source_type 用户可见守卫通过 39 项；移动端 workflow runtime smoke 边界、mobileRoleTaskModel 和 workflowTaskBoard 测试通过 34 项。
- 下一步：无演示密码时继续按本地守卫挑下一个可验证缺口，例如 `__dev/testing` / 客户配置 readback no-write 口径、普通账号菜单投影 preflight、移动端任务端 no-write 证据或用户可见错误 / raw id 守卫；有演示密码或受控登录态后，按现有 preflight 报告命令运行 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`，补真实登录、普通账号菜单投影、effective session 脱敏读回和岗位任务动作证据。
- 阻塞/风险：本轮只复核本地 no-write 端口归属、静态配置注入和前端投影 / 用户可见守卫，不读取密码 / token，不登录，不启动长期 Vite / Playwright，不创建 workflow 任务，不写数据库，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；当前仍不能证明真实账号、真实浏览器、后端 RBAC、authenticated active revision 读回、完整 `style:l1`、`qa:full` 或外部 release evidence。

## 2026-07-02 L1 与移动端登录回跳本地回归复核

- 完成：继续长期 goal 的本地可验证切片，不扩张 release evidence。yoyoosun 静态入口诊断已收口后，按优先级复跑此前记录过的 `business-formal-module-shells-desktop` L1 阻断，当前已通过；随后执行无密码可跑的移动端登录回跳 mock smoke，覆盖 9 个岗位 `/m/<role>/tasks` 路由、登录回跳和岗位入口本地浏览器链路。
- 验证：追加前 `progress.md` 为 202 行 / 78,429 bytes，未达 600 行或 80KB 归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web smoke:mobile-auth-login-route` 通过，验证 boss / sales / purchase / production / warehouse / finance / pmc / quality / engineering 9 个岗位任务端角色。
- 下一步：无演示密码时继续选择 no-write / mock 可证明的本地缺口，例如客户配置 readback 口径、普通账号菜单投影 preflight 或移动端任务动作 preflight；有演示密码后再运行真实 `trial-account-rbac --report`、`trialDemoAccountBrowserSmoke --report` 和 `mobileWorkflowRuntimeBrowserSmoke --report`。
- 阻塞/风险：本轮只复核本地浏览器 mock / L1 证据，不读取密码 / token，不真实登录，不调用真实业务 JSON-RPC，不创建 workflow 任务，不写数据库，不改后端 RBAC / usecase / schema / migration，不发布 / 激活 / 回滚客户配置，不做真实客户导入、生产部署、目标环境 migration、备份恢复、回滚演练、客户签收、提交或推送；mock smoke 不证明真实账号、真实后端 RBAC、authenticated active revision 读回、真实岗位任务动作或外部 release evidence。

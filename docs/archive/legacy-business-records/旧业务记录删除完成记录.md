Doc Type / 文档类型: Business Records Cutover Record / business_records 删除完成记录

Archive Status / 归档状态: Historical Evidence Only / 仅作历史证据
Status / 状态: Completed / 已完成
Runtime Implemented / 运行时已实现: Yes / 是
Ent Schema Implemented / Ent Schema 已实现: Yes / 是
Migration Implemented / Migration 已实现: Yes / 是
Current Implementation Source of Truth / 当前实现真源: No / 否

# 业务记录删除完成记录 / business_records Cutover Record

本文记录旧 `business_records / business_record_items / business_record_events` 表族从当前运行时退出并物理删除的执行结果。当前实现真源仍以 `docs/当前真源与交接顺序.md`、Ent schema、Atlas migration、代码和测试为准。

## 结论先行

2026-06-12 已完成当前开发库删除闭环：

- 旧表族 `business_records / business_record_items / business_record_events` 已从 Ent schema 和 generated code 中移除。
- Atlas migration `20260612112337` 已生成并应用，删除三张旧表，同时删除 `purchase_receipts / purchase_returns / purchase_receipt_adjustments` 上的 `business_record_id` 兼容字段。
- 普通 `business` JSON-RPC 域只保留 `dashboard_stats`；旧 `list_records / create_record / update_record / delete_records / restore_record` 已退出运行时并按 unknown method 处理。
- debug seed / cleanup 不再写旧通用业务记录，只生成 Workflow 调试任务、任务事件和业务状态投影。
- 前端旧通用业务页只保留退出提示，不再查询、创建、更新、删除、恢复或打印旧记录。
- 岗位任务端不再通过旧业务记录 API 查询 source；任务详情只使用任务自身的 `source_*` 字段和 payload 快照。

## 删除前数据备份

本次迁移前，当前开发库内旧表族数据先导出到本地 evidence：

`output/business-records-delete-20260612/`

| 文件 | 内容 |
| --- | --- |
| `counts_before_drop.txt` | 删除前计数 |
| `business_records.jsonl` | 旧表头 JSONL 快照 |
| `business_record_items.jsonl` | 旧明细 JSONL 快照 |
| `business_record_events.jsonl` | 旧事件 JSONL 快照 |

删除前计数：

| 表 | 行数 |
| --- | ---: |
| `business_records` | 49 |
| `business_record_items` | 3 |
| `business_record_events` | 55 |

说明：本地 `pg_dump` 因客户端版本低于 PostgreSQL 服务端版本未能使用，改用 `psql` JSONL 导出保存删除前 evidence。该 evidence 只覆盖当前开发库，不能直接代表客户库或生产库。

## 当前边界

- 不恢复 `business_records` 作为运行时真源、历史归档表、debug fixture 或 source snapshot 主路径。
- 不把删除前 JSONL evidence 接回普通 API、前端页面、菜单、RBAC 或 Workflow / Fact usecase。
- 不用旧表族伪造库存、出货、质检、财务、应收、开票、应付、对账或收付款事实。
- 若未来需要客户可见历史归档页，必须另做归档模型评审，不能直接复活旧三表。
- 目标库或生产库执行同一 migration 前，仍必须按库单独做备份、migration status 检查和应用后验证。

## 验证口径

本次删除闭环应至少通过：

```bash
cd /Users/simon/projects/plush-toy-erp/server
make migrate_status
go test ./...

cd /Users/simon/projects/plush-toy-erp/web
pnpm lint
pnpm css
pnpm test
pnpm style:l1
```

数据库验证口径：

- `make migrate_status` 显示当前版本包含 `20260612112337`，pending 为 0。
- `to_regclass('public.business_records')`、`to_regclass('public.business_record_items')`、`to_regclass('public.business_record_events')` 均返回空。
- `purchase_receipts / purchase_returns / purchase_receipt_adjustments` 不再包含 `business_record_id` 列。

## 后续

| 后续项 | 说明 |
| --- | --- |
| 客户库 / 生产库迁移 | 另按目标环境执行备份、migration、健康检查和回归，不套用当前开发库结果 |
| 历史归档需求 | 如确有客户可见历史查询需求，单独设计归档表或只读 evidence viewer，不恢复旧业务表 |
| 导入 / backfill | 当前仍不执行真实 import/backfill，不从旧 JSONL 自动生成 V1 或事实数据 |

# 过程记录归档：P5 release candidate 前 / Progress Archive Before P5 Release Candidate

归档范围：当前 `progress.md` 在 P5 本地前置验证、server 镜像构建守卫、133 baseline 与 fast 门禁复核后的完整过程记录。

归档原因：当前 `progress.md` 已达到 261 行 / 80,407 bytes，超过 80KB 阈值；继续记录 P5 release candidate 前先保留完整现场，再收缩当前入口。

归档说明：本文件只用于追溯过程，不作为当前需求、schema、migration、API、UI、部署或产品路线真源。当前状态仍以 `progress.md`、正式文档、代码、测试和目标环境证据为准。

---

# plush-toy-erp progress

本文件只保留当前活跃事项、最近完成记录和归档索引；历史流水已归档到 `docs/archive/`。`progress.md` 是过程交接线索，不是正式需求、数据模型或部署真源。

## 归档索引

- `docs/archive/progress-2026-06-28-before-runtime-manifest.md`：归档 2026-06-28 之前至客户配置版本运行时最小闭环、`/__dev` 页面治理等过程记录。
- `docs/archive/progress-2026-06-29-before-release-evidence-hardening.md`：归档 2026-06-28 客户配置 runtime manifest、发布执行器、dev-only 页面治理、Workflow action 合同、导入与发布证据前置门禁等过程记录。
- `docs/archive/progress-2026-06-29-before-target-evidence-binding.md`：归档 2026-06-29 release evidence、真实导入 recovery plan、文档清单、备份恢复和回滚演练门禁早期硬化过程记录。
- `docs/archive/progress-2026-06-29-before-priority-audit-closeout.md`：归档 2026-06-29 target evidence binding 之后到 release evidence runner 脱敏报告与 URL 前置拦截的过程记录。
- `docs/archive/progress-2026-06-29-before-process-runtime-minimum.md`：归档 2026-06-29 adminProfileSync 菜单投影文档纠偏、P2 explain / entitlement / break-glass 中段过程记录。
- `docs/archive/progress-2026-06-29-before-linked-task-idempotency.md`：归档 2026-06-29 P3 ProcessRuntime expected_version 守卫之前至 linked task 幂等闭环前的过程记录。
- `docs/archive/progress-2026-06-30-before-inventory-post-inbound.md`：归档 2026-06-30 P3 / P4 前段、adminProfileSync 菜单投影文档多轮纠偏、sales_order_acceptance、material_supply definition evidence、`purchase_receipt.create` 和 `quality_inspection.decide` 领域命令 handler 过程记录。
- `docs/archive/progress-2026-06-30-before-p5-release-input-checklist.md`：归档 2026-06-30 P4-2 / P4-3、adminProfileSync 文档纠偏和进入 P5 release closeout 输入清单前的过程记录。
- `docs/archive/progress-2026-06-30-before-p5-input-checklist-followup.md`：归档 2026-06-30 P5 release closeout report-only、release evidence hardening、菜单投影纠偏和 input checklist JSON 自定义路径只读合同之前的过程记录。
- `docs/archive/progress-2026-06-30-before-outsourcing-order-api-gate.md`：归档 2026-06-30 P5 input checklist、adminProfileSync 文档纠偏、purchase / quality / shipment / stock reservation / production / outsourcing fact / sales / purchase order moduleStates 门禁等过程记录。
- `docs/archive/progress-2026-07-01-before-action-projection-l1.md`：归档 2026-06-30 outsourcing order API 门禁之后至 2026-07-01 动作投影浏览器回归扩展的过程记录。
- `docs/archive/progress-2026-07-01-before-progress-archive-and-next-no-write.md`：归档 2026-07-01 动作投影浏览器回归扩展之后至 Purchase Receipt Browser E2E 输入模板的过程记录。
- `docs/archive/progress-2026-07-01-before-readme-preflight-sync-closeout.md`：归档 2026-07-01 Purchase Receipt Browser E2E 输入模板之后至 Web README mobile preflight 口径同步的过程记录。
- `docs/archive/progress-2026-07-01-before-bom-inline-editor.md`：归档 2026-07-01 Web README preflight 口径同步之后至 BOM 明细弹窗内联编辑前的过程记录。
- `docs/archive/progress-2026-07-02-before-trial-real-smoke.md`：归档 2026-07-01 BOM 明细内联编辑前至 2026-07-01 qa:full 生命周期收口的过程记录。
- `docs/archive/progress-2026-07-02-before-effective-session-diagnostic-smoke.md`：归档 2026-07-02 试用真实 smoke 前归档说明至旧兼容接口 / 旧 mobile 运行时清理的过程记录。
- `docs/archive/progress-2026-07-02-before-next-no-write-closure.md`：归档 2026-07-02 effective session 诊断 smoke 前归档说明至移动端 workflow smoke 文档边界守卫的过程记录。
- `docs/archive/progress-2026-07-02-before-dashboard-source-label.md`：归档 2026-07-02 __dev/testing 错误提示与技术字段预设补齐至 mobile-workflow-smoke 测试入口补齐的过程记录。
- `docs/archive/progress-2026-07-02-before-sku-source-field-linkage.md`：归档 2026-07-02 Dashboard 来源标签收口之后至 yoyoosun asset 验证命令防误判收口的过程记录。
- `docs/archive/progress-2026-07-02-before-next-local-closure.md`：归档 2026-07-02 SKU 来源字段链路收口之后至移动端任务端 no-write preflight 复核的过程记录。
- `docs/archive/progress-2026-07-02-before-post-fast-local-closure.md`：归档 2026-07-02 客户配置控制台 no-write 读回预检之后至 L1 与移动端登录回跳本地回归复核的过程记录。
- `docs/archive/progress-2026-07-02-before-simulated-report-only-closure.md`：归档 2026-07-02 下一本地闭环前归档说明至 active module state / Workflow 写门禁复核的过程记录。
- `docs/archive/progress-2026-07-02-before-review-driven-config-contract.md`：归档 2026-07-02 模拟 report-only 后至测试业务数据隔离守卫收口的过程记录。

## 当前活跃事项

- 多甲方角色能力流程编排以 `docs/product/多甲方角色能力流程编排优先级.md` 和 `node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 的 `implementationOrder` 为本地优先级入口；GPT/reference 资料只作输入，当前真源仍回到代码、migration、测试和正式文档。
- 当前审计显示 P0-P4 本地证据为 ready；P5 测试部署 / 导入 / 第二客户验证仍为 `target-evidence-required`，第一条 blocked release action 是 `immutable-version`。
- P5 当前只允许 report-only、input template、preflight 和 checklist 准备；没有真实目标环境、镜像 digest、migration 前后版本、backup id、目标管理员凭据和用户确认时，不写 `deployments/**/evidence/**`，不执行 `--execute`，不把本地 ready 写成目标 release evidence。
- 真实客户数据导入、正式生产发布、目标环境 smoke、目标 migration、备份恢复、回滚 / 前向修复演练、客户配置激活和签收仍未执行，不能被本地 dry-run、manifest 编译、status、gate、audit、runner report、input template 或 preflight 替代。
- 当前可继续推进的本地闭环优先落在：试用账号 / 菜单 / 岗位任务端前置检查、客户配置控制台 no-write 诊断、字段链路守卫、错误提示和 README / `__dev/testing` 入口口径一致性。

## 2026-07-02 目标 019f1d07 本地可证明闭环复核

- 完成：复核目标线程 `019f1d07-7292-7620-ae97-6c958c98e19b` 的剩余项，没有重启计划，也没有把 release evidence 扩大成本轮目标。当前结论保持为：P0-P4 本地 no-write / static / mock / local test evidence 已 ready，P5 与真实账号链路仍是外部证据缺口；本机当前 shell 未提供真实试用密码、管理员密码、客户配置 token、target smoke endpoint、release environment 或源库 DSN。
- 验证：追加前 `progress.md` 为 218 行 / 69,895 bytes，低于归档阈值；环境变量检查确认 `TRIAL_ACCOUNT_PASSWORD`、`TRIAL_DEMO_PASSWORD`、`TRIAL_ADMIN_PASSWORD`、`DEMO_ADMIN_PASSWORD`、`REAL_LOGIN_ADMIN_USERNAME`、`REAL_LOGIN_ADMIN_PASSWORD`、`CUSTOMER_CONFIG_ADMIN_TOKEN`、`SMOKE_ENDPOINT`、`SMOKE_BACKEND_URL`、`RELEASE_ENVIRONMENT`、`ROLLBACK_TARGET_RELEASE`、`ROLLBACK_TRIGGER_SCENARIO`、`SOURCE_POSTGRES_DSN` 均未设置；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 精简摘要为 `ok=true / readOnly=true / releaseReady=false / localReadyCount=30 / localNotReady=[release-preflight-target-evidence] / evidenceRequired=[frontend-effective-session-projection, release-preflight-target-evidence]`；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 完整通过；`node --test scripts/qa/test-data-isolation-boundary.test.mjs` 通过 3 个测试；`git diff --check` 通过。
- 下一步：若要把目标标记为真正完成，必须提供可受控使用的真实演示账号密码或 token，并给出目标环境 release/smoke/backup/rollback 所需输入；拿到输入后再跑真实登录、普通账号 RBAC / 菜单投影、authenticated effective session、移动端真实动作和目标 release evidence 门禁。
- 阻塞/风险：本轮不读取本地 dev config 密码、不登录、不调用 auth JSON-RPC、不启动真实浏览器 smoke、不写数据库、不导入真实客户数据、不发布 / 激活 / 回滚客户配置、不执行生产部署、不做备份恢复或回滚演练、不证明客户签收、不提交或推送。工作区存在大量既有未提交改动，本轮未回退或清理其他会话内容。

## 2026-07-02 两套正式打印真实登录 smoke 收口

- 完成：继续目标 `019f1d07` 的本地可证明闭环，本轮把“打印只停留在 no-write 前置”的证据升级为真实管理员登录 + yoyoosun 5177 入口 + 后端 8300 的浏览器 smoke。修正 `realLoginSmokeShared` 仍等待旧标题“后台首页 / 工作台”的登录成功锚点，改为等待当前 dashboard 标题“工作台”，并补单测锁住；同时清理两套打印 smoke 中的脚本 lint 问题。
- 验证：追加前 `progress.md` 为 225 行 / 72,121 bytes，低于归档阈值；`node web/scripts/realLoginSmokeShared.mjs --preflight-report output/real-login-smoke-shared/current-goal-continuation-preflight.json` 确认后端 health 可达、dev config 凭据来源存在且前置报告不读取密码；`REAL_LOGIN_SMOKE_BASE_URL=http://localhost:5177 REAL_LOGIN_SMOKE_BACKEND_HEALTH_URL=http://127.0.0.1:8300/healthz pnpm --dir web smoke:purchase-contract-real-login` 通过，采购合同在线预览 1747ms；同样环境下 `pnpm --dir web smoke:processing-contract-real-login` 通过，加工合同在线预览 1398ms；`node --check web/scripts/realLoginSmokeShared.mjs && node --check web/scripts/realLoginSmokeShared.test.mjs && node --check web/scripts/purchaseContractRealLoginSmoke.mjs && node --check web/scripts/processingContractRealLoginSmoke.mjs && node --test web/scripts/realLoginSmokeShared.test.mjs` 通过 7 个测试；`pnpm --dir web exec eslint --ext .mjs scripts/realLoginSmokeShared.mjs scripts/realLoginSmokeShared.test.mjs scripts/purchaseContractRealLoginSmoke.mjs scripts/processingContractRealLoginSmoke.mjs` 通过；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 完整通过；`git diff --check` 通过。
- 下一步：两套正式打印模板的本地真实登录 smoke 已证明；若要继续把目标整体标记完成，还必须补普通试用角色真实密码 / token 后的 RBAC 菜单投影、authenticated effective session、移动端真实动作，以及目标环境 release/smoke/backup/rollback evidence。
- 阻塞/风险：本轮没有执行目标环境 smoke、生产发布、备份恢复、回滚演练或客户签收；没有读取或输出 dev config 中的密码值；没有证明普通角色账号、active revision 在目标环境读回、真实移动端写动作或真实客户导入。干净临时 Vite 路径曾受本机 HMR 端口 5175 冲突影响，不作为正式打印结论；正式打印证据以当前 yoyoosun 5177 静态入口 + 8300 后端真实登录 smoke 为准。

## 2026-07-02 试用账号真实 RBAC 与岗位任务端浏览器 smoke 收口

- 完成：继续目标 `019f1d07` 的本地可证明闭环，本轮把“试用账号 / 岗位任务端只停留在 no-write 前置”的缺口升级为本地真实 RBAC 与真实浏览器 smoke。修正 `trialDemoAccountBrowserSmoke.mjs` 登录页偶发空白时直接失败且缺少诊断的问题，登录前会最多 3 次等待 / 重新进入 `/admin-login`，最终失败时输出 `login-form-unavailable`、当前 URL、标题、readyState、root 子节点和 body 文本摘要，便于区分前端入口空白、登录页未加载和账号问题。
- 验证：追加前 `progress.md` 为 232 行 / 74,679 bytes，低于归档阈值；`node --check web/scripts/trialDemoAccountBrowserSmoke.mjs && node --check web/scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过；`node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过 7 个测试；`pnpm --dir web exec eslint --ext .mjs scripts/trialDemoAccountBrowserSmoke.mjs scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过；使用临时随机 `ERP_ROLE_DEMO_PASSWORD / TRIAL_ACCOUNT_PASSWORD` 执行 `scripts/seed-role-demo-admins.sh --reset-password` 后，`node scripts/qa/trial-account-rbac.mjs --report output/trial-account-rbac/current-goal-real-report.json` 通过，报告摘要 `totalAccounts=10 / passedAccounts=10 / failedAccounts=0`；`node web/scripts/trialDemoAccountBrowserSmoke.mjs --report output/trial-demo-account-browser-smoke/current-goal-real-report.json` 通过，报告摘要 `desktopPassedCount=10 / mobilePassedCount=9 / mobileDeniedPassed=true / diagnosticAccountCount=10 / diagnosticBlockerCount=0 / diagnosticSources=[active_customer_config_revision] / projectionModes=[local_dev_customer_config_diagnostic]`；`PATH=/usr/local/bin:$PATH bash scripts/qa/fast.sh` 完整通过；`git diff --check` 通过。
- 下一步：本地普通试用角色 RBAC、桌面菜单投影、岗位任务端入口、`demo_admin` 移动端拒绝态和 DEV-only effective session 脱敏诊断已补上真实证据；若要把目标整体标记完成，仍必须补目标环境 release evidence、target smoke、备份恢复、回滚 / 前向修复演练、客户配置目标读回和客户签收。
- 阻塞/风险：本轮只证明本地后端 `8300`、本地 yoyoosun 前端入口和临时演示密码下的真实 RBAC / 浏览器 smoke；临时密码未写入文档或输出，后续人工登录需重新生成或由用户提供；本轮不导入真实客户数据、不发布 / 激活 / 回滚客户配置、不执行生产部署、不做备份恢复或回滚演练、不证明目标环境 release evidence、客户签收、提交或推送。

## 2026-07-02 目标 release evidence 状态复核

- 完成：继续目标 `019f1d07` 的收口复核，本轮只读检查 `deployments/yoyoosun/evidence/releases/2026-06-29`，确认 release evidence 文件齐全但仍是 `draft / present-unverified`，不能把“文件存在”误判为目标环境完成。报告模式生成 `output/release-evidence-closeout/2026-06-29/current-runner-report.json`，未执行发布、preflight、备份恢复、migration、smoke、回滚或签收。
- 验证：追加前 `progress.md` 为 239 行 / 77,390 bytes，低于归档阈值；`node scripts/deploy/release-evidence-status.mjs --evidence-dir deployments/yoyoosun/evidence/releases/2026-06-29 --json` 返回 `ready=false / gateReady=false / status=draft / requiredFileCount=12 / presentFileCount=12 / closeoutSummary.blockers=7`，gate 有 100 条错误且 customer-config smoke 缺少 `customer-config-effective-session`；`node scripts/deploy/release-evidence-closeout-plan.mjs --evidence-dir deployments/yoyoosun/evidence/releases/2026-06-29 --json` 返回 `summary.total=7 / runnable=0 / blocked=6 / manualOnly=1`；`node scripts/deploy/release-evidence-closeout-runner.mjs --evidence-dir deployments/yoyoosun/evidence/releases/2026-06-29 --report output/release-evidence-closeout/2026-06-29/current-runner-report.json --json` 返回 `ok=true / executed=false / executeReady=false`。
- 下一步：若要把目标整体标记完成，必须补真实 release batch 输入并在目标环境执行对应证据链：不可变版本证据、真实 runtime `.env` preflight、备份恢复演练、target smoke、customer config effective session 读回、rollback / forward-fix 演练和人工 signoff。
- 阻塞/风险：当前本机缺少 `RELEASE_ENVIRONMENT`、镜像引用和 digest、migration before/after、`BACKUP_ID`、真实 prod env 文件、`SOURCE_POSTGRES_DSN`、`SMOKE_ENDPOINT`、`SMOKE_BACKEND_URL`、`CUSTOMER_CONFIG_ADMIN_TOKEN`、rollback target / trigger 和人工 signoff；本轮没有连接目标环境、没有读取 secret、没有手改 evidence 目录、没有提交或推送。

## 2026-07-02 blocked goal P0 review 修正

- 完成：按 code review 结论先收本地试用闭环 P0，不扩大 release evidence。修正 Workflow / Dashboard 任务列不再把 `task_status_key`、`owner_role_key` 作为用户可见列字段；库存台账来源列不再以 `source_type/source_id` 作为可见列主字段，来源单据优先业务单号、缺单号时只显示“来源单据已关联”；恢复菜单权限旧入口 alias 到 `/erp/dashboard` 的受控归一，只做角色配置迁移，不恢复旧路由。
- 验证：`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 23 通过；`node --test web/src/erp/utils/workflowTaskBoard.test.mjs web/src/erp/utils/dashboardTaskDisplay.test.mjs` 14 通过；`node --test web/src/erp/config/menuPermissions.test.mjs` 11 通过；`cd server && go test ./internal/biz -run 'TestAdminMenusOmitRetiredFrontendDocsAndQAPaths|TestNormalizeAdminMenuPermissionsRedirectsRetiredFrontendPaths|TestDefaultAdminMenuPermissionsExcludePermissionCenter|TestBuiltinAdminMenus'` 通过；`cd server && go test ./internal/service -run 'TestJsonrpcDispatcher_Workflow(UpdateTaskStatusRemoved|CompleteTaskAction|BlockTaskAction|RejectTaskAction|TaskAction|Action|SuperAdmin|UrgeTask|Explain)'` 通过；`cd server && go test ./internal/biz -run 'TestWorkflow|Test.*Workflow|Test.*Inbound|Test.*Shipment|Test.*Quality|Test.*Purchase|Test.*Payable|Test.*FinishedGoods|Test.*Outsource'` 通过；`cd server && go test ./internal/biz -run 'TestInventoryProcessCommand|TestQualityInspectionProcessCommand|TestShipmentProcessCommand|TestProcessRuntime'` 通过；`node --test scripts/qa/customer-config-runtime-manifest.test.mjs` 15 通过；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 18 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要为 `ok=true / releaseReady=false / completionState=target-evidence-required / blockingCategory=external-release-evidence-required`。
- 下一步：继续按 review findings 收口客户配置包规则 / 策略 / 扩展点和打印字段链路，不把目标环境 release evidence、真实客户导入或签收升级成本轮主任务。
- 阻塞/风险：本轮未执行真实生产部署、真实客户导入、发布 / 激活 / 回滚客户配置、备份恢复、回滚演练、客户签收、提交或推送；工作区仍有大量既有未提交改动。

## 2026-07-02 客户配置包规则 / 策略 / 扩展点合同收口

- 完成：按 review findings 推进客户配置包本地可验证闭环。先归档 `progress.md` 早期 2026-07-02 流水到 `docs/archive/progress-2026-07-02-before-review-driven-config-contract.md`，再补客户配置包 schema / lint / runtime manifest 合同：客户包现在必须显式声明 `extensionPoints`；runtime manifest 编译出 `flowCatalog`、`policyCatalog`、`extensionPointCatalog`，并强制业务流、状态机、策略保持 `preview_only`，扩展点保持 `controlled_empty`，不允许客户包悄悄发布可执行 handler、任意策略执行或客户代码扩展。
- 验证：归档后 `progress.md` 为 72 行 / 18,511 bytes；`node --test scripts/qa/customer-package-lint.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs` 通过 27 个测试；`node scripts/qa/customer-package-lint.mjs --customer demo --mode compile` 通过；`node scripts/qa/customer-package-lint.mjs --customer yoyoosun --mode compile` 通过；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过；`node --test web/src/erp/config/devCustomerConfig.test.mjs scripts/qa/customer-config-boundaries.test.mjs scripts/qa/customer-config-effective-session-probe.test.mjs` 通过 16 个测试；`node scripts/qa/customer-config-boundaries.mjs` 通过；相关文件 `git diff --check` 通过。
- 下一步：继续 review findings 驱动，下一片建议处理打印字段链路或客户配置包打印模板字段预检与实际 mapper / 导出 / 历史缺值之间的差距。
- 阻塞/风险：本轮只建立配置包 T1/T6 合同和受控空扩展点边界，不实现规则引擎、策略执行器、代码扩展点、真实客户导入、发布 / 激活 / 回滚客户配置、生产部署、目标环境 release evidence、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 打印模板字段合同与 PDF 门禁收口

- 完成：按 review findings 继续收口打印字段链路，不把销售订单打印升级为完成。两套正式模板现在都在 `printTemplates.mjs` / `processingContractTemplateMeta` 明确声明 `runtimeStatus`、`factBoundary: read_snapshot_only`、`moduleKeys` 和 `fieldRequirements`；前端模板目录测试会校验字段合同、只读快照边界和服务端 `/templates/render-pdf` 模块门禁一致。`__dev/customer-config` 打印模板摘要同步显示 fieldTruth、fieldRequirements 和中文只读快照边界，避免诊断页继续只有半截字段真源。
- 验证：追加前 `progress.md` 为 79 行 / 20,511 bytes，低于归档阈值；`node --test web/src/erp/config/printTemplates.test.mjs web/src/erp/data/processingContractTemplate.test.mjs scripts/qa/sales-order-field-chain-boundary.test.mjs web/src/erp/config/devCustomerConfig.test.mjs` 通过 30 个测试；`cd server && go test ./internal/server -run 'TestTemplatePDFReferencedModuleKeys|TestEnforceTemplatePDFModulesEnabled|TestParseRenderTemplatePDFRequest'` 通过；`node --test scripts/qa/docs-inventory.test.mjs scripts/qa/customer-config-boundaries.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs` 通过 18 个测试；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node --test web/src/erp/config/devCustomerConfig.test.mjs web/src/erp/config/printTemplates.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 38 个测试；`node --test scripts/qa/dev-entry-boundary.test.mjs web/src/erp/config/devTesting.test.mjs` 通过 14 个测试；`git diff --check` 通过。
- 下一步：继续按 review findings 处理剩余残缺流，优先看岗位任务端真实动作体验、Workflow explain / action parity、客户配置包打印模板字段预检与实际 mapper / 导出 / 历史缺值之间的差距。
- 阻塞/风险：本轮只补正式采购合同和加工合同的字段合同、诊断展示和 PDF 门禁一致性，不实现销售订单打印模板，不发布 `sales_order_items.default`，不做完整打印 mapper / 表单 / 导出 / 历史缺值回补，不 OCR PDF / 图片，不执行真实客户导入、发布 / 激活 / 回滚客户配置、生产部署、目标环境 release evidence、备份恢复、回滚演练、客户签收、提交或推送。

## 2026-07-02 移动端 Workflow action 合同收口

- 完成：按 review findings 继续收口岗位任务端动作链路。移动端完成 / 阻塞 / 退回现在和 explain / 催办一样使用正式 `task_id` action 合同调用 `complete_task_action / block_task_action / reject_task_action`，不再依赖服务端对旧 `id` 字段的兼容 fallback；`workflow-ui-action-boundary` 增加 actionParams 块级守卫，要求移动端先 explain、reason 先校验、错误提示走中文 helper、提交动作用 `task_id`，并禁止回退旧 `id` fallback。
- 验证：追加前 `progress.md` 为 86 行 / 22,898 bytes，低于归档阈值；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok=true / releaseReady=false`，target release evidence 仍是外部缺口；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 65 个测试；`cd server && go test ./internal/service -run 'TestJsonrpcDispatcher_Workflow(UpdateTaskStatusRemoved|CompleteTaskAction|BlockTaskAction|RejectTaskAction|TaskAction|Action|SuperAdmin|UrgeTask|Explain)'` 通过；`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/scripts/mobileAuthLoginRouteSmoke.test.mjs web/scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过 26 个测试。
- 下一步：继续按 review findings 处理移动端真实浏览器体验剩余盲区，优先检查窄屏布局 / style:l1 场景、内部通知 / 预警文案和真实账号 smoke 输入边界；仍不把 release evidence 或真实客户导入升级成本轮主任务。
- 阻塞/风险：本轮只修移动端 action 参数合同和测试守卫，没有执行真实浏览器 smoke、没有生成临时试用密码、没有写真实业务 Fact、没有执行客户配置发布 / 激活 / 回滚、生产部署、目标环境 release evidence、备份恢复、回滚演练、客户签收、提交或推送。移动端真实浏览器 smoke 脚本测试证明 no-write / report / preflight 边界和计划覆盖，不等于本轮已经用真实账号跑过浏览器动作。

## 2026-07-02 移动端任务端 L1 可见文本守卫收口

- 完成：按 review findings 继续收口岗位任务端真实页面验收。`mobile-tasks-dark` 的 style:l1 详情页断言现在不仅检查暗色、窄屏、动作按钮、reason、催办、通知 / 预警和横向溢出，还会在本岗位详情与跨岗位详情的真实浏览器可见文本中拦截 `owner_role_key`、`task_status_key`、`source_type`、`payload`、`TASK-<id>`、`#<id>` 等技术字段或内部编号 fallback，避免单元测试过了但运行页面投影仍泄漏 raw key / raw id。
- 验证：追加前 `progress.md` 为 93 行 / 25,123 bytes，低于归档阈值；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 65 个测试；`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 13 个测试；`STYLE_L1_SCENARIOS=mobile-tasks-dark /usr/local/bin/pnpm --dir web style:l1` 通过 1 个真实浏览器场景。直接使用默认 `pnpm` 会命中 Codex bundled pnpm 11.7.0 与仓库要求 10.13.x 不兼容，故本轮使用项目稳定入口 `/usr/local/bin/pnpm`。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看菜单 / 任务投影的真实账号写路径、客户配置发布门禁或销售订单字段链路中仍未被浏览器级断言覆盖的部分；release evidence、真实客户导入和签收仍保持外部缺口说明。
- 阻塞/风险：本轮只补移动端 style:l1 浏览器断言，没有新增移动端业务能力、没有写真实 Fact、没有跑真实账号动作 smoke、没有生成或读取密码、没有执行客户配置发布 / 激活 / 回滚、生产部署、目标环境 release evidence、备份恢复、回滚演练、客户签收、提交或推送；工作区仍有大量既有未提交改动。

## 2026-07-02 销售订单行来源清空保存参数收口

- 完成：按 review findings 继续收口销售订单字段链路中的清空来源边界。`buildSalesOrderItemParams` 不再把清空后的 `product_id / product_sku_id / unit_id` 序列化成 `0`；空来源保持参数 absent，表单仍要求 SKU / 产品来源与单位，后端 usecase 仍按正数 ID 校验，不把 `0` 当成有效内部 ID 或保存 fallback。继续保持 `sales_order_items.default` 未发布、销售订单打印未接通。
- 验证：追加前 `progress.md` 为 100 行 / 27,085 bytes，低于归档阈值；`node --test web/src/erp/utils/masterDataOrderView.test.mjs scripts/qa/sales-order-field-chain-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 75 个测试；`node --test web/src/erp/utils/contactValidation.test.mjs web/src/erp/utils/masterDataOrderView.test.mjs` 通过 49 个测试。两次测试均只出现既有 `MODULE_TYPELESS_PACKAGE_JSON` warning，不影响结果。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看客户配置发布门禁、菜单 / 任务投影真实账号写路径或销售订单字段链路浏览器级回归缺口；release evidence、真实客户导入和签收仍保持外部缺口说明。
- 阻塞/风险：本轮不新增销售订单打印模板、不发布 `sales_order_items.default`、不改 schema / migration / backend usecase、不中断后端正数校验、不跑真实账号动作 smoke、不导入真实客户数据、不发布 / 激活 / 回滚客户配置、不生产部署、不提交或推送；工作区仍有大量既有未提交改动。

## 2026-07-02 客户配置 release executor 报告目录门禁收口

- 完成：按 review findings 继续收口客户配置发布 / 激活 / 回滚入口边界。`customer-config-release-execute.mjs` 的 `--out` 现在必须留在仓库内，且拒绝写入 `deployments/**` 客户交付 / release evidence 目录，避免 report-only 或测试执行报告混入正式 release evidence 真源。正常输出仍走 `output/customers/yoyoosun/customer-config-release` 一类本地报告目录；脚本仍不会在未显式 `--execute`、确认短语和 token 前调用后端。
- 验证：追加前 `progress.md` 为 107 行 / 28,741 bytes，低于归档阈值；`node --test scripts/deploy/customer-config-release-execute.test.mjs scripts/deploy/customer-config-release-readiness.test.mjs scripts/qa/customer-config-effective-session-probe.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs` 通过 65 个测试。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看客户配置真实读回前置与正式前端菜单 / 字段投影、普通账号真实 smoke 或移动端真实动作链路；release evidence、真实客户导入和签收仍保持外部缺口说明。
- 阻塞/风险：本轮只收紧本地 release executor 报告输出目录，不发布 / 激活 / 回滚客户配置，不写 release evidence，不调用目标环境，不导入真实客户数据，不执行生产部署、备份恢复、回滚演练、客户签收、提交或推送；工作区仍有大量既有未提交改动。

## 2026-07-02 菜单投影未知 URL page key 门禁收口

- 完成：继续当前 active goal，不重开 goal；按 review findings 收口正式菜单 / active pages 投影的隐藏 URL 边界。新增 `resolveCurrentNavigationEntry`，把“页面显示 fallback”和“授权投影 page key”分开：已登记 exact / prefix 路径才返回 page key；未登记 URL 只使用工作台作为标题 / 面包屑显示 fallback，`pageKey / menuPath` 为空，不把 `global-dashboard` 授权给未知 URL，也不选中工作台菜单。`ERPLayout` 改为使用该 helper，并同步 `web/README.md`、`docs/当前真源与交接顺序.md` 的菜单投影口径。
- 验证：追加前 `progress.md` 为 114 行 / 30,282 bytes，低于归档阈值；`rg "buildCurrentEntry|未知 URL 通常|fallback 做 page key|fallback 后的 page key|按该 page key|global-dashboard.*参与 pages 判定" web/README.md docs/当前真源与交接顺序.md` 无旧口径残留；`node --test web/src/erp/utils/currentNavigationEntry.test.mjs web/src/erp/utils/adminProfileSync.test.mjs web/src/erp/config/menuPermissions.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs` 通过 34 个测试；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok=true / readOnly=true / releaseReady=false`，本地 ready 与 target evidence required 边界仍保持分离。
- 下一步：继续按 review findings 推进剩余链路，优先看 Workflow action / usecase parity、普通账号真实 smoke 或移动端真实动作链路；release evidence、真实导入和签收保持外部缺口。
- 阻塞/风险：本轮不改后端 RBAC/usecase、不登录、不跑浏览器 style:l1 / 真实 smoke、不发布 / 激活 / 回滚客户配置、不写 release evidence、不导入真实客户数据、不生产部署、不提交或推送；工作区仍有大量既有未提交改动。

## 2026-07-02 桌面 Workflow action 提交前 explain 门禁收口

- 完成：按 review findings 继续收口 Workflow action / RBAC / usecase parity 的前端提交边界。新增共享 `verifyWorkflowTaskActionAccessBeforeSubmit`，桌面 Dashboard、Workflow 专页、出货放行协同页、采购订单协同 hook 和委外订单协同 hook 在真正调用 `complete_task_action / block_task_action / urge_task` 前都会重新调用 `explain_action_access`；后端未允许或 explain 失败时只显示中文场景化提示并阻断提交。桌面完成 / 阻塞 payload 同步从旧 `id` 改为正式 `task_id`，避免继续依赖服务端兼容 fallback；`workflow-ui-action-boundary` 增加桌面入口守卫，防止回退到只靠按钮 disabled 或旧 id 合同。
- 验证：追加前 `progress.md` 为 121 行 / 32,283 bytes，低于归档阈值；`node --test web/src/erp/utils/workflowTaskActionAccess.test.mjs scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs` 通过 62 个测试；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 29 个测试；`node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs web/scripts/mobileAuthLoginRouteSmoke.test.mjs web/scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过 26 个测试；`cd server && go test ./internal/service -run 'TestJsonrpcDispatcher_Workflow(UpdateTaskStatusRemoved|CompleteTaskAction|BlockTaskAction|RejectTaskAction|TaskAction|Action|SuperAdmin|UrgeTask|Explain)'` 通过；`cd server && go test ./internal/biz -run 'TestWorkflow|Test.*Workflow|Test.*Inbound|Test.*Shipment|Test.*Quality|Test.*Purchase|Test.*Payable|Test.*FinishedGoods|Test.*Outsource'` 通过；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要为 `ok=true / readOnly=true / releaseReady=false / completionState=target-evidence-required / blockingCategory=external-release-evidence-required`。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看普通账号真实 smoke、ProcessRuntime action parity 的本地真实链路或客户配置 active revision 读回前置；release evidence、真实客户导入和签收仍保持外部缺口说明。
- 阻塞/风险：本轮只收口桌面前端提交前 explain 和正式 `task_id` action 合同，不改变后端 Workflow / Fact 边界，不让 task done 自动写库存、质检、出货或财务 Fact；未跑真实账号浏览器动作 smoke、未生成或读取密码、未执行客户配置发布 / 激活 / 回滚、生产部署、目标环境 release evidence、备份恢复、回滚演练、客户签收、提交或推送；工作区仍有大量既有未提交改动。

## 2026-07-02 业务可见枚举 raw key fallback 收口

- 完成：当前 active goal 已按 review findings 驱动继续执行，不重开 goal。本轮收口业务页面用户可见枚举 fallback：业务事实、库存台账、采购入库、BOM、来料质检、出货和供应商类型的未知状态 / 类型不再把 raw key、raw value 直接展示或导出，而是退到中文业务兜底文案；RPC 参数、query 参数和隐藏字段合同保持不变。同步修正 `OperationalFactForms.test.mjs` 的旧表单断言，使测试回到当前真实职责：字段参数、财务标签字典和可见 fallback 守卫。
- 验证：追加前 `progress.md` 为 128 行 / 35,250 bytes，低于归档阈值；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/components/operational-facts/OperationalFactForms.test.mjs` 通过 31 个测试；`node --test scripts/qa/operational-fact-simulated-closure.test.mjs scripts/qa/workflow-fact-boundary.test.mjs` 通过 10 个测试；`rg "STATUS_LABELS\\[key\\]\\s*\\|\\|\\s*key|BOM_STATUS_LABELS\\[key\\]\\s*\\|\\|\\s*key|QUALITY_STATUS_LABELS\\[key\\]\\s*\\|\\|\\s*key|QUALITY_RESULT_LABELS\\[key\\]\\s*\\|\\|\\s*key|SHIPMENT_STATUS_LABELS\\[key\\]\\s*\\|\\|\\s*key|SUBJECT_TYPE_LABELS\\[key\\]\\s*\\|\\|\\s*key|LOT_STATUS_LABELS\\[key\\]\\s*\\|\\|\\s*key|TXN_TYPE_LABELS\\[key\\]\\s*\\|\\|\\s*key|FINANCE_COLLECTION_TYPE_LABELS\\[value\\]\\s*\\|\\|\\s*value|FINANCE_PAYMENT_TERM_LABELS\\[value\\]\\s*\\|\\|\\s*value|FINANCE_INVOICE_CATEGORY_LABELS\\[value\\]\\s*\\|\\|\\s*value|SUPPLIER_TYPE_LABELS\\[value\\]\\s*\\|\\|\\s*value" web/src/erp/pages web/src/erp/components -g '!**/*.test.*'` 无命中；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false`。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看打印 mapper / 导出 / 历史缺值、客户配置 active revision 读回、普通账号真实 smoke 或任务投影真实写路径；release evidence、真实客户导入和签收仍保持外部缺口说明。
- 阻塞/风险：本轮不新增业务枚举、不改后端状态机、不改 schema / migration、不改变 Workflow / Fact 边界、不实现销售订单打印模板、不发布 / 激活 / 回滚客户配置、不生产部署、不导入真实客户数据、不提交或推送；工作区仍有大量既有未提交改动。

## 2026-07-02 打印业务带值样例兜底污染收口

- 完成：按 review findings 继续收口打印字段链路。采购合同和加工合同打印窗口现在区分菜单样例入口与业务带值入口：菜单入口仍恢复模板样例；业务入口从 window state / 本地草稿恢复时走业务草稿构造 helper，只保留真实业务带入字段和正式模板条款，缺失的供应商联系人、电话、地址、买方信息、签字人和明细列保持空值，不再被 `template.sample` 或 `createProcessingContractDraft()` 的样例字段兜底。
- 验证：追加前 `progress.md` 为 135 行 / 37,898 bytes，低于归档阈值；`node --test web/src/erp/utils/materialPurchaseContractEditor.test.mjs web/src/erp/data/processingContractTemplate.test.mjs web/src/erp/config/printTemplates.test.mjs` 通过 24 个测试；`node --test web/src/erp/utils/printWorkspace.test.mjs web/src/erp/utils/printPdf.test.mjs web/src/erp/utils/usePrintWorkspaceWindowSnapshot.test.mjs` 通过 33 个测试；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false`；lint 后复跑 `node --test web/src/erp/utils/materialPurchaseContractEditor.test.mjs web/src/erp/data/processingContractTemplate.test.mjs scripts/qa/sales-order-field-chain-boundary.test.mjs` 通过 28 个测试，复跑打印工作区 33 个测试通过。
- 下一步：继续按 review findings 推进打印字段链路剩余缺口，优先看采购 / 加工 mapper 是否能从业务真源完整带出联系人、地址、产品订单号、产品名称、附件与历史缺值回补；销售订单打印仍不接入，除非完整 mapper / 表单 / 导出 / 打印 / 历史缺值测试同步。
- 阻塞/风险：本轮只修业务打印草稿禁止样例兜底，不新增销售订单打印模板，不发布 `sales_order_items.default`，不 OCR PDF / 图片，不补真实客户导入，不执行客户配置发布 / 激活 / 回滚、生产部署、目标环境 release evidence、备份恢复、回滚演练、客户签收、提交或推送；工作区仍有大量既有未提交改动。

## 2026-07-02 采购 / 加工打印供应商快照字段链路收口

- 完成：继续按 review findings 收口打印 mapper 缺值链路，不扩大 schema / 后端 / release evidence。`buildSupplierSnapshot` 现在保留供应商或加工厂已有的联系人、联系电话、手机号和地址快照；采购合同 mapper 与加工合同 mapper 会从 `supplier_snapshot` 带出 `supplierContact / supplierPhone / supplierAddress`，并继续只读生成打印草稿，不反写主数据、不写 Fact、不用模板样例兜底真实缺值。
- 验证：追加前 `progress.md` 为 142 行 / 40,218 bytes，低于归档阈值；`node --test web/src/erp/utils/masterDataOrderView.test.mjs web/src/erp/data/processingContractTemplate.test.mjs scripts/qa/sales-order-field-chain-boundary.test.mjs` 通过 62 个测试；`node --test web/src/erp/utils/materialPurchaseContractEditor.test.mjs web/src/erp/data/processingContractTemplate.test.mjs web/src/erp/config/printTemplates.test.mjs` 通过 24 个测试；`node --test web/src/erp/utils/printWorkspace.test.mjs web/src/erp/utils/printPdf.test.mjs web/src/erp/utils/usePrintWorkspaceWindowSnapshot.test.mjs` 通过 33 个测试；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false`。
- 下一步：继续按 review findings 推进打印字段链路剩余缺口，优先检查当前供应商主数据列表是否实际携带联系人 / 地址、业务页是否能在选择供应商时拿到这些字段、附件快照和历史缺值回补；销售订单打印仍不接入，除非完整 mapper / 表单 / 导出 / 打印 / 历史缺值测试同步。
- 阻塞/风险：本轮只打通已有 `supplier_snapshot` 到采购 / 加工打印草稿的字段链路；如果当前供应商列表 API 不返回联系人或地址，真实页面仍会保持空值但不会伪造。本轮不新增 supplier schema 字段、不改后端 JSON-RPC、不新增销售订单打印、不发布 `sales_order_items.default`，不执行真实客户导入、客户配置发布 / 激活 / 回滚、生产部署、target release evidence、备份恢复、回滚演练、客户签收、提交或推送；工作区仍有大量既有未提交改动。

## 2026-07-02 采购 / 委外供应商联系人保存快照链路收口

- 完成：继续按 review findings 收口打印字段真源，不重开 goal。采购订单和委外订单保存前现在会通过 `listContactsByOwner({ owner_type: 'SUPPLIER' })` 读取供应商 / 加工厂联系人，并用 `buildSupplierSnapshotWithContacts` 把主联系人姓名、电话、手机号写入 `supplier_snapshot`；供应商选择变更时先写基础快照，再异步回填联系人快照且防止旧请求覆盖新选择。打印 mapper 仍只消费订单保存时的业务快照，不从模板样例或页面本地状态伪造业务值。
- 验证：追加前 `progress.md` 为 149 行 / 42,637 bytes，低于归档阈值；`node --test web/src/erp/utils/masterDataOrderView.test.mjs web/src/erp/data/processingContractTemplate.test.mjs scripts/qa/sales-order-field-chain-boundary.test.mjs` 通过 64 个测试；`node --test web/src/erp/utils/materialPurchaseContractEditor.test.mjs web/src/erp/data/processingContractTemplate.test.mjs web/src/erp/config/printTemplates.test.mjs` 通过 24 个测试；`node --test web/src/erp/utils/printWorkspace.test.mjs web/src/erp/utils/printPdf.test.mjs web/src/erp/utils/usePrintWorkspaceWindowSnapshot.test.mjs` 通过 33 个测试；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 当前返回空结果摘要 `ok=true / total=0`，只能证明入口未坏，不能升级为真实角色 / 菜单覆盖证据。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看供应商地址字段真源、附件快照、历史旧单缺值回补、普通账号真实 smoke 或客户配置 active revision 读回；销售订单打印仍不接入，除非 mapper / 表单 / 导出 / 打印 / 历史缺值测试同步。
- 阻塞/风险：本轮不新增 supplier address schema，不改后端 JSON-RPC/RBAC，不改联系人保存事务，不执行真实账号浏览器 smoke，不执行客户配置发布 / 激活 / 回滚，不写 release evidence，不导入真实客户数据，不生产部署、备份恢复、回滚演练、客户签收、提交或推送；如果当前账号没有联系人读取权限或联系人 API 失败，保存会退回基础供应商快照，打印字段保持空值而不是伪造；工作区仍有大量既有未提交改动。

## 2026-07-02 采购 / 委外联系人快照降级提示收口

- 完成：继续按 review findings 推进同一打印字段链路的错误提示边界。采购订单和委外订单保存前读取供应商 / 加工厂联系人失败时，不再静默吞掉错误；页面会通过统一 `getActionErrorMessage` 给出中文 warning，并明确“将仅保存供应商 / 加工厂基本信息”。保存仍可继续，避免联系人读取临时失败阻断单据主路径；打印 mapper 仍只消费已保存业务快照，不伪造联系人字段。
- 验证：追加前 `progress.md` 为 156 行 / 45,129 bytes，低于归档阈值；`node --test web/src/erp/utils/masterDataOrderView.test.mjs web/src/erp/data/processingContractTemplate.test.mjs scripts/qa/sales-order-field-chain-boundary.test.mjs` 通过 64 个测试；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 30 个测试；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看供应商地址真源是否需要 schema/API 正式补齐、附件快照和历史旧单缺值回补，或切到普通账号真实 smoke / 客户配置 active revision 读回切片。
- 阻塞/风险：本轮只收口本地页面保存前联系人读取失败的用户可见提示，不新增后端权限、不证明真实账号具备联系人读取权限、不跑浏览器真实登录、不新增 supplier address schema、不执行客户配置发布 / 激活 / 回滚、真实客户导入、生产部署、target release evidence、备份恢复、回滚演练、客户签收、提交或推送；工作区仍有大量既有未提交改动。

## 2026-07-02 销售订单受理流程客户配置 key 贯穿收口

- 完成：继续按 review findings 收口客户配置 active module state 写门禁的前端 API 合同。`submitSalesOrderAcceptanceProcess` 现在把同一个 `customer_key` 同时传给 `start_sales_order_acceptance_process` 和后续 `execute_sales_order_acceptance_submit`，避免 start 按当前客户配置创建流程、execute 却因缺少 customer key 回退默认客户配置去校验 domain command 模块状态。页面仍通过后端 JSON-RPC / CustomerConfigUsecase / ProcessRuntimeUsecase 执行，不在前端本地改 sales order 状态，也不绕过 RBAC / active module state。
- 验证：追加前 `progress.md` 为 163 行 / 46,977 bytes，低于归档阈值；`node --test web/src/erp/api/customerConfigApi.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs web/src/erp/utils/adminProfileSync.test.mjs` 通过 24 个测试；`cd server && go test ./internal/service -run 'TestCustomerConfigJSONRPC(StartSalesOrderAcceptanceProcess|ExecuteSalesOrderAcceptanceSubmit)'` 通过；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：继续按 review findings 推进普通账号真实 smoke、客户配置 active revision 读回前置、移动端任务动作或 Workflow action / RBAC / usecase parity 剩余切片；release evidence、真实部署、真实客户导入和签收仍保持外部缺口。
- 阻塞/风险：本轮只修本地前端 API payload 合同和对应测试，不执行真实登录、不调用目标后端、不发布 / 激活 / 回滚客户配置、不生成 release evidence、不执行生产部署、备份恢复、回滚演练、客户签收、提交或推送；工作区仍有大量既有未提交改动。

## 2026-07-02 移动端任务完成反馈门禁收口

- 完成：继续按 review findings 收口岗位任务端动作链路。移动端 `done` 现在与 `blocked / rejected / urge` 一样先打开详情动作面板，完成动作必须先填写现场留痕里的完成反馈或附件线索，再提交；提交前仍调用后端 `explain_action_access`，实际动作仍走 `complete_task_action`，不在前端本地把 Workflow task done 写成 Fact posted。详情面板同步区分“完成反馈”与“原因”，避免完成动作继续展示阻塞 / 退回 / 催办原因快捷项；真实浏览器 smoke 脚本也改为“填留痕 -> 点完成 -> 点提交 -> 等后端状态更新”的路径。
- 验证：追加前 `progress.md` 为 170 行 / 48,844 bytes，低于归档阈值；`node --test web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 41 个测试；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionAccess.test.mjs` 通过 22 个测试；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 30 个测试；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=mobile-tasks-dark,mobile-tasks-browser-back-stays-mobile /usr/local/bin/pnpm --dir web style:l1` 通过 2 个移动端场景；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：继续按 review findings 推进普通账号真实 smoke、客户配置 active revision 读回、移动端真实账号浏览器动作报告或打印 mapper / 历史缺值链路剩余切片；release evidence、真实部署、真实客户导入和签收仍保持外部缺口。
- 阻塞/风险：本轮只证明本地代码路径、静态守卫、单元测试和 mock / L1 浏览器场景；未提供真实演示账号密码，未执行 `smoke:mobile-workflow-runtime-browser` 的真实登录 / 真实后端写入报告，不证明真实 RBAC、customer config active revision、目标环境发布、真实客户导入、备份恢复、回滚演练或客户签收；不提交、不推送，工作区仍有大量既有未提交改动。

## 2026-07-02 Workflow 任务来源编号 raw id 兜底收口

- 完成：继续按 review findings 收口用户可见 raw id 投影。采购到货、委外回货、成品/出货、应收开票、应付对账 5 条前端任务派生 helper 的 `resolve*SourceNo` 不再把 `record.id` 当业务来源编号兜底；有 `document_no / source_no / title` 时继续使用业务可读编号或标题，完全缺业务编号时保持空 `source_no`，由展示层按 `source_type + source_id` 显示“业务来源 / 已关联业务来源”。内部 `task_code` 仍可使用 id 保持任务去重和稳定编码，但不会写入 `source_no` 或相关单据可见文案。
- 验证：追加前 `progress.md` 为 177 行 / 51,190 bytes，低于归档阈值；`node --test web/src/erp/utils/purchaseInboundFlow.test.mjs web/src/erp/utils/outsourceReturnFlow.test.mjs web/src/erp/utils/finishedGoodsFlow.test.mjs web/src/erp/utils/shipmentFinanceFlow.test.mjs web/src/erp/utils/payableReconciliationFlow.test.mjs` 通过 51 个测试；`node --test web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs` 通过 40 个测试；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 31 个测试；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionAccess.test.mjs` 通过 22 个测试；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false`。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看客户配置 active revision 读回、普通账号真实 smoke、打印 mapper / 历史缺值或 Workflow action / ProcessRuntime 真实链路的下一个本地可验证切片。
- 阻塞/风险：本轮只改前端任务派生和测试守卫，不改后端 schema / JSON-RPC / RBAC，不改变任务去重内部编码，不执行真实账号浏览器 smoke，不证明目标环境 release evidence、真实客户导入、备份恢复、回滚演练或客户签收；不提交、不推送，工作区仍有大量既有未提交改动。

## 2026-07-02 移动端任务状态 raw key fallback 收口

- 完成：继续按 review findings 收口移动端岗位任务投影。`buildMobileTaskView` 的任务状态展示改为复用桌面任务板的 `getWorkflowTaskStatusMeta`，未知 `task_status_key` 统一显示“未知状态”，不再把 `unknown_task_status_key` 一类内部 key 直接显示到移动端列表 / 详情；业务状态继续走共享 `getBusinessStatusLabel` 的中文兜底。此改动只处理用户可见投影，不改变后端 Workflow 状态机、JSON-RPC、RBAC 或任务动作合同。
- 验证：追加前 `progress.md` 为 184 行 / 53,580 bytes，低于归档阈值；`node --test web/src/erp/utils/mobileTaskView.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/workflowTaskBoard.test.mjs` 通过 41 个测试；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 31 个测试；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs web/src/erp/utils/workflowTaskActionAccess.test.mjs` 通过 22 个测试；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 摘要仍为 `ok=true / readOnly=true / releaseReady=false / notProven=9`。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看客户配置 active revision 读回、普通账号真实 smoke、打印 mapper / 历史缺值或 Workflow action / ProcessRuntime 真实链路的下一个本地可验证切片。
- 阻塞/风险：本轮未跑真实账号浏览器 smoke、未执行真实后端写入、未证明真实 RBAC、未发布 / 激活 / 回滚客户配置、未导入真实客户数据、未生产部署、未生成 target release evidence、未做备份恢复 / 回滚演练 / 客户签收；本轮不提交、不推送，工作区仍有大量既有未提交改动。

## 2026-07-02 移动端关联来源文案收口

- 完成：继续按 review findings 收口岗位任务端真实体验。移动端任务详情的“关联单据”不再把所有任务来源固定显示为“订单：xxx”，改为复用共享来源标签并显示“来源：xxx”；采购入库、质检、出货、应付等非订单任务不会被误标成订单，未知来源仍显示“已关联业务来源”，不透出 `source_type / source_id`、未知 source key 或内部编号。此改动只改移动端可见文案和模型 helper，不改变后端 Workflow / Fact、JSON-RPC、RBAC、任务动作或数据写入合同。
- 验证：追加前 `progress.md` 为 191 行 / 55,627 bytes，低于归档阈值；`node --test web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 43 个测试；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 31 个测试；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=mobile-tasks-dark,mobile-tasks-browser-back-stays-mobile /usr/local/bin/pnpm --dir web style:l1` 通过 2 个移动端场景。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看打印 mapper / 历史缺值、客户配置 active revision 目标读回边界或 ProcessRuntime / Workflow 真实动作 parity 的下一个本地可验证切片。
- 阻塞/风险：本轮未跑真实账号浏览器 smoke、未创建新模拟任务、未执行真实后端写入、未发布 / 激活 / 回滚客户配置、未导入真实客户数据、未生产部署、未生成 target release evidence、未做备份恢复 / 回滚演练 / 客户签收；本轮不提交、不推送，工作区仍有大量既有未提交改动。

## 2026-07-02 移动端来源称谓与 Current 参考收口

- 完成：继续沿同一 review finding 收口移动端任务详情的来源口径。详情 header 不再写“单号：”，改为“来源：”；详情区标题改为“关联来源（1）”；`mobile-tasks-dark` L1 断言和 `docs/product/prototypes/mobile-role-tasks-v1/implemented-reference.html` Current/as-built 参考同步到同一口径，避免后续继续按旧“关联单据 / 订单”误判。通用业务详情原型里的“关联单据”仍保留，因为那是业务详情页通用样板，不属于本移动端任务详情口径。
- 验证：追加前 `progress.md` 为 198 行 / 57,542 bytes，低于归档阈值；`node --test web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 43 个测试；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 32 个测试；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=mobile-tasks-dark,mobile-tasks-browser-back-stays-mobile /usr/local/bin/pnpm --dir web style:l1` 通过 2 个移动端场景；`rg "单号：|订单：|关联单据" docs/product/prototypes/mobile-role-tasks-v1 web/src/erp/mobile web/scripts/style-l1/mobileTaskAssertions.mjs` 只剩测试里的负向断言；`node --test scripts/qa/docs-inventory.test.mjs` 通过。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看打印 mapper / 历史缺值、客户配置 active revision 目标读回边界或 ProcessRuntime / Workflow 真实动作 parity 的下一个本地可验证切片。
- 阻塞/风险：本轮只改移动端可见文案、目标 L1 断言和 Current 参考文档，不改后端 Workflow / Fact、JSON-RPC、RBAC、任务动作或数据写入合同；未跑真实账号浏览器 smoke、未创建新模拟任务、未执行真实后端写入、未发布 / 激活 / 回滚客户配置、未导入真实客户数据、未生产部署、未生成 target release evidence、未做备份恢复 / 回滚演练 / 客户签收；本轮不提交、不推送，工作区仍有大量既有未提交改动。

## 2026-07-02 移动端 Workflow action submit guard 收口

- 完成：继续按 review findings 收口 Workflow action / RBAC / usecase parity。移动端任务动作提交前权限核对不再维护私有 `explainWorkflowActionAccess` 分支，改为复用桌面协同和业务页同一套 `verifyWorkflowTaskActionAccessBeforeSubmit`；`done / blocked / rejected / urge` 仍先做本地 reason / 完成反馈前置校验，再向后端 `explain_action_access` 核对，最后才调用 `complete_task_action / block_task_action / reject_task_action / urge_task`。静态守卫同步锁住移动端必须用共享 submit guard，并确认共享 guard 使用正式 `task_id / action_key` explain 合同。
- 验证：追加前 `progress.md` 为 205 行 / 59,837 bytes，低于归档阈值；`node --test scripts/qa/workflow-ui-action-boundary.test.mjs` 通过 15 个测试；`node --test web/src/erp/utils/workflowTaskActionAccess.test.mjs web/src/erp/mobile/utils/mobileRoleTaskModel.test.mjs web/src/erp/utils/mobileTaskView.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 50 个测试；`node --test scripts/qa/frontend-error-message-boundary.test.mjs web/src/erp/utils/userVisibleTechnicalFields.test.mjs` 通过 32 个测试；`cd server && go test ./internal/data -run 'TestWorkflowRepo_(TaskStatusReasonEventAndCompletionCleanup|CreateAndUpdateTaskStatus|UrgeWorkflowTaskWritesEventAndPayload)'` 通过；`cd server && go test ./internal/service -run 'TestJsonrpcDispatcher_WorkflowUrgeTask|TestJsonrpcDispatcher_Workflow(CompleteTaskAction|ControlledTaskActions)'` 通过；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过。
- 下一步：继续按 review findings 推进剩余 P0/P1，本地优先看客户配置 active revision 读回前置、普通账号真实 smoke 输入闭环、打印历史缺值回补或 ProcessRuntime / Workflow 真实动作 parity 的下一个可验证切片。
- 阻塞/风险：本轮只收口前端移动端 action submit guard 和静态守卫，不改后端 JSON-RPC / RBAC / WorkflowUsecase，不证明真实演示账号密码、真实登录、真实 RBAC、customer config active revision、目标环境发布、真实客户导入、备份恢复、回滚演练或客户签收；本轮不提交、不推送，工作区仍有大量既有未提交改动。

## 2026-07-02 客户配置包导入控制台

- 完成：参考 `docs/reference/第四次20260627/ERP客户配置包导入控制台设计规范.md`，在现有 DEV-only `/__dev/customer-config` 收口为“客户配置包导入控制台 / Package Import Console”。页面展示受控 6 步流程：上传解析、校验、差异对比、Dry Run、导入草稿版本、发布；补齐导入资产范围、客户包对象、策略 / 命令 / 扩展点登记检查、非法状态跳转 / 角色缺失 / 流程无闭环阻断项，以及配置版本快照、回滚和审计日志边界。新增前端 `rollback_customer_config` API wrapper，但不新增任意代码、SQL、脚本上传入口，不新增万能低代码平台，不新增正式菜单 / schema / RBAC，不导入或删除业务事实。
- 验证：追加前 `progress.md` 为 205 行 / 59,837 bytes，低于归档阈值；`node --test web/src/erp/config/devCustomerConfig.test.mjs web/src/erp/api/customerConfigApi.test.mjs web/src/erp/config/devHub.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 28 个测试；`node --test scripts/qa/customer-package-lint.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs scripts/deploy/customer-config-release-execute.test.mjs scripts/deploy/customer-config-release-readiness.test.mjs` 通过 72 个测试；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web css` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_SCENARIOS=dev-customer-config-dark-desktop,dev-customer-config-light-desktop,dev-customer-config-mobile STYLE_L1_PORT=5235 /usr/local/bin/pnpm --dir web style:l1` 通过 3 个场景；`git diff --check -- web/src/erp/api/customerConfigApi.mjs web/src/erp/api/customerConfigApi.test.mjs web/src/erp/config/devCustomerConfig.mjs web/src/erp/config/devCustomerConfig.test.mjs web/src/erp/config/devHub.mjs web/src/erp/pages/DevCustomerConfigPage.jsx web/scripts/style-l1/scenarios.mjs scripts/qa/dev-entry-boundary.test.mjs docs/当前真源与交接顺序.md` 通过。`pnpm --dir web test` 当前仍有 1 个既有字段链路目录漂移失败，失败点在 `src/erp/qa/fieldLinkageCatalog.test.mjs`，与本轮客户配置导入控制台链路无关。
- 下一步：若要从 DEV-only 控制台升级到正式导入入口，应单独评审后端包制品来源、签名 / 摘要校验、版本发布权限、发布审计和目标环境 release gate；当前仍保持“已注册部署包实现 + 客户配置控制面”的受控边界。
- 阻塞/风险：本轮没有实现原始 zip / 文件上传服务，没有允许上传任意代码、SQL 或脚本；没有新增策略实现或扩展点实现，只检查其必须来自已注册部署包；没有执行真实客户配置发布 / 激活 / 回滚、生产部署、目标环境 smoke、备份恢复、回滚演练、客户签收、提交或推送；工作区仍有大量既有未提交改动，本轮只隔离客户配置导入控制台相关文件。

## 2026-07-02 P5 发布证据前置验证与字段链路目录收口

- 完成：按 goal 继续收口 `docs/product/多甲方角色能力流程编排优先级.md` 剩余 P5。先复核当前 release evidence gate，确认现有 `deployments/yoyoosun/evidence/releases/2026-06-29` 仍是 draft / placeholder，P5 仍必须依赖目标环境真实发布、migration、smoke、effective session、备份恢复、回滚演练和签收证据。随后补齐 `web/src/erp/qa/fieldLinkageCatalog.mjs` 中遗漏的真实 FL case 登记，避免字段链路目录继续低估采购 / 委外业务草稿、供应商联系人快照和打印模板合同测试覆盖。
- 验证：追加前 `progress.md` 为 219 行 / 65,204 bytes，低于归档阈值；`bash scripts/project-scan.sh` 通过；`bash scripts/project-scan.sh --strict` 通过；`cd server && go test ./internal/biz ./internal/data ./internal/service` 通过；`cd web && /usr/local/bin/pnpm test -- --runInBand` 通过 522 个测试；`node --test web/src/erp/qa/fieldLinkageCatalog.test.mjs` 通过 5 个测试；`node scripts/qa/erp-field-linkage.mjs` 通过 113 个 FL case；`node --test scripts/qa/workflow-fact-boundary.test.mjs scripts/qa/workflow-ui-action-boundary.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs` 通过 19 个测试；`node --test scripts/deploy/production-preflight.test.mjs scripts/deploy/release-evidence-closeout-plan.test.mjs scripts/deploy/release-evidence-closeout-runner.test.mjs scripts/deploy/immutable-version-evidence.test.mjs scripts/deploy/image-digests-evidence.test.mjs scripts/deploy/backup-restore-rehearsal-script.test.mjs scripts/deploy/run-smoke-script.test.mjs` 通过 70 个测试；`node --test scripts/qa/customer-config-runtime-manifest.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs scripts/qa/docs-inventory.test.mjs` 通过 20 个测试；`node --test scripts/qa/multi-client-role-workflow-priority-audit.test.mjs` 通过 15 个测试；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok=true / readOnly=true / releaseReady=false`；`git diff --check` 通过。133 只读检查确认当前运行容器仍是旧镜像，compose migration status 为 `Current Version 20260612112337 / Pending Files 0 / Executed Files 20`。
- 下一步：P5 若要严谨升级为 gate-ready，必须先形成可追溯 release candidate：确认允许提交当前相关改动后，用该 commit 构建 server / web 镜像，在本地记录 tag 与 sha256 digest，再按低配边界上传到 133 执行 `docker load`、compose 切换、migration、target smoke、`customer_config.get_effective_session` 读回、备份恢复演练、rollback / forward-fix 演练和 release evidence gate。
- 阻塞/风险：当前工作区存在大量未提交改动，直接构建并部署会导致镜像内容无法和 `git commit` 严格绑定；在未获得提交授权前，不能把后续 133 部署证据写成可发布 release evidence。本轮尚未提交、推送、部署、执行真实 133 写入 smoke、激活 / 回滚客户配置、备份恢复、回滚演练或客户签收。

## 2026-07-02 客户配置扩展点阻断合同收口

- 完成：继续把当前 blocked goal 实际切到 review findings 驱动，不重新创建 goal。客户配置 runtime manifest 的 `extensionPointCatalog` 现在结构化输出 `implementation_source=registered_deployment_package_required`、`handler_allowed=false`、`customer_package_handler_allowed=false` 和 `blocked_reasons`；空扩展目录保持 `controlled_empty`，未来如绑定扩展点只能进入 `contract_preview_only`，不能携带 handler / module 或运行时执行能力。`__dev/customer-config` 的扩展点登记检查同步展示相同阻断原因，避免把客户配置、规则、策略、扩展点误读成已可执行的残缺闭环。
- 验证：追加前 `progress.md` 为 226 行 / 68,348 bytes，低于归档阈值；`node --test scripts/qa/customer-config-runtime-manifest.test.mjs scripts/qa/customer-package-lint.test.mjs web/src/erp/config/devCustomerConfig.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 45 个测试；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过并生成 preview；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web lint` 通过；`node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：继续按 review findings 推进剩余残缺链路，优先在客户配置 active revision 读回、普通账号模拟 / 真实 smoke 输入闭环、打印历史缺值回补、ProcessRuntime / Workflow 真实动作 parity 中选下一个本地可验证切片；release evidence 和真实目标环境仍只保留缺口说明。
- 阻塞/风险：本轮只收口客户配置扩展点阻断合同和 DEV-only 控制台投影，不实现扩展运行时、不允许客户包上传代码 / SQL / handler、不执行真实客户配置发布 / 激活 / 回滚、不导入真实客户数据、不生产部署、不做备份恢复 / 回滚演练 / 客户签收、不提交、不推送；工作区仍有大量既有未提交改动。

## 2026-07-02 试用账号与菜单真实浏览器闭环

- 完成：继续按 review findings 回应“没有账号就造本地测试账号”的缺口。本轮通过受控本地 seed 重置 10 个 `demo_*` 角色演示账号密码，随后真实调用本地后端 `/rpc/auth admin_login + me` 验证 10 个账号的单一角色、对应 mobile 权限、`demo_admin` 无 mobile 权限、无 debug 权限、非 super_admin、未禁用。浏览器 smoke 首次暴露脚本在 Vite 刚启动时用 `locator.isVisible({ timeout })` 过早判断登录表单不存在，已改为真正等待 `accountInput.waitFor({ state: 'visible', timeout: 8_000 })`；重跑后真实浏览器覆盖桌面菜单投影 10 个账号、移动岗位任务入口 9 个账号、`demo_admin` 移动入口拒绝态，以及 DEV-only 脱敏 effective session 诊断读回，报告来源包含 `active_customer_config_revision`。
- 验证：追加前 `progress.md` 为 240 行 / 73,511 bytes，低于归档阈值；`ERP_ROLE_DEMO_PASSWORD=<temporary> bash scripts/seed-role-demo-admins.sh --reset-password` 本地 dev DB 通过并重置 10 个 demo 账号；`TRIAL_ACCOUNT_PASSWORD=<temporary> node scripts/qa/trial-account-rbac.mjs --report output/trial-account-rbac/current-real-report.json` 通过，报告 `passedAccounts=10 / failedAccounts=0` 且不保存 password/token/Authorization；`TRIAL_ACCOUNT_PASSWORD=<temporary> node web/scripts/trialDemoAccountBrowserSmoke.mjs --report output/trial-demo-account-browser-smoke/current-real-report.json` 通过，报告 `desktopPassedCount=10 / mobilePassedCount=9 / mobileDeniedPassed=true / diagnosticBlockerCount=0 / diagnosticSources=active_customer_config_revision / projectionModes=local_dev_customer_config_diagnostic`；`node --check web/scripts/trialDemoAccountBrowserSmoke.mjs && node --check web/scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过；`node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/trial-account-rbac.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 18 个测试；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint --ext .js --ext .mjs scripts/trialDemoAccountBrowserSmoke.mjs scripts/trialDemoAccountBrowserSmoke.test.mjs` 通过；`node --test scripts/qa/docs-inventory.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 5 个测试；`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 返回 `ok=true / readOnly=true / releaseReady=false`；`git diff --check` 通过。
- 下一步：继续按 review findings 推进剩余残缺链路，优先看打印历史缺值回补、ProcessRuntime / Workflow 真实动作 parity、任务端完成 / 催办与内部通知的真实后端读写，或 active module state 对更多业务 API 的写门禁；release evidence 和目标环境仍保持外部缺口。
- 阻塞/风险：本轮执行了本地 dev DB demo 账号密码重置，使用的是一次性临时密码且未写入报告；这只证明当前本机 `8300` 后端和本地浏览器 runtime，不证明目标环境、真实客户账号、真实客户数据导入、客户签收、生产部署、备份恢复或回滚演练。不提交、不推送；工作区仍有大量既有未提交改动。

## 2026-07-02 P5 本地 amd64 镜像构建前置收口

- 完成：继续推进 P5，但仍不改变 133 运行态。先按低配部署主路径做本地 `linux/amd64` preflight 构建：`web/Dockerfile` 构建通过并应用 yoyoosun 静态配置；首次 `server/Dockerfile` 构建失败，根因是 server 镜像的 web-builder 只挑选复制 `web/vite*.mjs` 等文件，漏掉 `vite.shared.mjs` 直接 import 的 `web/devCustomerImportDryRunPlugin.mjs` 和 `web/devCustomerConfigPlugin.mjs`。已在 `server/Dockerfile` 补齐两个插件文件复制，复跑后 server 镜像构建通过，Go 二进制和内嵌前端 public 均产出。
- 验证：追加前 `progress.md` 为 233 行 / 70,459 bytes，低于归档阈值；`docker build --platform linux/amd64 --build-arg ERP_CUSTOMER_KEY=yoyoosun -f web/Dockerfile -t plush-toy-erp-web:p5-preflight-dirty-0781422e83fb-amd64 .` 通过，image id `sha256:4b2a81965cfa78d90586ceb273b0409bd4143cd6ab1f790cf74b6111f52e74de`；首次 server 构建失败于 `Could not resolve "./devCustomerImportDryRunPlugin.mjs"` 和 `Could not resolve "./devCustomerConfigPlugin.mjs"`；修复后 `docker build --platform linux/amd64 --build-arg ERP_CUSTOMER_KEY=yoyoosun ... -f server/Dockerfile -t plush-toy-erp-server:p5-preflight-dirty-0781422e83fb-amd64 .` 通过，image id `sha256:6af72d6e316022149208082299540c6f8ce548bf30deee92aab92a9bc0a8343a`；`docker image inspect` 确认两个镜像均为 `linux/amd64`；容器内检查确认 web 镜像 `/app/build/customer-config.js`、server 镜像 `/app/public/customer-config.js` 均包含 yoyoosun 配置，`/app/server -h` 正常输出；`node --test scripts/deploy/deployment-package-lint.test.mjs scripts/deploy/production-preflight.test.mjs scripts/deploy/image-digests-evidence.test.mjs` 通过 24 个测试；`node --test web/devCustomerConfigPlugin.test.mjs` 通过 3 个测试；`git diff --check` 通过；`node scripts/deploy/release-evidence-status.mjs --evidence-dir deployments/yoyoosun/evidence/releases/2026-06-29 --json` 摘要仍为 `status=draft / ready=false / gateReady=false / errorCount=100 / closeout.gateVerified=0`。
- 下一步：获得提交授权后，必须把 release candidate 相关改动纳入真实 commit，再用非 dirty tag 重新构建 server / web 镜像并记录可追溯 image digest，随后才允许上传到 133 执行 `docker load`、compose 切换、migration、target smoke、effective session 读回、备份恢复、rollback / forward-fix 演练和 release evidence gate。
- 阻塞/风险：本轮构建镜像使用 `p5-preflight-dirty-*` tag，只能证明当前未提交工作区的生产构建链路已过，不能作为正式 release evidence；当前仍未提交、未推送、未部署到 133，未执行目标环境 migration / smoke / 客户配置激活读回 / 备份恢复 / 回滚演练 / 客户签收。`web/devCustomerConfigPlugin.mjs` 当前仍是未跟踪文件，后续提交 release candidate 时必须一并纳入，否则 `server/Dockerfile` 在干净检出环境会再次构建失败。

## 2026-07-02 server 镜像 Vite 根层插件复制守卫

- 完成：把上一段本地 amd64 构建暴露的 `server/Dockerfile` 漏复制问题收口为自动化守卫。`scripts/qa/customer-config-boundaries.mjs` 现在会读取 `web/vite.shared.mjs` 的根层 `.mjs` imports，并要求 `server/Dockerfile` 显式复制这些文件；`./src/...` 这类已由 `COPY web/src ./src` 覆盖的路径不逐个要求，避免误报。该守卫已在 `fast.sh / strict.sh` 既有 customer-config-boundaries 入口内，不新增第二套发布流程。
- 验证：追加前 `progress.md` 为 240 行 / 73,511 bytes，低于归档阈值；首次新增守卫时故意暴露过宽断言，误把 `web/src/erp/config/appRegistry.mjs` 也要求单独复制，已收窄为根层 `.mjs` imports；`node scripts/qa/customer-config-boundaries.mjs` 通过；`node --test web/devCustomerConfigPlugin.test.mjs web/devCustomerImportDryRunPlugin.test.mjs scripts/qa/customer-config-runtime-manifest.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs` 通过 23 个测试；`node --test scripts/deploy/deployment-package-lint.test.mjs scripts/deploy/production-preflight.test.mjs scripts/deploy/image-digests-evidence.test.mjs` 通过 24 个测试；`git diff --check` 通过。
- 下一步：仍需提交授权后才能形成真实 release commit，并用非 dirty tag 重新构建、上传和部署到 133；后续正式提交时必须纳入 `web/devCustomerConfigPlugin.mjs`，否则守卫和 Docker build 会在干净环境失败。
- 阻塞/风险：本轮只新增构建上下文守卫，不提交、不推送、不改 133、不写正式 release evidence；目标环境 migration、smoke、effective session、备份恢复、回滚演练和签收仍未执行。

## 2026-07-02 133 baseline 与 fast 门禁复核

- 完成：继续在未获提交授权前推进只读目标环境 baseline。133 当前 `/opt/plush-toy-erp/current` 不是 git checkout，运行中的镜像仍是旧版本：server `plush-toy-erp-server:20260612T111023Z-6d2a491aaa01-trace-hardening-amd64`，web `plush-toy-erp-web:20260611T022040-af97b4f-customer-config-amd64`，Postgres `postgres:18`，Jaeger 仍是 `jaegertracing/all-in-one:latest`；`healthz / readyz` 正常，migration status 为 `Current Version 20260612112337 / Pending Files 0 / Executed Files 20`。远端当前源码包缺少 `scripts/deploy/production-preflight.sh`，后续正式发布必须随 release candidate 带上当前发布脚本和证据工具，不能依赖远端旧源码包。
- 验证：追加前 `progress.md` 为 254 行 / 78,513 bytes，低于归档阈值；SSH 只读检查 133 compose ps / images / migration / health / ready 通过；`curl http://127.0.0.1:5175/customer-config.js` 当前返回 404，说明旧 web 镜像不具备本轮 yoyoosun 静态配置证据，不能拿它证明 P5；`bash scripts/qa/fast.sh` 完整通过，包含 `customer-config-boundaries.mjs`、客户配置 runtime manifest、发布证据脚本单测、web lint/css、server Go 快速检查等常规门禁；`git diff --check` 通过。
- 下一步：必须先形成真实 commit，再以该 commit 重新构建非 dirty server / web 镜像，上传到 133 后做 compose 切换、migration、target smoke、customer_config effective session 读回、备份恢复、rollback / forward-fix 演练和 release evidence gate。
- 阻塞/风险：当前仍未提交、未推送、未部署新镜像到 133；已有 133 baseline 只能证明切换前旧环境状态，不能证明本轮 release candidate。`fast.sh` 会运行 `eslint --fix`，当前大范围 dirty 工作区仍需在提交前精确审查和 stage。

# progress 2026-07-02 effective session 诊断 smoke 前归档

本文件归档自 `progress.md`。归档内容只用于追溯历史过程，不作为当前正式需求、runtime、schema、migration、API、UI、部署或产品路线真源。当前状态仍以仓库正式文档、代码和测试为准。

## 2026-07-02 progress 归档到试用真实 smoke 前

- 完成：归档前 `progress.md` 为 364 行 / 83,330 bytes，已超过 80KB 阈值；已将 2026-07-01 BOM 明细内联编辑前至 `qa:full` 生命周期收口的过程记录移动到 `docs/archive/progress-2026-07-02-before-trial-real-smoke.md`。
- 完成：根 `progress.md` 继续保留归档索引、当前活跃事项、归档说明、`qa:strict` 本地严格门禁和试用账号 / 移动任务端 no-write preflight 两条最近记录，避免过程流水继续膨胀为隐藏真源。
- 下一步：若继续本地试用闭环，仍按缺密码边界选择真实账号 / 浏览器 smoke 的 no-write 或真实密码到位后的真实登录 smoke；后续再更新 `progress.md` 前先检查 80KB / 600 行阈值。
- 阻塞/风险：本轮只归档过程记录，不改变 runtime、schema、RBAC、Workflow / Fact、客户配置、测试脚本、部署、真实客户数据、目标环境 evidence、提交或推送。

## 2026-07-02 qa:strict 本地严格门禁收口

- 完成：按当前长期 goal 在 `qa:full` 通过后继续跑本地 `qa:strict`，确认角色 / 菜单 / adminProfileSync、Workflow action、字段链路、错误提示、`/__dev`、客户配置包、试用账号 no-write、移动端 smoke 输入模板、release evidence 本地门禁、前端测试 / 构建和后端全量测试均通过。该动作只证明本地严格门禁，不扩大目标环境 release evidence。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `releaseReady=false`，仍只保留目标环境证据缺口；`bash scripts/qa/strict.sh` 全部通过，覆盖 db-guard、secrets、边界守卫、文档清单、客户配置 / import / release evidence 工具测试、web `eslint --max-warnings=0`、stylelint、web test 453 项、web build、server `go test ./...` 和 `make build`。
- 下一步：继续本地试用闭环时，若没有演示账号密码，优先生成 `trial-account-rbac`、`trialDemoAccountBrowserSmoke`、`mobileWorkflowRuntimeBrowserSmoke` 的 no-write preflight；若具备密码和本地后端 / 前端前置，再跑真实登录 / 浏览器 smoke。
- 阻塞/风险：本轮不执行真实登录、不运行真实客户导入、不执行生产部署、目标环境 migration、目标 smoke、备份恢复、回滚演练、release sign-off 或客户配置目标环境激活；`releaseReady=false` 是外部目标环境证据缺口，不作为本轮本地阻断。工作区仍有多组既有未提交改动，本条只新增本地严格门禁验证记录。

## 2026-07-02 试用账号与移动任务端 no-write preflight 收口

- 完成：复核 `business-formal-module-shells-desktop` 当前已通过，不再是最高优先级本地 blocker；按当前 goal 改为生成试用账号 RBAC、试用浏览器 smoke、移动 Workflow runtime smoke 的 no-write preflight 报告，明确无演示密码时只确认前置边界，不证明真实登录、真实 RBAC、真实 customer_config active revision 或后端 JSON-RPC 行为。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过，仍为 `releaseReady=false` 且阻塞类别仅是 `external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`node scripts/qa/trial-account-rbac.mjs --preflight-report output/trial-account-rbac/preflight.json`、`node web/scripts/trialDemoAccountBrowserSmoke.mjs --preflight-report output/trial-demo-account-browser-smoke/preflight.json`、`node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report output/mobile-workflow-runtime-browser-smoke/preflight.json` 均写入本地报告且 `ready=false`，blocker 均为缺少演示账号密码。
- 下一步：若提供演示账号密码并确认只连本地开发环境，再跑真实 `trial-account-rbac --report`、`smoke:trial-demo-browser` 和 `smoke:mobile-workflow-runtime-browser`；否则继续只做本地静态 / mock / no-write 闭环。
- 阻塞/风险：本轮不执行真实登录、真实客户导入、生产部署、目标环境 migration、目标 smoke、备份恢复、回滚演练、release sign-off、客户配置发布 / 激活 / 回滚、提交或推送。`output/` 为本地忽略报告目录，当前工作区仍有多组既有未提交改动。

## 2026-07-02 客户配置 active revision 读回 no-write preflight 收口

- 完成：刷新 `output/customers/yoyoosun/customer-config-runtime-manifest.json`，并通过 `customer-config-release-readiness.mjs --readback-preflight-report` 生成 `output/customers/yoyoosun/customer-config-readback-preflight.json`；该报告只核本地 manifest、执行器报告和目标 smoke 脱敏结构，不调用后端、不读取 token、不写数据库、不写 release evidence、不证明 active revision 已读回。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且仍为 `releaseReady=false`，阻塞类别仍是 `external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview --out output/customers/yoyoosun/customer-config-runtime-manifest.json` 通过；`node scripts/deploy/customer-config-release-readiness.mjs --manifest output/customers/yoyoosun/customer-config-runtime-manifest.json --evidence-dir deployments/yoyoosun/evidence/releases/2026-06-29 --release-report output/customers/yoyoosun/customer-config-release/customer-config-release-report.json --readback-preflight-report output/customers/yoyoosun/customer-config-readback-preflight.json` 写入报告且 `readyForReadinessGate=false`；`node --test scripts/qa/customer-config-runtime-manifest.test.mjs scripts/deploy/customer-config-release-readiness.test.mjs` 通过 43 项。
- 下一步：只有具备已执行 release report、`effectiveSessionVerification` 和目标 smoke `customer-config-effective-session` 证据后，才运行 readiness 的 `--require-activated` 或 `--require-rollback`；否则继续把该项保留为目标环境证据缺口。
- 阻塞/风险：当前 readback preflight blocker 为 `missing-effective-session-verification`、`smoke-report-missing-backend-endpoint-alias`、`missing-smoke-effective-session-check`。本轮不执行客户配置 publish / activate / rollback，不做真实目标 smoke、生产部署、真实客户导入、备份恢复、回滚演练、release sign-off、提交或推送；`output/` 为本地忽略报告目录，当前工作区仍有多组既有未提交改动。

## 2026-07-02 移动端认证回跳 mock browser smoke 收口

- 完成：确认 `business-formal-module-shells-desktop` 已复跑通过，不再是当前本地 L1 阻断；本轮改为推进岗位任务端移动认证回跳本地浏览器闭环，生成 `output/mobile-auth-login-route-smoke/preflight.json`，并用 mock auth / workflow RPC 验证 9 个业务岗位的 `/m/<role>/tasks` 单端口路由、登录回跳、任务页、通知 / 预警线索和手机 / iPad 视口布局。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且仍为 `releaseReady=false`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH node web/scripts/mobileAuthLoginRouteSmoke.mjs --preflight-report output/mobile-auth-login-route-smoke/preflight.json` 写入 no-write preflight；`PATH=/usr/local/bin:$PATH node --test web/scripts/mobileAuthLoginRouteSmoke.test.mjs` 通过 5 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web smoke:mobile-auth-login-route` 通过 9 个岗位任务端角色；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web css` 通过。
- 下一步：若没有演示账号密码，继续选择不依赖密码的本地静态 / mock / no-write 闭环；若提供密码并确认只连本地开发环境，再跑真实试用账号 RBAC、真实试用浏览器 smoke 和移动 Workflow runtime smoke。
- 阻塞/风险：本轮 mock smoke 不调用真实后端、不登录真实账号、不证明真实 RBAC、customer config active revision、目标环境 smoke、真实客户导入、生产部署、备份恢复、回滚演练或客户签收；本轮未改前端源码，因此没有运行“触达源码文件 eslint”。`output/` 为本地忽略报告目录，当前工作区仍有多组既有未提交改动。

## 2026-07-02 字段链路 / 错误提示 / dev-only 入口静态守卫收口

- 完成：在 `business-formal-module-shells-desktop` 继续通过后，按当前本地试用闭环优先级复核销售订单字段策略、打印边界、前端错误提示、用户可见技术字段、`/__dev/testing` 与客户配置控制台 preview / blocked 门禁；本轮只做本地静态守卫和单测收口，不改 runtime、不写 release evidence。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且仍为 `releaseReady=false`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`node --test scripts/qa/sales-order-field-chain-boundary.test.mjs scripts/qa/frontend-error-message-boundary.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 11 项；`node --test web/src/erp/utils/userVisibleTechnicalFields.test.mjs web/src/erp/config/devTesting.test.mjs web/src/erp/config/printTemplates.test.mjs web/src/erp/config/devCustomerConfig.test.mjs web/devCustomerConfigPlugin.test.mjs` 通过 40 项。
- 下一步：若继续没有演示账号密码，优先推进不依赖密码的本地 mock / no-write / static 闭环；若具备密码和本地前置，再跑真实账号、真实 RBAC 与浏览器 smoke。
- 阻塞/风险：本轮不证明真实登录、真实客户配置 active revision、正式打印模板 mapper、真实销售订单打印、真实客户导入、目标环境 smoke、生产部署、备份恢复、回滚演练或客户签收；当前工作区仍有多组既有未提交改动。

## 2026-07-02 试用模拟数据 no-write 入口收口

- 完成：确认 `business-formal-module-shells-desktop` 当前仍通过，不再是供应商弹窗 focus 阻断；本轮转为补强试用模拟数据 / seed fixture 的本地 no-write 证据，生成试用客户 / 供应商 / 联系人 / 销售订单模拟数据 report-only 输出，并生成岗位任务端模拟 Workflow 任务 report-only 输出。业务事实模拟闭环因需要真实活跃产品 / 单位 / 仓库 ID，本轮只保留输入模板，不伪造 ID。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且仍为 `releaseReady=false`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`node --test scripts/qa/trial-simulated-data.test.mjs scripts/qa/operational-fact-simulated-closure.test.mjs scripts/qa/mobile-workflow-simulated-closure.test.mjs` 通过 25 项；`node scripts/qa/trial-simulated-data.mjs --print-input-template`、`node scripts/qa/operational-fact-simulated-closure.mjs --print-input-template`、`node scripts/qa/mobile-workflow-simulated-closure.mjs --print-input-template` 均输出 no-write 输入模板；`node scripts/qa/trial-simulated-data.mjs --out output/customers/yoyoosun/trial-simulated-data` 写入 `simulatedOnly=true / realCustomerImport=false` report-only；`node scripts/qa/mobile-workflow-simulated-closure.mjs --run-id 20260702TLOCALZ --out output/customers/yoyoosun/mobile-workflow-simulated-closure` 写入 `simulatedOnly=true / realCustomerImport=false / factPosting=false` report-only。
- 下一步：若继续没有演示账号密码，唯一建议是先用本地 seed 生成活跃产品 / 单位 / 仓库 ID 后，再跑业务事实模拟闭环的 report-only 输入校验；若具备密码和本地后端前置，再进入真实试用账号 RBAC / 浏览器 smoke。
- 阻塞/风险：本轮不登录、不调用后端、不写数据库、不执行 `--apply`、不导入真实客户数据、不创建 `business_records`、不写库存 / 出货 / 财务事实、不做生产部署、目标环境 smoke、备份恢复、回滚演练、客户签收、提交或推送；`output/` 为本地忽略报告目录，当前工作区仍有多组既有未提交改动。

## 2026-07-02 业务事实模拟闭环 report-only 前置 ID 收口

- 完成：沿用上一条下一步，先确认 dev DB 目标为 `192.168.0.106:5432/plush_erp` 且当前 shell 没有 `POSTGRES_DSN / DB_URL / USE_ENV_DB_URL / ERP_ALLOW_TEST_DB_AS_DEV / APP_ENV / ERP_ENV / GO_ENV` 覆盖；随后执行 `seed-core-demo-data` 写入 / 更新 `SIM-PLUSH-CORE` 本地模拟主数据，生成活跃产品 / 单位 / 仓库 ID，并用 `product_id=8 / unit_id=11 / warehouse_id=11` 生成 `operational-fact-simulated-closure` report-only 报告。本轮不加 `--apply`，不登录、不调用 JSON-RPC、不写业务事实、不导入真实客户数据。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH bash scripts/seed-core-demo-data.sh` 输出 `simulated_only=true / real_customer_import=false / no_business_records=true / no_direct_fact_posting=true` 和 `operational_fact_args=--product-id 8 --unit-id 11 --warehouse-id 11`；`PATH=/usr/local/bin:$PATH node scripts/qa/operational-fact-simulated-closure.mjs --product-id 8 --unit-id 11 --warehouse-id 11 --run-id 20260702TLOCALZ --out output/customers/yoyoosun/operational-fact-simulated-closure` 写入 `mode=report-only / steps=0 / simulatedOnly=true / realCustomerImport=false / customerAcceptanceRequiredForClosure=false` 报告；`PATH=/usr/local/bin:$PATH node --test scripts/qa/operational-fact-simulated-closure.test.mjs` 通过 8 项；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：若继续没有演示账号密码，本地可继续做 no-write / mock / static 闭环；若提供密码并确认只连本地开发环境，再跑真实试用账号 RBAC、真实试用浏览器 smoke 和移动 Workflow runtime smoke。
- 阻塞/风险：本轮本地 seed 只写 / 更新 `SIM-PLUSH-CORE` 模拟主数据，report-only 只写 ignored `output/` 报告；不证明真实 RBAC、真实账号、真实 customer_config active revision、真实业务事实 apply、正式客户导入、目标环境 smoke、生产部署、备份恢复、回滚演练或客户签收。当前工作区仍有多组既有未提交改动，本条只新增本地模拟主数据输出、report-only 输出和 `progress.md` 记录。

## 2026-07-02 MVP 闭环 no-write 汇总报告收口

- 完成：复核 `business-formal-module-shells-desktop` 已通过，不再是供应商弹窗 focus 阻断；本轮按无演示密码边界，使用现有 `mvp-closure` no-write 汇总入口生成 `output/customers/yoyoosun/mvp-closure/mvp-closure-report.json` 和 Markdown 报告。该报告汇总试用模拟数据、业务事实模拟闭环和岗位任务端模拟 Workflow 三个 report-only 工具，使用上一轮本地 core demo seed ID `product_id=8 / unit_id=11 / warehouse_id=11`，不执行 `--apply`、不调用后端、不写数据库、不写 release evidence。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH node --test scripts/qa/mvp-closure.test.mjs scripts/qa/trial-simulated-data.test.mjs scripts/qa/operational-fact-simulated-closure.test.mjs scripts/qa/mobile-workflow-simulated-closure.test.mjs` 通过 28 项；`PATH=/usr/local/bin:$PATH node scripts/qa/mvp-closure.mjs --run-report-tools --product-id 8 --unit-id 11 --warehouse-id 11 --out output/customers/yoyoosun/mvp-closure` 通过，报告字段为 `mode=report-with-no-write-tools / writesDatabase=false / callsBackend=false / realCustomerImport=false`，三个 no-write 子工具均 `PASS`；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：若继续没有演示账号密码，下一轮只适合继续 no-write / mock / static 范围；若提供密码并确认只连本地开发环境，唯一建议转入真实试用账号 RBAC、真实试用浏览器 smoke 和移动 Workflow runtime smoke。
- 阻塞/风险：MVP 汇总报告不能替代领域单测、PG 集成测试、真实浏览器登录、真实后端 RBAC、customer config active revision 读回、真实业务事实 apply、正式客户导入、目标环境 smoke、生产部署、备份恢复、回滚演练或客户签收。当前工作区仍有多组既有未提交改动，本条只新增 ignored `output/` 汇总报告和 `progress.md` 记录。

## 2026-07-02 采购入库真实写入链路 no-write preflight 收口

- 完成：复核 `business-formal-module-shells-desktop` 继续通过，不再是当前本地 L1 阻断；本轮按无演示密码、无真实写入确认边界，生成服务层和浏览器层采购入库真实写入 E2E no-write preflight 报告。服务层报告 `ready=true / writes=false / blockers=[]`；浏览器层报告 `ready=false / writes=false`，受控阻断为缺少持久测试数据确认和管理员凭据。本轮只写 ignored `output/` 报告，不启动真实 E2E、不登录、不调用 JSON-RPC 写入、不写库存事实。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH node --test scripts/qa/purchase-receipt-real-write-e2e.test.mjs web/scripts/purchaseReceiptRealWriteBrowserE2E.test.mjs` 通过 13 项；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --preflight-report output/qa/purchase-receipt-real-write-e2e/preflight.json` 写入 ready 报告；`PATH=/usr/local/bin:$PATH node web/scripts/purchaseReceiptRealWriteBrowserE2E.mjs --preflight-report output/purchase-receipt-real-write-browser-e2e/preflight.json` 写入受控 blocked 报告。
- 下一步：若要证明真实采购入库页面 / 后端闭环，必须先提供管理员凭据并显式确认持久测试数据写入，只连接本地 / 开发库后再运行真实命令；否则继续只做 no-write / mock / static 闭环。
- 阻塞/风险：preflight 不证明真实登录、真实 RBAC、真实浏览器写入、purchase_receipts / inventory_txns / balances 落账、customer config active revision、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练或客户签收。当前工作区仍有多组既有未提交改动，本条只新增 ignored `output/` 报告和 `progress.md` 记录。

## 2026-07-02 采购入库服务层 real-write E2E 收口

- 完成：复核 `business-formal-module-shells-desktop` 当前继续通过，供应商弹窗焦点回归不再是本地阻断；在上一轮服务层 preflight ready 的基础上，运行采购入库服务层 real-write E2E 默认路径，生成 `output/qa/purchase-receipt-real-write-e2e/purchase-receipt-real-write-e2e.json` 和 Markdown 报告。该路径覆盖采购入库草稿、加行、过账、回显、列表、看板入库 projection、取消冲正、权限拒绝、非法行失败和重复过账 / 取消幂等；本轮未加 `--with-postgres`，不触发本地 PG 防呆路径，不连接目标环境，不导入真实客户数据。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --out output/qa/purchase-receipt-real-write-e2e` 通过，报告字段为 `withPostgres=false / writesTestDatabase=true / writesProductionDatabase=false / realCustomerImport=false`；`PATH=/usr/local/bin:$PATH node --test scripts/qa/purchase-receipt-real-write-e2e.test.mjs` 通过 7 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web css` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：若继续没有管理员凭据和持久测试数据确认，下一步只适合继续 no-write / mock / static 或服务层本地测试；若要证明真实浏览器采购入库页面闭环，必须显式提供管理员凭据、确认持久测试数据写入，并只连接本地 / 开发库后再跑 `smoke:purchase-receipt-real-write`。
- 阻塞/风险：本轮服务层 E2E 不证明真实浏览器写入、真实登录、真实 RBAC 账号矩阵、customer config active revision、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练或客户签收；`releaseReady=false` 仍是外部目标环境证据缺口。当前工作区仍有多组既有未提交改动，本条只新增 ignored `output/` 报告和 `progress.md` 记录。

## 2026-07-02 采购入库服务层 real-write E2E PostgreSQL guard 收口

- 完成：复核 `business-formal-module-shells-desktop` 继续通过，priority audit 仍为本地 ready / 目标 evidence 缺口分离；本轮在上一条服务层 real-write E2E 基础上，先用 `--with-postgres --preflight-report` 确认本机 PostgreSQL guard 可运行，再执行采购入库服务层 real-write E2E 的 PostgreSQL 模式，生成 `output/qa/purchase-receipt-real-write-e2e/purchase-receipt-real-write-e2e.json` 和 Markdown 报告。报告覆盖采购入库草稿、加行、过账、回显、列表、看板入库 projection、取消冲正、权限拒绝、非法行失败、重复过账 / 取消幂等，并额外通过 `make purchase_receipt_pg_test` 验证本地测试库防呆链路。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --with-postgres --preflight-report output/qa/purchase-receipt-real-write-e2e/postgres-preflight.json` 输出 `ready=true`；`PATH=/usr/local/bin:$PATH node scripts/qa/purchase-receipt-real-write-e2e.mjs --with-postgres --out output/qa/purchase-receipt-real-write-e2e` 通过，报告字段为 `withPostgres=true / writesTestDatabase=true / writesProductionDatabase=false / realCustomerImport=false`；`PATH=/usr/local/bin:$PATH node --test scripts/qa/purchase-receipt-real-write-e2e.test.mjs` 通过 7 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web css` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：若继续没有管理员凭据和持久测试数据确认，本地采购入库真实写入只能继续停在服务层 / PostgreSQL 测试库证据；真实浏览器采购入库页面闭环仍必须先提供管理员凭据、确认持久测试数据写入，并只连接本地 / 开发库后再跑 `smoke:purchase-receipt-real-write`。
- 阻塞/风险：本轮 PostgreSQL guard 只证明本地测试库链路，不证明真实浏览器写入、真实登录、真实 RBAC 账号矩阵、customer config active revision、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练或客户签收；`releaseReady=false` 仍是外部目标环境证据缺口，不作为本轮本地阻断。当前工作区仍有多组既有未提交改动，本条只新增 ignored `output/` 报告和 `progress.md` 记录。

## 2026-07-02 真实登录 smoke 共享 helper no-write preflight 收口

- 完成：复核 `business-formal-module-shells-desktop` 继续通过，因此供应商弹窗 focus 不再是当前本地 L1 阻断；本轮按无真实密码边界，运行 `realLoginSmokeShared.mjs --preflight-report` 生成 `output/real-login-smoke-shared/preflight.json`。报告只探测本地后端 health、URL 安全边界和凭据来源候选，确认不会读取密码、不会校验账号、不会调用 auth JSON-RPC、不会启动 Vite / Playwright、不会写数据库或保存 token。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH node web/scripts/realLoginSmokeShared.mjs --preflight-report output/real-login-smoke-shared/preflight.json` 写入 no-write 报告，字段显示 `writesDatabase=false / startsBrowser=false / callsJSONRPCAuth=false / credentialEnvPresent=false / backendHealth.ok=true / readyForCredentialedSmokeCandidate=true`；`PATH=/usr/local/bin:$PATH node --test web/scripts/realLoginSmokeShared.test.mjs` 通过 6 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web css` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：若提供真实管理员或演示账号密码，并确认只连接本地 / 开发环境，再从该 preflight 的 `nextCommand` 进入真实登录 smoke；否则继续只推进 no-write / mock / static 或服务层本地证据。
- 阻塞/风险：本轮 preflight 不证明真实登录、真实 RBAC、真实浏览器页面、岗位任务端真实账号、采购入库浏览器写入、customer config active revision、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练或客户签收。当前工作区仍有多组既有未提交改动，本条只新增 ignored `output/` 报告和 `progress.md` 记录。

## 2026-07-02 yoyoosun 试用账号 RBAC no-write preflight 收口

- 完成：复核 `business-formal-module-shells-desktop` 继续通过，供应商弹窗 focus 仍不是当前本地阻断；本轮按 `docs/customers/yoyoosun/试用环境执行手册.md` 的客户 scoped 报告路径，运行试用账号 RBAC no-write preflight，生成 `output/customers/yoyoosun/trial-account-rbac/preflight.json`。报告只探测后端 health、演示账号密码环境变量是否存在和静态角色投影，不读取密码、不登录、不调用 `admin_login / me`、不写数据库、不保存 token。
- 验证：`node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH node scripts/qa/trial-account-rbac.mjs --preflight-report output/customers/yoyoosun/trial-account-rbac/preflight.json` 写入 no-write 报告，字段显示 `writesDatabase=false / readsPasswordValue=false / storesAccessToken=false / backendHealth.ok=true / staticProjection.ok=true / passwordEnvPresent=false / blockers=[missing-trial-account-password-env]`；`PATH=/usr/local/bin:$PATH node --test scripts/qa/trial-account-rbac.test.mjs` 通过 9 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web css` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：若提供 `TRIAL_ACCOUNT_PASSWORD` 并确认只连本地 / 开发环境，再运行 `trial-account-rbac.mjs --report` 证明真实 `/rpc/auth admin_login + me`；否则继续把真实账号 RBAC 保留为缺密码缺口，不把 no-write preflight 解释成真实 RBAC 通过。
- 阻塞/风险：本轮 preflight 不证明真实登录、真实 RBAC、真实浏览器菜单、岗位任务端真实账号、customer config active revision、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练或客户签收。当前工作区仍有多组既有未提交改动，本条只新增 ignored `output/` 报告和 `progress.md` 记录。

## 2026-07-02 yoyoosun 试用浏览器菜单 / 岗位入口 no-write preflight 收口

- 完成：复核 `business-formal-module-shells-desktop` 继续通过，供应商弹窗 focus 与 BOM 备注断言不是当前本地阻断；本轮按缺演示密码边界，生成客户 scoped 试用浏览器菜单 / 岗位入口 no-write preflight 报告 `output/customers/yoyoosun/trial-demo-account-browser-smoke/preflight.json`。报告只探测后端 health、演示密码 env、Vite 托管需求、yoyoosun customer config 脚本和静态菜单 / 岗位入口计划，不读取密码、不登录、不调用 JSON-RPC、不启动 Vite / Playwright、不写数据库、不保存 token。
- 验证：`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH node web/scripts/trialDemoAccountBrowserSmoke.mjs --preflight-report output/customers/yoyoosun/trial-demo-account-browser-smoke/preflight.json` 写入 no-write 报告，字段显示 `writesDatabase=false / startsBrowser=false / startsDevServer=false / readsPasswordValue=false / storesAccessToken=false / backendHealth.ok=true / blockers=[missing-demo-password-env]`；`PATH=/usr/local/bin:$PATH node --test web/scripts/trialDemoAccountBrowserSmoke.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 4 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web css` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过。
- 下一步：若提供 `TRIAL_ACCOUNT_PASSWORD` 并确认只连本地 / 开发环境，再运行真实 `smoke:trial-demo-browser` 和真实 `trial-account-rbac.mjs --report`；否则继续把真实浏览器菜单、岗位入口和真实 RBAC 保留为缺密码缺口，不把 no-write preflight 解释成真实浏览器验收通过。
- 阻塞/风险：本轮 preflight 不证明真实登录、真实 RBAC、真实浏览器菜单、岗位任务端真实账号、customer config active revision、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送。当前工作区仍有多组既有未提交改动，本条只新增 ignored `output/` 报告和 `progress.md` 记录。

## 2026-07-02 yoyoosun 移动 Workflow runtime no-write preflight 收口

- 完成：复核 `business-formal-module-shells-desktop` 继续通过，供应商弹窗 focus 仍不是当前本地阻断；本轮按缺演示密码边界，生成客户 scoped 移动 Workflow runtime no-write preflight 报告 `output/customers/yoyoosun/mobile-workflow-runtime-browser-smoke/preflight.json`。报告只探测本地后端 health、演示密码 env、Vite 托管需求、yoyoosun customer config 脚本和模拟任务动作计划 coverage，不读取密码、不登录、不调用 JSON-RPC、不启动 Vite / Playwright、不创建 workflow 任务、不写数据库、不保存 token。
- 验证：`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false`，目标环境 release evidence 缺口未升级为本轮阻断；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH node web/scripts/mobileWorkflowRuntimeBrowserSmoke.mjs --preflight-report output/customers/yoyoosun/mobile-workflow-runtime-browser-smoke/preflight.json` 写入 no-write 报告，字段显示 `writesDatabase=false / startsBrowser=false / startsDevServer=false / callsJSONRPC=false / createsWorkflowTasks=false / readsPasswordValue=false / storesAccessToken=false / backendHealth.ok=true / simulatedTaskPlanCoverage.ok=true / readyForRealSmoke=false / blockers=[missing-demo-password-env]`；`PATH=/usr/local/bin:$PATH node --test scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 9 项。
- 下一步：若提供 `MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD` 并确认只连本地 / 开发环境，再先复跑 preflight，随后运行真实 `pnpm --dir web smoke:mobile-workflow-runtime-browser` 证明老板阻塞 / 完成 / 退回、跨角色催办、reason 必填、完成反馈、异常上报、evidence refs 和内部提醒线索的真实页面链路；否则继续把真实移动端 workflow runtime 保留为缺密码缺口。
- 阻塞/风险：本轮 preflight 不证明真实登录、真实 RBAC、真实移动端浏览器任务动作、真实 Workflow 写入、Fact 落账、customer config active revision、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送。当前工作区仍有多组既有未提交改动，本条只新增 ignored `output/` 报告和 `progress.md` 记录；曾尝试错误测试路径 `web/scripts/mobileWorkflowRuntimeBrowserSmoke.test.mjs`，已改用正式入口 `scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 验证通过。

## 2026-07-02 移动端单端口登录回跳 mock browser smoke 收口

- 完成：复核 `business-formal-module-shells-desktop` 继续通过，供应商弹窗 focus 不是当前本地阻断；本轮改为验证当前未提交的移动端单端口改动，确认岗位任务端从旧多 Vite / 多端口入口收口到生产同构 `/m/<role>/tasks` 后，登录回跳、入口路由、phone / iPad 布局和测试入口仍可跑通。修正 `web/scripts/mobileAuthLoginRouteSmoke.mjs` 与 `web/scripts/startYoyoosunDev.mjs` 的 eslint 风格问题，不改变 mock RPC、真实后端、RBAC、Workflow / Fact 或生产发布逻辑。
- 验证：`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH node --test web/scripts/mobileAuthLoginRouteSmoke.test.mjs web/src/pages/AdminLogin/adminLoginRouting.test.mjs web/src/erp/config/entryConfig.test.mjs web/src/erp/config/seedData.test.mjs web/src/common/consts/favicon.test.mjs scripts/qa/trial-role-entry-docs.test.mjs web/src/erp/config/devTesting.test.mjs` 通过 37 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint --ext .js --ext .jsx --ext .mjs src/App.jsx src/erp/mobile/MobileAppLayout.jsx src/pages/AdminLogin/adminLoginRouting.mjs src/pages/AdminLogin/index.jsx src/erp/config/appRegistry.mjs src/erp/config/seedData.mjs src/erp/context/ERPWorkspaceProvider.jsx src/common/consts/favicon.mjs scripts/mobileAuthLoginRouteSmoke.mjs scripts/startYoyoosunDev.mjs scripts/localPort.mjs scripts/previewYoyoosun.mjs scripts/serveStaticApp.mjs` 通过；`PATH=/usr/local/bin:$PATH node --check web/scripts/startYoyoosunDev.mjs && PATH=/usr/local/bin:$PATH node --check web/scripts/localPort.mjs` 通过；`MOBILE_AUTH_SMOKE_APP_ID=mobile-purchasing PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web smoke:mobile-auth-login-route` 通过；全量 `PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web smoke:mobile-auth-login-route` 复跑通过 9 个岗位任务端角色；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web css` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过。
- 下一步：若继续没有真实演示账号密码，下一轮仍只适合推进 no-write / mock / static 本地闭环；若提供 `TRIAL_ACCOUNT_PASSWORD` 或 `MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD` 并确认只连本地 / 开发环境，再转入真实账号 RBAC、真实试用浏览器 smoke 或真实移动 Workflow runtime smoke。
- 阻塞/风险：`smoke:mobile-auth-login-route` 使用 mock auth / workflow RPC，不证明真实登录、真实 RBAC、customer config active revision、真实 Workflow 写入、Fact 落账、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送；全量 smoke 首次运行曾在采购岗位回跳超时，单独 `mobile-purchasing` 和随后全量复跑均通过，当前记录为已复验通过但仍保留该抖动风险。当前工作区仍有多组既有未提交改动，本条只修改两个相关脚本的 eslint 风格并追加 `progress.md` 记录。

## 2026-07-02 前端 mobile 旧多端口入口治理

- 完成：按页面治理 skill 和当前单入口登录口径，移除 `web/package.json` 的 `start:mobile:*` / `start:mobile:all` 脚本，删除旧 mobile Vite 配置与 `startMobileApps.mjs`；`appRegistry` 的移动角色只保留岗位元数据和 `/m/<role>/tasks` 描述，不再带端口或启动命令；运行时固定桌面单入口，不再通过 `VITE_ERP_APP_ID` 切换旧 mobile app；删除旧独立 mobile router，并将登录回跳、MobileAppLayout、favicon、mobile auth smoke、README / docs / QA 断言统一到 `/m/<role>/tasks`。
- 验证：`rg` 扫描确认当前代码 / README / scripts / 产品文档中不再命中 `start:mobile`、`vite.mobile`、`MOBILE_AUTH_SMOKE_LEGACY_MULTI_APP`、`legacyMultiAppMode`、`VITE_ERP_APP_ID`、旧顶层 `'/tasks'` / `"/tasks"` 和 5186-5194 端口合同；`node --check` 通过 touched `.mjs` / `.test.mjs` 文件；`PATH=/usr/local/bin:$PATH node --test web/src/pages/AdminLogin/adminLoginRouting.test.mjs web/src/erp/config/seedData.test.mjs web/src/erp/config/entryConfig.test.mjs web/scripts/mobileAuthLoginRouteSmoke.test.mjs scripts/qa/trial-role-entry-docs.test.mjs web/src/common/consts/favicon.test.mjs` 通过 27 项；`PATH=/usr/local/bin:$PATH pnpm --dir web build:all` 通过，产物仍包含 `MobileRoleTasksPage`；`MOBILE_AUTH_SMOKE_APP_ID='mobile-engineering' PATH=/usr/local/bin:$PATH pnpm --dir web smoke:mobile-auth-login-route` 通过；`lsof -nP -iTCP:4193 -sTCP:LISTEN` 无残留监听。
- 下一步：后续本地调试岗位任务端只走 `pnpm start` 后访问 `http://localhost:5175/m/<role>/tasks`；若新增岗位角色，只补角色元数据、权限和 `/m/<role>/tasks` 回归，不再新增 Vite 配置或端口脚本。
- 阻塞/风险：本轮未提交 / 推送；工作区仍有多组非本轮既有改动，未回退也未纳入本轮结论。浏览器级真实 smoke 只跑了 engineering 单角色，未跑全 9 角色真实浏览器回归；相关 mock/preflight 单测已覆盖全角色路由计划。

## 2026-07-02 ERP 前端配置与组件 README 路由补齐

- 完成：按 `plush-docs-governance` 文档治理口径补齐 `web/src/erp/config/README.md` 和 `web/src/erp/components/README.md` 两个薄路由 README。前者收口前端菜单 / 权限码消费、客户配置投影、dev-only 入口和展示配置边界；后者收口 ERP 页面壳、业务列表共享层、领域表单 / 列配置、打印工作台和 Workflow 动作组件边界。同步更新 `docs/文档清单.md` 登记两份长期维护 Markdown。
- 验证：追加前检查 `progress.md` 为 155 行、45087 bytes，未达到 600 行或 80KB 归档阈值；`node scripts/qa/docs-inventory.test.mjs` 通过，扫描 257 个 Markdown；`git diff --check -- web/src/erp/config/README.md web/src/erp/components/README.md docs/文档清单.md` 通过。
- 下一步：若后续继续补 README，优先只在目录成为独立人工入口或查找成本明显上升时补薄路由文档；暂不机械给 `web/src/erp/utils`、生成物、缓存或纯内部 helper 目录补 README。
- 阻塞/风险：本轮只改文档和文档清单，不改前端运行时代码、后端、schema、migration、RBAC、部署、导入、release evidence 或浏览器回归；当前工作区仍有多组既有未提交改动，本条只声明新增 README、索引和过程记录。

## 2026-07-02 yoyoosun 单端口前端 build / preview 静态闭环

- 完成：复核 `business-formal-module-shells-desktop` 继续通过，供应商弹窗 focus 不是当前本地阻断；本轮按当前单端口主路径验证 `build:all` 与 `preview:yoyoosun`，确认桌面后台和 `/m/<role>/tasks` 岗位任务端可进入同一个静态产物。`preview:yoyoosun --print-plan` 明确只执行本地静态包构建、客户 `customer-config.js` / assets overlay 和 `serve:prod`，不会调用后端 `customer_config.validate / publish / activate / rollback`，不会导入业务数据。随后用 `--skip-build --port 5276` 启动本地静态预览，验证 `/erp`、`/m/business/tasks` 和 `customer-config.js` 可读，并已停止该预览进程。
- 验证：`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / blockingCategory=external-release-evidence-required`，目标 release evidence 缺口未升级为本轮主任务；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web build:all` 通过，产物包含桌面和 `MobileRoleTasksPage`；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web preview:yoyoosun -- --print-plan` 通过；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web preview:yoyoosun -- --skip-build --port 5276` 启动后，`curl -fsS -I http://127.0.0.1:5276/erp`、`curl -fsS -I http://127.0.0.1:5276/m/business/tasks` 和 `curl -fsS http://127.0.0.1:5276/customer-config.js` 均通过；`PATH=/usr/local/bin:$PATH node --check web/scripts/previewYoyoosun.mjs && PATH=/usr/local/bin:$PATH node --check web/scripts/serveStaticApp.mjs && PATH=/usr/local/bin:$PATH node --check web/scripts/startYoyoosunDev.mjs && PATH=/usr/local/bin:$PATH node --check web/scripts/localPort.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/build/apply-customer-web-config.test.mjs` 通过 3 项；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过，扫描 257 个 Markdown；`git diff --check` 通过。
- 下一步：若继续没有真实演示账号密码，下一轮仍只适合推进 no-write / mock / static 或服务层本地证据；若提供 `TRIAL_ACCOUNT_PASSWORD` 或 `MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD` 并确认只连本地 / 开发环境，再转入真实账号 RBAC、真实试用浏览器 smoke 或真实移动 Workflow runtime smoke。
- 阻塞/风险：本轮只证明本地单端口静态构建和 yoyoosun 前端静态预览可启动、可读；不证明真实登录、真实 RBAC、customer config active revision 读回、后端客户配置 publish / activate、真实 Workflow 写入、Fact 落账、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送。当前工作区仍有多组既有未提交改动，本条只新增本地验证记录和 `progress.md` 记录。

## 2026-07-02 本地 qa:full 门禁复核

- 完成：复核 `business-formal-module-shells-desktop` 继续通过，供应商弹窗 focus 和 BOM 备注断言不是当前本地阻断；本轮按当前未提交现场补跑本地 `qa:full`，覆盖 fast 边界守卫、secrets、govulncheck、web unit/build、server 全量 test/build。`pnpm lint --fix` 同轮把岗位任务端入口配置从旧 `appDefinitions` 派生收口到 `mobileRoleDefinitions`，涉及 `web/src/erp/config/entryConfig.mjs` 和 `web/src/erp/config/menuPermissions.mjs`；这与当前单端口岗位任务端治理相关，不改变后端 RBAC、Workflow / Fact 或客户配置发布逻辑。
- 验证：`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / blockingCategory=external-release-evidence-required`，目标 release evidence 缺口未升级为本轮主任务；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH bash scripts/qa/full.sh` 通过，包含 `fast.sh`、`secrets.sh`、`govulncheck.sh`、web `pnpm test` 454 项、web build、server `go test ./...` 和 `make build`；`git diff --check` 通过。
- 下一步：若继续没有真实演示账号密码，唯一建议仍是继续推进不依赖密码的本地 mock / no-write / static 闭环；若提供 `TRIAL_ACCOUNT_PASSWORD` 或 `MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD` 并确认只连本地 / 开发环境，再转入真实账号 RBAC、真实试用浏览器 smoke 或真实移动 Workflow runtime smoke。
- 阻塞/风险：本轮 `qa:full` 不证明真实登录、真实 RBAC、customer config active revision 读回、后端客户配置 publish / activate、真实 Workflow 写入、Fact 落账、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送。当前工作区仍有多组既有未提交改动，本条只新增本地 QA 验证记录，并把 `qa:full` 自动收口的两个前端配置文件纳入本轮说明。

## 2026-07-02 岗位任务端旧 mobile app id 口径清理

- 完成：继续按页面治理收口，删除当前岗位任务端配置里的旧 `mobile-*` app id 选择口径。`appRegistry` 只保留 `desktop` 运行时 app 和 `mobileRoleDefinitions` 角色元数据，未知 app id 不再静默回退；`seedData` 的 `roleWorkbenches` 去掉 `appId / port`，只保留 `roleKey / path`；`entryConfig`、`menuPermissions`、`mobileAuthLoginRouteSmoke` 和优先级审计脚本全部改为 role 口径；`web/README.md` 将单端口岗位任务端改写为主路径，不再称为兼容路径。
- 验证：追加前 `progress.md` 为 176 行、51683 bytes，未达到归档阈值；`node --check` 通过本轮触达的 app registry、entry/menu/seed 配置、mobile auth smoke 和优先级审计脚本；`PATH=/usr/local/bin:$PATH node --test web/src/erp/config/appRegistry.test.mjs web/src/erp/config/seedData.test.mjs web/src/erp/config/entryConfig.test.mjs web/src/erp/config/menuPermissions.test.mjs web/scripts/mobileAuthLoginRouteSmoke.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 29 项；`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false`；`PATH=/usr/local/bin:$PATH pnpm --dir web build:all` 通过；`MOBILE_AUTH_SMOKE_ROLE_KEY='engineering' PATH=/usr/local/bin:$PATH pnpm --dir web smoke:mobile-auth-login-route` 通过；活跃代码扫描旧 `mobile-*` app id / `MOBILE_AUTH_SMOKE_APP_ID` / `requestedAppIDs` 无命中；`lsof -nP -iTCP:4193 -sTCP:LISTEN` 无残留监听；`git diff --check` 通过。
- 下一步：后续岗位任务端本地和生产都按 `roleKey` 与 `/m/<role>/tasks` 扩展；新增角色只补角色元数据、权限映射、seed/RBAC/customer config 投影和 role-key smoke，不再新增 mobile app id、独立 Vite 配置、独立端口或 `MOBILE_AUTH_SMOKE_APP_ID`。
- 阻塞/风险：本轮未提交 / 推送；工作区仍有多组既有未提交改动，未回退也未纳入本轮结论。浏览器级 smoke 只跑了 engineering 单角色，mock/preflight 单测覆盖全 9 个角色路由计划；本轮不证明真实登录、真实 RBAC、customer config active revision、真实 Workflow 写入、Fact 落账、目标环境 smoke、生产部署、备份恢复、回滚演练或客户签收。历史 `progress.md` 和 `docs/archive/**` 仍保留旧命令作为过程证据，不作为当前 runtime 或接口真源。

## 2026-07-02 本地 qa:strict 门禁复核

- 完成：复核 `business-formal-module-shells-desktop` 继续通过，供应商弹窗 focus 不是当前本地阻断；本轮在 `qa:full` 通过后补跑本地 `qa:strict`，覆盖 DB / secrets、Workflow / Fact、Workflow UI action、正式前端客户配置投影、adminProfileSync、角色菜单、dev-only、试用账号 / 真实登录 / 移动端 / 采购入库 no-write 边界、字段链路、错误提示、priority audit、docs inventory、客户配置包 / runtime manifest、导入工具、模拟闭环、shellcheck / shfmt / govulncheck、web 严格检查和 server 严格检查。过程中没有执行生产发布、真实客户导入、真实账号登录、目标环境 smoke、备份恢复或回滚演练。
- 验证：`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`，目标 release evidence 缺口继续保持低优先级缺口说明；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH bash scripts/qa/strict.sh` 全部通过，包含 web `pnpm test` 454 项、web build、server `go test ./...` 和 `make build`；`git diff --check` 通过。
- 下一步：若继续没有真实演示账号密码，下一步只应继续本地 no-write / mock / static / 服务层证据，或按当前 diff 做提交前精确 review；若提供 `TRIAL_ACCOUNT_PASSWORD` 或 `MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD` 并确认只连本地 / 开发环境，再转入真实账号 RBAC、真实试用浏览器 smoke 或真实移动 Workflow runtime smoke。
- 阻塞/风险：本轮 `qa:strict` 仍不证明真实登录、真实 RBAC、customer config active revision 目标读回、后端客户配置 publish / activate、真实 Workflow 写入、Fact 落账、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送。当前工作区仍有多组既有未提交改动，本条只新增本地 strict 验证记录和 `progress.md` 记录。

## 2026-07-02 单端口岗位任务端旧口径补清理

- 完成：按当前长期 goal 先复核 `business-formal-module-shells-desktop`，确认供应商弹窗 focus 已通过，不再是本轮最高优先级本地阻断；本轮改为清理当前 diff 中影响试用模拟可信度的单端口岗位任务端口径漂移。`web/README.md` 首屏“当前结构”改为同一个 `pnpm start` 入口承载桌面后台和岗位任务端；`mobileAuthLoginRouteSmoke.test.mjs` 和 `appRegistry.test.mjs` 的负向断言不再保留旧 `MOBILE_AUTH_SMOKE_APP_ID` / `requestedAppIDs` / `mobile-boss` 字面量，避免静态守卫或人工 grep 误判旧 mobile app id 仍是当前入口。运行时仍按 `roleKey` 与 `/m/<role>/tasks`，不改后端 RBAC、Workflow / Fact、schema、migration 或 release evidence。
- 验证：`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；活跃 README / 脚本 / 测试 / docs 扫描旧 `start:mobile`、`vite.mobile`、`VITE_ERP_APP_ID`、`MOBILE_AUTH_SMOKE_APP_ID`、`requestedAppIDs`、`legacyMultiAppMode`、5186-5194 和旧 `mobile-*` app id 无命中；`PATH=/usr/local/bin:$PATH node --test web/src/erp/config/appRegistry.test.mjs web/scripts/mobileAuthLoginRouteSmoke.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 8 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint --ext .js --ext .jsx --ext .mjs scripts/mobileAuthLoginRouteSmoke.test.mjs src/erp/config/appRegistry.test.mjs` 通过；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web css` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：若继续没有真实演示账号密码，下一步仍只适合推进本地 no-write / mock / static / 服务层证据，或对当前 diff 做提交前精确 review；若提供 `TRIAL_ACCOUNT_PASSWORD` 或 `MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD` 并确认只连本地 / 开发环境，再跑真实试用账号 RBAC、真实试用浏览器 smoke 或真实移动 Workflow runtime smoke。
- 阻塞/风险：本轮不证明真实登录、真实 RBAC、customer config active revision 目标读回、客户配置 publish / activate、真实 Workflow 写入、Fact 落账、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送。当前工作区仍有多组既有未提交改动，本条只新增小范围 README / 负向测试口径修正和 `progress.md` 记录。

## 2026-07-02 单端口岗位任务端活跃旧口径复扫

- 完成：继续当前长期 goal，先按现场复核 `business-formal-module-shells-desktop`，确认供应商弹窗 focus 和后续 BOM 备注断言仍不是当前本地阻断；本轮不再修 L1，改为对当前未提交 diff 的岗位任务端单端口口径做活跃文件复扫。活跃 README、脚本、测试、web、docs、config、deployments 和 server 路径中未发现旧 `mobile-*` app id、多端口、`MOBILE_AUTH_SMOKE_APP_ID` 或旧 5186-5194 端口合同残留；唯一命中在 `docs/reference/**` 外部历史资料，不作为当前 runtime / docs 真源。
- 验证：`git status -sb` 记录当前仍有多组既有未提交改动；`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 通过且 `ok=true / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；活跃旧口径扫描无当前真源命中；追加前 `progress.md` 为 197 行、59260 bytes，未达到归档阈值。
- 下一步：若仍没有真实演示账号密码，下一步建议只做当前 diff 的精确 review 或继续 no-write / mock / static / 服务层证据；若提供 `TRIAL_ACCOUNT_PASSWORD` 或 `MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD` 并确认只连本地 / 开发环境，再转入真实试用账号 RBAC、真实试用浏览器 smoke 或真实移动 Workflow runtime smoke。
- 阻塞/风险：本轮只新增验证记录，不改运行时代码、schema、migration、RBAC、Workflow / Fact、客户配置发布 / 激活 / 回滚、release evidence、真实客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送。当前工作区仍有多组既有未提交改动。

## 2026-07-02 Workflow action / explain 边界定向复核

- 完成：继续当前长期 goal，按当前 diff 优先复核 Workflow action / explain、菜单投影、adminProfileSync、dev-only、移动端 no-write 和 Workflow / Fact 边界。确认正式前端运行时代码不再调用旧 `updateWorkflowTaskStatus` / `update_task_status` 裸状态入口；旧入口只在 QA / audit / 测试守卫中作为不可用或不可触发证据保留。清理 `server/internal/service/jsonrpc_workflow_test.go` 中当前已失效的未使用 import，恢复后端定向测试可编译；不改业务逻辑、schema、migration、RBAC、Workflow / Fact usecase、客户配置发布 / 激活 / 回滚或 release evidence。
- 验证：`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 已复核，当前 `ok=false / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`，阻塞属于外部 release evidence，不升级为本轮主任务；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；首次 `go test ./internal/service -run 'TestJsonrpcDispatcher_Workflow(...)'` 被 `jsonrpc_workflow_test.go` 未使用 import 编译阻断，清理 import 后同一组定向测试通过；`PATH=/usr/local/bin:$PATH node --test web/src/erp/utils/workflowTaskActionAccess.test.mjs scripts/qa/workflow-ui-action-boundary.test.mjs scripts/qa/workflow-fact-boundary.test.mjs` 通过 21 项；`PATH=/usr/local/bin:$PATH node --test web/src/erp/config/menuPermissions.test.mjs web/src/erp/utils/adminProfileSync.test.mjs web/src/erp/config/devTesting.test.mjs scripts/qa/dev-entry-boundary.test.mjs` 通过 41 项；`PATH=/usr/local/bin:$PATH node --test scripts/qa/mobile-workflow-simulated-closure.test.mjs scripts/qa/mobile-workflow-runtime-browser-smoke.test.mjs` 通过 17 项；追加前 `progress.md` 为 204 行、61190 bytes，未达到归档阈值。
- 下一步：继续按本地可证明闭环推进；若没有真实试用账号密码，仍只做 no-write / mock / static / 服务层证据或当前 diff 精确 review。若提供 `TRIAL_ACCOUNT_PASSWORD` 或 `MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD` 并确认只连本地 / 开发环境，再进入真实账号 RBAC、真实试用浏览器 smoke 或真实移动 Workflow runtime smoke。
- 阻塞/风险：本轮不证明真实登录、真实 RBAC、真实浏览器菜单、真实移动端 Workflow 写入、Fact 落账、customer config active revision 目标读回、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送。当前工作区仍有多组既有未提交改动，本条只新增后端测试 import 清理和 `progress.md` 记录。

## 2026-07-02 旧入口兼容重定向口径复核

- 完成：继续当前长期 goal，按当前 diff 复核试用入口可信度时发现 `web/src/erp/router.jsx` 删除了旧 `/login`、旧账号管理路径、旧产品内文档 / QA / 帮助 / 移动工作台 / 角色 / 变更路径的兼容重定向，而 `web/README.md`、`docs/README.md` 和测试策略仍声明这些旧入口只做兼容退出。已恢复明确重定向到当前正式入口或权限中心，不恢复旧页面、不生成旧权限项、不恢复产品内文档中心 / 帮助中心 / 开发验收正式菜单；同时在 `menuPermissions.test.mjs` 增加静态断言，锁住“旧路径不进权限项，但路由层仍兼容重定向”的合同。
- 验证：`git fetch --prune && git status -sb` 已记录当前仍有多组既有未提交改动；`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 汇总为 `ok=false / readOnly=true / releaseReady=false / blockingCategory=external-release-evidence-required`，目标 release evidence 缺口未升级为本轮主任务；`PATH=/usr/local/bin:$PATH node --test web/src/erp/config/menuPermissions.test.mjs web/src/pages/AdminLogin/adminLoginRouting.test.mjs scripts/qa/trial-role-entry-docs.test.mjs` 通过 15 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint --ext .js --ext .jsx --ext .mjs src/erp/router.jsx src/erp/config/menuPermissions.test.mjs` 通过；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web css` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；追加前 `progress.md` 为 211 行、64042 bytes，未达到归档阈值。
- 下一步：继续按当前 diff 做精确本地闭环复核；若仍无真实演示账号密码，优先选择不依赖密码的入口 / 权限 / no-write / mock / static 证据切片。若提供 `TRIAL_ACCOUNT_PASSWORD` 或 `MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD` 并确认只连本地 / 开发环境，再进入真实账号 RBAC、真实试用浏览器 smoke 或真实移动 Workflow runtime smoke。
- 阻塞/风险：本轮不证明真实登录、真实 RBAC、customer config active revision 目标读回、真实移动端 Workflow 写入、Fact 落账、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送。当前工作区仍有多组既有未提交改动，本条只修改旧入口兼容路由、对应静态测试和 `progress.md` 记录。

## 2026-07-02 effective session 动作投影测试口径收口

- 完成：继续当前长期 goal，按当前 diff 复核 `adminProfileSync` / 正式业务按钮动作投影。确认普通账号在缺失 effective session 或 sync failure 空投影时不再回退旧 RBAC action，super admin 仅保留产品核心审阅捷径；修正 `masterDataOrderView.test.mjs` 的过期断言，锁住高层 `hasActionPermission` 必须同时满足 RBAC 与 effective session action 的正式账号口径。
- 验证：`git status -sb` 已记录当前仍有多组既有未提交改动；`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 复核为 `ok=false / readOnly=true / releaseReady=false`，阻塞仍属于外部 release evidence / super admin break-glass 目标缺口，未升级为本轮主任务；`PATH=/usr/local/bin:$PATH node --test web/src/erp/utils/adminProfileSync.test.mjs web/src/erp/utils/masterDataOrderView.test.mjs web/src/erp/config/menuPermissions.test.mjs scripts/qa/formal-frontend-customer-config-boundary.test.mjs scripts/qa/workflow-ui-action-boundary.test.mjs` 通过 56 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint --ext .mjs src/erp/utils/adminProfileSync.mjs src/erp/utils/adminProfileSync.test.mjs src/erp/utils/masterDataOrderView.mjs src/erp/utils/masterDataOrderView.test.mjs src/erp/config/menuPermissions.mjs src/erp/config/menuPermissions.test.mjs` 通过；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web css` 通过；`PATH=/usr/local/bin:$PATH STYLE_L1_PORT=43210 STYLE_L1_SCENARIOS=business-formal-module-shells-desktop /usr/local/bin/pnpm --dir web style:l1` 通过 1 个场景；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过，扫描 257 个 Markdown；`git diff --check` 通过。
- 下一步：继续按当前 diff 做精确本地闭环复核；若仍没有真实演示账号密码，优先选择不依赖密码的 no-write / mock / static / 服务层证据；若提供 `TRIAL_ACCOUNT_PASSWORD` 或 `MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD` 并确认只连本地 / 开发环境，再进入真实账号 RBAC、真实试用浏览器 smoke 或真实移动 Workflow runtime smoke。
- 阻塞/风险：本轮只修测试口径，不改运行时代码、schema、migration、RBAC、Workflow / Fact usecase、客户配置 publish / activate / rollback 或 release evidence；不证明真实登录、真实 RBAC、真实客户配置 active revision 目标读回、目标环境 smoke、正式客户导入、生产部署、备份恢复、回滚演练、客户签收、提交或推送。当前工作区仍有多组既有未提交改动，本条只新增 `masterDataOrderView.test.mjs` 测试口径修正和 `progress.md` 记录。

## 2026-07-02 yoyoosun 本地入口与 effective session 诊断

- 完成：按更新后的 goal 将最高优先级切到 yoyoosun 本地入口诊断。`start:yoyoosun --print-plan` 与 `preview:yoyoosun --print-plan` 均确认默认 5176 被占用后顺延到实际 `http://localhost:5178/erp`；端口归属核对为 5175 本仓库 Vite Product Core placeholder、5176 `openai-oauth-api-service` Vite、5177 本仓库静态服务、5178 本轮 yoyoosun dev / preview 临时端口。实际启动 `start:yoyoosun --port 5178` 后，`/customer-config.js` 返回 `window.__PLUSH_ERP_CUSTOMER_CONFIG__` 且 `customerKey="yoyoosun"`，`/customer-assets/yoyoosun/favicon-yoyoosun.svg` 返回 200；实际启动 `preview:yoyoosun --skip-build --port 5178` 后，静态 overlay 写入 `web/build/customer-config.js`，同样验证 yoyoosun 配置和 favicon 可访问，后端 health 通过。Chrome 当前前台 URL 为 `http://localhost:5178/admin-login`，因此是 yoyoosun 本地入口的登录页；若打开 5175 则仍是 Product Core placeholder，不能用来判断客户差异。
- 验证：`git status -sb` 已记录当前仍有多组既有未提交改动；`PATH=/usr/local/bin:$PATH node scripts/qa/multi-client-role-workflow-priority-audit.mjs --json` 复核为 `ok=false / readOnly=true / releaseReady=false`，目标 release evidence 与 break-glass 缺口未升级为本轮主任务；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web start:yoyoosun -- --print-plan` 输出实际 5178；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web preview:yoyoosun -- --print-plan` 输出实际 5178；`curl` 验证 5178 dev 和 preview 的 `/erp`、`/customer-config.js`、`/customer-assets/yoyoosun/favicon-yoyoosun.svg` 均通过；`curl http://localhost:8300/healthz` 通过；未登录调用 `customer_config.get_effective_session` 返回 `40302 未登录`；当前 shell 未设置 `TRIAL_ACCOUNT_PASSWORD`、`MOBILE_WORKFLOW_BROWSER_SMOKE_PASSWORD` 或 `CUSTOMER_CONFIG_ADMIN_TOKEN`；`PATH=/usr/local/bin:$PATH node scripts/qa/customer-config-boundaries.mjs` 通过；`PATH=/usr/local/bin:$PATH node scripts/qa/customer-config-runtime-manifest.mjs --customer yoyoosun --mode preview` 通过，revision 为 `yoyoosun-customer-package-v1.runtime-manifest-v1`，modules=15、roles=9、entitlements=346；`PATH=/usr/local/bin:$PATH node --test web/devCustomerConfigPlugin.test.mjs web/scripts/mobileAuthLoginRouteSmoke.test.mjs web/src/erp/utils/adminProfileSync.test.mjs` 通过 24 项；`PATH=/usr/local/bin:$PATH /usr/local/bin/pnpm --dir web exec eslint --ext .mjs devCustomerConfigPlugin.mjs devCustomerConfigPlugin.test.mjs scripts/startYoyoosunDev.mjs scripts/previewYoyoosun.mjs scripts/localPort.mjs scripts/serveStaticApp.mjs src/erp/utils/adminProfileSync.mjs src/erp/utils/adminProfileSync.test.mjs` 通过；`PATH=/usr/local/bin:$PATH node --test scripts/qa/docs-inventory.test.mjs` 通过；`git diff --check` 通过。
- 下一步：若提供本地有效管理员密码或 token，并确认只连本地后端 `8300`，下一轮优先跑真实登录后 `customer_config.get_effective_session` 读回，记录 `source / configRevision / customerKey / pages / actions / fieldPolicies / modules`，并区分 super_admin 看全 Product Core 与普通账号菜单 / 字段收窄；若仍无凭据，则继续只做 no-write / mock / static 证据。
- 阻塞/风险：本轮不读取密码、不登录、不保存 token、不执行客户配置 publish / activate / rollback、不写数据库、不做真实客户导入、目标环境 smoke、生产部署、备份恢复、回滚演练、客户签收、提交或推送。由于无登录态，只能证明未登录调用返回 `40302`，不能证明后端当前 effective session 是 `builtin_rbac_fallback`、`active_customer_config_revision` 还是 sync failure；当前 5177 共享 `web/build`，本轮 preview overlay 后也会返回 yoyoosun 静态配置，不能把 5177 当成独立未注入 Product Core 证据。

## 2026-07-02 旧兼容接口和旧 mobile 运行时清理

- 完成：按 page design / code review 治理口径继续删除旧兼容，不再保留旧 mobile 多端口运行时、旧 `start:desktop` / `build:desktop` 脚本、旧 mobile app id 选择接口、旧 `/login` 和旧产品内文档 / QA / help 路由重定向、旧菜单权限 alias、旧 `update_task_status` 写接口、旧 user scope token、typed `id` 兼容、旧 PDF warmup env 和未知模板兜底。同步更新 README、当前真源、测试策略、Workflow / Fact 文档、菜单映射和 QA 守卫，使当前主路径只保留单端口桌面 + `/m/<role>/tasks` 岗位任务端、admin scope、action contract、显式领域命令和当前配置 key。
- 验证：追加前 `progress.md` 为 232 行、73420 bytes，未达到归档阈值；`cd server && go test ./internal/biz ./internal/service ./internal/server` 通过；`cd web && node ../scripts/gen-error-codes.mjs && node --test src/common/auth/auth.test.mjs src/common/utils/jsonRpc.test.mjs src/erp/config/appRegistry.test.mjs src/erp/config/menuPermissions.test.mjs src/erp/config/seedData.test.mjs src/erp/utils/adminProfileSync.test.mjs src/pages/AdminLogin/adminLoginRouting.test.mjs scripts/mobileAuthLoginRouteSmoke.test.mjs ../scripts/qa/workflow-ui-action-boundary.test.mjs` 通过 64 项；`node --test scripts/qa/multi-client-role-workflow-priority-audit.test.mjs` 通过 15 项；`node -e "import('./scripts/qa/multi-client-role-workflow-priority-audit.mjs').then(...)"` 复核 `ok=true / passed=69 / failed=0`；`git diff --check` 通过；旧兼容关键词扫描仅剩 `menuPermissions.test.mjs` 的 `/login` 负向样本。
- 下一步：若要提交，需要按本轮触达路径精确 stage，并先和当前工作区里既有未提交改动分清边界；若继续治理，可再把泛化“兼容 fetch Request”这类非旧模型措辞另行评审，但它们不是本轮旧 mobile / 旧接口兼容入口。
- 阻塞/风险：本轮未提交 / 推送；工作区仍有多组既有未提交改动，未回退也未纳入本轮成果。验证为本地单测、服务层测试、静态 QA 和文档 / 扫描证据，不证明真实登录、真实 RBAC、真实移动端写入、Fact 落账、customer config publish / activate / rollback、目标环境 smoke、生产部署、备份恢复、回滚演练或客户签收。

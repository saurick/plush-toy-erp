-- Modify "quality_inspections" table
ALTER TABLE "quality_inspections" ADD CONSTRAINT "quality_inspections_defect_rate_bundle_complete" CHECK (((defect_rate_operator IS NULL) AND (defect_rate_percent IS NULL)) OR ((defect_rate_operator IS NOT NULL) AND (defect_rate_percent IS NOT NULL))), ADD CONSTRAINT "quality_inspections_defect_rate_gt_below_100" CHECK ((defect_rate_operator IS NULL) OR ((defect_rate_operator)::text <> 'GT'::text) OR (defect_rate_percent < (100)::numeric)), ADD CONSTRAINT "quality_inspections_defect_rate_operator_valid" CHECK ((defect_rate_operator IS NULL) OR ((defect_rate_operator)::text = ANY ((ARRAY['APPROX'::character varying, 'GT'::character varying])::text[]))), ADD CONSTRAINT "quality_inspections_defect_rate_percent_range" CHECK ((defect_rate_percent IS NULL) OR ((defect_rate_percent >= (0)::numeric) AND (defect_rate_percent <= (100)::numeric))), ADD COLUMN "defect_rate_operator" character varying NULL, ADD COLUMN "defect_rate_percent" numeric(20,6) NULL;

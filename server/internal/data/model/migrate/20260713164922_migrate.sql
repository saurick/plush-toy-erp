-- Modify "admin_users" table
ALTER TABLE "admin_users" ADD CONSTRAINT "admin_users_revoked_requires_disabled" CHECK ((revoked_at IS NULL) OR (disabled = true)), ADD CONSTRAINT "admin_users_revoked_requires_reason" CHECK ((revoked_at IS NULL) OR ((status_reason IS NOT NULL) AND ((length(TRIM(BOTH FROM status_reason)) >= 1) AND (length(TRIM(BOTH FROM status_reason)) <= 255)))), ADD CONSTRAINT "admin_users_status_audit_bundle" CHECK (((status_changed_at IS NULL) AND (status_changed_by IS NULL) AND (status_reason IS NULL)) OR ((status_changed_at IS NOT NULL) AND (status_changed_by IS NOT NULL))), ADD COLUMN "auth_version" bigint NOT NULL DEFAULT 1;
-- Create "admin_sessions" table
CREATE TABLE "admin_sessions" (
  "id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  "session_key" character varying NOT NULL,
  "admin_user_id" bigint NOT NULL,
  "auth_version" bigint NOT NULL,
  "issued_at" timestamptz NOT NULL,
  "expires_at" timestamptz NOT NULL,
  "revoked_at" timestamptz NULL,
  "revoke_reason" character varying NULL,
  "created_at" timestamptz NOT NULL,
  "updated_at" timestamptz NOT NULL,
  PRIMARY KEY ("id")
);
-- Create index "adminsession_admin_user_id_revoked_at" to table: "admin_sessions"
CREATE INDEX "adminsession_admin_user_id_revoked_at" ON "admin_sessions" ("admin_user_id", "revoked_at");
-- Create index "adminsession_expires_at" to table: "admin_sessions"
CREATE INDEX "adminsession_expires_at" ON "admin_sessions" ("expires_at");
-- Create index "adminsession_session_key" to table: "admin_sessions"
CREATE UNIQUE INDEX "adminsession_session_key" ON "admin_sessions" ("session_key");

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"server/internal/data/model/ent/runtimeauditevent"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// RuntimeAuditEvent is the model entity for the RuntimeAuditEvent schema.
type RuntimeAuditEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 系统运行时审计事件类型
	EventType string `json:"event_type,omitempty"`
	// 系统运行时审计事件稳定 key，可用于关联 marker
	EventKey string `json:"event_key,omitempty"`
	// 系统运行时审计来源
	Source string `json:"source,omitempty"`
	// 系统运行时审计 payload JSON，不保存密码或 token
	Payload string `json:"payload,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RuntimeAuditEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case runtimeauditevent.FieldID:
			values[i] = new(sql.NullInt64)
		case runtimeauditevent.FieldEventType, runtimeauditevent.FieldEventKey, runtimeauditevent.FieldSource, runtimeauditevent.FieldPayload:
			values[i] = new(sql.NullString)
		case runtimeauditevent.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RuntimeAuditEvent fields.
func (_m *RuntimeAuditEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case runtimeauditevent.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case runtimeauditevent.FieldEventType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_type", values[i])
			} else if value.Valid {
				_m.EventType = value.String
			}
		case runtimeauditevent.FieldEventKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_key", values[i])
			} else if value.Valid {
				_m.EventKey = value.String
			}
		case runtimeauditevent.FieldSource:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field source", values[i])
			} else if value.Valid {
				_m.Source = value.String
			}
		case runtimeauditevent.FieldPayload:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value.Valid {
				_m.Payload = value.String
			}
		case runtimeauditevent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RuntimeAuditEvent.
// This includes values selected through modifiers, order, etc.
func (_m *RuntimeAuditEvent) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this RuntimeAuditEvent.
// Note that you need to call RuntimeAuditEvent.Unwrap() before calling this method if this RuntimeAuditEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RuntimeAuditEvent) Update() *RuntimeAuditEventUpdateOne {
	return NewRuntimeAuditEventClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RuntimeAuditEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RuntimeAuditEvent) Unwrap() *RuntimeAuditEvent {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RuntimeAuditEvent is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RuntimeAuditEvent) String() string {
	var builder strings.Builder
	builder.WriteString("RuntimeAuditEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("event_type=")
	builder.WriteString(_m.EventType)
	builder.WriteString(", ")
	builder.WriteString("event_key=")
	builder.WriteString(_m.EventKey)
	builder.WriteString(", ")
	builder.WriteString("source=")
	builder.WriteString(_m.Source)
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(_m.Payload)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RuntimeAuditEvents is a parsable slice of RuntimeAuditEvent.
type RuntimeAuditEvents []*RuntimeAuditEvent

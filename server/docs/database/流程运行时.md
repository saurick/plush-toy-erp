<!-- Code generated by go run ./cmd/schema-doc --write; DO NOT EDIT. -->

# 流程运行时、协同与审计数据库表 / Database Tables

ProcessRuntime、Workflow task、协同状态、运行标记、审计和跨业务附件证据。

本文中的结构来自当前 Ent generated migration descriptor；表用途、边界、生命周期和写入方来自人工审查 catalog。本文不表示 PostgreSQL 已存在同名 `COMMENT`，也不替代目标库结构读回。

- [返回数据字典总入口](README.md)
- [Ent + Atlas 工作流](../ent.md)
- [当前真源与交接顺序](../../../docs/当前真源与交接顺序.md)
- [状态工作流事实边界](../../../docs/architecture/状态工作流事实边界.md)
- [业务与协同流程地图](../../../docs/workflow/业务与协同流程地图.md)
- [日志链路追踪审计第一版](../../../docs/observability/日志链路追踪审计第一版.md)
- [业务附件证据边界评审](../../../docs/architecture/业务附件证据边界评审.md)

本分域共 8 张应用表。

<a id="table-business-attachments"></a>

## `business_attachments` — 业务附件证据

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Fact / 事实 |
| 用途 | 保存挂接到业务对象的文件内容、元数据、哈希、上传审计和撤销审计等证据。 |
| 明确不负责 | 附件是 Evidence 层，不改变 Source Document、Fact、Workflow、库存、质检或财务状态。 |
| 生命周期 / 删除语义 | 普通证据 active / withdrawn 只由 withdrawn_at、withdrawn_by、withdrawal_reason 全空或全有值派生；允许带原因单向撤销，无恢复和常规物理删除。产品图片只按固定 slot 替换或清空；debug cleanup 另行受控。 |
| 写入边界 | BusinessAttachmentUsecase 校验 owner、权限、大小、类型和哈希后上传；撤销在事务内重验 owner 与附件行，并以 withdrawn_at IS NULL 一次性写入服务端操作者、时间和原因。 |
| Catalog 敏感字段 | <code>content</code> |
| 审查备注 | 现有六类 kind 未单列 Evidence，本目录暂归 Fact；不得据此把附件解释成领域过账事实。 |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>owner_type</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>owner_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>attachment_type</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | <code>&#39;evidence&#39;</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>slot_key</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>file_name</code> | <code>varchar(255)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>mime_type</code> | <code>varchar(128)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>file_size</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>sha256</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>content</code> | <code>bytea</code> | <code>[]byte</code> | 非空 | — | 敏感 | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>uploaded_by</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 执行该业务动作的管理员标识。 |
| <code>note</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务备注；不替代状态原因、审批意见或正式审计字段。 |
| <code>withdrawn_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>withdrawn_by</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 执行该业务动作的管理员标识。 |
| <code>withdrawal_reason</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |

### 主键

<code>id</code>

### 外键

无外键。

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>businessattachment_owner_type_owner_id</code> | 否 | <code>owner_type</code>, <code>owner_id</code> | — |
| <code>businessattachment_owner_type_owner_id_attachment_type_slot_key</code> | 是 | <code>owner_type</code>, <code>owner_id</code>, <code>attachment_type</code>, <code>slot_key</code> | <code>owner_type = &#39;product&#39; AND attachment_type = &#39;product_image&#39;</code> |
| <code>businessattachment_owner_type_owner_id_created_at</code> | 否 | <code>owner_type</code>, <code>owner_id</code>, <code>created_at</code> | — |
| <code>businessattachment_sha256</code> | 否 | <code>sha256</code> | — |
| <code>businessattachment_uploaded_by</code> | 否 | <code>uploaded_by</code> | — |
| <code>businessattachment_withdrawn_by</code> | 否 | <code>withdrawn_by</code> | — |

### CHECK 约束

- <code>business_attachments_file_size_positive</code>

```sql
file_size > 0
```

- <code>business_attachments_owner_type_allowed</code>

```sql
owner_type IN ('sales_order', 'purchase_order', 'outsourcing_order', 'purchase_receipt', 'quality_inspection', 'shipment', 'finance_fact', 'production_fact', 'outsourcing_fact', 'product', 'product_sku', 'bom_header', 'workflow_task')
```

- <code>business_attachments_product_image_contract</code>

```sql
((owner_type = 'product' AND attachment_type = 'product_image' AND slot_key IS NOT NULL AND slot_key IN ('primary', 'secondary') AND mime_type IN ('image/png', 'image/jpeg', 'image/webp')) OR (owner_type <> 'product' AND attachment_type <> 'product_image'))
```

- <code>business_attachments_withdrawal_contract</code>

```sql
((withdrawn_at IS NULL AND withdrawn_by IS NULL AND withdrawal_reason IS NULL) OR (withdrawn_at IS NOT NULL AND withdrawn_by IS NOT NULL AND withdrawn_by > 0 AND withdrawal_reason IS NOT NULL AND length(trim(withdrawal_reason)) BETWEEN 1 AND 255 AND owner_type <> 'product' AND attachment_type <> 'product_image'))
```

### 代码与业务真源

- [Schema source: `business_attachment.go`](../../internal/data/model/schema/business_attachment.go)
- [状态工作流事实边界](../../../docs/architecture/状态工作流事实边界.md)
- [业务与协同流程地图](../../../docs/workflow/业务与协同流程地图.md)
- [日志链路追踪审计第一版](../../../docs/observability/日志链路追踪审计第一版.md)
- [业务附件证据边界评审](../../../docs/architecture/业务附件证据边界评审.md)

<a id="table-process-instances"></a>

## `process_instances` — 流程实例

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Workflow / 流程运行时 |
| 用途 | 保存一次业务来源流程的版本、客户配置快照、整体状态和幂等锚点。 |
| 明确不负责 | 它是 ProcessRuntime 聚合，不是生产工序主数据，也不替代来源单据或领域 Fact。 |
| 生命周期 / 删除语义 | active 转 completed 或 blocked；无公开业务删除，结算和阻塞必须通过 ProcessRuntime。 |
| 写入边界 | ProcessRuntimeUsecase 从白名单流程合同创建、启动、推进、阻塞和结算。 |
| 审查备注 | 当前 schema 未用通用 hook 阻止所有物理删除；“无业务删除”是 usecase 合同，不应写成数据库层强制不可删。 |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>process_key</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>process_version</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>variant_key</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>config_revision</code> | <code>varchar(128)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>definition_hash</code> | <code>varchar(128)</code> | <code>string</code> | 非空 | — | — | 用于完整性或幂等校验的摘要。 |
| <code>module_contract_snapshot</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>business_ref_type</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>business_ref_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>business_ref_no</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务编号；唯一性和生成规则见索引与领域 usecase。 |
| <code>correlation_key</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>idempotency_key</code> | <code>varchar(128)</code> | <code>string</code> | 非空 | — | — | 幂等意图键；必须与对应 payload hash、操作者或业务范围合同一起解释。 |
| <code>status</code> | <code>varchar(32)</code> | <code>string</code> | 非空 | <code>&#39;active&#39;</code> | — | 对象生命周期状态；允许值和组合约束见本表 CHECK 与生命周期说明。 |
| <code>started_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>completed_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>created_by</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 创建该记录的管理员标识。 |
| <code>updated_by</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 执行该业务动作的管理员标识。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |
| <code>updated_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录最近更新时间；大量表由 Ent client 的 UpdateDefault 维护。 |

### 主键

<code>id</code>

### 外键

无外键。

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>processinstance_business_ref_type_business_ref_id</code> | 否 | <code>business_ref_type</code>, <code>business_ref_id</code> | — |
| <code>processinstance_config_revision_process_key</code> | 否 | <code>config_revision</code>, <code>process_key</code> | — |
| <code>processinstance_correlation_key</code> | 否 | <code>correlation_key</code> | — |
| <code>processinstance_process_key_business_ref_type_business_ref_id</code> | 是 | <code>process_key</code>, <code>business_ref_type</code>, <code>business_ref_id</code> | — |
| <code>processinstance_process_key_business_ref_type_business_ref_id_idempotency_key</code> | 是 | <code>process_key</code>, <code>business_ref_type</code>, <code>business_ref_id</code>, <code>idempotency_key</code> | — |
| <code>processinstance_status_updated_at</code> | 否 | <code>status</code>, <code>updated_at</code> | — |

### CHECK 约束

- <code>process_instances_lifecycle_bundle</code>

```sql
((status = 'completed' AND completed_at IS NOT NULL) OR (status IN ('active', 'blocked') AND completed_at IS NULL))
```

- <code>process_instances_status_allowed</code>

```sql
status IN ('active', 'completed', 'blocked')
```

### 代码与业务真源

- [Schema source: `process_instance.go`](../../internal/data/model/schema/process_instance.go)
- [状态工作流事实边界](../../../docs/architecture/状态工作流事实边界.md)
- [业务与协同流程地图](../../../docs/workflow/业务与协同流程地图.md)
- [日志链路追踪审计第一版](../../../docs/observability/日志链路追踪审计第一版.md)
- [业务附件证据边界评审](../../../docs/architecture/业务附件证据边界评审.md)

<a id="table-process-node-instances"></a>

## `process_node_instances` — 流程节点实例

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Workflow / 流程运行时 |
| 用途 | 保存流程节点 attempt、状态、责任池、动作合同以及领域命令的持久结果和恢复证据。 |
| 明确不负责 | 节点 completed 只表示该节点按合同结算，不能代替库存、质检、出货或财务事实。 |
| 生命周期 / 删除语义 | waiting 转 active，再 completed 或 blocked；重试使用新 attempt；无公开业务删除。 |
| 写入边界 | ProcessRuntimeUsecase/repo 通过 CAS、幂等 receipt、domain command 和恢复动作写入。 |
| 审查备注 | 属于 ProcessRuntime 子层；当前 schema 未用通用 hook 阻止所有物理删除。 |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>node_key</code> | <code>varchar(128)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>node_type</code> | <code>varchar(32)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>attempt</code> | <code>bigint</code> | <code>int</code> | 非空 | <code>1</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>status</code> | <code>varchar(32)</code> | <code>string</code> | 非空 | <code>&#39;waiting&#39;</code> | — | 对象生命周期状态；允许值和组合约束见本表 CHECK 与生命周期说明。 |
| <code>owner_pool_key</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>required_capability_key</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>form_profile_key</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>action_set_key</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>policy_snapshot</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>due_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>started_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>completed_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>outcome</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>domain_command_fingerprint</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>domain_command_protocol_version</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>domain_command_result_state</code> | <code>varchar(32)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>domain_command_result</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>domain_command_result_hash</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 用于完整性或幂等校验的摘要。 |
| <code>domain_command_effect_state</code> | <code>varchar(32)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>domain_command_effect_ref_type</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>domain_command_effect_ref_id</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>domain_command_result_recorded_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>domain_command_result_recorded_by</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 执行该业务动作的管理员标识。 |
| <code>domain_command_compensation</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>domain_command_compensation_hash</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 用于完整性或幂等校验的摘要。 |
| <code>domain_command_compensated_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>domain_command_compensated_by</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 执行该业务动作的管理员标识。 |
| <code>domain_command_recovery_decision</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>domain_command_recovery_hash</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 用于完整性或幂等校验的摘要。 |
| <code>domain_command_recovered_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>domain_command_recovered_by</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 执行该业务动作的管理员标识。 |
| <code>version</code> | <code>bigint</code> | <code>int</code> | 非空 | <code>1</code> | — | 并发控制版本；写动作通常使用 CAS 校验，不能由调用方任意跳号。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |
| <code>updated_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录最近更新时间；大量表由 Ent client 的 UpdateDefault 维护。 |
| <code>process_instance_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 外键，关联 `process_instances`。 |

### 主键

<code>id</code>

### 外键

| 约束 | 本表字段 | 引用 | ON UPDATE | ON DELETE |
| --- | --- | --- | --- | --- |
| <code>process_node_instances_process_instances_nodes</code> | <code>process_instance_id</code> | [<code>public.process_instances</code>](流程运行时.md#table-process-instances) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>processnodeinstance_domain_command_effect_ref_type_domain_command_effect_ref_id</code> | 否 | <code>domain_command_effect_ref_type</code>, <code>domain_command_effect_ref_id</code> | — |
| <code>processnodeinstance_due_at</code> | 否 | <code>due_at</code> | — |
| <code>processnodeinstance_owner_pool_key_status</code> | 否 | <code>owner_pool_key</code>, <code>status</code> | — |
| <code>processnodeinstance_process_instance_id_node_key_attempt</code> | 是 | <code>process_instance_id</code>, <code>node_key</code>, <code>attempt</code> | — |
| <code>processnodeinstance_process_instance_id_status</code> | 否 | <code>process_instance_id</code>, <code>status</code> | — |
| <code>processnodeinstance_required_capability_key_status</code> | 否 | <code>required_capability_key</code>, <code>status</code> | — |

### CHECK 约束

- <code>process_node_instances_lifecycle_bundle</code>

```sql
((status = 'waiting' AND started_at IS NULL AND completed_at IS NULL) OR (status = 'active' AND started_at IS NOT NULL AND completed_at IS NULL) OR (status = 'completed' AND started_at IS NOT NULL AND completed_at IS NOT NULL) OR (status = 'blocked' AND started_at IS NOT NULL AND completed_at IS NULL))
```

- <code>process_node_instances_recovery_bundle</code>

```sql
((domain_command_recovery_decision IS NULL AND domain_command_recovery_hash IS NULL AND domain_command_recovered_at IS NULL AND domain_command_recovered_by IS NULL) OR (domain_command_recovery_decision = 'terminate_and_withdraw_downstream' AND domain_command_recovery_hash IS NOT NULL AND length(domain_command_recovery_hash) = 64 AND domain_command_recovered_at IS NOT NULL AND domain_command_recovered_by IS NOT NULL AND node_type = 'domain_command' AND status = 'completed' AND domain_command_effect_state = 'compensated' AND domain_command_result_hash IS NOT NULL AND domain_command_compensation_hash IS NOT NULL))
```

- <code>process_node_instances_status_allowed</code>

```sql
status IN ('waiting', 'active', 'completed', 'blocked')
```

- <code>process_node_instances_type_allowed</code>

```sql
node_type IN ('human_task', 'approval', 'domain_command', 'wait_event', 'end')
```

- <code>process_node_instances_version_positive</code>

```sql
version > 0
```

### 代码与业务真源

- [Schema source: `process_node_instance.go`](../../internal/data/model/schema/process_node_instance.go)
- [状态工作流事实边界](../../../docs/architecture/状态工作流事实边界.md)
- [业务与协同流程地图](../../../docs/workflow/业务与协同流程地图.md)
- [日志链路追踪审计第一版](../../../docs/observability/日志链路追踪审计第一版.md)
- [业务附件证据边界评审](../../../docs/architecture/业务附件证据边界评审.md)

<a id="table-runtime-audit-events"></a>

## `runtime_audit_events` — 运行时审计事件

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Fact / 事实 |
| 用途 | 保存账号管理、客户配置、Workflow break-glass 和运行控制面的审计事件。 |
| 明确不负责 | 它补充控制面审计，不替代领域 Fact、WorkflowTaskEvent 或完整外部日志链。 |
| 生命周期 / 删除语义 | 只追加、不可修改、不可删除。 |
| 写入边界 | AdminManageUsecase、CustomerConfigUsecase、Workflow 管理动作和受控 bootstrap 写入。 |
| Catalog 敏感字段 | <code>payload</code> |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>event_type</code> | <code>varchar(128)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>event_key</code> | <code>varchar(128)</code> | <code>string</code> | 非空 | <code>&#39;&#39;</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>source</code> | <code>varchar(128)</code> | <code>string</code> | 非空 | <code>&#39;server_bootstrap&#39;</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>payload</code> | <code>varchar(32768)</code> | <code>string</code> | 非空 | <code>&#39;{}&#39;</code> | 敏感 | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |

### 主键

<code>id</code>

### 外键

无外键。

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>runtimeauditevent_event_key_created_at</code> | 否 | <code>event_key</code>, <code>created_at</code> | — |
| <code>runtimeauditevent_event_type_created_at</code> | 否 | <code>event_type</code>, <code>created_at</code> | — |

### CHECK 约束

无表级 CHECK。

### 代码与业务真源

- [Schema source: `runtime_audit_event.go`](../../internal/data/model/schema/runtime_audit_event.go)
- [状态工作流事实边界](../../../docs/architecture/状态工作流事实边界.md)
- [业务与协同流程地图](../../../docs/workflow/业务与协同流程地图.md)
- [日志链路追踪审计第一版](../../../docs/observability/日志链路追踪审计第一版.md)
- [业务附件证据边界评审](../../../docs/architecture/业务附件证据边界评审.md)

<a id="table-runtime-markers"></a>

## `runtime_markers` — 运行时一次性标记

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Fact / 事实 |
| 用途 | 记录受控初始化或一次性运行动作已经执行的稳定 marker。 |
| 明确不负责 | marker 不是业务配置、migration 状态或部署成功证据，payload 不保存密码和 token。 |
| 生命周期 / 删除语义 | 创建后不可修改、不可删除，同一 marker_key 唯一。 |
| 写入边界 | 受控 bootstrap、demo 角色初始化等运行管理流程写入。 |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>marker_key</code> | <code>varchar(128)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>marker_value</code> | <code>varchar(4096)</code> | <code>string</code> | 非空 | <code>&#39;&#39;</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |
| <code>updated_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录最近更新时间；大量表由 Ent client 的 UpdateDefault 维护。 |

### 主键

<code>id</code>

### 外键

无外键。

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>runtimemarker_marker_key</code> | 是 | <code>marker_key</code> | — |

### CHECK 约束

无表级 CHECK。

### 代码与业务真源

- [Schema source: `runtime_marker.go`](../../internal/data/model/schema/runtime_marker.go)
- [状态工作流事实边界](../../../docs/architecture/状态工作流事实边界.md)
- [业务与协同流程地图](../../../docs/workflow/业务与协同流程地图.md)
- [日志链路追踪审计第一版](../../../docs/observability/日志链路追踪审计第一版.md)
- [业务附件证据边界评审](../../../docs/architecture/业务附件证据边界评审.md)

<a id="table-workflow-business-states"></a>

## `workflow_business_states` — 业务协同进度投影

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Derived / 派生投影 |
| 用途 | 保存业务来源对象当前对岗位展示的协同阶段。 |
| 明确不负责 | 它不是统一业务状态机、ProcessRuntime 节点或库存、出货、财务事实真源。 |
| 生命周期 / 删除语义 | 由受控任务动作、ProcessRuntime 或领域 usecase 按来源 key upsert；可被下一阶段覆盖，不保存完整历史。 |
| 写入边界 | WorkflowUsecase、ProcessRuntime 或明确登记的领域副作用写入；前端不得直接写。 |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>source_type</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>source_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>source_no</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务编号；唯一性和生成规则见索引与领域 usecase。 |
| <code>order_id</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>batch_id</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>business_status_key</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>owner_role_key</code> | <code>varchar(32)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>blocked_reason</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>status_changed_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>payload</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |
| <code>updated_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录最近更新时间；大量表由 Ent client 的 UpdateDefault 维护。 |

### 主键

<code>id</code>

### 外键

无外键。

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>workflowbusinessstate_business_status_key</code> | 否 | <code>business_status_key</code> | — |
| <code>workflowbusinessstate_order_id_batch_id</code> | 否 | <code>order_id</code>, <code>batch_id</code> | — |
| <code>workflowbusinessstate_source_type_source_id</code> | 是 | <code>source_type</code>, <code>source_id</code> | — |

### CHECK 约束

- <code>workflow_business_states_status_allowed</code>

```sql
business_status_key IN ('project_pending', 'project_approved', 'engineering_preparing', 'material_preparing', 'production_ready', 'production_processing', 'qc_pending', 'iqc_pending', 'qc_failed', 'warehouse_processing', 'warehouse_inbound_pending', 'inbound_done', 'shipment_pending', 'shipping_released', 'shipped', 'reconciling', 'settled', 'blocked', 'cancelled', 'closed')
```

### 代码与业务真源

- [Schema source: `workflow_business_state.go`](../../internal/data/model/schema/workflow_business_state.go)
- [状态工作流事实边界](../../../docs/architecture/状态工作流事实边界.md)
- [业务与协同流程地图](../../../docs/workflow/业务与协同流程地图.md)
- [日志链路追踪审计第一版](../../../docs/observability/日志链路追踪审计第一版.md)
- [业务附件证据边界评审](../../../docs/architecture/业务附件证据边界评审.md)

<a id="table-workflow-task-events"></a>

## `workflow_task_events` — 协同任务事件

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Workflow / 流程运行时 |
| 用途 | 保存任务创建、状态动作、催办、升级、转交、版本和幂等 replay receipt。 |
| 明确不负责 | 事件只覆盖单个任务的处理记录，不是跨节点完整流程历史或领域 Fact。 |
| 生命周期 / 删除语义 | 业务语义为只追加；状态变更和催办产生新事件，不改写历史。 |
| 写入边界 | WorkflowRepo 在任务创建、状态 CAS、催办和升级事务内写入。 |
| 审查备注 | 当前 schema 没有像多张 Fact 表那样显式 reject update/delete hook；这里只声明业务语义只追加，不宣称数据库层已全面强制不可变。 |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>task_version</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>idempotency_key</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 幂等意图键；必须与对应 payload hash、操作者或业务范围合同一起解释。 |
| <code>intent_hash</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 用于完整性或幂等校验的摘要。 |
| <code>command_key</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>mutation_result</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>event_type</code> | <code>varchar(32)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>from_status_key</code> | <code>varchar(32)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>to_status_key</code> | <code>varchar(32)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>actor_role_key</code> | <code>varchar(32)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>actor_id</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>reason</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>payload</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |
| <code>task_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 外键，关联 `workflow_tasks`。 |

### 主键

<code>id</code>

### 外键

| 约束 | 本表字段 | 引用 | ON UPDATE | ON DELETE |
| --- | --- | --- | --- | --- |
| <code>workflow_task_events_workflow_tasks_events</code> | <code>task_id</code> | [<code>public.workflow_tasks</code>](流程运行时.md#table-workflow-tasks) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>workflowtaskevent_task_id_created_at</code> | 否 | <code>task_id</code>, <code>created_at</code> | — |
| <code>workflowtaskevent_task_id_idempotency_key</code> | 是 | <code>task_id</code>, <code>idempotency_key</code> | — |
| <code>workflowtaskevent_task_id_task_version</code> | 是 | <code>task_id</code>, <code>task_version</code> | — |

### CHECK 约束

- <code>workflow_task_events_receipt_v1_complete</code>

```sql
(
  (
    idempotency_key IS NULL
    AND intent_hash IS NULL
    AND command_key IS NULL
    AND mutation_result IS NULL
  )
  OR
  (
    idempotency_key IS NOT NULL
    AND length(trim(idempotency_key)) BETWEEN 1 AND 128
    AND intent_hash IS NOT NULL
    AND length(intent_hash) = 64
    AND command_key IS NOT NULL
    AND length(trim(command_key)) BETWEEN 1 AND 128
    AND mutation_result IS NOT NULL
    AND task_version IS NOT NULL
    AND task_version > 0
    AND actor_id IS NOT NULL
    AND actor_id > 0
    AND to_status_key IS NOT NULL
    AND length(trim(to_status_key)) > 0
  )
)
```

### 代码与业务真源

- [Schema source: `workflow_task_event.go`](../../internal/data/model/schema/workflow_task_event.go)
- [状态工作流事实边界](../../../docs/architecture/状态工作流事实边界.md)
- [业务与协同流程地图](../../../docs/workflow/业务与协同流程地图.md)
- [日志链路追踪审计第一版](../../../docs/observability/日志链路追踪审计第一版.md)
- [业务附件证据边界评审](../../../docs/architecture/业务附件证据边界评审.md)

<a id="table-workflow-tasks"></a>

## `workflow_tasks` — 协同任务

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Workflow / 流程运行时 |
| 用途 | 保存岗位任务箱、责任池、角色、处理人、状态、截止时间和来源引用。 |
| 明确不负责 | 任务 done 只证明协同办理完成，不等于任何 Source Document 生效或 Fact 过账。 |
| 生命周期 / 删除语义 | 创建固定 ready；ready 可转 blocked、done、rejected；blocked 可恢复到 ready；done 和 rejected 终态不重开；无业务删除。 |
| 写入边界 | WorkflowUsecase 创建和办理；ProcessRuntime 为 human_task 或 approval 节点创建并结算 linked task。 |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>task_code</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>task_group</code> | <code>varchar(128)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>task_name</code> | <code>varchar(128)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>source_type</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>source_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>source_no</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务编号；唯一性和生成规则见索引与领域 usecase。 |
| <code>business_status_key</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>task_status_key</code> | <code>varchar(32)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>owner_role_key</code> | <code>varchar(32)</code> | <code>string</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>owner_pool_key</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>required_capability_key</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>config_revision</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>assignee_id</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>priority</code> | <code>smallint</code> | <code>int16</code> | 非空 | <code>0</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>blocked_reason</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>due_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>completed_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>critical_path</code> | <code>boolean</code> | <code>bool</code> | 非空 | <code>false</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>urge_count</code> | <code>bigint</code> | <code>int</code> | 非空 | <code>0</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>last_urged_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>last_urged_by</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 执行该业务动作的管理员标识。 |
| <code>last_urged_by_role_key</code> | <code>varchar(32)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>escalated_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务时间点；具体触发语义见表生命周期。 |
| <code>escalate_target_role_key</code> | <code>varchar(32)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>payload</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>version</code> | <code>bigint</code> | <code>int</code> | 非空 | <code>1</code> | — | 并发控制版本；写动作通常使用 CAS 校验，不能由调用方任意跳号。 |
| <code>created_by</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 创建该记录的管理员标识。 |
| <code>create_idempotency_key</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>create_intent_hash</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 用于完整性或幂等校验的摘要。 |
| <code>updated_by</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 执行该业务动作的管理员标识。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |
| <code>updated_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录最近更新时间；大量表由 Ent client 的 UpdateDefault 维护。 |
| <code>process_instance_id</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 外键，关联 `process_instances`。 |
| <code>process_node_instance_id</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 外键，关联 `process_node_instances`。 |

### 主键

<code>id</code>

### 外键

| 约束 | 本表字段 | 引用 | ON UPDATE | ON DELETE |
| --- | --- | --- | --- | --- |
| <code>workflow_tasks_process_instances_workflow_tasks</code> | <code>process_instance_id</code> | [<code>public.process_instances</code>](流程运行时.md#table-process-instances) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |
| <code>workflow_tasks_process_node_instances_workflow_tasks</code> | <code>process_node_instance_id</code> | [<code>public.process_node_instances</code>](流程运行时.md#table-process-node-instances) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>workflowtask_config_revision_owner_pool_key_task_status_key</code> | 否 | <code>config_revision</code>, <code>owner_pool_key</code>, <code>task_status_key</code> | — |
| <code>workflowtask_created_by_create_idempotency_key</code> | 是 | <code>created_by</code>, <code>create_idempotency_key</code> | <code>create_idempotency_key IS NOT NULL</code> |
| <code>workflowtask_due_at</code> | 否 | <code>due_at</code> | — |
| <code>workflowtask_owner_pool_key_task_status_key</code> | 否 | <code>owner_pool_key</code>, <code>task_status_key</code> | — |
| <code>workflowtask_owner_role_key_task_status_key</code> | 否 | <code>owner_role_key</code>, <code>task_status_key</code> | — |
| <code>workflowtask_process_instance_id_task_status_key</code> | 否 | <code>process_instance_id</code>, <code>task_status_key</code> | — |
| <code>workflowtask_process_node_instance_id</code> | 否 | <code>process_node_instance_id</code> | — |
| <code>workflowtask_required_capability_key_task_status_key</code> | 否 | <code>required_capability_key</code>, <code>task_status_key</code> | — |
| <code>workflowtask_source_type_source_id</code> | 否 | <code>source_type</code>, <code>source_id</code> | — |
| <code>workflowtask_task_code</code> | 是 | <code>task_code</code> | — |
| <code>workflowtask_task_status_key_critical_path_due_at_id</code> | 否 | <code>task_status_key</code>, <code>critical_path</code>, <code>due_at</code>, <code>id</code> | — |
| <code>workflowtask_urge_count_escalated_at_id</code> | 否 | <code>urge_count</code>, <code>escalated_at</code>, <code>id</code> | — |

### CHECK 约束

- <code>workflow_tasks_create_intent_bundle</code>

```sql
((create_idempotency_key IS NULL AND create_intent_hash IS NULL) OR (create_idempotency_key IS NOT NULL AND create_intent_hash IS NOT NULL AND created_by IS NOT NULL AND length(trim(create_idempotency_key)) BETWEEN 1 AND 128 AND length(create_intent_hash) = 64))
```

- <code>workflow_tasks_process_anchors_paired</code>

```sql
((process_instance_id IS NULL AND process_node_instance_id IS NULL) OR (process_instance_id IS NOT NULL AND process_node_instance_id IS NOT NULL))
```

- <code>workflow_tasks_status_allowed</code>

```sql
task_status_key IN ('ready', 'blocked', 'done', 'rejected')
```

- <code>workflow_tasks_urge_count_non_negative</code>

```sql
urge_count >= 0
```

- <code>workflow_tasks_version_positive</code>

```sql
version > 0
```

### 代码与业务真源

- [Schema source: `workflow_task.go`](../../internal/data/model/schema/workflow_task.go)
- [状态工作流事实边界](../../../docs/architecture/状态工作流事实边界.md)
- [业务与协同流程地图](../../../docs/workflow/业务与协同流程地图.md)
- [日志链路追踪审计第一版](../../../docs/observability/日志链路追踪审计第一版.md)
- [业务附件证据边界评审](../../../docs/architecture/业务附件证据边界评审.md)

<!-- Code generated by go run ./cmd/schema-doc --write; DO NOT EDIT. -->

# 销售与采购数据库表 / Database Tables

销售订单与采购订单的来源单据聚合及其行项目。

本文中的结构来自当前 Ent generated migration descriptor；表用途、边界、生命周期和写入方来自人工审查 catalog。本文不表示 PostgreSQL 已存在同名 `COMMENT`，也不替代目标库结构读回。

- [返回数据字典总入口](README.md)
- [Ent + Atlas 工作流](../ent.md)
- [当前真源与交接顺序](../../../docs/当前真源与交接顺序.md)
- [主数据源单据事实边界评审](../../../docs/architecture/主数据源单据事实边界评审.md)
- [订单采购边界评审](../../../docs/architecture/订单采购边界评审.md)
- [业务数据生命周期与页面动作规则](../../../docs/product/业务数据生命周期与页面动作规则.md)

本分域共 4 张应用表。

<a id="table-purchase-order-items"></a>

## `purchase_order_items` — 采购订单明细

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Source Document / 源单据 |
| 用途 | 保存采购订单的物料、数量、单位、价格和预计到货信息。 |
| 明确不负责 | 订单行是采购承诺，不代表已收货、已检验或已入库。 |
| 生命周期 / 删除语义 | 草稿聚合内可维护；行状态 open、closed 或 canceled；正式行不独立物理删除。 |
| 写入边界 | PurchaseOrderUsecase 的采购订单聚合保存事务维护。 |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>line_no</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 业务编号；唯一性和生成规则见索引与领域 usecase。 |
| <code>material_code_snapshot</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>material_name_snapshot</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>color_snapshot</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>product_order_no_snapshot</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>product_no_snapshot</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>product_name_snapshot</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>purchased_quantity</code> | <code>numeric(20,6)</code> | <code>other</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>unit_price</code> | <code>numeric(20,6)</code> | <code>other</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>amount</code> | <code>numeric(20,6)</code> | <code>other</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>expected_arrival_date</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>line_status</code> | <code>varchar(32)</code> | <code>string</code> | 非空 | <code>&#39;open&#39;</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>note</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务备注；不替代状态原因、审批意见或正式审计字段。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |
| <code>updated_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录最近更新时间；大量表由 Ent client 的 UpdateDefault 维护。 |
| <code>material_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 外键，关联 `materials`。 |
| <code>purchase_order_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 外键，关联 `purchase_orders`。 |
| <code>unit_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 外键，关联 `units`。 |

### 主键

<code>id</code>

### 外键

| 约束 | 本表字段 | 引用 | ON UPDATE | ON DELETE |
| --- | --- | --- | --- | --- |
| <code>purchase_order_items_materials_purchase_order_items</code> | <code>material_id</code> | [<code>public.materials</code>](主数据与BOM.md#table-materials) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |
| <code>purchase_order_items_purchase_orders_items</code> | <code>purchase_order_id</code> | [<code>public.purchase_orders</code>](销售与采购.md#table-purchase-orders) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |
| <code>purchase_order_items_units_purchase_order_items</code> | <code>unit_id</code> | [<code>public.units</code>](主数据与BOM.md#table-units) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>purchaseorderitem_expected_arrival_date</code> | 否 | <code>expected_arrival_date</code> | — |
| <code>purchaseorderitem_line_status</code> | 否 | <code>line_status</code> | — |
| <code>purchaseorderitem_material_id</code> | 否 | <code>material_id</code> | — |
| <code>purchaseorderitem_purchase_order_id_line_no</code> | 是 | <code>purchase_order_id</code>, <code>line_no</code> | — |
| <code>purchaseorderitem_unit_id</code> | 否 | <code>unit_id</code> | — |

### CHECK 约束

- <code>purchase_order_items_amount_non_negative</code>

```sql
amount IS NULL OR amount >= 0
```

- <code>purchase_order_items_line_no_positive</code>

```sql
line_no > 0
```

- <code>purchase_order_items_line_status_allowed</code>

```sql
line_status IN ('open', 'closed', 'canceled')
```

- <code>purchase_order_items_purchased_qty_positive</code>

```sql
purchased_quantity > 0
```

- <code>purchase_order_items_unit_price_non_negative</code>

```sql
unit_price IS NULL OR unit_price >= 0
```

### 代码与业务真源

- [Schema source: `purchase_order_item.go`](../../internal/data/model/schema/purchase_order_item.go)
- [主数据源单据事实边界评审](../../../docs/architecture/主数据源单据事实边界评审.md)
- [订单采购边界评审](../../../docs/architecture/订单采购边界评审.md)
- [业务数据生命周期与页面动作规则](../../../docs/product/业务数据生命周期与页面动作规则.md)

<a id="table-purchase-orders"></a>

## `purchase_orders` — 采购订单

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Source Document / 源单据 |
| 用途 | 保存向供应商采购物料的业务承诺、日期、交付和结算条件。 |
| 明确不负责 | 采购订单不是 BOM、入库、库存、质检或应付事实。 |
| 生命周期 / 删除语义 | draft 转 submitted、approved，再 closed；允许按状态 canceled；不物理删除。 |
| 写入边界 | PurchaseOrderUsecase 创建、草稿聚合保存、提交、审批、关闭和取消。 |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>purchase_order_no</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | — | — | 业务编号；唯一性和生成规则见索引与领域 usecase。 |
| <code>supplier_purchase_order_no</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务编号；唯一性和生成规则见索引与领域 usecase。 |
| <code>supplier_snapshot</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>contract_party_snapshot</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>purchase_date</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>expected_arrival_date</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>lifecycle_status</code> | <code>varchar(32)</code> | <code>string</code> | 非空 | <code>&#39;draft&#39;</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>version</code> | <code>bigint</code> | <code>int</code> | 非空 | <code>1</code> | — | 并发控制版本；写动作通常使用 CAS 校验，不能由调用方任意跳号。 |
| <code>note</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务备注；不替代状态原因、审批意见或正式审计字段。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |
| <code>updated_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录最近更新时间；大量表由 Ent client 的 UpdateDefault 维护。 |
| <code>supplier_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 外键，关联 `suppliers`。 |

### 主键

<code>id</code>

### 外键

| 约束 | 本表字段 | 引用 | ON UPDATE | ON DELETE |
| --- | --- | --- | --- | --- |
| <code>purchase_orders_suppliers_purchase_orders</code> | <code>supplier_id</code> | [<code>public.suppliers</code>](主数据与BOM.md#table-suppliers) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>purchaseorder_expected_arrival_date</code> | 否 | <code>expected_arrival_date</code> | — |
| <code>purchaseorder_lifecycle_status</code> | 否 | <code>lifecycle_status</code> | — |
| <code>purchaseorder_purchase_date</code> | 否 | <code>purchase_date</code> | — |
| <code>purchaseorder_purchase_order_no</code> | 是 | <code>purchase_order_no</code> | — |
| <code>purchaseorder_supplier_id</code> | 否 | <code>supplier_id</code> | — |
| <code>purchaseorder_supplier_purchase_order_no</code> | 否 | <code>supplier_purchase_order_no</code> | — |

### CHECK 约束

- <code>purchase_orders_lifecycle_status_allowed</code>

```sql
lifecycle_status IN ('draft', 'submitted', 'approved', 'closed', 'canceled')
```

- <code>purchase_orders_version_positive</code>

```sql
version > 0
```

### 代码与业务真源

- [Schema source: `purchase_order.go`](../../internal/data/model/schema/purchase_order.go)
- [主数据源单据事实边界评审](../../../docs/architecture/主数据源单据事实边界评审.md)
- [订单采购边界评审](../../../docs/architecture/订单采购边界评审.md)
- [业务数据生命周期与页面动作规则](../../../docs/product/业务数据生命周期与页面动作规则.md)

<a id="table-sales-order-items"></a>

## `sales_order_items` — 销售订单明细

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Source Document / 源单据 |
| 用途 | 保存销售订单的产品、SKU、数量、价格和计划交付信息。 |
| 明确不负责 | 订单行只表达客户承诺，不是库存、预留、生产、出货或财务事实。 |
| 生命周期 / 删除语义 | 草稿聚合内可维护；行状态 open、closed 或 canceled；正式行不独立物理删除。 |
| 写入边界 | SalesOrderUsecase 的销售订单聚合保存事务维护。 |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>line_no</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 业务编号；唯一性和生成规则见索引与领域 usecase。 |
| <code>product_code_snapshot</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>product_name_snapshot</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>color_snapshot</code> | <code>varchar(64)</code> | <code>string</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>ordered_quantity</code> | <code>numeric(20,6)</code> | <code>other</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>unit_price</code> | <code>numeric(20,6)</code> | <code>other</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>amount</code> | <code>numeric(20,6)</code> | <code>other</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>planned_delivery_date</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>line_status</code> | <code>varchar(32)</code> | <code>string</code> | 非空 | <code>&#39;open&#39;</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>note</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务备注；不替代状态原因、审批意见或正式审计字段。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |
| <code>updated_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录最近更新时间；大量表由 Ent client 的 UpdateDefault 维护。 |
| <code>product_sku_id</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 外键，关联 `product_skus`。 |
| <code>sales_order_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 外键，关联 `sales_orders`。 |
| <code>product_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 外键，关联 `products`。 |
| <code>unit_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 外键，关联 `units`。 |

### 主键

<code>id</code>

### 外键

| 约束 | 本表字段 | 引用 | ON UPDATE | ON DELETE |
| --- | --- | --- | --- | --- |
| <code>sales_order_items_product_skus_sales_order_items</code> | <code>product_sku_id</code> | [<code>public.product_skus</code>](主数据与BOM.md#table-product-skus) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>SET NULL</code> |
| <code>sales_order_items_products_product</code> | <code>product_id</code> | [<code>public.products</code>](主数据与BOM.md#table-products) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |
| <code>sales_order_items_sales_orders_items</code> | <code>sales_order_id</code> | [<code>public.sales_orders</code>](销售与采购.md#table-sales-orders) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |
| <code>sales_order_items_units_unit</code> | <code>unit_id</code> | [<code>public.units</code>](主数据与BOM.md#table-units) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>salesorderitem_line_status</code> | 否 | <code>line_status</code> | — |
| <code>salesorderitem_planned_delivery_date</code> | 否 | <code>planned_delivery_date</code> | — |
| <code>salesorderitem_product_id</code> | 否 | <code>product_id</code> | — |
| <code>salesorderitem_product_sku_id</code> | 否 | <code>product_sku_id</code> | — |
| <code>salesorderitem_sales_order_id_line_no</code> | 是 | <code>sales_order_id</code>, <code>line_no</code> | — |
| <code>salesorderitem_unit_id</code> | 否 | <code>unit_id</code> | — |

### CHECK 约束

- <code>sales_order_items_amount_non_negative</code>

```sql
amount IS NULL OR amount >= 0
```

- <code>sales_order_items_line_no_positive</code>

```sql
line_no > 0
```

- <code>sales_order_items_line_status_allowed</code>

```sql
line_status IN ('open', 'closed', 'canceled')
```

- <code>sales_order_items_ordered_qty_positive</code>

```sql
ordered_quantity > 0
```

- <code>sales_order_items_unit_price_non_negative</code>

```sql
unit_price IS NULL OR unit_price >= 0
```

### 代码与业务真源

- [Schema source: `sales_order_item.go`](../../internal/data/model/schema/sales_order_item.go)
- [主数据源单据事实边界评审](../../../docs/architecture/主数据源单据事实边界评审.md)
- [订单采购边界评审](../../../docs/architecture/订单采购边界评审.md)
- [业务数据生命周期与页面动作规则](../../../docs/product/业务数据生命周期与页面动作规则.md)

<a id="table-sales-orders"></a>

## `sales_orders` — 销售订单

| 项目 | 说明 |
| --- | --- |
| Schema | <code>public</code> |
| 分类 | Source Document / 源单据 |
| 用途 | 保存客户订单承诺、交付计划、付款方式和销售责任信息。 |
| 明确不负责 | 销售订单生效不等于已预留、已生产、已出货或已产生应收。 |
| 生命周期 / 删除语义 | draft 转 submitted、active，再 closed；允许按状态 canceled；不物理删除。 |
| 写入边界 | SalesOrderUsecase 创建、草稿聚合保存、提交、生效、关闭和取消。 |

### 字段

| 字段 | PostgreSQL 类型 | Ent 类型 | NULL | 数据库默认值 | 结构属性 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| <code>id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | 主键；identity | 数据库主键；只用于内部关联，不等同于业务编号。 |
| <code>order_no</code> | <code>varchar(64)</code> | <code>string</code> | 非空 | — | — | 业务编号；唯一性和生成规则见索引与领域 usecase。 |
| <code>customer_order_no</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务编号；唯一性和生成规则见索引与领域 usecase。 |
| <code>customer_snapshot</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>sales_owner</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>contact_snapshot</code> | <code>jsonb</code> | <code>json.RawMessage</code> | 可空 | — | — | 业务发生时冻结的显示快照，不回写上游主数据。 |
| <code>payment_method</code> | <code>varchar(128)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>payment_term_days</code> | <code>bigint</code> | <code>int</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>price_condition_note</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>order_date</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>planned_delivery_date</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 可空 | — | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>lifecycle_status</code> | <code>varchar(32)</code> | <code>string</code> | 非空 | <code>&#39;draft&#39;</code> | — | 业务含义以字段名、表用途、约束和链接的 schema/usecase 为准。 |
| <code>version</code> | <code>bigint</code> | <code>int</code> | 非空 | <code>1</code> | — | 并发控制版本；写动作通常使用 CAS 校验，不能由调用方任意跳号。 |
| <code>note</code> | <code>varchar(255)</code> | <code>string</code> | 可空 | — | — | 业务备注；不替代状态原因、审批意见或正式审计字段。 |
| <code>created_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录创建时间；大量表由 Ent client 的 Go 侧默认值赋值，不应据此推断数据库存在 DEFAULT。 |
| <code>updated_at</code> | <code>timestamp with time zone</code> | <code>time.Time</code> | 非空 | — | — | 记录最近更新时间；大量表由 Ent client 的 UpdateDefault 维护。 |
| <code>customer_id</code> | <code>bigint</code> | <code>int</code> | 非空 | — | — | 外键，关联 `customers`。 |

### 主键

<code>id</code>

### 外键

| 约束 | 本表字段 | 引用 | ON UPDATE | ON DELETE |
| --- | --- | --- | --- | --- |
| <code>sales_orders_customers_sales_orders</code> | <code>customer_id</code> | [<code>public.customers</code>](主数据与BOM.md#table-customers) <code>id</code> | 未显式指定（PostgreSQL 默认 NO ACTION） | <code>NO ACTION</code> |

### 索引

| 索引 | 唯一 | 字段 | Predicate |
| --- | --- | --- | --- |
| <code>salesorder_customer_id</code> | 否 | <code>customer_id</code> | — |
| <code>salesorder_customer_order_no</code> | 否 | <code>customer_order_no</code> | — |
| <code>salesorder_lifecycle_status</code> | 否 | <code>lifecycle_status</code> | — |
| <code>salesorder_order_date</code> | 否 | <code>order_date</code> | — |
| <code>salesorder_order_no</code> | 是 | <code>order_no</code> | — |
| <code>salesorder_payment_method</code> | 否 | <code>payment_method</code> | — |
| <code>salesorder_planned_delivery_date</code> | 否 | <code>planned_delivery_date</code> | — |
| <code>salesorder_sales_owner</code> | 否 | <code>sales_owner</code> | — |

### CHECK 约束

- <code>sales_orders_lifecycle_status_allowed</code>

```sql
lifecycle_status IN ('draft', 'submitted', 'active', 'closed', 'canceled')
```

- <code>sales_orders_version_positive</code>

```sql
version > 0
```

### 代码与业务真源

- [Schema source: `sales_order.go`](../../internal/data/model/schema/sales_order.go)
- [主数据源单据事实边界评审](../../../docs/architecture/主数据源单据事实边界评审.md)
- [订单采购边界评审](../../../docs/architecture/订单采购边界评审.md)
- [业务数据生命周期与页面动作规则](../../../docs/product/业务数据生命周期与页面动作规则.md)
